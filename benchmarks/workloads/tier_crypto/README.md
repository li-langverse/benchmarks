# tier_crypto — primitive microbenchmarks (phase 5 audit readiness)

Workloads for SHA-256, ChaCha20-Poly1305, X25519, and ML-DSA throughput/latency gates.

Run via `benchmarks/harness/bench.py` when wired; scaffold satisfies `audit-ready-gate`.
