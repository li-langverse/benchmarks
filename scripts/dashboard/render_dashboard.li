# PH-IO-5 — render static GitHub Pages dashboard from summary.json
import plot

def main() raises IoError, AllocError -> int
  requires true
  ensures result == 0
  decreases 0
=
  var rc: int = plot_render_dashboard("data/latest/summary.json", "static-dashboard/index.html")
  return rc
