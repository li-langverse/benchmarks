name = "keepalive_pipelining"

[server]
listen = ":8080"
workers = 1

[verify]
requests = [
  { method = "GET", path = "/", expect_status = 200 },
]

[load]
tool = "wrk"
duration_sec = 10
connections = 16
threads = 2
pipeline = 8
