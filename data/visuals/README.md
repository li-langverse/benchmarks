# Benchmark visuals (`data/visuals/latest/`)

Generated by `./scripts/render-benchmark-visuals.sh` (runs `lic/scripts/plot_shareables.sh`).

| Output | Purpose |
|--------|---------|
| `*.png` | Speed bars, speedup vs cpp, MD energy, stability |
| `*.gif` | MD Lennard-Jones animations (li / cpp / rust / julia) |
| `manifest.json` | File list + **download link templates** for PRs |
| `benchmark-visuals-*.zip` | Single download bundle |

**Do not commit large GIFs to `main` routinely** — attach via PR branch or automation artifact. Cursor Automation: `.cursor/automations/benchmark-visual-validation.md`.
