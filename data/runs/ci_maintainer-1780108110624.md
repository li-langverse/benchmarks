# CI maintainer digest — 2026-05-30

**Agent:** `ci_maintainer`  
**Run id:** `ci_maintainer-1780108110624`  
**Source:** proactive ecosystem sweep  
**North star fit:** Ecosystem **secure** + **provable** gate — org repos require `ci.yml` on default branch before branch protection can enforce proof-bearing CI (PH-8p throughput, WP-H0 hygiene).

---

## Executive summary

- Refreshed `org-repo-ci-audit.json` and `ecosystem-audit.json`; **0 org repos missing CI** on their default branch.
- **32 repos OK** with `.github/workflows/ci.yml` (or exempt); **1 gated** (`lidb`, default `feat/ph-db-2-liorm-liq`, WP-H0); **0 audit-incomplete**.
- No new `ci.yml` template PRs opened — all non-exempt repos already have org CI on their active default branch.
- `research-findings` is **CI-exempt** per `ensure-org-repo-ci.py` (`EXEMPT_REPOS`); `ecosystem-audit.py` still lists it in `missing_ci_on_main` (signal drift).
- Open [research-findings#6](https://github.com/li-langverse/research-findings/pull/6) adds optional CI with **green `check`** — awaits human merge (governance repo).
- Branch protection verify: **11 repos OK**, **22 repos missing active rulesets** — CI present but rulesets not applied (`verify-org-branch-protection.sh`).
- Required check names in `roadmap/scripts/org-branch-protection.json` align with per-repo `latest_check_job()` hints (`check`, `bootstrap`, `build-and-test`, etc.).
- Ecosystem metrics: 165 open PRs, 43 failing CI, 90 ready for review — CI rollout complete; follow-up is protection + failing-PR triage.

---

## Deliverable / findings

### Preflight audits

| Audit | Result |
|-------|--------|
| `ensure-org-repo-ci.py` | exit **0** — OK: 32 · missing: 0 · gated: 1 · incomplete: 0 |
| `ecosystem-audit.py` | exit **0** — `repos_missing_ci_main`: 1 (`research-findings`, exempt) |
| `verify-org-branch-protection.sh` | exit **1** — 22 repos missing rulesets |

### Per-repo CI status

- **OK (main default):** benchmarks, li-demo, li-gui, li-httpd, li-language, li-local-ci, li-net, li-std-core, li-std-math, lic, lip, lis, lit, mmo, net.httpd, physics.*, proof-library, render, roadmap, sim.*, store.realtime, studio, studio.ai, ui, world.
- **Gated:** `lidb` — has `ci.yml` on feature default branch; org policy requires `main` default before counting (WP-H0).
- **Exempt:** `research-findings` — whitepaper archive; optional CI via open PR #6.

### Branch protection vs required checks

Ruleset config (`roadmap/scripts/org-branch-protection.json`) maps repo-specific contexts:

| Repo class | Required check(s) |
|------------|-------------------|
| lic, li-language | `version`, `build-and-test` |
| lip | `bootstrap` |
| lit | `test` |
| lis | *(empty — matrix jobs)* |
| benchmarks | `ingest-smoke`, `dashboard-build` |
| roadmap | `verify-kit` |
| default package repos | `check` |

Repos with CI but **no active ruleset** (22): li-cursor-agents, li-gui, li-local-ci, lidb, mmo, net.httpd, physics.custom, physics.runtime, proof-library, render, research-findings, sim, sim.additive, sim.automotive, sim.drug_design, sim.robotics, sim.scientific, store.realtime, studio, studio.ai, ui, world.

### PRs opened this run

None — no repos in `repos_missing_ci`.

---

## Recommended issues/PRs

| Repo | Title | Labels | Action |
|------|-------|--------|--------|
| **research-findings** | Merge [PR #6](https://github.com/li-langverse/research-findings/pull/6) — chore(ci): add org workflow | `ecosystem-ci`, human review | Human merge (governance) |
| **roadmap** | Run `apply-org-branch-protection.sh` for 22 unprotected repos | `ecosystem-governance` | Human-only org settings |
| **lidb** | WP-H0: set default branch to `main` after PH-DB-2 merge | `WP-H0` | Human repo settings |
| **benchmarks** | Align `ecosystem-audit.py` exempt list with `ensure-org-repo-ci.py` | `ecosystem-ci` | Optional follow-up PR |

---

## Deferred

- **Branch protection rollout** for 22 repos — requires human `apply-org-branch-protection.sh` (org-owner action per engineering standards).
- **lidb WP-H0** — default branch change blocked until PH-DB-2 feature branch merges.
- **research-findings CI** — exempt by policy; optional PR #6 merge is human decision.
- **Failing PR CI triage** (43 repos) — owned by `bug_fixer` / `code_implementer`, not ci_maintainer.
- **Self-merge** — no governance repos merged by agent.
