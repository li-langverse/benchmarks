# Swarm observer digest — `swarm_observer-1780124879899`

**Date:** 2026-05-30T07:10Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T07:06Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (69.8, grade D)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md`

---

## Executive summary

- **Posture: degraded** — ecosystem grade **D** (69.8/100); `unattended_safe: false` per scorecard.
- **Unattended operation: not safe** — 6/8 goal-directed runners stopped, 53 open swarm gaps, mass historical `error` status in control-plane DB (7,189 errors in 48h).
- **`orch-r2-competitor-stubs` completed** — 30 `competitor_feature` gaps in registry; 12 vertical stub todos patched to sim backlogs; evidence in orchestrator note.
- **Briefing alignment mostly OK** — P0 `proof_gap_researcher` + `workspace_sweeper` preserved; heap still prioritizes `coord_numerics` (bench_improver/numerics_researcher) — acceptable proof-before-perf ordering.
- **Programmatic observer idle** — `retry_counts: {}`, `stopped_agents: []` at 07:06Z; no auto-heals triggered despite degraded scorecard dispatching this meta-audit.
- **Stuck-run signal** — CP DB: 22 `running`, 7,553 cumulative `error`; local run sample (n=120) showed 110 `running` stubs — terminalization / status reconciliation needed on **li-cursor-agents**.
- **Preflight gap** — `org_agent_kit_audit` exit 1; 8 slow preflights skipped (`--skip-slow`); stale plan/PR/security signals.
- **Human blockers** — lic dirty on perf branch; 132 unpushed branches; no open org PRs; governance merges remain human-only.

---

## Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade D; not unattended-safe | `data/latest/ecosystem-quality-report.json` | high |
| Goal-directed runners | 6/8 stopped; 24 plan todos pending | `lic/data/goal-directed-agents/snapshot.json` | high |
| Gap registry | 53 open gaps (30 competitor, 20 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Benchmarks | 6 red tier-1/2 rows | `data/latest/agent-briefing.json` → `ecosystem_audit.benchmarks.red` | high |
| CP status reconciliation | 7,189 errors / 48h; batch errors @ 07:04Z | Supabase `agent_runs` (status counts) | high |
| `workspace_sweeper` | lic uncommitted (4 files, perf branch) | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| `agent_kit_maintainer` | org kit audit exit 1; premature incomplete runs | `agent-briefing.preflight_runs.org_agent_kit_audit`; run `agent_kit_maintainer-1780124464321` | medium |
| Preflight | 8 scripts skipped (--skip-slow) | `agent-briefing.preflight_runs` | medium |
| `httpd` loop | agent_exit 124 on perf soak todos | `snapshot.json` runner=httpd | medium |
| verticals.toml infra | File on lic, missing benchmarks main | `gap-infra-verticals-toml-missing-benchmarks-main` | low |
| Docs debt | 10 repos without live pages | `ecosystem_audit.metrics.repos_without_live_pages` | low |

### Error sample classification (2026-05-30 batch @ 07:04Z)

| Run pattern | Agents | Likely root cause |
|-------------|--------|-------------------|
| Simultaneous `error` @ 07:04:22 | workspace_sweeper, implementation_gaps, org_repo_onboarder, pr_merger, agent_kit_maintainer, proof_gap_researcher | **Supervisor wave / SDK preemption** — no completion payload; not repo conflict |
| `incomplete (premature)` → stored `error` | agent_kit_maintainer (107s, 46 tool calls) | **SDK timeout / step budget** — prompt gap on large multi-repo kit rollout |
| Historical volume | All leaf agents (400–460 errors each) | **Status reconciliation drift** — finished/incomplete runs mislabeled `error` in CP DB |

`CURSOR_API_KEY`: **set** (programmatic heal can invoke SDK).

---

## Self-heal actions taken (this cycle)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | Registry refreshed (79 total gaps) |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 21 backlog patches written; `swarm-gap-actions.json` updated |
| orch-r2 close | Registry + `swarm-observer-plan-backlog.md` | `orch-r2-competitor-stubs` → completed |
| Observer scan | CP `control_plane_state.observer` @ 07:06Z | Scan ran; **no retries** (`retry_counts: {}`) |

Programmatic observer did **not** auto-retry the 07:04Z error batch (empty retry_counts — likely fresh tick or retry budget not wired for batch kills).

---

## Recommended control-plane fixes

| Fix | Repo / path | Rationale |
|-----|-------------|-----------|
| Terminalize stuck `running` rows after SDK TTL | `li-cursor-agents/src/observer/` | 110 local + 22 CP `running` vs terminal completions |
| Reconcile `error` vs `incomplete`/`finished` in run persistence | `li-cursor-agents` store layer | 95%+ historical error rate is false-positive |
| Refresh `control_plane_reports.swarm_health` on each tick | supervisor report writer | Latest report still 2026-05-25 `healthy: true` — stale |
| Enable slow preflights in automation (drop `--skip-slow`) | benchmarks briefing script / supervisor env | plan_audit, ci_bug_triage, security_cwe stale |
| Add `suggested_loop: sim` default for PH-CAE vertical stubs in ingest | `lic/scripts/swarm-gap-ingest.py` | orch-r2 manual registry edit for pde/fea/cfd |
| Land `verticals.toml` on benchmarks main | `benchmarks/competitive/verticals.toml` | Unblocks cross-repo ingest without lic fallback |
| Wire observer retry for simultaneous multi-agent error bursts | `li-cursor-agents/src/observer/` | 07:04Z batch pattern |

**Prompt edits (no product code):**

- `prompts/agent-kit-maintainer.md` — chunk org kit audit per-repo; exit cleanly on partial progress
- `prompts/swarm-observer.md` — reference CP DB path when local `data/runs/` empty

---

## Human-only blockers

- Governance PRs and **trusted.lean** changes — never auto-merge
- **132 branches** without PRs — volume triage; `pr_branch_opener` assists but human review for stacks
- **lic#439** / local-ci red on perf branch — human decides merge vs rebase
- **9 repos** agent-kit drift — org-wide rollout PRs need human approval

---

## Agent deliverable checklist

- [x] Ecosystem quality report read (fresh @ 07:07Z)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed
- [x] `orch-r2-competitor-stubs` orchestrator note
- [x] Recommended agents vs recent runs compared (briefing P0 honored; numerics in heap)
- [x] Error runs sampled + classified
- [ ] orch-r3 missing-package sweep (deferred — next loop)
- [ ] orch-r4 ui_ux signals (deferred — apply touched studio-ux rows incidentally; needs dedicated pass)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + fix error/incomplete status drift | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Land competitive verticals.toml on main for gap ingest | benchmarks | `ecosystem`, `docs` |
| Refresh control_plane_reports.swarm_health each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Enable full preflight (remove --skip-slow) in briefing automation | benchmarks | `orchestration` |
| Tier-1 red matmul_blocked / matmul_naive codegen follow-up | lic | `PH-7e`, `performance`, `bench` |
| Agent-kit org audit: per-repo chunked rollout prompt | li-cursor-agents | `agent-kit`, `prompt` |

---

## Deferred

- **orch-r3** missing-package sweep (`std.plot`, `std.summary`, line_profiler seeds)
- **orch-r4** ui_ux gap signals from `gui_ux_tester` / studio-ui loop
- **8 master-plan plan_debt** rows without runner backlog mapping
- **security-research** sec-r1..r3 plan todos (no backlog file in `RUNNER_BACKLOGS`)
- **Restart** stopped goal runners (compiler-studio, sim-algo, studio-ui-ux, sim-md-research, sim-chem-research, security-research) — control-plane scheduler, not swarm_observer product work
- **Full ecosystem-audit refresh** after lic perf branch merges

---

## Goal orientation drift

**Briefing recommended (13):** proof_gap_researcher, workspace_sweeper, plan_verifier, implementation_gaps, gap_explorer, agent_kit_maintainer, docs_maintainer, pr_branch_opener, bug_fixer, code_implementer, security_auditor, numerics_researcher, bench_improver

**Recent CP dispatches (6h):** All recommended agents have run entries; current wave (07:04–07:08Z) matches briefing heap. **Drift: none material** — P0 research/sweep ahead of numerics is respected in dispatch order.

**Scorecard override agents** (`swarm_observer`, `gap_explorer`, `ecosystem_grader`) correctly triggered by dimension scores < 75/80.
