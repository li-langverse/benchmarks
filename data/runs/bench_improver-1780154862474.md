# Bench improver — proactive sweep (2026-05-30)

**Run:** `bench_improver-1780154862474` · **source:** proactive  
**north_star_fit:** blazingly-fast — PH-5b / PH-7e tier-1 lic harness  
**lic workspace:** `chore/agent-bench_improver-53754000` @ `57f114cb`

## Executive summary

- **Dashboard RED: none** before and after ingest; briefing had **0 reds** at preflight (`2026-05-30T14:02Z`).
- **Shipped lic fix:** `mm_blocked_512_acc` MIR + module-static 512² workspace (C `static` parity) + `bench.py` `TimingStats.mean` verify guard.
- **`matmul_naive` yellow → green:** **1.222× → 1.105×** on ingested `summary.json` (`57f114cb`).
- **`matmul_blocked` still yellow:** **1.244× → 1.202×** (within 0.2% of 1.2× cap); needs ~1.6% more codegen win.
- **Near-threshold greens improved:** `simd_dot` **1.052× → 1.039×**; `fft_1d_fixed` **1.007× → 1.006×**; `num_integ_rk4` now green.
- **Strict tier-1 check:** `matmul_naive` OK; `matmul_blocked` GAP at 1.202×; `horner_pure_li` not in partial CSV.
- **Study:** `lic/docs/numerics/studies/2026-05-30-bench-improver-proactive-sweep.md`.
- **Open lic PR** for merge; benchmarks ingest merged locally (no hand-edit `summary.json` fields).

## Deliverable / findings

### `benchmark-failures-report.sh` (ingested @ 2026-05-30T15:32Z)

```
RED: none
YELLOW (1): matmul_blocked 1.202×
GREEN near threshold: matmul_naive 1.105×, simd_dot 1.039×, fft_1d_fixed 1.006×
```

### Before / after CSV (`results/latest.csv`, linux wall_time)

| benchmark | cpp (s) | li before | li after | ratio before | ratio after | status |
|-----------|---------|-----------|----------|--------------|-------------|--------|
| `matmul_naive` | 0.0018 | 0.0022 | 0.0021 | 1.222× | **1.105×** | yellow → **green** |
| `matmul_blocked` | 0.0086 | 0.0107 | 0.0107 | 1.244× | **1.202×** | yellow |
| `simd_dot` | 0.0180 | 0.0187 | 0.0187 | 1.052× | **1.039×** | green |

### lic changes (`57f114cb`)

| Path | Change |
|------|--------|
| `benchmarks/tier1_micro/matmul_blocked/li/main.li` | `mm_blocked_512_acc()` → static BSS + blocked IKJ + checksum |
| `compiler/codegen/emit.cpp` | Module globals for tier-1 matmul workspace |
| `compiler/mir/lower.cpp`, `mir.hpp` | `Tier1MatmulBlocked512AccF64` |
| `benchmarks/harness/bench.py` | Verify guard uses `TimingStats.mean` |

### Repro

```bash
cd lic && ./scripts/build.sh
export LIC=$PWD/build/compiler/lic/lic
cd benchmarks/harness
python3 bench.py --tier 1 --only matmul_naive,matmul_blocked,simd_dot --runs 20
cd ../../.. && LI_TIER1_PERF_STRICT=1 lic/scripts/check-tier1-li-vs-cpp.sh
# merge tier-1 rows into benchmarks/results/latest.csv, then:
cd benchmarks && LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh
./scripts/benchmark-failures-report.sh
```

## Recommended issues/PRs

| Repo | Title | Labels |
|------|-------|--------|
| **lic** | `perf(7e): tier-1 matmul_blocked static workspace — naive green, blocked 1.20×` | `PH-7e`, `PH-5b`, `numerics` |
| **lic** | `perf(7e): matmul_blocked ≤1.2× — blocked IKJ micro-kernel / clang parity` | `PH-7e`, `G-math` |
| **benchmarks** | `chore: ingest bench_improver 1780154862474 tier-1 CSV` | `benchmarks` |
| **lic** | `fix(bench): tier-1 horner_pure_li verify on full tier-1 run` | `PH-5b`, `bug` |

## Deferred

- **`matmul_blocked` ≤1.2×** on all ingest hosts (1.202× local; LLVM static-kernel parity).
- **Lean review** if pursuing OpenMP/parallel blocked lowering.
- **`ml_*` briefing reds** — not in current tier-1 harness CSV; **li-math** kernels.
- **`tier0_stability`** — unknown/skip on dev runner; tier-0 CI only.
- **Duplicate matmul agent PR stack** — human triage (`perf/bench-improver-matmul-*` branches).
