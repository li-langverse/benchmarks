# Swarm observer digest — `swarm_observer-1780132501488`

**Date:** 2026-05-30T09:16Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T08:04Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (73.8, grade **C**)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md`

---

## Executive summary

- **Posture: degraded** — ecosystem grade **C** (73.8/100); `unattended_safe: false`.
- **Unattended operation: not safe** — 65 open swarm gaps, 6/9 goal-directed runners stopped, 106/120 sampled local runs still `running` (SDK terminalization drift).
- **`orch-r2-competitor-stubs` completed** — 30 `competitor_feature` registry rows; 12 `verticals.toml` stubs mapped; apply-actions patched sim-md-research backlog (9 rows this cycle).
- **Briefing alignment OK** — P0 `proof_gap_researcher` + `workspace_sweeper` in recommended list; heap `coord_numerics` still prioritizes `bench_improver` / `numerics_researcher` (proof-before-perf).
- **Programmatic observer idle** — CP `observer.retry_counts: {}` at 09:16Z; no auto-retries despite 754 `error` runs in last 2h (batch preemption pattern).
- **Stuck-run signal** — scorecard: 106 `running` / 120 sampled; CP: 18 `running` in 2h window vs 754 `error`.
- **Preflight gap** — `org_agent_kit_audit` exit 1; 8 slow preflights skipped (`--skip-slow`).
- **CURSOR_API_KEY** — present; SDK auth not the blocker for orch-r2 (orchestration-only pass).

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; not unattended-safe | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 65 open (30 competitor, 32 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/9 stopped; 37 plan todos pending | `lic/data/goal-directed-agents/snapshot.json` | high |
| Benchmarks | 6 red rows (matmul_*, ml_*, num_gmres) | `agent-briefing.json` → `ecosystem_audit.benchmarks.red` | high |
| CP run hygiene | 754 errors / 2h; mass `running` stubs | Supabase `agent_runs`; `li-cursor-agents/data/runs/` | high |
| `code_implementer` | SDK error, 0 tool calls, 47s | `code_implementer-1780130277424.json` | medium |
| `workspace_sweeper` | Dirty lic + benchmarks; systemctl restart failed | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| CP report stale | Latest `swarm_health.healthy: true` @ 2026-05-25 | `control_plane_reports` is_latest | medium |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| verticals.toml | On lic only; benchmarks main gap open | `gap-infra-verticals-toml-missing-benchmarks-main` | low |

### Error sample classification

| Pattern | Example | Root cause |
|---------|---------|------------|
| SDK `tools=0`, short duration | `code_implementer-1780130277424` | **SDK preemption / empty run** — not repo conflict |
| Simultaneous errors @ tick | 6 agents @ 09:14Z (workspace_sweeper, docs_maintainer, …) | **Supervisor wave kill** — retry_counts empty |
| Historical `error` volume | 400–500 errors/agent lifetime | **Status reconciliation drift** (finished mislabeled) |

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (73.8, grade C)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed (`swarm-gap-ingest.py`, `swarm-gap-apply-actions.py`)
- [x] `orch-r2-competitor-stubs` orchestrator note + backlog/registry close
- [x] Recommended agents vs heap compared (numerics P20, governance P30)
- [x] Error runs sampled + classified
- [ ] `orch-r3-missing-package-sweep` (deferred)
- [ ] `orch-r4-ui-ux-signals` (deferred)

---

## Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | Idempotent; 90 gaps total, 65 open |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 20 backlog patches; `swarm-gap-actions.json` @ 09:16Z |
| Observer scan | CP `control_plane_state.observer` | `last_scan_at: 2026-05-30T09:16:22Z`; **no retries** |
| Meta dispatch | ecosystem-quality scorecard | Dispatched `swarm_observer` when `swarm_execution` &lt; 75 (prior tick) |

No programmatic heal for SDK batch errors this cycle (`retry_counts: {}`).

---

## Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile `error` vs `incomplete`/`finished` in persistence | `li-cursor-agents` store layer |
| Refresh `control_plane_reports.swarm_health` each tick | supervisor report writer |
| Enable slow preflights (drop `--skip-slow`) | benchmarks briefing / supervisor env |
| Default `suggested_loop: sim` for PH-CAE vertical stubs in ingest | `lic/scripts/swarm-gap-ingest.py` |
| Land `verticals.toml` on benchmarks main | `benchmarks/competitive/verticals.toml` |

---

## Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- **133 branches** without PR (`pr_branch_opener` assists; human review for stacks)
- lic dirty on `perf/bench-improver-matmul-simd-j-20260530` — human decides merge vs rebase
- **9 repos** agent-kit drift — org rollout needs human approval

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + fix error/incomplete drift | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Land competitive verticals.toml on benchmarks main | benchmarks | `ecosystem`, `docs` |
| Refresh control_plane_reports.swarm_health each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Tier-1 red matmul_blocked / matmul_naive — SIMD lowering follow-up | lic | `PH-7e`, `performance`, `bench` |
| Enable full preflight in briefing automation | benchmarks | `orchestration` |
| orch-r3: std.summary / std.plot package placement | lic | `ecosystem`, `PH-IO` |

---

## Deferred

- `orch-r3-missing-package-sweep` — 3 open `missing_package` gaps; backlog already patched
- `orch-r4-ui-ux-signals` — studio-ux-16/17 appended incidentally; needs dedicated `ui_ux` taxonomy pass
- ph-db runner plan_debt (8 todos) — no lic backlog mapping in apply-actions
- security-research sec-r1–r3 — deferred mapping; needs `security-research-backlog.md` wiring in apply script

---

## Gap orchestration evidence (`swarm_coverage`)

| Artifact | Path |
|----------|------|
| Registry | `lic/data/swarm-gap-registry/registry.yaml` |
| Apply actions | `benchmarks/data/latest/swarm-gap-actions.json` |
| Orchestrator note | `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md` |
| Observer backlog | `lic/docs/ecosystem/swarm-observer-plan-backlog.md` (`orch-r2` → completed) |
