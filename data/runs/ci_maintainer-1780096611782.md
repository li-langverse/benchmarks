# CI maintainer — proactive ecosystem sweep

**Run id:** `ci_maintainer-1780096611782`  
**Agent id:** `ci_maintainer`  
**north_star_fit:** ecosystem platform / secure — org-wide `ci.yml` on default branch is the functionality gate before proof-backed merges (PH-8p CI throughput, engineering standards).

## Executive summary

- Refreshed `org-repo-ci-audit.json`: **32 OK**, **0 missing**, **1 gated** (`lidb`), **0 incomplete** — `ensure-org-repo-ci.py` exit **0**.
- Refreshed `ecosystem-audit.json`: `repos_missing_ci_main: 1` (`research-findings` only — exempt in org CI audit).
- **No repos require new `ci.yml` PRs** this pass; all non-exempt org repos on `main` already ship `.github/workflows/ci.yml`.
- **`lidb`** remains **gated (WP-H0)**: default branch `feat/ph-db-2-liorm-liq` has `ci.yml` but org policy defers enforcement until default → `main` (human action).
- **`research-findings`** is **CI-exempt** in `ensure-org-repo-ci.py`; open PR [#6](https://github.com/li-langverse/research-findings/pull/6) adds optional CI (`check` job **green**).
- Branch protection verify: **11 OK / 22 missing** active rulesets — CI present but protection not applied on many package mirrors.
- Sampled ruleset ↔ CI alignment: `lip` requires `bootstrap`, `lic` requires `version` + `build-and-test` — matches workflow job names.
- **No isolated clone edits** to sibling package repos; zero workflow rollouts warranted.

## Deliverable / findings

### Org CI audit (`ensure-org-repo-ci.py`)

| Bucket | Count | Notes |
|--------|------:|-------|
| `repos_ok` | 32 | All audited repos on `main` with `ci.yml` on default branch |
| `repos_missing_ci` | 0 | — |
| `repos_gated_non_main_default` | 1 | `lidb` — WP-H0 |
| `repos_audit_incomplete` | 0 | — |

**Required check hints** (from audit policy / templates):

| Repo class | Suggested required check |
|------------|-------------------------|
| `lic`, `li-language` | `build-and-test` |
| `benchmarks` | `ingest-smoke` |
| `roadmap` | `verify-kit` |
| `lip` | `bootstrap` |
| `lit` | `test` |
| Package mirrors | `check` |

### Branch protection vs CI

Verified via `roadmap/scripts/verify-org-branch-protection.sh`:

**Protected (11):** `benchmarks`, `li-demo`, `li-httpd`, `li-language`, `li-net`, `li-std-core`, `li-std-math`, `lic`, `lip`, `lis`, `lit`, `roadmap`

**Missing active ruleset (22):** `li-cursor-agents`, `li-gui`, `li-local-ci`, `lidb`, `mmo`, `net.httpd`, `physics.custom`, `physics.runtime`, `proof-library`, `render`, `research-findings`, `sim`, `sim.additive`, `sim.automotive`, `sim.drug_design`, `sim.robotics`, `sim.scientific`, `store.realtime`, `studio`, `studio.ai`, `ui`, `world`

CI workflows exist on these repos; gap is **ruleset application**, not missing workflows. Remediation: `cd roadmap && ./scripts/apply-org-branch-protection.sh <repo>` (human/governance — not self-applied by agent).

### Ecosystem audit cross-check

- `ecosystem-audit.json` `missing_ci_on_main`: `["research-findings"]` only.
- `metrics.repos_missing_ci_main`: **1** (same exempt repo).
- **Recommendation:** align `ecosystem-audit.py` exempt list with `ensure-org-repo-ci.py` to avoid false P0 signals.

### PRs opened this run

None — no missing CI targets.

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| WP-H0: set `lidb` default branch to `main` | `lidb` | `governance`, `ecosystem-ci` |
| chore(roadmap): apply branch protection to 22 package mirrors | `roadmap` | `governance`, `ecosystem-ci` |
| fix(audit): exempt `research-findings` in ecosystem-audit missing_ci | `benchmarks` | `ecosystem-ci`, `agent:ci_maintainer` |
| Merge: chore(ci): add org workflow for research whitepapers | `research-findings` | `ecosystem-ci`, `merge-approved` |

## Deferred

- **lidb WP-H0** — default branch rename is human-owned; `ci_maintainer` resumes after `main` is default.
- **22 repos without rulesets** — apply via roadmap governance script; out of scope for workflow-only rollouts.
- **research-findings CI** — intentionally exempt (research archive); PR #6 optional for human review.
- **lic CI improvements** (WP-C1–C6: reusable `package-ci`, cache, LLVM alignment) — separate serialized `ci_maintainer` lic specialist pass per org hygiene plan.
