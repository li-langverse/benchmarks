# Swarm observer digest — meta audit

> `swarm_observer` · run `swarm_observer-1780081782468` · 2026-05-29T19:10Z · north_star_fit: ecosystem orchestration (PH-7e numerics pressure)

## Executive summary

- **Health: degraded** — grade **D** (66.3); `unattended_safe: false`. Not critical: `CURSOR_API_KEY` is set and numerics/httpd lanes are active.
- **Swarm execution is the weakest dimension** (50/100): 31 DB rows still `running` (24h window); 2,598 `error` vs 120 `finished` in the same window; programmatic observer idle (`retry_counts: {}`).
- **Control-plane report is stale** — latest `control_plane_reports` row is 2026-05-25 and marks swarm healthy with zero runs sampled; dashboard cannot trust `swarm_health` until refresh.
- **Briefing ↔ heap alignment is good** — current `recommended_agents` match `heap_plan` priorities (numerics, governance, ecosystem, platform).
- **Gap pipeline healthy** — ingest + apply ran at 19:07Z; 53 open gaps (30 competitor, 20 plan_debt, 3 missing_package); 21 backlog patches applied.
- **6 goal-directed runners stopped** (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`); `httpd` active on `gap-phase2-perf-wrk-soak`.
- **Primary failure mode: SDK premature/error** — not auth; sampled errors are `SDK run status: error` after partial tool use, often without deliverable sections.
- **Next self-heal target:** finalize stuck SDK runs, refresh control-plane report, enqueue `orch-r4` ui_ux gap signals via swarm goals (not systemd loops).

## Deliverable / findings

Full report: [lic/data/runs/swarm_observer-orch-r4-stuck-run-finalize.md](../../../lic/data/runs/swarm_observer-orch-r4-stuck-run-finalize.md)

Orchestrator note: [lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r4-stuck-run-finalize.md](../../../lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r4-stuck-run-finalize.md)

### Findings

| Agent / area | Symptom | Evidence | Severity |
|--------------|---------|----------|----------|
| Swarm (all) | 31+ runs stuck `running` in DB | `control_plane_state` + `agent_runs` last 24h | **high** |
| Swarm (all) | 2,598 errors / 120 finished (24h) | MCP `agent_runs` status counts | **high** |
| Observer | No auto-retries (`retry_counts: {}`) | `control_plane_state.payload.observer` | **high** |
| Control plane | Stale report (2026-05-25, healthy=false positive) | `control_plane_reports.is_latest` | **high** |
| `bench_improver` | SDK error ~349s, no bench evidence | `li-cursor-agents/data/runs/bench_improver-1780079811014.json` | **high** |
| `code_implementer` | GitHub rate limit + branch divergence on httpd | `code_implementer-1780081276557.json` | **medium** |
| `implementation_gaps` | SDK error after 3 tools (premature) | `implementation_gaps-1780080167544.json` | **medium** |
| `studio_ui_ux_builder` | Wrong-repo confusion (lic vs studio-ui) | `studio_ui_ux_builder-1780081364557.json` | **medium** |
| Goal runners | 6/8 runners stopped | `lic/data/goal-directed-agents/snapshot.json` | **high** |
| Preflight | `org_agent_kit_audit` + `security_cwe_audit` exit 1 | `agent-briefing.preflight_runs` | **medium** |
| Numerics | 6 red benchmark rows (PH-5b/7e) | `ecosystem-audit.benchmarks.red` | **high** |
| Gap registry | 53 open; httpd todos pending vs registry dedupe drift | `swarm-gap-actions.json`, plan-verifier digest | **medium** |
| Briefing queue | `queued_agent_tasks` on old hash `e5fad788…` | MCP `queued_agent_tasks` | **low** |

### Self-heal actions taken (programmatic observer this cycle)

| Action | Result |
|--------|--------|
| Gap ingest (`swarm-gap-ingest.py`) | Registry updated 19:09:42Z |
| Gap apply (`swarm-gap-apply-actions.py`) | 21 backlog patches; 3 pkg + 18 sim/competitor rows |
| Observer scan | Last scan 19:09:42Z; **no retries dispatched** |
| Stopped agents | `stopped_agents: []` — no agents budget-exhausted |
| Active healers | `bench_improver` ×3 running; `swarm_observer` dispatched (this run) |

### Recommended control-plane fixes

| Fix | Path |
|-----|------|
| Finalize stuck SDK runs after timeout (running → incomplete/error) | `li-cursor-agents/src/observer/` |
| Emit fresh `control_plane_reports` each supervisor tick | `li-cursor-agents/src/supervisor/` |
| Reconcile `queued_agent_tasks.briefing_hash` with latest briefing | supervisor heap sync |
| Retry SDK premature errors with per-agent budget | `src/observer/retry-policy.ts` (or equivalent) |
| Route studio UI builder to `lic-studio-ui` / `studio` workflow repo | `prompts/studio-ui-ux-builder.md` |
| Fix httpd gap dedupe: snapshot pending vs registry closed | `lic/scripts/swarm-gap-ingest.py` |
| Restart stopped research runners via goals | `li-cursor-agents/config/research-goals.yaml` |

### Human-only blockers

- **`trusted.lean`** / provability gate changes — human-approved issues only.
- **129 pushed branches without open PR** — `pr_branch_opener` volume needs human triage before auto-open flood.
- **Governance merges** on `roadmap` — agents must not self-merge.
- **Agent-kit rollout** across 9 repos — review before bulk merge.

### Agent deliverable checklist

- [x] Read `ecosystem-quality-report.json` (fresh, 19:07Z)
- [x] Query control-plane DB (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Compare `recommended_agents` vs recent runs / heap plan
- [x] Sample error runs and classify root causes
- [x] Reconcile gap registry + apply pipeline
- [x] Write report under `data/runs/` + orchestrator note
- [ ] Control-plane code fix PR (recommended, not in scope for this leaf run)

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| fix(observer): finalize stuck SDK runs after timeout | li-cursor-agents | swarm, control-plane |
| fix(supervisor): refresh control_plane_reports each tick | li-cursor-agents | swarm, dashboard |
| fix(gap-ingest): httpd plan_pending vs registry closed dedupe | lic | swarm, plan-debt |
| feat(std): PH-IO-7 `std.summary` for benchmark ingest | lic | PH-IO-7, ecosystem |
| feat(std): PH-IO-5 `std.plot` static dashboard hooks | lic | PH-IO-5, ecosystem |
| research: PH-7e tier-1 matmul_blocked / num_gmres red rows | lic | numerics, PH-7e |
| chore(agent-kit): sync 1.3.5 to 9 drifted repos | org-wide | agent-kit |

## Deferred

- `orch-r4-ui-ux-signals` — link `gui_ux_tester` / `studio-ui-ux` plan todos to gap registry (`ui_ux` kind).
- Master-plan `plan_debt` rows without runner backlog mapping (14 deferred in apply-actions).
- HPC competitor_feature gaps (Kokkos, PETSc, FFTW) — research handoffs only, no product code.
- `suggested_new_loops: [sim, security-research]` — route via `research-goals.yaml`, not systemd install scripts.
