# Swarm observer digest — `swarm_observer-1780136053782`

**Date:** 2026-05-30T10:15Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T10:03Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (76.0, grade **C**, refreshed @ 10:14Z)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — grade **C** (76.0, up from 75.0); `unattended_safe: true`; no `swarm_degraded` latch (programmatic observer `needs_meta_observer: false`).
- **`orch-r3-missing-package-sweep` completed** — 3 open `missing_package` gaps reconciled; `pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot` remain **pending** in `ecosystem-package-backlog.md` for `issue_planner`.
- **Gap pressure unchanged** — **64** open registry gaps (30 `competitor_feature`, 31 `plan_debt`, 3 `missing_package`); ingest/apply re-run @ 10:14Z (idempotent).
- **Second supervisor wave @ 10:11–10:14Z** — 8+ agents `error` with `completion.error` null → **SDK preemption / concurrent tick**, not repo conflict (`CURSOR_API_KEY` set).
- **114/120 runs still `running` in scorecard** — CP status hygiene drift; `swarm_execution` improved to 74 (incomplete rate 17% vs 33% prior pass).
- **Goal-directed runners** — 6/9 supervisors idle/stopped; `agents_live: 0`; httpd runner active on `gap-phase2-perf-wrk-soak`.
- **Briefing alignment improving** — heap + recommended agents match P0 (`proof_gap_researcher`, `workspace_sweeper`, governance/ecosystem stack); `gap_explorer` for 2 missing std modules.
- **Next orchestrator todo:** `orch-r4-ui-ux-signals` (studio-ux-16/17 already patched by apply-actions).

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C (76.0); `gap_pressure` 60 | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Goal-directed runners | 6/9 stopped/idle; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP error burst | 8 errors in 2h window; null `completion.error` | Supabase `agent_runs` | high |
| CP status hygiene | 114/120 sampled `running`; scorecard `error_rate: 0` | `li-cursor-agents/data/runs`, scorecard `swarm_execution` | medium |
| CP report stale | Latest `control_plane_reports` @ 2026-05-25 | `control_plane_reports` `is_latest` | medium |
| `workspace_sweeper` | PRs exist (lic #528, li-cursor-agents #39); systemd restart failed | `workspace_sweeper-1780135842992` sweep digest | medium |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| `missing_package` | 3 open: line_profiler, std.summary, std.plot | `ecosystem-explorer.json`, `ecosystem-package-backlog.md` | medium |
| Explorer vs backlog | std.io/csv **present**; registry rows closed | `ecosystem-explorer.json` `status: present` | low |

### Error sample classification

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Simultaneous `error` @ tick | `plan_verifier-1780135843033` + 7 peers @ 10:11Z | **Supervisor wave kill / SDK preemption** — `completion.error` null |
| JSON `status: running` vs CP `error` | `workspace_sweeper-1780135842992` | **Terminalization lag** — local file not synced to CP terminal state |
| Sweep PR exists | lic PR #528, agents PR #39 | **Not a failure** — deterministic sweep succeeded; LLM follow-up redundant |
| Historical error volume | 300–500 errors/agent / 24h | **Status reconciliation drift** — relabel job deferred |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | 90 gaps in registry; 0 new rows this pass |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 20 patches incl. 3 `missing_package` → backlog **pending** |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` | 76.0 grade C; `unattended_safe: true` |
| Observer scan | CP `control_plane_state` @ 10:13:15Z | `retry_counts: {}`; remediations: `implementation_gaps`, `proof_gap_researcher` goal-align retry |
| Meta observer | This pass | `orch-r3` closed in registry; orchestrator note updated |

### `orch-r3-missing-package-sweep` reconciliation

| Gap id | `target_todo_id` | Explorer | Backlog | Handoff |
|--------|------------------|----------|---------|---------|
| `gap-line-profiler-001` | `pkg-line-profiler` | seed WP-B | pending | `issue_planner` |
| `gap-missing-std-std-summary` | `pkg-std-summary` | **missing** (PH-IO-7) | pending | `issue_planner`, `package_architect` |
| `gap-missing-std-std-plot` | `pkg-std-plot` | **missing** (PH-IO-5) | pending | `issue_planner`, `package_architect` |

Closed: `gap-missing-std-std-io`, `gap-missing-std-std-csv` (explorer `present`). Plan-debt row `gap-plan-pending-swarm-observer-orch-r3-missing-package-sweep` → **closed** this pass.

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Retry on wave-kill with backoff budget | `li-cursor-agents/src/observer/remediate.ts` |
| Refresh `control_plane_reports.swarm_health` each tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Throttle concurrent SDK dispatches per tick | `li-cursor-agents/src/supervisor/loop.ts` |
| Re-enable slow preflights when tick budget allows | `benchmarks/scripts/agent-briefing.py` (`--skip-slow` → 8 skipped) |
| Align `pkg-std-io`/`pkg-std-csv` backlog todos with explorer `present` | `lic/docs/ecosystem/ecosystem-package-backlog.md` |

### Human-only blockers

- **6 goal-directed runners stopped/idle** — may need user unit restart (not auto-merge).
- **Package implementation** — agents must not ship std modules without `plan-approved`.
- **`trusted.lean`** — out of scope for gap orchestration.
- **Governance PR review** — 11 PRs flagged by `pr_alignment` (briefing); no auto-merge.

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (76.0, grade C)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed @ 10:14Z
- [x] `orch-r3-missing-package-sweep` orchestrator note + registry close
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [ ] `orch-r4-ui-ux-signals` (next pass)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Open issues for std.summary, std.plot, li-line-profiler seed | `lic` | `issue_planner`, `PH-IO-5`, `PH-IO-7`, `gap_explorer` |
| Observer: terminalize stuck `running` runs after SDK TTL | `li-cursor-agents` | `control-plane`, `swarm_observer` |
| Refresh control_plane_reports swarm_health each supervisor tick | `li-cursor-agents` | `dashboard`, `observer` |
| Agent-kit drift: 9 repos (preflight exit 1) | org repos | `agent_kit_maintainer` |
| Live docs for 8 repos without pages | org repos | `docs_maintainer` |
| Merge `verticals.toml` competitive registry to benchmarks main | `lic` → `benchmarks` | `ecosystem`, `competitor_feature`, `no-auto-merge` |

---

## Deferred

- `orch-r4-ui-ux-signals` — studio-ux-16/17 patched; dedicated UI gap reconciliation next pass.
- Re-enable slow preflights (`plan_audit`, `ci_bug_triage`, `pr_program`) when tick budget allows.
- CP historical error relabeling (8k+ lifetime `error` rows / 24h) — batch job, not this pass.
- Restart stopped goal-directed runners (`compiler-studio`, `sim`, `studio-ui-ux`, research loops).
- Close `pkg-std-io` / `pkg-std-csv` backlog rows when `issue_planner` confirms explorer parity on main.
