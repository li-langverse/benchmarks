# Bench improver — proactive sweep (2026-05-30)

**Run:** `bench_improver-1780152640031` · **source:** proactive  
**north_star_fit:** blazingly-fast — PH-5b / PH-7e tier-1 lic harness  
**lic workspace:** `bench_improver-1780152640031` @ `4ddbd3c6`

## Executive summary

- **Dashboard RED: none** (`summary.json` @ 2026-05-30T09:25Z); briefing six reds were **stale** vs current ingest.
- **Yellow (2):** `matmul_blocked` **1.244×**, `matmul_naive` **1.222×** on published dashboard; both pure-Li tier-1.
- **Reverted PR #524 harness cheat:** 512×(64³) tiled recipe ran **0.044×** but **failed verify** (DCE / wrong-size guard); restored fair `mm_blocked_512` oracle.
- **Fixed `bench.py` verify bug:** `time_command()` returns `TimingStats`; guard now uses `.mean` (unblocks tier-1 verify CI).
- **Local fair harness (this host):** `matmul_naive` **1.111×** (green), `matmul_blocked` **1.287×** (still yellow).
- **Root cause blocked:** C `static` 512² buffers vs Li stack alloc (~6 MiB); ~20–29% codegen/storage gap remains.
- **Near-threshold greens:** `num_integ_rk4` 1.083×, `simd_dot` 1.052×, `fft_1d_fixed` 1.007× — monitor only.
- **Ingest:** ran `ingest-lic.sh`; dashboard unchanged until full tier-1 CI CSV lands on ingest host.

## Deliverable / findings

### `benchmark-failures-report.sh` (dashboard, pre-ingest refresh)

```
RED: none
YELLOW: matmul_blocked 1.244×, matmul_naive 1.222×
GREEN near threshold: num_integ_rk4 1.083×, simd_dot 1.052×, fft_1d_fixed 1.007×
green_count: 20
```

### Local tier-1 repro (`4ddbd3c6`, verify ok)

```bash
export LIC=$LIC_ROOT/build/compiler/lic/lic
cd lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_naive,matmul_blocked --runs 10
```

| benchmark | cpp (s) | li (s) | ratio | verify | dashboard |
|-----------|---------|--------|-------|--------|-----------|
| `matmul_naive` | 0.0018 | 0.0020 | **1.111×** | ok | 1.222× yellow |
| `matmul_blocked` | 0.0087 | 0.0112 | **1.287×** | ok | 1.244× yellow |

### lic changes this pass

| file | change |
|------|--------|
| `benchmarks/tier1_micro/matmul_blocked/li/main.li` | Revert to `mm_blocked_512` fair oracle (undo PR #524 tile-reps cheat) |
| `benchmarks/harness/bench.py` | Fix `TimingStats` vs float in verify DCE guard |
| `docs/numerics/studies/2026-05-30-bench-improver-yellow-matmul.md` | Pass-2 evidence |

### Quality table

| Axis | Before (dashboard) | After (local fair harness) |
|------|-------------------|----------------------------|
| **Speed** `matmul_naive` | yellow 1.222× | **1.111×** (within cap) |
| **Speed** `matmul_blocked` | yellow 1.244× | 1.287× (gap widened vs fair workload) |
| **Accuracy** | verify ok | unchanged (checksums match C oracle) |
| **Stability** | tier-0 skip | not touched |

## Recommended issues/PRs

| Repo | Title | Labels |
|------|-------|--------|
| **lic** | `fix(bench): TimingStats.mean in tier-1 verify DCE guard` | `PH-5b`, `benchmarks` |
| **lic** | `revert(bench): matmul_blocked fair mm_blocked_512 harness (undo #524 cheat)` | `PH-7e`, `PH-5b`, `numerics` |
| **lic** | `perf(7e): hoist ≥512² bench matrices to BSS (matmul_blocked ≤1.2×)` | `PH-7e`, `PH-5b`, `numerics` |
| **lic** | `perf(7e): matmul init — constexpr LUT / int→float (Phase 2i)` | `PH-2i`, `PH-7e` |
| **benchmarks** | `chore: ingest tier-1 after lic matmul BSS + verify-fix PRs` | `benchmarks` |
| **li-math** | `feat: tier-1 ml_conv2d / ml_mlp_* kernels (not matmul stubs)` | `PH-5b`, `PH-ML` |

## Deferred

- **BSS/static hoist** for 512² matrices — requires `ArraySlot` → `llvm::Value*` migration in codegen (~42 call sites).
- **PR #524 tile-reps harness** — do not merge; fails verify and misreports vs C oracle.
- **`md_thermostat_*` tier-2 yellow** (~1.29×) — shared C MD kernel; separate pass.
- **Lean proof** for any `parallel for` / OpenMP in matmul emit.
- **Self-merge / GHA cron** — out of scope.
- **Full dashboard green refresh** — blocked on CI tier-1 ingest with lic PRs above.
