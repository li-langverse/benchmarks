# Bench improver — coord_numerics pass (2026-05-30)

**Run:** `bench_improver` · **heap:** `coord_numerics:b97ff11f0fa3e1dd6357`  
**north_star_fit:** blazingly-fast — PH-5b / PH-7e tier-1 lic harness  
**lic workspace:** `bench_improver-1780146968205` @ `79da702b`

## Executive summary

- **Dashboard RED: none** (`summary.json` @ 2026-05-30T09:25Z); briefing six reds were **stale** vs current ingest.
- **Yellow (2):** `matmul_blocked` **1.244×**, `matmul_naive` **1.222×** — pure-Li, need ≤1.2× for green.
- **Prior fix landed:** PR #543 — `mm_naive_256` / `mm_blocked_512` MIR + blocked IKJ emit (`79da702b`).
- **Local harness:** `matmul_naive` ~1.17–1.22×, `matmul_blocked` ~1.25–1.31× (verify ok).
- **Codegen experiments** (256 IKJ-only routing, prefetch, BSS hoist) did not beat baseline; not merged.
- **Blocked gap driver:** C `static` 512² buffers vs Li stack alloc + LUT init overhead.
- **Do not** edit `benchmarks/data/latest/summary.json` manually; use `ingest-lic.sh`.
- **Next:** lic PR for BSS hoist + ingest refresh.

## Deliverable / findings

### `benchmark-failures-report.sh` (2026-05-30)

```
RED: none
YELLOW: matmul_blocked 1.244×, matmul_naive 1.222×
GREEN near threshold: num_integ_rk4 1.083×, simd_dot 1.052×, fft_1d_fixed 1.007×
```

### Before / after vs briefing audit

| benchmark | briefing ratio | dashboard now | status |
|-----------|----------------|---------------|--------|
| `horner_pure_li` | red 3.0× (stale) | green ~0.80× | fixed (prior ingest) |
| `matmul_naive` | red 1.333× | yellow 1.222× | improved, not green |
| `matmul_blocked` | red 1.549× | yellow 1.244× | improved, not green |
| `ml_*` ×3, `num_gmres` | red | not in current tier-1 failure set | verify on full CI |

### lic study

`lic/docs/numerics/studies/2026-05-30-bench-improver-yellow-matmul.md`

## Recommended issues/PRs

| Repo | Title | Labels |
|------|-------|--------|
| **lic** | `perf(7e): hoist ≥512² bench matrices to BSS (matmul_blocked ≤1.2×)` | `PH-7e`, `PH-5b`, `numerics` |
| **lic** | `perf(7e): matmul init — constexpr LUT / int→float (Phase 2i)` | `PH-2i`, `PH-7e` |
| **benchmarks** | `chore: ingest tier-1 after lic matmul BSS PR` | `benchmarks` |
| **li-math** | `feat: tier-1 ml_conv2d / ml_mlp_* kernels (not matmul stubs)` | `PH-5b`, `PH-ML` |

## Deferred

- Strict ≤1.2× for `matmul_blocked` without BSS/static parity or emit micro-kernel.
- `md_thermostat_berendsen` / `md_thermostat_nose_hoover` tier-2 yellow (~1.29×) — shared C MD; separate pass.
- Lean proof for any `parallel for` / OpenMP in matmul emit.
- Self-merge / cron — out of scope.
