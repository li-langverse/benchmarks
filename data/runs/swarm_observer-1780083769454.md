# Swarm observer report — `orch-r4-ui-ux-signals`

> `swarm_observer` · research goal `swarm_coverage` · 2026-05-29 · run `swarm_observer-1780083769454`

## Executive summary

- **Posture: degraded.** Ecosystem grade **D** (67.6); `unattended_safe: false` — swarm cannot run fully unattended.
- **Gap orchestration (orch-r4): complete** — ingested **6** new `ui_ux` gaps; patched studio-ui plan with wave2 orchestrator todos; closed orch-r4 plan_debt row.
- **Studio UI wave1:** All 11 plan todos complete per `lic-studio-ui/data/studio-ui-ux-plan-loop/daily-snapshot.json` (`ux_pass: true`, avg 2.45/3).
- **UX debt:** UX-01/08/12 below 2.5 threshold; bench reports `honest_simulate: false` on viewport FPS — native path still stub-heavy.
- **gui_ux_tester:** Last successful audit path blocked — agent searched `benchmarks/` root (19.6h SDK error); `ux-harness/` lives under `lic-studio-ui`.
- **Swarm execution:** 94 runs stuck `running`; 27% error rate on 26 terminal runs; batch supersede at 19:33Z left 7+ agents `error`.
- **Goal-directed:** 6/8 runners stopped; `studio-ui-ux` supervisor off; `httpd` active with 2 perf todos pending.
- **Credentials:** `CURSOR_API_KEY` and `GH_TOKEN` present — SDK auth not the blocker this cycle.

## Findings

| Agent / area | Symptom | Evidence | Severity |
|--------------|---------|----------|----------|
| Swarm SDK | 94 runs `running` (stuck / superseded) | `ecosystem-quality-report.json`, `li-cursor-agents/data/runs` | critical |
| Swarm SDK | 27% terminal error rate | `benchmarks/data/latest/ecosystem-quality-report.json` | critical |
| `gui_ux_tester` | Wrong repo cwd; 19.6h SDK stall | `li-cursor-agents/data/runs/gui_ux_tester-1779918664551.json` | high |
| `studio_ui_ux_builder` | Repo routing confusion (lic vs lic-studio-ui) | `swarm_observer-orch-r4-stuck-run-finalize.md` | medium |
| Studio UX | UX-01/08/12 scores 2.0–2.2 | `lic-studio-ui/.../daily-snapshot.json` | medium |
| Gap registry | 0 → 6 `ui_ux` rows | `lic/data/swarm-gap-registry/registry.yaml` | low (resolved) |
| Control plane | Latest report stale (2026-05-25), `swarm_health.healthy: true` | Supabase `control_plane_reports` | medium |
| Observer | `retry_counts` empty despite error streak | `control_plane_state` payload | medium |
| Goal-directed | 6 runners stopped | `lic/data/goal-directed-agents/snapshot.json` | high |
| Preflight | org agent-kit + CWE audit fail | `agent-briefing.preflight_runs` | high |
| Benchmarks | 6 red tier-1 rows | `agent-briefing.ecosystem_audit.benchmarks.red` | high |
| Batch dispatch | 7 agents errored ~19:33–19:35Z | `agent_runs` DB | high |

## Self-heal actions taken

Programmatic observer (this cycle):

- `observer.last_scan_at`: 2026-05-29T19:40:09Z
- `retry_counts`: `{}` — no auto-retries fired
- `stopped_agents`: `[]`

Meta-audit (this run):

- Regenerated `ecosystem-quality-report.json` (67.6, grade D)
- Ran `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`
- Added 6 `ui_ux` registry rows; patched 6 studio plan todos
- Closed orch-r4 plan_debt gap; completed swarm-observer backlog todo
- Updated `research-goals.yaml` `ui_ux_quality.handoff_to`
- Orchestrator note: `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r4-ui-ux-signals.md`

## Recommended control-plane fixes

| Fix | Path |
|-----|------|
| Route `gui_ux_tester` cwd to `lic-studio-ui` per `implement-goals.yaml` | `li-cursor-agents/config/implement-goals.yaml`, agent worker pool |
| Finalize superseded `running` runs on new dispatch | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Increment `observer.retry_counts` on SDK error streak | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports.is_latest` each supervisor tick | control-plane report writer |
| Ingest `ui_ux` from studio daily-snapshot in gap-ingest | `lic/scripts/swarm-gap-ingest.py` (automate orch-r4) |

## Human-only blockers

- Merge governance PRs (`trusted.lean`, master-plan phase claims)
- Studio native capture CI deps (#399) — needs libsdl2-dev / headless chrome on runner image
- Product UX work on viewport wgpu grid and error strip (proof-before-perf; no unproved shortcuts)

## Agent deliverable checklist

- [x] Ecosystem quality grade refreshed
- [x] Gap ingest + apply-actions
- [x] 6 `ui_ux` gaps registered and plan-patched
- [x] Orchestrator note (`orch-r4`)
- [x] Registry orch-r4 closed; plan backlog orch-r4 completed
- [x] Report under `benchmarks/data/runs/`
- [ ] PR on `li-cursor-agents` for gui_ux_tester cwd fix (recommended)

**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r4-ui-ux-signals.md`

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| fix(agents): route gui_ux_tester to lic-studio-ui ux-harness cwd | li-cursor-agents | `control-plane`, `ui-ux` |
| feat(studio-ui): studio-ux-13 agentic_ai SOTA refs (#398) | lic / lic-studio-ui | `studio-ui`, `ui-ux` |
| feat(studio-ui): studio-ux-16 CI capture deps (#399) | lic / lic-studio-ui | `studio-ui`, `ci` |
| chore(gap-ingest): auto-ingest studio daily-snapshot ui_ux scores | lic | `orchestration` |
| fix(observer): finalize superseded running SDK runs | li-cursor-agents | `control-plane`, `swarm` |

## Deferred

- Wave2 product implementation (viewport wgpu, native error strip) — `studio_ui_ux_builder`
- `implement-goals.yaml` agent swap gui_ux_tester → studio_ui_ux_builder for build lane
- Observer PR with `npm test` evidence (control-plane fixes batch)
- Re-enable goal-directed runners (`install-goal-plan-loop-systemd.sh` retired; use control plane only)
