# Swarm observer digest — `swarm_observer-1780133166367`

**Date:** 2026-05-30T09:30Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T08:04Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (68.8, grade **D**)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md`

---

## Executive summary

- **Posture: degraded** — ecosystem grade **D** (68.8/100); `unattended_safe: false`.
- **Unattended operation: not safe** — 65 open swarm gaps, 6/9 goal-directed runners stopped, 101/120 sampled runs still `running` (SDK terminalization drift).
- **`orch-r2-competitor-stubs` completed** — resolved `registry.yaml` merge conflicts; 30 `competitor_feature` rows; 12 `verticals.toml` stubs; apply patched sim-md-research backlog.
- **Briefing alignment OK** — heap `coord_numerics` P20 (`bench_improver`, `numerics_researcher`) matches 6 red benchmarks; P0 `proof_gap_researcher` + `workspace_sweeper` in recommended list.
- **Programmatic observer idle** — CP `observer.retry_counts: {}`; no auto-retries despite 7732 `error` runs in 24h.
- **Critical fix this cycle** — `registry.yaml` UU conflicts blocked YAML ingest until resolved (orchestration self-heal).
- **CURSOR_API_KEY / GH_TOKEN** — both set; SDK auth not the primary blocker.
- **Deferred** — `orch-r3-missing-package-sweep`, `orch-r4-ui-ux-signals`.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Gap registry | Merge conflicts blocked ingest | `lic/data/swarm-gap-registry/registry.yaml` (UU → fixed) | **critical** |
| Swarm (aggregate) | Grade D; not unattended-safe | `data/latest/ecosystem-quality-report.json` | high |
| Gap backlog | 65 open (30 competitor, 32 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/9 stopped; 37 plan todos pending | `lic/data/goal-directed-agents/snapshot.json` | high |
| Benchmarks | 6 red rows (matmul_*, ml_*, num_gmres) | `agent-briefing.json` → `ecosystem_audit.benchmarks.red` | high |
| CP run hygiene | 7732 errors / 24h; 21 `running` in DB now | Supabase `agent_runs` | high |
| `bench_improver` | SDK error after 682s; partial push | `bench_improver-1780130199108.json` | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25 | `control_plane_reports` is_latest | medium |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| `workspace_sweeper` | systemctl restart failed | sweep digest in run input | low |

### Error sample classification

| Pattern | Example | Root cause |
|---------|---------|------------|
| SDK timeout / error after long run | `bench_improver-1780130199108` (682s, 146 tools) | **SDK terminal error** — work partially pushed via post-hook |
| Simultaneous errors @ tick | 6 agents @ 09:14Z | **Supervisor wave** — empty `retry_counts` |
| `code_implementer` tools=0 | prior tick | **SDK preemption** |
| Historical error volume | 400–518 errors/agent lifetime | **Status reconciliation** + retry without cap |

### Recommended agents vs recent runs (goal drift)

| Briefing recommended | Heap priority | Recent CP activity (09:15–09:26Z) | Drift? |
|---------------------|---------------|-----------------------------------|--------|
| `bench_improver` | P20 numerics | running + historical errors | aligned |
| `numerics_researcher` | P20 | few runs | under-dispatched |
| `plan_verifier` | P30 | running | aligned |
| `gap_explorer` | P40 | 17 lifetime errors | aligned |
| `workspace_sweeper` | P0 | error @ 09:14, re-dispatched | aligned (dirty repos) |
| `proof_gap_researcher` | research P9 | running | aligned |

No major goal-orientation drift; numerics_researcher could use more dispatch given 6 red rows.

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (68.8, grade D)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply after conflict fix
- [x] `orch-r2-competitor-stubs` orchestrator note + registry/backlog close
- [x] Recommended agents vs heap compared
- [x] Error runs sampled + classified
- [ ] `orch-r3-missing-package-sweep` (deferred)
- [ ] `orch-r4-ui-ux-signals` (deferred)

---

## Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state` | `last_scan_at: 2026-05-30T09:26:23Z`; **retry_counts: {}** |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | **Failed** until conflicts fixed; then 92 gaps, 65 open |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | Live write @ 09:30Z; 9 vertical patches |
| Meta (this pass) | `swarm_observer` | Resolved `registry.yaml` UU; closed orch-r2 |

No programmatic SDK retries this cycle despite error streaks.

---

## Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Pre-commit: reject conflict markers in `registry.yaml` | `lic/scripts/swarm-gap-ingest.py` or `.git/hooks` |
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports` swarm_health | `li-cursor-agents/src/supervisor/` |
| Enable retry budget for wave-killed agents | `src/observer/` retry_counts |
| Merge `verticals.toml` to benchmarks main | human PR; closes `gap-infra-verticals-toml-missing-benchmarks-main` |
| Full preflight (drop `--skip-slow`) for unattended | `benchmarks/scripts/build-agent-briefing.py` |

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| chore(swarm-gap): merge verticals.toml to benchmarks main | `benchmarks` | `ecosystem`, `agent:gap_explorer` |
| fix(observer): terminalize SDK runs stuck in `running` | `li-cursor-agents` | `control-plane`, `agent:swarm_observer` |
| chore(registry): prevent merge conflicts in swarm-gap-registry | `lic` | `ecosystem`, `agent:workspace_sweeper` |
| perf(tier-1): matmul_blocked / matmul_naive ≤1.2× cpp | `lic` | `PH-7e`, `agent:bench_improver` |
| research: competitor vertical stubs → sim-md oracle plan | `lic` | `agent:numerics_researcher` |

Do not auto-merge governance or provability PRs.

---

## Human-only blockers

- **Registry merge conflicts** — caused concurrent agent/sweeper edits; fixed manually this pass
- **133 branches without PR** — `pr_branch_opener` scale; human triage for duplicates
- **Goal-directed runners stopped** — 6/9; requires systemd/user session or control-plane goal enablement
- **Governance** — roadmap / `trusted.lean` changes need human review

---

## Deferred

- `orch-r3-missing-package-sweep` — std.summary, std.plot, line_profiler
- `orch-r4-ui-ux-signals` — studio-ui-ux pending todos (ux-16, ux-17)
- Stuck-run mass relabeling in Supabase (needs observer PR + migration policy)
- Full slow preflight refresh (`plan_audit`, `ci_bug_triage`, …)

---

## Gap orchestration summary (`swarm_coverage`)

| `gap_kind` | Open | Primary handoff |
|------------|-----:|-----------------|
| `competitor_feature` | 30 | `numerics_researcher`, `bench_improver`, `gap_explorer` |
| `plan_debt` | 32 | `plan_verifier`, per-runner backlogs |
| `missing_package` | 3 | `issue_planner`, `package_architect` |

Evidence: `lic/data/swarm-gap-registry/registry.yaml`, `benchmarks/data/latest/swarm-gap-actions.json`, orchestrator note above.

---

_Formatted by swarm_observer · 2026-05-30T09:30Z_
