# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780149270137`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**Generated:** 2026-05-30T14:02Z  
**north_star_fit:** ecosystem pull-requests — PH-5b / PH-7e (tier-1 matmul, proof-before-perf); PH-DB deferred (`plan-needed` without `plan-approved`).

## Executive summary

- **GitHub GraphQL rate limit exceeded** — `gh pr view/list` and `pr-merge-gate.py` return `pr_not_found`; REST unauthenticated API works for per-PR verification.
- **~225 open org PRs** (REST search); preflight JSON **false empty** — `pr-merge-queue-plan.json` (14:02Z) and `pr-program-run.json` report `open_prs: 0` after GraphQL timeout/failure.
- **0 mergeable non-draft PRs** org-wide (REST `is:mergeable` search); **0 gate-ready** — no `merge-approved` label added this run.
- **Canonical tier-1 matmul:** [lic#437](https://github.com/li-langverse/lic/pull/437) — `merge-approved` retained; **blocked** by merge conflicts (`dirty`) + human APPROVED.
- **Closed since last pass:** lic#541, #542, #544 (duplicate matmul/Pages stack); [lit#19](https://github.com/li-langverse/lit/pull/19) **merged** (Pages CI fix).
- **7 open `merge-approved` PRs** — lic#437, #530–532 (draft), benchmarks#132, roadmap#19/#21; all blocked by conflicts, draft status, or governance policy.
- **5 feature PRs** carry `plan-needed` without `plan-approved` (lic#489/#517/#495, lis#20, li-httpd#10).
- **0 merges, 0 labels** — policy: no PR passes all gates + human APPROVED + conflict-free.

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-merge-queue-plan.json` | ❌ False empty | Generated 14:02Z; `open_prs: 0` after >5 min run (GraphQL rate limit) |
| `pr-program-run.json` | ❌ Stale / false empty | `open_prs: 0` at 12:52Z; `run-pr-program.py` exit 124 at 120s |
| REST org search | ✅ | ~225 open PRs; 0 mergeable; 7 `merge-approved` |
| `pr-merge-gate.py` (per-PR) | ❌ Blocked | GraphQL rate limit → `pr_not_found` for all candidates |
| Control plane | ⚠️ | Current run `running`; 4+ prior `pr_reviewer` runs `error` (GraphQL/preflight) |

### Merge queue snapshot (REST-derived)

| Metric | Value |
|--------|-------|
| Open PRs (REST) | ~225 |
| `merge-approved` (labeled) | 7 |
| Gate-ready | 0 |
| Mergeable (non-draft) | 0 |
| `merge_first` / `next_merge` | null (preflight empty) |

### Standards review (REST-verified)

#### Tier-1 matmul / PH-5b / PH-7e (lic)

| PR | State | Mergeable | Labels | Verdict |
|----|-------|-----------|--------|---------|
| [#437](https://github.com/li-langverse/lic/pull/437) | open | ❌ dirty | `merge-approved` | **Retain canonical** — rebase + human APPROVED → `pr_merger` |
| [#499](https://github.com/li-langverse/lic/pull/499) | open | ❌ dirty | `numerics-research` | **Defer** — CI/gate unknown; overlaps #437 stack |
| [#520](https://github.com/li-langverse/lic/pull/520) | open | ❌ dirty | — | **Defer** — blocked matmul emit; merge after #437 |
| #541 / #542 / #544 | **closed** | — | — | Superseded — no action |

**Checklist (#437 — feature perf):**

| Gate | Verdict |
|------|---------|
| Vision / PH | ✅ PH-5b, PH-7e |
| Strict by default | ✅ lowering paths; no trusted creep cited |
| Security | N/A (codegen surface) |
| Performance | ✅ tier-1 ≤1.2×; no `catalog.toml` weakening |
| Release notes | ✅ (prior review) |
| Ecosystem-first | Post-merge benchmarks ingest for 6 red rows |

#### Draft plan PRs (lic#530–532)

| PR | State | Mergeable | Blockers |
|----|-------|-----------|----------|
| [#530](https://github.com/li-langverse/lic/pull/530) | draft | ✅ clean | Undraft + human APPROVED + release notes check |
| [#531](https://github.com/li-langverse/lic/pull/531) | draft | ✅ clean | same |
| [#532](https://github.com/li-langverse/lic/pull/532) | draft | ✅ clean | same |

Label present but **draft blocks gate** — undraft before merge sweep.

#### benchmarks#132 — macOS CI fix

| Gate | Verdict |
|------|---------|
| CI | Unknown (GraphQL blocked) |
| Scope | CI profile fix — aligned |
| Labels | `merge-approved` |
| Blockers | `mergeable_state: unknown`; human APPROVED |
| Verdict | APPROVED path after CI confirm + mergeable check |

#### roadmap (governance)

| PR | Mergeable | `merge-approved` | Blockers |
|----|-----------|------------------|----------|
| [#19](https://github.com/li-langverse/roadmap/pull/19) | ❌ dirty | yes | APPROVED + rebase + **human merge only** |
| [#21](https://github.com/li-langverse/roadmap/pull/21) | ❌ dirty | yes | same |

#### Feature / plan blocks

| PR | Verdict |
|----|---------|
| [lic#489](https://github.com/li-langverse/lic/pull/489) | **needs plan** — `plan-needed` without `plan-approved`; parent of stack #497 |
| [lic#517](https://github.com/li-langverse/lic/pull/517) | **needs plan** — Studio GPU decorators (PH-7d) |
| [lic#495](https://github.com/li-langverse/lic/pull/495) | **needs plan** — PH-CAD slice |
| [lis#20](https://github.com/li-langverse/lis/pull/20) | **needs plan** — PH-DB |
| [li-httpd#10](https://github.com/li-langverse/li-httpd/pull/10) | **needs plan** + CI |

### Actions taken this run

- **0 merges** (policy).
- **0 new labels** — GraphQL blocked gate verification; no PR fully aligned + APPROVED + conflict-free.
- Preflight: `pr-merge-queue-plan.py` completed with false-empty output; `run-pr-program.py` timed out (120s).
- Verification via REST API for priority candidates (see above).

### Error

```
GitHub GraphQL API rate limit exceeded for user ID 207167228.
gh pr view / pr-merge-gate.py return pr_not_found for all candidates.

pr-merge-queue-plan.py and run-pr-program.py produce open_prs: 0 (false negative)
when GraphQL org sweeps fail or timeout.

Workaround: unset GH_TOKEN; use curl api.github.com (REST) for per-PR status.
Recommend: REST fallback + rate-limit backoff in pr-merge-queue-plan.py / run-pr-program.py.
```

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | **lic** [#437](https://github.com/li-langverse/lic/pull/437) | Rebase → human APPROVED → `pr_merger` (label retained) |
| fix(ci): macOS nightly tier-1/2 core profile | **benchmarks** [#132](https://github.com/li-langverse/benchmarks/pull/132) | Confirm CI green → APPROVED (label already on) |
| docs(plan): P-linalg / for-range / NumPy broadcast | **lic** [#530](https://github.com/li-langverse/lic/pull/530)–[#532](https://github.com/li-langverse/lic/pull/532) | Undraft → APPROVED → merge-approved sweep |
| docs(ecosystem): point compiler to lic (WP-A5) | **roadmap** [#19](https://github.com/li-langverse/roadmap/pull/19) | Rebase → **human** APPROVED + merge |
| docs(ecosystem): httpd mirror canonical (WP-B4) | **roadmap** [#21](https://github.com/li-langverse/roadmap/pull/21) | Rebase → **human** APPROVED + merge |
| feat(httpd): gap-phase2 perf wrk soak | **lic** [#489](https://github.com/li-langverse/lic/pull/489) | Add `plan-approved`; merge before #497 |
| REST/rate-limit fallback in pr-program / queue-plan | **benchmarks** | Prevent false `open_prs: 0` on GraphQL exhaustion |
| Batch-close redundant agent digest PRs | **benchmarks**, **lic**, **lis** | Human triage after queue-plan refresh |

## Deferred

- **GraphQL rate-limit recovery** — wait for reset before org-wide gate sweep or `pr_merger` tick.
- **Full preflight refresh** — re-run `run-pr-program.py` + `pr-merge-queue-plan.py` after rate limit clears.
- **Canonical matmul rebase** — lic#437 conflicts with main; #499/#520 defer until #437 lands.
- **Post-merge ingest** — refresh 6 dashboard red rows (`matmul_*`, `ml_*`, `num_gmres`) after lic numerics merges.
- **PH-DB / httpd feature stacks** — until `plan-approved` + dependency order (`lic#489` before `#497`).
- **Agent digest backlog** — dedupe workspace-sweep PR cluster once queue-plan `redundant[]` repopulates.
- **li-language agent-kit** — org-agent-kit audit exit 1 (`missing_kit`).
