# PR merger — proactive ecosystem sweep

**Run id:** `pr_merger-1780152649767`  
**Agent id:** `pr_merger`  
**Source:** proactive  
**Generated:** 2026-05-30T14:58Z  
**north_star_fit:** ecosystem pull-requests — PH-5b / PH-7e (tier-1 matmul, proof-before-perf); package CI wave before lic numerics.

## Executive summary

- **No merge this run** — `merge_plan.next_merge` and `merge_first` are **null**; `merge_sequence` is **empty** (`open_prs: 0` in plan JSON).
- **`LI_AUTO_MERGE=0`** — dry-run only; real merge blocked by supervisor policy.
- **GraphQL rate limit exhausted** (5000/5001 used; reset ~**2026-05-30T15:38Z**) — `gh pr list` / gate scripts hang or return false empty; prior REST snapshot (~225 open org PRs) is authoritative until reset.
- **`pr-merge-queue-plan.py`** refreshed plan at 14:09Z; re-run at 14:51Z **still running** (>7 min, no output) — same quota stall.
- **`pr-auto-merge-sweep.py --use-plan`** dry-run **not completed** (hung on GraphQL); per-PR `pr-auto-merge.py --dry-run` **skipped** — requires `--repo` + `--pr`; no `next_merge` candidate.
- **0 gate-ready, 0 mergeable non-draft** org-wide (REST-derived from `pr_reviewer-1780149270137` at 14:02Z); **7 `merge-approved`** PRs all blocked (conflicts, draft, or governance).
- **Vision order unchanged:** package CI / mirrors → benchmarks → lic → lip/lit/lis → roadmap (roadmap never auto-merge).
- **Canonical numerics path:** [lic#437](https://github.com/li-langverse/lic/pull/437) — `merge-approved`, **CONFLICTING/dirty**; rebase required before any merge tick.

## Deliverable / findings

### Merge queue plan (`data/latest/pr-merge-queue-plan.json` @ 2026-05-30T14:09Z)

| Field | Value |
|-------|-------|
| `next_merge` | null |
| `merge_first` | null |
| `merge_sequence` | [] |
| `merge_order` | [] |
| `open_prs` | 0 (false negative — GraphQL) |
| `merge_approved` | 0 |
| `gate_ready` | 0 |
| `stacks` | 0 |
| `redundant` | 0 |
| `pair_risks` | absent (empty plan) |
| `repo_merge_plans` | absent (empty plan) |

**Ordering rules (active when queue repopulates):**

1. Repo tier: mirrors/httpd → benchmarks → lic → lip/lit/lis → roadmap  
2. Title hints: `ci.yml` / blocker fixes before agent-kit / bench / feature work  
3. Stacks: parent branch PR before child  
4. Redundant pairs: close superseded PR after including PR merges  
5. Only `merge-approved` + gate-ready PRs enter `merge_sequence`  
6. Supervisor: one merge per tick; re-plan after each merge  

### Auto-merge dry-run

| Step | Result |
|------|--------|
| `pr-auto-merge.py --dry-run` (no args) | **Error:** `--repo` and `--pr` required |
| `pr-auto-merge-sweep.py --use-plan` | **Hung** (>5 min, GraphQL quota) |
| `LI_AUTO_MERGE=0` evaluation | **blocked** · `dry_run_only=true` |
| **Blocked reason** | no `next_merge` or `merge_first` in merge_plan |

**Would-merge report:** none — no candidate passed plan + gate + non-CONFLICTING checks.

### REST-derived queue state (last reliable: `pr_reviewer-1780149270137`)

| Metric | Value |
|--------|-------|
| Open org PRs | ~225 |
| `merge-approved` | 7 |
| Gate-ready | 0 |
| Mergeable (non-draft) | 0 |
| CONFLICTING / dirty | lic#437, lip#43, roadmap#21, benchmarks#132, + others |

#### Blocked `merge-approved` PRs (no merge until gates pass)

| PR | Blocker | Rebase / action |
|----|---------|-----------------|
| [lic#437](https://github.com/li-langverse/lic/pull/437) perf(7e) tier-1 matmul | CONFLICTING + human APPROVED pending | `git fetch origin && git checkout <branch> && git merge origin/main` — resolve keeping main + PH-7e work |
| [lic#530–532](https://github.com/li-langverse/lic/pull/530) plan docs | **draft** | Undraft → human APPROVED |
| [benchmarks#132](https://github.com/li-langverse/benchmarks/pull/132) macOS CI | mergeable unknown (GraphQL) | Re-run gate after quota reset |
| [roadmap#19](https://github.com/li-langverse/roadmap/pull/19), [#21](https://github.com/li-langverse/roadmap/pull/21) | **governance** — human merge only | Never auto-merge without `--allow-governance` + human clearance |

#### Package CI first (vision P0 when mergeable)

| PR | Notes |
|----|-------|
| [lip#43](https://github.com/li-langverse/lip/pull/43) fix(ci) lis main checkout | Aligned; blocked mergeable — rebase before merge wave |

#### Overlap / stack (defer until parent lands)

- **lic#437** canonical for tier-1 matmul; lic#499/#520 overlap — merge #437 first, then rebase children.
- Superseded stack lic#541/#542/#544 **closed** — no redundant merge pair this tick.

### Merges executed

**None.** Policy: at most one PR per run via `next_merge` only; queue empty + `LI_AUTO_MERGE=0`.

### Repos needing rebase (after next successful merge on main)

When lip#43 or lic#437 eventually merge, re-plan; expect **lic**, **benchmarks** digest cluster, and **lip/lit** package CI PRs to need `origin/main` integration.

### Control plane

| run_id | agent_id | status | started_at |
|--------|----------|--------|------------|
| pr_merger-1780152649767 | pr_merger | running | 2026-05-30T14:50:58Z |
| pr_merger-1780152581177 | pr_merger | error | 2026-05-30T14:50:00Z |

Prior `pr_merger` ticks errored in the same GraphQL window.

### Error

```
GraphQL API rate limit exceeded (graphql used 5001/5000; core 5000/5000).
gh pr list → "GraphQL: API rate limit already exceeded for user ID 207167228."
pr-merge-queue-plan.py and pr-auto-merge-sweep.py hang waiting on gh.
Plan JSON reports open_prs=0 — false negative. Reset ~2026-05-30T15:38:48 UTC.

pr-auto-merge.py --dry-run (no --repo/--pr):
  usage: pr-auto-merge.py [-h] --repo REPO --pr PR ...
  pr-auto-merge.py: error: the following arguments are required: --repo, --pr
```

**Recovery:** After GraphQL reset, re-run:

```bash
cd benchmarks
python3 scripts/pr-merge-queue-plan.py
cat data/latest/pr-merge-queue-plan.json | jq '.next_merge, .repo_merge_plans[0], .pair_risks[:2]'
python3 scripts/pr-auto-merge-sweep.py --use-plan          # dry-run default
python3 scripts/pr-auto-merge.py --repo lip --pr 43        # if plan names lip#43 first
```

For CONFLICTING lic#437, integrate main locally before merge gate:

```bash
cd ../lic
git fetch origin
git checkout <pr-437-branch>
git merge origin/main
# resolve conflicts per docs/ecosystem/merge-conflict-resolution.md
git push origin HEAD
python3 ../benchmarks/scripts/pr-merge-gate.py --repo lic --pr 437 --json
```

## Recommended issues/PRs

| Priority | Repo | Item | Labels / notes |
|----------|------|------|----------------|
| P0 | lip | [#43](https://github.com/li-langverse/lip/pull/43) fix(ci) lis main checkout | Rebase → human APPROVED → first in vision order when gate-ready |
| P0 | roadmap | [#19](https://github.com/li-langverse/roadmap/pull/19), [#21](https://github.com/li-langverse/roadmap/pull/21) | `merge-approved` — **human merge only** (governance) |
| P1 | lic | [#437](https://github.com/li-langverse/lic/pull/437) tier-1 matmul | `merge-approved` — rebase off main, resolve CONFLICTING, then pr_merger |
| P1 | lic | [#530](https://github.com/li-langverse/lic/pull/530)–[#532](https://github.com/li-langverse/lic/pull/532) plan docs | Undraft + APPROVED |
| P2 | benchmarks | [#132](https://github.com/li-langverse/benchmarks/pull/132) macOS CI | Re-gate after quota reset |
| Infra | benchmarks | REST fallback in `pr-merge-queue-plan.py` | Prevent false `open_prs: 0` when GraphQL exhausted |
| Hygiene | lic / benchmarks | 14 draft PRs in `pr_branch_hygiene` | Confirm abandoned before close (`prs_safe_close_now: 0`) |

## Deferred

- **All merges** until `merge_sequence` non-empty, gates green or `local-ci passed`, and `LI_AUTO_MERGE=1`.
- **Full queue refresh** after GraphQL reset (~15:38Z) — re-run `pr-merge-queue-plan.py` + `pr-auto-merge-sweep.py --use-plan`.
- **82 redundant pairs** (13:37Z stale plan) — human batch-close; no auto-close without confirmation.
- **benchmarks digest wave** (#204–#239 cluster) — dedupe after lic#437 numerics land.
- **Feature PRs** with `plan-needed` without `plan-approved` (lic#489/#517/#495, lis#20, li-httpd#10) — out of merge_sequence until plan gate clears.
- **roadmap** governance PRs — never auto-merge without explicit human intervention.
