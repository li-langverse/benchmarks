# Bench improver — proactive sweep (run 1780139342882)

**Agent:** `bench_improver` · **Source:** proactive ecosystem sweep  
**north_star_fit:** blazingly-fast — PH-5b / PH-7e tier-1 lic harness; proof-before-perf (checksum parity, no `trusted.lean` edits)  
**Dashboard:** https://li-langverse.github.io/benchmarks/  
**Oracle:** `data/latest/summary.json` @ `2026-05-30T10:00:49Z` · `lic_sha` `c18c48e6` · `benchmarks_sha` `83438d6`

## Executive summary

- **Public dashboard: no RED or YELLOW** tier-1 lic rows on linux; **22 greens** under the 1.2× catalog gate (`threshold_ratio_cpp`).
- **Five near-threshold greens** remain with ratio **>1.0×** cpp — headroom polish, not gate failures: `num_integ_rk4` (1.083×) is the largest slack.
- **Recent ingest delta:** 15 tier-1 rows flipped `skip → green` since prior snapshot (matmul, horner, integrators, solvers); tier-1 red crisis cleared on published CSV.
- **`horner_pure_li`** is **green at 0.75×** cpp on dashboard (pure-Li FMA path); prior red narrative is stale.
- **`tier0_stability`** still **unknown** — no `stability.csv` on this runner; tier-0 must run on CI, not hand-edited JSON.
- **Swarm gap registry** still lists stale `gap-benchmark-red-matmul-naive-tier1` / `num_gmres` — close or repoint after confirming dashboard greens.
- **`perf/bench-improver-matmul-tier1-green-20260530`** (lic) carries PH-7e emit work + study; **not merged to main** — open PR, then full ingest for N=1024 variants still red in flat table.
- HPC competitive registry gate **clear** (`lic/scripts/check-hpc-competitive.sh`).

## Deliverable / findings

### Failures report (`./scripts/benchmark-failures-report.sh`)

```
RED: none
YELLOW: none
GREEN near threshold (>1.0× cpp, 5):
  num_integ_rk4     1.083×  PH-5b
  matmul_naive      1.056×  PH-5b, PH-7e
  simd_dot          1.052×  PH-5b, PH-7e
  matmul_blocked    1.023×  PH-5b
  fft_1d_fixed      1.007×  PH-5b, PH-7e
UNKNOWN: tier0_stability (tier 0)
```

Fresh `python3 scripts/ecosystem-audit.py` (2026-05-30T11:10Z) matches: `red=[]`, `yellow=[]`, same near-threshold set.

### Ingested linux tier-1 rows (`results/latest.csv`)

| benchmark | ratio vs cpp | status | li (s) | cpp (s) | variant / notes |
|-----------|--------------|--------|--------|---------|-----------------|
| `num_integ_rk4` | **1.083×** | green | 0.0013 | 0.0012 | shared C kernel + lic wrapper |
| `matmul_naive` | **1.056×** | green | 0.0019 | 0.0018 | pure lic `@` N=256 |
| `simd_dot` | **1.052×** | green | 0.0181 | 0.0172 | shared C kernel |
| `matmul_blocked` | **1.023×** | green | 0.0090 | 0.0088 | pure lic `@` N=512 BK=64 |
| `fft_1d_fixed` | **1.007×** | green | 0.0153 | 0.0152 | FFTW reference path |
| `horner_pure_li` | **0.800×** | green | 0.0004 | 0.0005 | pure lic — li faster than cpp |

### Residual flat-table reds (non-primary sizes / repos)

The `summary.json` flat `benchmarks[]` table still carries **stale red ratios** for catalog size variants not in the current ingest CSV:

| benchmark | size | ratio | note |
|-----------|------|-------|------|
| `matmul_blocked` | N=512 (flat) | 1.549× | contradicts chart row (1.023×) — ingest size mismatch |
| `matmul_naive` | N=1024 | 1.333× | needs tier-1 bench + ingest after lic 7e merge |
| `ml_*` | stub | 1.333× | **li-math** repo, not lic codegen |

### lic branch ready for PR (not merged)

Branch: `perf/bench-improver-matmul-tier1-green-20260530`  
Study: `lic/docs/numerics/studies/2026-05-30-matmul-blocked-7e.md`

| Change | Purpose |
|--------|---------|
| `compiler/codegen/emit.cpp` | Blocked IKJ tiles, 4-wide FMA inner-`j`, 32-byte `ArrayAlloc` alignment |
| `matmul_naive/li/main.li`, `matmul_blocked/li/main.li` | `C = A @ B` pure-Li paths |
| Study claims (local median) | naive **~0.95×**, blocked **~1.0×** — pending CI ingest |

### Mode A numerics notes (near-threshold targets)

| benchmark | SOTA lever | lic path |
|-----------|-----------|----------|
| `num_integ_rk4` | Hairer RK4 fused stages; minimize temp alloc | RK4 loop in shared `common/*_core.c` + lic call overhead |
| `simd_dot` | BLIS dot micro-kernel / FMA reduction | `@vectorized` on reduction loop (PH-7d/7e) |
| `matmul_*` | Goto/BLIS IKJ + register blocking | PH-7e emit on `main` — merge perf branch |
| `fft_1d_fixed` | FFTW plan reuse | within noise; defer |

### Repro (assessment)

```bash
cd benchmarks
./scripts/benchmark-failures-report.sh
python3 scripts/ecosystem-audit.py

cd ../lic && ./scripts/check-hpc-competitive.sh

# After lic PR merge:
cd lic/benchmarks/harness && python3 bench.py --tier 1 --runs 10
cd benchmarks && LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh
```

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| `perf(7e): merge matmul blocked/naive emit (IKJ tiles, FMA SIMD, aligned alloc)` | **lic** | `PH-7e`, `PH-5b`, `numerics` — branch `perf/bench-improver-matmul-tier1-green-20260530` |
| `perf(7e): RK4 integrator loop — bring num_integ_rk4 ≤1.0× cpp` | **lic** | `PH-5b`, `numerics` — highest near-threshold row |
| `perf(7e): simd_dot shared-kernel wrapper — reduce call overhead ≤1.0×` | **lic** | `PH-7e`, `PH-5b` |
| `chore(swarm): close stale gap-benchmark-red-matmul-naive / num_gmres` | **lic** | `ecosystem-gap` — primary linux rows green on dashboard |
| `ci: tier0_stability ingest on linux runner` | **benchmarks** / **lic** | `PH-5b` — populate `stability.csv` |
| `chore(bench): ingest tier-1 N=1024 matmul variants after lic 7e merge` | **benchmarks** | `ingest`, `PH-5b` — via `ingest-lic.sh` only |

## Deferred

- **≤1.0× cpp advisory** for all tier-1 micro rows — optional polish after 1.2× gate is stable; prioritize `num_integ_rk4`, then matmul N=1024 flat-table reds.
- **`md_thermostat_*`** tier-2 yellow (~1.29×) — shared MD oracle; separate numerics pass (not in current tier-1 ingest).
- **`ml_conv2d_forward` / `ml_mlp_*`** — **li-math** stubs; not lic codegen.
- **Parallel/simd codegen** beyond matmul — requires Lean / contract evidence per master plan PH-7d.
- **Full `./scripts/run-full-benchmark-suite.sh` on CI** — partial `--only` ingest must not replace oracle rows.
