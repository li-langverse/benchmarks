# Swarm observer digest — `swarm_observer-1780135160479`

**Date:** 2026-05-30T10:00Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T09:46Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (80.0, grade **B**, refreshed)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md`

---

## Executive summary

- **Posture: recovering (operational degraded)** — refreshed grade **B** (80.0); `unattended_safe: true` on scorecard, but CP DB shows mass preemption errors and stale dashboard health.
- **Benchmarks improved** — briefing `ecosystem_audit.benchmarks.red: []`; tier-1 matmul rows **yellow** only (`matmul_blocked`, `matmul_naive`).
- **`orch-r2-competitor-stubs` completed** — 14 `verticals.toml` stubs + explorer catalog ingested; 9 vertical rows patched to `sim-md-research-backlog.md` via apply-actions.
- **Gap pressure high** — **65** open registry gaps (30 `competitor_feature`, 32 `plan_debt`, 3 `missing_package`); ingest/apply ran @ 09:59Z.
- **Supervisor wave @ 09:58Z** — 15+ agents `error` within 10s; root cause **SDK preemption** + concurrent tick (not `CURSOR_API_KEY` — set).
- **`workspace_sweeper` blocked** — GitHub **GraphQL rate limit** on push/PR for benchmarks, lic, li-cursor-agents.
- **Programmatic observer idle** — `observer.retry_counts: {}` @ 09:58Z; no auto-retries despite error burst.
- **CP report stale** — `control_plane_reports.swarm_health.healthy: true` from **2026-05-25**; dashboard under-reports live errors.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade B after refresh; gap + runner debt remains | `data/latest/ecosystem-quality-report.json` | medium |
| Gap registry | 65 open (30 competitor, 32 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Goal-directed runners | 6/9 stopped; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP error burst | 15 agents `error` @ 09:58:16–26Z | Supabase `agent_runs` | high |
| CP status hygiene | 119/120 sampled runs `running`; 0 terminal errors in scorecard window | `li-cursor-agents/data/runs`, scorecard `swarm_execution` | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25 | `control_plane_reports` is_latest | medium |
| `workspace_sweeper` | GH GraphQL rate limit; no PR opened | `workspace_sweeper-1780135096387.json` extra_instruction | high |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| Briefing vs heap | P0 research/sweep OK; heap lacks `bench_improver` despite yellow matmul | `agent-briefing.json` heap_plan vs ecosystem_audit | low |

### Error sample classification

| Pattern | Example | Root cause |
|---------|---------|------------|
| Simultaneous `error` @ tick | 15 agents @ 09:58:16Z | **Supervisor wave kill / SDK preemption** — `completion.error` null in CP |
| `run_started` only, status flip to error | `ecosystem_grader-1780135096381` | **Empty terminalization** — not repo conflict |
| GH rate limit | workspace_sweeper sweep digest | **Human-only** — wait for rate limit reset |
| Historical error volume | 300–500 errors/agent / 24h | **Status reconciliation drift** in CP ingest |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | 90 gaps total; idempotent verticals/catalog |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 20 patches incl. 9 vertical stubs → sim-md-research backlog |
| Observer scan | CP `control_plane_state.observer` | `last_scan_at: 2026-05-30T09:58:44Z`; **no retries** |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` | 80.0 grade B; `unattended_safe: true` |

No programmatic heal for 09:58Z error wave (`retry_counts: {}`, `stopped_agents: []`).

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Retry on wave-kill with backoff budget | `li-cursor-agents/src/observer/retry.ts` (or equivalent) |
| Refresh `control_plane_reports` swarm_health each tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Throttle concurrent SDK dispatches per tick | `li-cursor-agents/src/supervisor/` |
| GH rate-limit aware workspace_sweeper | `li-cursor-agents` workspace_sweeper prompt / sweep script |

### Human-only blockers

- GitHub **API rate limit** — workspace sweeper cannot open PRs until reset.
- **`verticals.toml` on benchmarks main** — merge from lic perf branch (governance).
- **6 goal-directed runners stopped** — may need systemd/user unit restart (not auto-merge).
- **11 PRs flagged** for close/supersede — `pr_alignment` human review.

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (80.0, grade B)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed
- [x] `orch-r2-competitor-stubs` orchestrator note + backlog patches
- [x] Recommended agents vs heap compared
- [x] Error runs sampled + classified
- [ ] `orch-r3-missing-package-sweep` (deferred — prior partial pass)
- [ ] `orch-r4-ui-ux-signals` (deferred)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Merge `verticals.toml` competitive registry to benchmarks main | `lic` → `benchmarks` | `ecosystem`, `competitor_feature`, `no-auto-merge` |
| Observer: terminalize stuck `running` runs after SDK TTL | `li-cursor-agents` | `control-plane`, `swarm_observer` |
| Refresh control_plane_reports swarm_health each supervisor tick | `li-cursor-agents` | `dashboard`, `observer` |
| Tier-1 matmul yellow → green (`matmul_blocked`, `matmul_naive`) | `lic` | `bench_improver`, `tier-1`, `PH-7e` |
| std.summary / std.plot package placement | `lic` | `gap_explorer`, `issue_planner`, `PH-IO` |
| Agent-kit drift: 9 repos | org repos | `agent_kit_maintainer` |

---

## Deferred

- `orch-r3-missing-package-sweep` — package backlog already patched; full sweep next observer pass.
- `orch-r4-ui-ux-signals` — studio-ux-16/17 incidentally patched; dedicated UI gap reconciliation later.
- Re-enable slow preflights (`plan_audit`, `ci_bug_triage`, `pr_program`) when tick budget allows.
- CP historical error relabeling (8k+ lifetime `error` rows) — batch job, not this pass.
