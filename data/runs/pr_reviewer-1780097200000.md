# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780097200000`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**north_star_fit:** ecosystem pull-requests / PH-5b, PH-7e — tier-1 numerics merge queue after proof-preserving codegen (proof → easy → fast).

## Executive summary

- Live org scan: **117** open PRs, **82** CI-green (non-draft); preflight `pr-program-run.json` was **stale** (`open_prs: 0`) — `pr-merge-queue-plan.py` gate enrichment hung >5 min on full org sweep.
- **No PRs gate-ready** for `pr_merger` (`ready: true` + `blockers: []`); all lack human `APPROVED` review and/or `merge-approved` label.
- **Top aligned candidate:** [lic#437](https://github.com/li-langverse/lic/pull/437) — PH-5b/7e matmul tier-1 fix (naive 1.118×, blocked 0.909×); CI green; release notes + study present; no catalog threshold edits.
- **Supersede stack:** lic#439, lic#446 overlap #437 — reviewer comments posted; close after #437 merges + benchmarks ingest.
- **Second aligned chore:** [benchmarks#164](https://github.com/li-langverse/benchmarks/pull/164) — centralize `CI_EXEMPT_REPOS`; CI green; release notes waived (internal audit).
- **9 PRs** carry `merge-approved` but remain blocked on human review and/or governance policy (**roadmap** ×3 — never auto-merge without human).
- Agent auth (`cap-jmk-real`) **cannot approve owner PRs** — human must run `gh pr review --approve` before automation merge.
- Posted **4** PR comments this run (#437 aligned, #439/#446 supersede, #164 aligned); **0** merges, **0** labels added.

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-program-run.json` | ⚠️ Stale | `open_prs: 0`, `ci_green: 0` — contradicts live `gh` scan |
| `pr-merge-queue-plan.json` | ⚠️ Stale | Same timestamp; `merge_first: null`, `redundant: []` |
| Live org scan | ✅ | 117 open / 82 CI-green / 9 `merge-approved` |

**Root cause:** `pr-merge-queue-plan.py` calls `pr-merge-gate.py` per PR during `enrich_pr()` — full-org run exceeds supervisor tick budget. Recommend caching gate results or limiting enrichment to CI-green PRs.

### Standards review (max 3 CI-green targets)

#### 1. lic#437 — `perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ PH-5b, PH-7e; master-plan Phase 7e |
| Strict by default | ✅ `@` lowering + codegen; no `trusted.lean` / `unsafe` |
| Security | ✅ N/A |
| Performance | ✅ Strict tier-1 evidence; no `catalog.toml` weakening |
| Release notes | ✅ `docs/release-notes/2026-05-29-bench-improver-matmul-tier1.md` |
| Ecosystem-first | ✅ Post-merge benchmarks ingest documented |

**Gate blockers:** `review_approved`, `merge-approved` (human action).  
**Comment:** https://github.com/li-langverse/lic/pull/437#issuecomment-4580677450

#### 2. lic#439 / lic#446 — redundant matmul stack

| PR | Verdict | Reason |
|----|---------|--------|
| #439 | **superseded** | Weaker blocked ratio (~1.30×); `local_ci_recorded` gate fail |
| #446 | **superseded** | Blocked borderline ~1.20×; #437 has stronger codegen + evidence |

**Comments:** #439, #446 supersede guidance posted.

#### 3. benchmarks#164 — `fix(audit): align ecosystem-audit CI exempt`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ Ecosystem audit hygiene |
| Release notes | ✅ Waived (internal `org_repos.py` alignment) |
| CI | ✅ Green |

**Gate blockers:** `review_approved`, `merge-approved` (human).  
**Comment:** https://github.com/li-langverse/benchmarks/pull/164#issuecomment-4580677469

### Merge-approved inventory (awaiting human)

| PR | Repo | Blocker |
|----|------|---------|
| #132 | benchmarks | Human `APPROVED` review |
| #19, #20, #21 | roadmap | **Governance — human merge only** |
| #12 | li-net | Human review |
| #13 | li-httpd | Human review |
| #8 | li-std-core | Human review |
| #7, #9 | li-std-math | Human review |

### pr_alignment handoff

- **10 draft PRs** flagged for close in `pr-branch-hygiene.json` (lic#430–432, benchmarks#123–128, #135–137).
- **Matmul redundant stack:** consolidate on #437; `pr_alignment` should close #439/#446 after merge.

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | `lic` #437 | Human: `APPROVED` + `merge-approved` → `pr_merger` |
| fix(audit): align ecosystem-audit CI exempt with org-repo-ci | `benchmarks` #164 | Human: `APPROVED` + `merge-approved` |
| Close superseded matmul PRs (#439, #446) | `lic` | `superseded`, pointer → #437 |
| Close abandoned draft PRs (hygiene list) | `lic`, `benchmarks` | `pr_alignment` |
| perf: optimize `pr-merge-queue-plan.py` gate enrichment | `benchmarks` | `ecosystem-ci`, `agent:ci_maintainer` |

## Deferred

- **Human review bottleneck** — agent cannot `gh pr review --approve` on owner-authored PRs; needs alternate bot account or human reviewer on duty.
- **Roadmap PRs #19–21** — labeled `merge-approved` but policy forbids agent merge; human governance required.
- **82 CI-green PR noise** — majority are agent digest / workspace-sweep / fuzz-corpus sync PRs; `pr_alignment` should batch-close stale digests.
- **PH-DB stack** (lis#17–20, lip#24–30, roadmap#32–35) — CI red or conflicting; defer until base merges.
- **Post-merge ingest** — after lic#437, run `LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh` in benchmarks to clear 6 dashboard red rows (matmul_*, ml_*, num_gmres).
- **Preflight script timeout** — re-run `pr-merge-queue-plan.py` with gate cache before next `pr_merger` tick.
