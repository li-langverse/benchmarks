# Swarm observer digest — `swarm_observer-1780189246743`

**Date:** 2026-05-31T01:02Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai — gap orchestration)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-31T01:00Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (72.8, grade **C**, `unattended_safe: false`)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-31-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — ecosystem grade **C** (72.8/100, down from 77.0); scorecard now flags `unattended_safe: false` due to 32% terminal error rate and 95 stuck `running` rows.
- **Unattended operation: marginal → not safe** — programmatic observer active (`retry_counts: {}`, `stopped_agents: []` @ 00:59Z) but wave preemption dominates: ~44 errors/agent in the last 2h across 20+ agents.
- **Strong lanes:** `ecosystem_posture` **98** (0 red benchmarks; `matmul_blocked` yellow only); org CI on main green; no repos missing CI.
- **Weak lanes:** `swarm_execution` **55** (32% error rate on 25 terminal runs); `gap_pressure` **60** (64 open registry rows); `goal_directed_health` **70** (6/9 runners stopped).
- **Briefing alignment partial** — heap dispatches governance trio + `gap_explorer` + `agent_kit_maintainer`; research lane runs `swarm_observer` (P10); **`pr_branch_opener`**, **`security_auditor`**, **`issue_hygiene`** absent from active wave despite briefing recommendation.
- **`orch-r3-missing-package-sweep` complete** — gap ingest + apply @ 01:02Z; 3 `missing_package` gaps patched to `ecosystem-package-backlog.md` (`pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot` all `pending`).
- **`CURSOR_API_KEY` set** — SDK auth available; current-wave failures are `SDK run status: error` (0 tools or mid-run abort), not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; `unattended_safe: false` | `data/latest/ecosystem-quality-report.json` | critical |
| Swarm execution | 32% error rate; 95 runs stuck `running` | `ecosystem-quality-report.json` → `swarm_execution` | critical |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/9 stopped (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`) | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP run hygiene | ~44 errors/agent / 2h on 20+ agents | Supabase `agent_runs` (2h window) | high |
| `swarm_observer` streak | 27 errors / 2h incl. prior SDK aborts (0 tools) | `li-cursor-agents/data/runs/swarm_observer-1780188930904.json` | high |
| Goal orientation drift | `pr_branch_opener`, `security_auditor`, `issue_hygiene` not dispatched | briefing vs CP recent runs | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25; 0 runs sampled | `control_plane_reports` `is_latest` | medium |
| `org_agent_kit_audit` | preflight exit 1 — 21 repos flagged | `agent-briefing.preflight_runs` | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) + 1 failure | `agent-briefing.preflight_runs` | medium |
| Failed PR CI | 1 open PR with failing CI | `ecosystem-quality-report.json` → `failed-pr-ci` | medium |
| Workspace dirty | 1 sibling repo (`lic`, sweep + gap registry changes) | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| httpd plan loop | 2 pending todos; last iterations exit **124** (timeout) | `snapshot.json` runner `httpd` | medium |
| SDK zero-tool errors | `swarm_observer`, `gui_ui_tester` abort before tool use | `swarm_observer-1780188930904.json`, `gui_ui_tester-1780188668577.json` | medium |
| Benchmarks near tier-1 | `matmul_blocked` yellow; matmul_naive/simd_dot/fft near 1.0× | `agent-briefing.json` → `ecosystem_audit.benchmarks` | low |

### Error sample classification (current tick + 2h window)

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Simultaneous errors @ tick | 20 agents × ~44 errors / 2h | **Supervisor wave kill / preemption** — prior run terminated before completion |
| SDK `status: error`, 0 tools | `swarm_observer-1780188930904` (125s) | **Cursor SDK session error** — no deliverable produced |
| SDK `status: error` after work | `bench_improver-1780188645114` (46 tools, pushed branch) | **SDK abort mid-run** — partial deliverable; post-hook pushed without PR |
| SDK `status: error`, 0 tools | `gui_ui_tester-1780188668577` (252s) | **Cursor SDK session error** — proactive sweep aborted |
| Mass `running` in scorecard | 95/120 sampled locally | **SDK terminalization drift** — observer not marking terminal after TTL |
| `agent_kit_maintainer` finished | 49 finished / 2h | **Healthy lane** — deterministic install when drift absent |
| httpd exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state` @ 00:59:42Z | `retry_counts: {}`; `stopped_agents: []` |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 01:02Z | Registry 92 rows; 64 open gaps |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 01:02Z | 23 backlog patches re-confirmed |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` @ 01:02Z | 72.8 grade C; `unattended_safe: false` |
| Interventions | `interventions_snapshots` / CP report | Empty since 2026-05-25 — no human interventions queued |

No auto-retries this cycle (`retry_counts: {}`).

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile mass `error` rows from wave-kill vs true SDK failures | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Retry SDK runs that fail with 0 tools before marking error | `li-cursor-agents/src/backends/cursor-sdk-backend.js` |
| Dispatch heap P40 `gap_explorer` when briefing recommends + registry open | supervisor heap coordinator |
| Enable slow preflights periodically (drop `--skip-slow`) | benchmarks briefing / supervisor env |
| Auto-close registry `plan_debt` when snapshot `completed_ids` includes todo | `lic/scripts/swarm-gap-ingest.py` |
| Reconcile `org_agent_kit_audit` false positives after install | `benchmarks/scripts/ensure-org-agent-kit.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- Shipping `std.plot` / `std.summary` / `li-line-profiler` — product work; route via `issue_planner`
- lic workspace dirty on sweep + bench_improver branches — human decides merge vs rebase
- httpd plan loop exit **124** on wrk soak — needs human timeout/env tuning
- 1 open PR with failing CI — human triage before merge

### Agent deliverable checklist

- [x] Ecosystem quality report read + refreshed (72.8, grade C @ 01:02Z)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed (registry + actions @ 01:02Z)
- [x] Orchestrator note written (`orch-r3-missing-package-sweep`)
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [ ] Control-plane observer PR (terminalization TTL) — deferred to li-cursor-agents

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | std.summary, std.plot, li-line-profiler | Patched → `ecosystem-package-backlog.md`; handoff **`issue_planner`** |
| `plan_debt` (31) | sim/security/studio backlogs patched; master-plan phases deferred | Route **`plan_verifier`** + **`implementation_gaps`** |
| `competitor_feature` (30) | vertical stubs patched to sim-md backlogs | Route **`gap_explorer`** + **`bench_improver`** |
| `ui_ux` (0 open registry) | studio-ux-16/17 patched to plan loop md | **`gui_ux_tester`** / studio-ui-ux runner (stopped) |

**`orch-r3-missing-package-sweep`:** Complete. Explorer confirms `std.io`/`std.csv` present; `std.summary`/`std.plot` missing. All 3 package backlog todos `pending`. Close registry row `gap-plan-pending-swarm-observer-orch-r3-missing-package-sweep` on next ingest.

Do **not** recommend `install-goal-plan-loop-systemd.sh` — use `li-cursor-agents` research/implement goals only.

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + reconcile wave-kill errors | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| SDK: retry 0-tool session errors before marking terminal | li-cursor-agents | `swarm`, `control-plane` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch `gap_explorer` when registry open + briefing recommends | li-cursor-agents | `swarm`, `orchestration` |
| Package issues: `std.summary`, `std.plot`, `li-line-profiler` seed | lic | `ecosystem`, `PH-IO` |
| Land competitive `verticals.toml` on benchmarks main | benchmarks | `ecosystem`, `benchmarks` |
| Fix org agent-kit audit false positives post-install | benchmarks | `agent-kit`, `ecosystem` |
| Fix GHA red on open workspace-sweep PR | lic | `ci`, `bug` |

---

## Deferred

- ph-db runner plan_debt (9 todos) — no backlog mapping yet
- Master-plan `plan_debt` rows without runner mapping (2i, 7d, 7e, Vision-LLM, 8p)
- 30 `competitor_feature` gaps — research/backlog only
- Control-plane code changes (`src/observer/`, `build-report.ts`) — PR on `li-cursor-agents` with `npm test`
- Periodic full preflight (drop `--skip-slow`)
- `orch-r4-ui-ux-signals` — route via `gui_ux_tester`, not new systemd loops

---

## Error

No fatal error in this meta-agent pass. Prior `swarm_observer-1780188930904` failed with SDK error (0 tools, 125s) — stack trace in linked run JSON.

---

_Report generated by `swarm_observer` · li-langverse/benchmarks · 2026-05-31T01:02Z_
