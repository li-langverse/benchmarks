# Swarm observer digest — `swarm_observer-1780177853523`

**Date:** 2026-05-30T21:55Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem orchestration — PH meta-audit)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T21:46Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (77.0, grade **C**, `unattended_safe: true`)  
**Orchestrator notes:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-swarm-meta-audit-2155Z.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — ecosystem grade **C** (77.0/100); strong `ecosystem_posture` (100) offset by weak `swarm_execution` (70) and `gap_pressure` (60).
- **Unattended operation: marginal** — scorecard marks `unattended_safe: true`, but local observer flags `healthy: false`, async-swarm restarts every ~2–3 min with `lanes_settled=false`, and CP shows **~870 wave-kill errors / 2h** across 24 agents.
- **Execution churn is orchestration, not auth** — `CURSOR_API_KEY` is set; terminal failures are predominantly **SDK session error after partial work** and **async-swarm preemption**, not missing credentials.
- **Briefing alignment partial** — heap dispatches governance/PR agents; **`gap_explorer`**, **`issue_hygiene`**, **`security_auditor`** absent from the 2h CP window despite briefing recommendation.
- **Gap pipeline current** — registry ingest + apply @ 21:48Z (**64 open**: 30 competitor, 31 plan_debt, 3 missing_package); 23 backlog patches idempotent.
- **Goal-directed lane stalled** — `agents_live: 0`; **6/9** plan runners stopped; `httpd` runner active with 2 pending wrk-soak todos (history exit **124**).
- **Programmatic observer @ 21:52Z** — `retry_counts: {}`, `stopped_agents: []`; remediations queued `implementation_gaps` + `swarm_observer` goal-align retry; `needs_meta_observer: false` (threshold stale vs scorecard).
- **CP dashboard drift** — Supabase `control_plane_reports.is_latest` still **2026-05-25** (`healthy: true`, 0 runs sampled); local `state.json` is authoritative for this host.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; high historical error volume | `data/latest/ecosystem-quality-report.json`; Supabase `agent_runs` (16 179 errors all-time) | high |
| Async swarm restarts | Stop/start every ~2–3 min; `lanes_settled=false` | `li-cursor-agents/data/control-plane/supervisor-activity.jsonl` | high |
| Wave preemption | ~31–45 errors/agent in 2h window (uniform) | Supabase `agent_runs` (2h filter) | high |
| Gap registry | 64 open gaps | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Goal-directed runners | 6/9 stopped; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| Goal orientation drift | `gap_explorer`, `issue_hygiene`, `security_auditor` not in 2h dispatch | briefing vs CP `agent_runs` | medium |
| Local observer mismatch | `sdk_unavailable` finding while API key set | `li-cursor-agents/data/control-plane/state.json` | medium |
| CP report stale | `control_plane_reports` @ 2026-05-25 | Supabase `control_plane_reports` | medium |
| `org_agent_kit_audit` | preflight exit 1 — 21 repos missing/drifted kit | `agent-briefing.preflight_runs` | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) | `agent-briefing.preflight_runs` | medium |
| Workspace dirty | 2 sibling repos (`lic`, `benchmarks`) on agent branches | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| Scorecard run drift | 107/120 sampled runs still `running` (local dir) | `ecosystem-quality-report` → `swarm_execution` | medium |
| Benchmarks near tier-1 | `matmul_blocked` yellow; matmul_naive/simd_dot/fft ~1.0–1.1× | `agent-briefing.json` → `ecosystem_audit` | low |

### Error sample classification

| Pattern | Example run | Root cause |
|---------|-------------|------------|
| SDK terminal error after productive work | `docs_maintainer-1780177405649` — 45 tools, 241s, completion audit **yes** | **Cursor SDK session error** — not auth; partial deliverable lost |
| Uniform ~40 errors/agent / 2h | `bug_fixer`, `plan_verifier`, `pr_alignment`, … | **Async-swarm wave kill** — prior run terminated before finalize |
| `completion: null` on recent errors | `gui_ui_tester-1780177253399`, `bench_improver-1780177242878` | **Preemption before finalize-run** — status reconciled as error |
| Concurrent meta-audits | `swarm_observer` 23 errors / 2h; 2 runs `running` | **Meta-agent overlap** during `swarm_coverage` goal |
| httpd plan loop exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |
| Lifetime volume | 1028 `workspace_sweeper`, 991 `bug_fixer` errors all-time | **Historical mislabel** includes wave-kill + SDK errors |

**Error (representative SDK failure):**

```
Error: SDK run status: error; run_id=run-9bc95d78-efcb-43c2-8130-7bf4b2b4963c; model=default; attempt=1; tools=45; duration_ms=240979
    at errorRunResult (cursor-sdk-backend.js:236:43)
```

Evidence: `li-cursor-agents/data/runs/docs_maintainer-1780177405649.json`

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state` @ 21:52:53Z | `last_scan_at` set; `retry_counts: {}` |
| Remediation dispatch | `state.json` → `swarm_health.remediations` | `implementation_gaps` (deliverable gaps); `swarm_observer` goal-align retry |
| Stopped agents | CP `control_plane_state` | `stopped_agents: []` — no budget exhaustion |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 21:48Z | Registry refreshed (64 open) |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 21:48Z | 23 backlog patches re-confirmed |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` @ 21:48Z | 77.0 grade C |
| Interventions | `interventions_snapshots` | Empty since 2026-05-25 |

No auto-retries recorded in `retry_counts` for the 21:52Z scan.

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Debounce async-swarm restart; wait `lanes_settled` before SIGTERM pool | `li-cursor-agents/src/async-swarm/` |
| Classify wave-kill as `preempted` not `error` in finalize-run | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Reconcile stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports.swarm_health` each tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Set `needs_meta_observer` when `swarm_execution.score < 75` | `li-cursor-agents/src/observer/` |
| Dispatch heap P40 `gap_explorer` when registry has open `missing_package` | supervisor heap coordinator |
| Fix `sdk_unavailable` heuristic — require auth substring in `error` field | `li-cursor-agents/src/observer/` |
| Periodic full preflight (drop `--skip-slow` nightly) | benchmarks briefing / supervisor env |
| Route `issue_hygiene` when `dup_clusters > 0` | `benchmarks/scripts/agent-briefing.py` |

### Human-only blockers

| Blocker | Why human |
|---------|-----------|
| Governance / provability PRs | `trusted.lean`, master-plan phase gates — no auto-merge |
| 21 repos agent-kit drift | `org_agent_kit_audit` exit 1 — org-wide kit rollout needs review |
| `verticals.toml` missing on benchmarks main | Blocks vertical ingest (`gap-infra-verticals-toml-missing-benchmarks-main`) |
| httpd wrk soak (exit 124) | Long-running bench; may need hardware/time budget approval |
| ph-db plan_debt (9 todos) | No backlog mapping yet — see `orch-ph-db-backlog-mapping.md` |

### Agent deliverable checklist

| Check | Status |
|-------|--------|
| `plan_open_items` (65) | Open — route `plan_verifier` + `implementation_gaps` |
| `incomplete_runs` | 0 in briefing gate |
| `agent_prs_blocked` | 0 |
| `numerics_without_evidence` | 0 |
| Meta-audit digest written | **yes** — this file |
| Gap orchestrator note | **yes** — `orch-swarm-meta-audit-2155Z.md` |

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| `chore(control-plane): debounce async-swarm restart until lanes settled` | li-cursor-agents | `swarm-health`, `control-plane` |
| `fix(observer): classify wave-kill vs SDK auth vs SDK session error` | li-cursor-agents | `swarm-health` |
| `chore(benchmarks): land competitive/verticals.toml on main for gap ingest` | benchmarks | `gap-explorer`, `ecosystem` |
| `[master-plan-gap] goal-directed runners stopped — sim/security/studio-ui-ux` | lic | `master-plan-gap`, `plan-needed` |
| `chore(ecosystem): agent-kit sync wave — 21 repos` | li-langverse (multi) | `agent-kit` |
| `docs(orchestrator): ph-db backlog mapping for 9 deferred plan_debt gaps` | lic | `plan-debt` |
| `chore(agent): issue_hygiene — 4 dup clusters + explorer spam` | benchmarks / org repos | `issue-hygiene` |
| `feat(std): std.summary + std.plot package placement` | lic | `missing-package`, `PH-IO-7` |

---

## Deferred

- Full preflight without `--skip-slow` (issue_hygiene, plan_audit, ci_bug_triage, security_cwe_audit) — schedule off-peak.
- Retired systemd plan loops (`sim`, `ph-db`) — work routes via `research-goals.yaml` / implement lane only.
- Tier-1 red competitor gaps (matmul_naive, num_gmres, …) — defer to `bench_improver` after harness stability.
- Auto-merge of any agent PRs — out of scope for swarm_observer.
- Product code in `lic` — gap orchestration limited to backlog patches and goal routing.

---

## Gap orchestration (Mode B — `swarm_coverage`)

**Prep confirmed:** `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py` @ 21:48Z.

| `gap_kind` | Open | Orchestrator action |
|------------|-----:|---------------------|
| `missing_package` | 3 | Route `issue_planner` via `ecosystem-package-backlog.md` (line_profiler, std.summary, std.plot) |
| `plan_debt` | 31 | Patched sim/security/studio backlogs; 9 ph-db + 8 master-plan rows deferred (mapping doc) |
| `competitor_feature` | 30 | Patched vertical stubs → sim-md-research backlog; tier-1 reds → `bench_improver` handoff |

**Not dispatched this cycle (briefing gap):** `gap_explorer` — enqueue on next heap tick when `missing_std_modules` signal persists.

See `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-swarm-meta-audit-2155Z.md` for handoff routing.
