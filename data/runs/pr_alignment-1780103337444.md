# PR alignment — close/supersede review pass

**Run id:** `pr_alignment-1780103337444`  
**Agent id:** `pr_alignment`  
**Source:** heap · `coord_pull_requests` — 1 PR flagged for close/supersede review  
**north_star_fit:** ecosystem governance / agent handoff docs — proof → easy → fast; defer perf work until plan-approved (PH-DB, PH-7e).

## Executive summary

- **Preflight partially complete:** `issue-feature-triage.py` OK; `pr-merge-queue-plan.json` and `pr-program-run.json` report **0 open PRs** (stale — GitHub API rate limit exhausted during org scan).
- **156 open PRs** per `ecosystem-audit.json` (85 CI-green, 42 CI-red); merge queue planning must be re-run after rate limit reset.
- **1 close candidate:** [roadmap#26](https://github.com/li-langverse/roadmap/pull/26) (draft vision snapshot) — **`safe_now: false`**; **not closed** this run.
- **roadmap#26** is **merge CONFLICTING** with `main`; snapshot doc (`vision-implementation-status-2026-05-27.md`) is **not on `main`** — draft still has agent-handoff value.
- Prior alignment comment on #26 (2026-05-29, verdict **defer**) remains valid; no duplicate comment posted (API rate limit).
- **0 PRs closed** (policy: never close roadmap without explicit supersede; hygiene marks `safe_now: false`).
- **Feature without plan:** [lis#20](https://github.com/li-langverse/lis/pull/20) has `plan-needed` but not `plan-approved` — blocked for merge queue.
- **Redundant stack (lic matmul):** [#466](https://github.com/li-langverse/lic/pull/466) canonical; [#465](https://github.com/li-langverse/lic/pull/465) and bench_improver siblings superseded — close after #466 merges (not `safe_now` yet).

## Deliverable / findings

### Preflight artifacts

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-branch-hygiene.json` | ✅ (01:06Z) | `prs_recommended_close: 1`, `prs_safe_close_now: 0` |
| `pr-merge-queue-plan.json` | ⚠️ Stale | `open_prs: 0`, empty `merge_sequence` — rate-limited `gh` during scan |
| `pr-program-run.json` | ⚠️ Stale | `open_prs: 0`, `all_open: []` |
| `issue-feature-triage.json` | ✅ | 36 `plan-needed` issues (lic), 3 candidates |
| `ecosystem-audit.json` | ✅ | 156 open / 85 ready / 42 failed |
| GitHub API | ❌ | Rate limit 0 remaining at 01:17Z — blocked additional `gh pr diff` / comments |

### Per-PR alignment (max 8 reviewed)

#### roadmap#26 — `docs(ecosystem): vision implementation status snapshot 2026-05-27`

| Check | Result |
|-------|--------|
| Feature / plan | Informational agent-handoff doc (not feature code) |
| PH / PKG | Ecosystem governance; Wave A gate snapshot |
| Merge order | **roadmap last** (tier 90) |
| Redundant / superseded | No — doc absent from `main`; PR **CONFLICTING** |
| Hygiene | `prs_recommended_close`, `safe_now: false`, `action: close` (draft confirm) |
| **Verdict** | **defer** — retain draft until maintainer confirms abandoned or refreshes snapshot |

Existing comment: https://github.com/li-langverse/roadmap/pull/26#issuecomment-4580688509

#### lis#20 — `feat(PH-DB): lis db supervisor and control-plane hosting profile`

| Check | Result |
|-------|--------|
| Plan | ❌ `plan-needed` without `plan-approved` |
| PH traceability | ✅ PH-DB in title/body |
| Merge order | lip/lit/lis tier after lic |
| **Verdict** | **needs plan** |

#### lic#466 — `fix(bench): matmul_naive @ codegen — red row → green (PH-7e)`

| Check | Result |
|-------|--------|
| Plan | Bench fix aligned with master plan Phase 7e / PH-5b |
| PH traceability | ✅ PH-7e in title/body |
| Merge order | lic tier (after benchmarks CI) |
| Redundant | Canonical vs #465, #446, #439, … |
| **Verdict** | **aligned** (pending pr_reviewer + stack cleanup) |

#### lic#465 — `fix(bench): matmul_naive @ lowering — tier-1 ≤1.2× cpp`

| Check | Result |
|-------|--------|
| Duplicate | Superseded by #466 (better evidence / release note) |
| **Verdict** | **close as superseded** (after #466 merges; not `safe_now`) |

#### li-net#12, li-httpd#13, li-std-core#8, li-std-math#9 — agent-kit sync

| Check | Result |
|-------|--------|
| Labels | ✅ `merge-approved` |
| Scope | Chore / CI mirror — matches title |
| Merge order | Package mirrors **before** lic (tier 10–20) |
| **Verdict** | **aligned** — route to `pr_reviewer` / merge queue |

#### roadmap#34 — `feat(PH-DB-0): lidb data platform ADR and release note`

| Check | Result |
|-------|--------|
| Plan | Governance ADR — roadmap human-merge policy |
| PH traceability | ⚠️ PH-DB-0 in title; body lacks linked issue |
| Merge order | **roadmap last** |
| **Verdict** | **aligned** (human merge only; add issue link in body) |

### Actions taken

| Action | Count | Detail |
|--------|-------|--------|
| PRs closed | 0 | roadmap#26 blocked (`safe_now: false`, governance) |
| Alignment comments | 0 new | #26 already commented; API rate limit |
| Labels added | 0 | No `merge-approved` (pr_reviewer scope) |

## Recommended issues/PRs

| Priority | Repo | Item | Labels / action |
|----------|------|------|-----------------|
| P0 | roadmap | [#26](https://github.com/li-langverse/roadmap/pull/26) | **defer** — maintainer: rebase or close when newer status lands on `main` |
| P0 | lic | [#466](https://github.com/li-langverse/lic/pull/466) | Merge first in matmul stack; then close #465, #446, #439, … |
| P1 | lis | [#20](https://github.com/li-langverse/lis/pull/20) | `plan-needed` → get `plan-approved` via issue planner |
| P1 | li-net, li-httpd, li-std-core, li-std-math | #12, #13, #8, #9 | `merge-approved` — package tier merge queue |
| P2 | benchmarks | Re-run `pr-merge-queue-plan.py` | After GitHub API rate limit reset |
| P2 | org | 133 branches without PR | Route to `pr_branch_opener` |

## Deferred

- **roadmap#26 close** — wait for maintainer confirmation or superseding doc on `main` (`safe_now: false`).
- **lic matmul stack closes** — defer until #466 merges and hygiene marks superseded PRs `safe_now`.
- **Full org alignment sweep** — `pr-merge-queue-plan.py` / `run-pr-program.py` refresh blocked by API rate limit and long-running `pr-branch-hygiene.py` (>7 min).
- **Duplicate alignment comments** on lis#20 / lic#466 — already covered by concurrent `pr_reviewer` pass (`pr_reviewer-1780103337419`).

## Error

**GitHub API rate limit exceeded** (user ID 207167228, reset ~01:19Z). Preflight merge-plan artifacts show `open_prs: 0` despite 156 live open PRs. Re-run preflight after reset:

```bash
cd benchmarks
python3 scripts/pr-merge-queue-plan.py
python3 scripts/run-pr-program.py
```
