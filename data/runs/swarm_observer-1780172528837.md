# Swarm observer digest — `swarm_observer-1780172528837`

**Date:** 2026-05-30T20:25Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai-first)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T19:57Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (79.5, grade **C**, refreshed this pass)  
**Orchestrator notes:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`, `2026-05-30-orch-r4-ui-ux-signals.md`, `2026-05-30-orch-ph-db-backlog-mapping.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — ecosystem grade **C** (79.5/100); scorecard `unattended_safe: true` but operational drift persists.
- **Unattended operation: marginal** — programmatic observer active (`retry_counts: {}`, `stopped_agents: []`); CP last 2h shows **1609 error** / **24 running** / **30 finished** — wave-preemption pattern, not SDK auth failure.
- **Strong lanes:** `ecosystem_posture` **100** (0 red benchmarks; `matmul_blocked` yellow only); CI-on-main gate green; 0 failed PRs in scorecard snapshot.
- **Weak lanes:** `gap_pressure` **60** (64 open registry rows); `goal_directed_health` **70** (6/8 runners stopped); **110** runs still marked `running` (SDK terminalization drift).
- **Briefing alignment partial** — current tick dispatches heap P10 `pr_alignment`, P30 governance, P50 `agent_kit_maintainer`; **`gap_explorer`**, **`issue_hygiene`**, **`security_auditor`** absent from run wave despite briefing recommendation.
- **Programmatic observer @ 20:22Z** — scan only; no auto-retries (`retry_counts: {}`); mass errors are preemption, not exhausted retry budget.
- **Gap pipeline current** — ingest + apply re-run @ 20:24Z (idempotent); 64 open gaps (30 competitor, 31 plan_debt, 3 missing_package); 23 backlog patches confirmed.
- **`CURSOR_API_KEY` set** — SDK auth not the primary blocker; errors are wave-kill / status-reconciliation drift.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; marginal unattended | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/8 stopped (compiler-studio, sim, studio-ui-ux, md/chem research, security) | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP run hygiene | 1609 errors / 2h; 13 agents × ~13 errors / 30m (wave kill) | Supabase `agent_runs` | high |
| Goal orientation drift | `gap_explorer`, `issue_hygiene`, `security_auditor` not dispatched | briefing vs CP recent runs | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25; 0 runs sampled | `control_plane_reports` is_latest | medium |
| `org_agent_kit_audit` | preflight exit 1 — audit flags 21 repos; rollout all skipped (no drift after install) | `agent-briefing.preflight_runs`, `agent_kit_maintainer-1780172416577` | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) | `agent-briefing.preflight_runs` | medium |
| Workspace dirty | 2 sibling repos (lic, benchmarks); lic PR #573 already open | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| httpd plan loop | 2 pending todos; last iterations exit **124** (timeout); log stale 49k s | `goal-directed-agents/snapshot.json` runner=httpd | medium |
| Failed PRs (briefing) | 4 red PRs (benchmarks #251/#252, li-gui #3, lic #568) | `agent-briefing.json` → `ecosystem_audit.failed_prs` | medium |
| ph-db plan_debt | 9 todos deferred in apply-actions (no backlog mapping) | `swarm-gap-actions.json`, orchestrator note | medium |
| Benchmarks near tier-1 | `matmul_blocked` yellow; matmul_naive/simd_dot/fft near 1.0× | `agent-briefing.json` → `ecosystem_audit.benchmarks` | low |

### Error sample classification

| Pattern | Example | Root cause |
|---------|---------|------------|
| Simultaneous errors @ tick | 13 agents × ~13 errors / 30m (pr_reviewer, bug_fixer, plan_verifier, …) | **Supervisor wave kill / preemption** — prior run terminated before completion |
| CP `error` with null completion | `docs_maintainer-1780172298882`, `workspace_sweeper-1780172298831` | **SDK preemption / empty run** — local JSON still `running` while CP marks `error` |
| Mass `running` in scorecard | 110/120 sampled locally | **SDK terminalization drift** — observer not marking terminal after TTL |
| `agent_kit_maintainer` finished | All 21 repos skipped (no changes after install) | **Audit false positive vs install** — preflight exit 1 but deterministic install found no drift |
| httpd exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |
| Historical `error` volume | 1007 workspace_sweeper, 959 bug_fixer lifetime | **Status reconciliation drift** (finished mislabeled as error over time) |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state.observer` @ 20:22Z | `last_scan_at: 2026-05-30T20:22:08Z`; `retry_counts: {}` |
| Stopped agents | CP `control_plane_state` | `stopped_agents: []` — no agents budget-exhausted |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 20:24Z | Registry idempotent refresh; 64 open gaps |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 20:24Z | 23 backlog patches re-confirmed |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` | 79.5 grade C @ 20:24Z |
| Workspace sweep (deterministic) | pre-sweep digest @ 20:19Z | lic swept; PR #573 exists; async-swarm restart failed |

No auto-retries for the 20:19–20:20Z error wave (`retry_counts: {}`).

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile mass `error` rows from wave-kill vs true failures | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Dispatch heap P40 `gap_explorer` when briefing recommends + registry open | supervisor heap coordinator |
| Enable slow preflights periodically (drop `--skip-slow`) | benchmarks briefing / supervisor env |
| Auto-close registry `plan_debt` when snapshot `completed_ids` includes todo | `lic/scripts/swarm-gap-ingest.py` |
| ph-db plan_debt backlog mapping (9 todos deferred) | `lic/docs/ecosystem/ph-db-plan-backlog.md` (new) |
| Reconcile org_agent_kit_audit false positives after install | `benchmarks/scripts/ensure-org-agent-kit.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- **16 PRs** flagged close/supersede (`pr_alignment`) — human review required
- **4 red PRs** (benchmarks #251/#252, li-gui #3, lic #568) — CI must green before merge
- lic dirty on `cursor/swarm-observer-plan-loop` (5 files) — human decides merge vs rebase
- httpd plan loop exit **124** on wrk soak — needs human timeout/env tuning before re-enable
- `systemctl try-restart li-agents-async-swarm.service` failed after workspace sweep

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (79.5, grade C)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`, `interventions_snapshots`)
- [x] Gap ingest + apply confirmed (registry + actions @ 20:24Z)
- [x] `orch-r3` / `orch-r4` orchestrator notes referenced; ph-db mapping note added
- [x] Recommended agents vs recent runs compared (gap_explorer / issue_hygiene / security_auditor drift)
- [x] Error runs sampled + classified (CP DB + local JSON sample)
- [ ] Control-plane observer PR (terminalization TTL) — deferred to li-cursor-agents

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + reconcile wave-kill errors | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Refresh control_plane_reports.swarm_health each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch gap_explorer when registry open + briefing recommends | li-cursor-agents | `swarm`, `orchestration` |
| Land competitive verticals.toml on benchmarks main | benchmarks | `ecosystem`, `docs` |
| Package issues: std.summary, std.plot, li-line-profiler seed | lic | `ecosystem`, `PH-IO` |
| ph-db plan_debt backlog + apply-actions mapping | lic | `orchestration`, `ph-db` |
| Fix org agent-kit audit false positives post-install | benchmarks | `agent-kit`, `ecosystem` |
| Close/supersede review — 16 flagged PRs | multi | `pr-hygiene` |

---

## Deferred

- ph-db runner plan_debt (9 todos) — no lic backlog mapping in apply-actions; orchestrator note documents handoff path
- 30 `competitor_feature` rows — research handoffs to `numerics_researcher` / `bench_improver`, not orch product code
- 9 master-plan `plan_debt` rows without runner mapping — stay with `plan_verifier` / `issue_planner`
- Control-plane code changes — file PR from separate implementer pass with `npm test` evidence
- Goal-directed runner restart (6/8 stopped) — human supervisor enable per `swarm-architecture.md`

---

## Gap orchestration evidence (`swarm_coverage`)

| Artifact | Path |
|----------|------|
| Registry | `lic/data/swarm-gap-registry/registry.yaml` |
| Apply actions | `benchmarks/data/latest/swarm-gap-actions.json` |
| Package backlog | `lic/docs/ecosystem/ecosystem-package-backlog.md` |
| UI/UX plan | `lic/docs/superpowers/plans/2026-05-24-studio-ui-ux-plan-loop.md` |
| Orchestrator note (orch-r3) | `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md` |
| Orchestrator note (orch-r4) | `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r4-ui-ux-signals.md` |
| Orchestrator note (ph-db) | `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-ph-db-backlog-mapping.md` |
