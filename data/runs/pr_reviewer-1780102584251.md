# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780102584251`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**north_star_fit:** ecosystem pull-requests / PH-5b, PH-7e — tier-1 numerics merge queue after proof-preserving codegen (proof → easy → fast).

## Executive summary

- **GitHub API rate limit exceeded** (user ID 207167228; reset ~2026-05-30T01:19:28Z) — live `gh pr view` / gate enrichment blocked; preflight JSON shows **`open_prs: 0`** (false negative).
- Pre-rate-limit org search captured **50+** open PRs including **new matmul stack** entries lic#466 and lic#465 (both updated ~00:42–00:45Z).
- **No PR verified gate-ready this run** — cannot confirm CI or post review comments until rate limit clears.
- **Prior aligned candidate retained:** [lic#437](https://github.com/li-langverse/lic/pull/437) — PH-5b/7e tier-1 matmul; `merge-approved` label added in run `1780101265615`; still needs human `APPROVED`.
- **New overlap risk:** lic#466 (`@codegen` path) and lic#465 (lowering) duplicate #437/#446/#439 — pick **one canonical** matmul PR before any merge-approved sweep.
- **Second aligned chore (prior run):** [benchmarks#164](https://github.com/li-langverse/benchmarks/pull/164) — audit CI-exempt alignment; release notes waivable on human review.
- **lis#20** still blocked: `plan-needed` without `plan-approved` (PH-DB feature work).
- **roadmap** PRs (#32–35) — governance policy: **human merge only**; never agent-merge.

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-program-run.json` | ❌ False empty | `open_prs: 0`, `ci_green: 0` — `gh pr list` fails under rate limit |
| `pr-merge-queue-plan.json` | ❌ False empty | `merge_first: null`, `redundant: []` — gate enrichment cannot run |
| `issue-feature-triage.json` | ⚠️ Degraded | All repos report `gh failed` |
| Pre-limit org search | ✅ | 50 PRs sampled via `gh search prs` before GraphQL limit |
| Control plane | ⚠️ | `pr_alignment`, `pr_merger`, `bench_improver` runs errored same window |

### Error

```
GraphQL: API rate limit already exceeded for user ID 207167228.
(gh pr view / pr-merge-gate.py — 2026-05-30T00:56Z onward)

Secondary auth issue: invalid GH_TOKEN env var shadows working gh hosts.yml credential.
Unset GH_TOKEN before org sweeps; reset at 2026-05-30T01:19:28Z.
```

### Standards review (inferred from prior gate + titles — **live gate deferred**)

#### Matmul tier-1 stack (lic) — PH-5b / PH-7e

| PR | Title hint | Prior verdict | This run |
|----|------------|---------------|----------|
| [#437](https://github.com/li-langverse/lic/pull/437) | perf(7e): tier-1 matmul naive + blocked | ✅ Aligned; `merge-approved` set | **Retain** — await human APPROVED |
| [#466](https://github.com/li-langverse/lic/pull/466) | fix(bench): matmul_naive @ **codegen** | — | **Defer gate** — may supersede #437; verify release notes + no catalog weakening |
| [#465](https://github.com/li-langverse/lic/pull/465) | fix(bench): matmul_naive @ lowering | ❌ Missing release notes (prior) | **Defer** — close or RN after canonical pick |
| [#446](https://github.com/li-langverse/lic/pull/446) | perf(tier1): matmul @ lowering | **superseded** (prior) | Close after canonical merges |
| [#439](https://github.com/li-langverse/lic/pull/439) | fix(bench): tier-1 matmul via @ lowering | **superseded** (prior) | local-ci exit=127 (`ci.sh` missing in sweep) |

**Checklist (all matmul PRs — apply after rate limit):**

| Gate | Requirement |
|------|-------------|
| Vision / PH | PH-5b, PH-7e; master-plan Phase 7e |
| Strict by default | `@` lowering / codegen only; no `trusted.lean`; Lean CI green |
| Performance | Bench evidence; **no** `catalog.toml` threshold edits |
| Release notes | `docs/release-notes/*` or CHANGELOG (not chore-only) |
| Ecosystem-first | Post-merge benchmarks ingest for 6 red rows |

**Recommendation:** Maintainer picks **#437 or #466** as canonical; close #439, #446, #465 as redundant. Do **not** add second `merge-approved` while stack open.

#### benchmarks#164 — `fix(audit): align ecosystem-audit CI exempt`

| Checklist | Verdict (prior) |
|-----------|-----------------|
| Vision / PH | ✅ Ecosystem audit hygiene |
| Strict by default | ✅ Config alignment only |
| CI | ✅ Green (prior run) |
| Release notes | ⚠️ Strict gate flags missing RN — waivable chore |

**Blockers (expected):** `review_approved`, `merge-approved`.

#### lis#20 — `feat(PH-DB): lis db supervisor…`

| Checklist | Verdict |
|-----------|---------|
| Plan | ❌ `plan-needed` without `plan-approved` |
| CI | Unknown (gate blocked) |

**Verdict:** **needs plan** — do not merge.

#### Agent / sweep noise

| Category | Examples | Action |
|----------|----------|--------|
| Workspace sweep digests | benchmarks#174, #176; lic#467 | Close stale; keep latest per agent |
| ci_maintainer digests | benchmarks#169–175 | Low merge priority; batch close |
| Fuzz-corpus bot sync | lic#448–453 | Merge latest only |
| Autoresearch deliverables | lic#455–458 | Human triage or close if artifact-only |

### Actions taken this run

- **0 merges** (policy + rate limit).
- **0 labels** — rate limit blocked `gh pr edit`.
- **0 review comments** — rate limit blocked posting.
- Preflight scripts re-run; artifacts written but empty due to API failure.

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | **lic** [#437](https://github.com/li-langverse/lic/pull/437) | Human: `APPROVED` → `pr_merger` (`merge-approved` already set) |
| fix(bench): matmul_naive @ codegen — red → green | **lic** [#466](https://github.com/li-langverse/lic/pull/466) | Gate after rate reset; compare vs #437 before label |
| fix(audit): align ecosystem-audit CI exempt | **benchmarks** [#164](https://github.com/li-langverse/benchmarks/pull/164) | Human: `APPROVED` + waive RN + `merge-approved` |
| Close superseded matmul PRs (#439, #446, #465) | **lic** | After canonical (#437 or #466) merges |
| feat(PH-DB): lis db supervisor | **lis** [#20](https://github.com/li-langverse/lis/pull/20) | Add `plan-approved`; fix CI |
| PH-DB / vision roadmap stack | **roadmap** #32–35 | **Human merge only** |
| Unset invalid `GH_TOKEN` in agent runners | **benchmarks** | `ecosystem-ci` — prevents false `pr_not_found` |
| Cache gate enrichment / CI-green-only scan | **benchmarks** | Reduce API burn (`pr-merge-queue-plan.py`) |

## Deferred

- **Rate limit recovery** (~01:19Z) — re-run `pr-merge-gate.py` on lic#437, #466, benchmarks#164; post supersede comments.
- **Canonical matmul pick** — maintainer decision between #437 (blocked GEMM + RN) vs #466 (codegen path).
- **Post-merge ingest** — refresh 6 dashboard red rows (`matmul_*`, `ml_*`, `num_gmres`) after lic numerics PR lands.
- **PH-DB stack** (lis#17–20, lip#28–30, roadmap#32–35) — defer until plan-approved + dependency order.
- **local-ci sweep fix** — lic#439 local-ci exit=127 (`./scripts/ci.sh` missing in ephemeral checkout); fix li-local-ci profile before relying on local-ci fallback gate.
- **Agent digest backlog** (~30 benchmarks PRs) — `pr_alignment` batch-close after numerics unblocked.
