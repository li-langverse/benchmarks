# Swarm observer report — `orch-r3-missing-package-sweep`

> `swarm_observer` · research goal `swarm_coverage` · 2026-05-29 · run `swarm_observer-1780087300000`

## Executive summary

- **Posture: degraded (recovering).** Ecosystem grade **C** (76.3); `unattended_safe: true` per scorecard — not fully hands-off due to stopped goal runners and stuck SDK runs.
- **Gap orchestration (orch-r3): complete** — 3 open `missing_package` gaps confirmed pending in `ecosystem-package-backlog.md`; `pkg-std-io` / `pkg-std-csv` backlog drift fixed (registry already closed).
- **Missing std modules:** `std.summary`, `std.plot` remain open; briefing correctly recommends `gap_explorer` for "2 missing std modules".
- **line_profiler seed:** `gap-line-profiler-001` routed to `issue_planner` via package backlog.
- **Swarm execution:** 108+ runs stuck `running`; DB shows high historical error counts; recent batch supersede ~20:28Z errored 10+ agents (SDK lifecycle, not auth).
- **Goal-directed:** 6/8 plan runners stopped; only `httpd` + `swarm-observer` supervisors active; `orch-r3` plan todo completed.
- **Programmatic observer:** Dispatched `implementation_gaps` + `workspace_sweeper` retry; `retry_counts` empty; `stopped_agents` empty; `needs_meta_observer: false`.
- **Credentials:** `CURSOR_API_KEY` and `GH_TOKEN` present — SDK auth not the primary blocker.

## Findings

| Agent / area | Symptom | Evidence | Severity |
|--------------|---------|----------|----------|
| Swarm SDK | 108 runs `running` (stuck / superseded) | `ecosystem-quality-report.json` → `swarm-many-running` | critical |
| Swarm SDK | Batch supersede: 10 agents `error` ~20:28Z | Supabase `agent_runs` (code_implementer, autoresearch, …) | high |
| Control plane | Latest `control_plane_reports` stale (2026-05-25) | `control_plane_reports.is_latest` | medium |
| Observer | `retry_counts` empty despite error streaks in DB | `control_plane_state.payload.observer` | medium |
| Goal-directed | 6/8 runners stopped (sim, compiler-studio, …) | `lic/data/goal-directed-agents/snapshot.json` | high |
| Preflight | org agent-kit + CWE audit exit 1 | `agent-briefing.preflight_runs` | high |
| Benchmarks | 6 red tier-1 rows (matmul, gmres, ML) | `agent-briefing.ecosystem_audit.benchmarks.red` | high |
| Gap registry | 52 open gaps (3 missing_package, 20 plan_debt, 29 competitor) | `benchmarks/data/latest/swarm-gap-actions.json` | medium |
| Briefing drift | Top recommended agents not in recent DB runs | `control_plane_state.swarm_health.findings` | medium |
| Package backlog | io/csv todos still `pending` while registry closed | `ecosystem-package-backlog.md` (fixed orch-r3) | low (resolved) |
| Missing packages | 3 open gaps unimplemented | `registry.yaml` → std.summary, std.plot, line_profiler | medium |

## Self-heal actions taken

Programmatic observer (this cycle, `control_plane_state`):

- `observer.last_scan_at`: 2026-05-29T20:34:21Z
- Remediations: dispatch `implementation_gaps`; retry `workspace_sweeper` (goal-align)
- `retry_counts`: `{}`
- `stopped_agents`: `[]`

Meta-audit (this run — orch-r3):

- Regenerated `ecosystem-quality-report.json` (76.3, grade C)
- Ran `swarm-gap-ingest.py` (`missing_std: 0`) + `swarm-gap-apply-actions.py` (live)
- Confirmed 3 `missing_package` backlog patches; fixed io/csv backlog drift
- Closed `gap-plan-pending-swarm-observer-orch-r3-missing-package-sweep`
- Orchestrator note: `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r3-missing-package-sweep.md`

## Recommended control-plane fixes

| Fix | Path |
|-----|------|
| Finalize superseded `running` runs on new dispatch | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Increment `observer.retry_counts` on SDK error streak | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports.is_latest` each supervisor tick | control-plane report writer |
| Auto-close backlog todos when registry gap closes (io/csv drift) | `lic/scripts/swarm-gap-apply-actions.py` |
| Route open `missing_package` gaps → heap `coord_ecosystem` | `benchmarks/scripts/agent-briefing.py` |

## Human-only blockers

- Merge gap registry + package backlog to `main` (governance)
- `trusted.lean` / master-plan phase claims
- Implement `std.summary`, `std.plot`, `li-line-profiler` (product/package work)
- Enable `LI_CURSOR_AGENTS_ENABLED=1` for agent deliverable gate scan
- Tier-1 numerics perf (matmul_blocked, gmres) — proof-before-perf

## Agent deliverable checklist

- [x] Ecosystem quality grade refreshed
- [x] Gap ingest + apply-actions confirmed
- [x] 3 missing_package backlogs pending (line_profiler, std.summary, std.plot)
- [x] io/csv backlog drift reconciled with registry
- [x] Orchestrator note (`orch-r3`)
- [x] Registry orch-r3 plan_debt gap closed; plan backlog orch-r3 completed
- [x] Report under `benchmarks/data/runs/`
- [ ] PR on `li-cursor-agents` for finalize-run / observer retry (recommended)

**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r3-missing-package-sweep.md`

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| fix(apply-actions): close backlog todos when registry gap status=closed | lic | `orchestration`, `gap-registry` |
| fix(observer): finalize superseded running SDK runs | li-cursor-agents | `control-plane`, `swarm` |
| feat(std): scaffold std.summary module (PH-IO-7) | lic | `stdlib`, `ecosystem` |
| feat(std): scaffold std.plot module (PH-IO-5) | lic | `stdlib`, `ecosystem` |
| chore(packages): seed li-line-profiler package issue | lic | `ecosystem`, `profiling` |

## Deferred

- `orch-r4-ui-ux-signals` — next swarm-observer plan todo
- Product implementation of open package gaps — `issue_planner` / `package_architect`
- Observer PR with `npm test` evidence (control-plane fixes batch)
- Restart stopped goal-directed runners (sim, compiler-studio, security-research)
