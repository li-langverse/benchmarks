# CI maintainer digest — 2026-05-30

**Agent:** `ci_maintainer`  
**Run id:** `ci_maintainer-1780132501519`  
**Source:** proactive ecosystem sweep  
**North star fit:** Ecosystem **secure** + **provable** gate — org repos require `ci.yml` on default branch before branch protection can enforce proof-bearing CI (PH-8p throughput, WP-H0 hygiene).

---

## Executive summary

- Refreshed `org-repo-ci-audit.json` and `ecosystem-audit.json`; **1 org repo missing `ci.yml`** on default branch (`lic-docs`).
- **33 repos OK** with `.github/workflows/ci.yml` (or exempt); **0 gated**; **0 audit-incomplete**.
- Opened **[lic-docs#3](https://github.com/li-langverse/lic-docs/pull/3)** — adds org `ci.yml` with **`check`** job (MkDocs build gate); CI **green** on PR.
- `ecosystem-audit.py` **under-reports** missing CI for `lic-docs` (counts any workflow, including `docs.yml` only) — org gate (`ensure-org-repo-ci.py`) is authoritative.
- `research-findings` remains **CI-exempt**; still listed in `ecosystem-audit.json` → `missing_ci_on_main` (signal drift).
- Branch protection on `lic-docs`: **none** yet — after merge, apply ruleset with required check `check` per `default_repo` policy.
- Required check name **`check`** matches `roadmap/scripts/org-branch-protection.json` `default_repo.required_checks`.
- Ecosystem metrics: 172 open PRs, 48 failing CI, 70 ready for review — CI rollout for `lic-docs` pending human merge.

---

## Deliverable / findings

### Preflight audits

| Audit | Result |
|-------|--------|
| `ensure-org-repo-ci.py` | exit **1** — OK: 33 · missing: 1 (`lic-docs`) · gated: 0 · incomplete: 0 |
| `ecosystem-audit.py` | exit **0** — `repos_missing_ci_main`: 1 (`research-findings`, exempt) |
| `lic-docs` branch protection | **none** — rulesets empty; apply after merge |

### Missing CI (org gate)

| Repo | Default branch | Workflows on main | Fix |
|------|----------------|-------------------|-----|
| `lic-docs` | `main` | `docs.yml` only | Add `ci.yml` — **PR [#3](https://github.com/li-langverse/lic-docs/pull/3)** open, CI green |

### CI template adaptation

- Source: `lic/scripts/templates/github-repo/ci.yml`
- Adaptation: docs-only repo — no `src/lib.li`; job **`check`** runs `scripts/build-docs.sh` and verifies `site/index.html` + `site/CNAME`.
- No OS matrix; no `continue-on-error`.

### Branch protection vs required checks

| Repo | Required check (policy) | PR check name | Match |
|------|-------------------------|---------------|-------|
| `lic-docs` | `check` (`default_repo`) | `check` | ✓ |

### PRs opened this run

| Repo | PR | Branch | Labels | CI |
|------|-----|--------|--------|-----|
| **lic-docs** | [#3](https://github.com/li-langverse/lic-docs/pull/3) | `chore/agent-ci_maintainer-1780132501519-ci` | `ecosystem-ci`, `li-swarm`, `agent:ci_maintainer` | **pass** (`check`) |

---

## Recommended issues/PRs

| Repo | Title | Labels | Action |
|------|-------|--------|--------|
| **lic-docs** | Merge [PR #3](https://github.com/li-langverse/lic-docs/pull/3) — chore(ci): add org ci.yml workflow | `ecosystem-ci` | Human review + merge |
| **roadmap** | Apply org branch protection ruleset to `lic-docs` (required check: `check`) | `ecosystem-governance` | Human org settings |
| **benchmarks** | Align `ecosystem-audit.py` `has_ci_on_main` with `ensure-org-repo-ci.py` (require `ci.yml`, not any workflow) | `ecosystem-ci` | Optional follow-up PR |
| **research-findings** | Merge [PR #6](https://github.com/li-langverse/research-findings/pull/6) or document exempt status | `ecosystem-ci` | Human decision (governance) |

---

## Deferred

- **lic-docs merge** — PR #3 awaits human review; org audit will pass after merge to `main`.
- **Branch protection rollout** for `lic-docs` — requires human `apply-org-branch-protection.sh`.
- **ecosystem-audit drift** — `has_ci_on_main` treats `docs.yml`-only repos as OK; should require `ci.yml` like org gate.
- **research-findings CI** — exempt by policy; optional PR #6 merge is human decision.
- **Failing PR CI triage** (48 repos) — owned by `bug_fixer` / `code_implementer`, not ci_maintainer.
- **Self-merge** — no governance repos merged by agent.
