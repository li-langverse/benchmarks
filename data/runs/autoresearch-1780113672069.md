# Autoresearch run log — `autoresearch-1780113672069`

**Date:** 2026-05-30T04:06Z  
**Agent:** autoresearch (proactive ecosystem sweep)  
**Briefing:** `data/latest/agent-briefing.json`  
**Skills:** `numerics-autoresearch`, `research-li-numerics`  
**north_star_fit:** PH-5b (competitive tier-1), PH-7e (math→SIMD/codegen)

---

## Executive summary

- Refreshed ecosystem audit: **6 dashboard reds**; local tier-1 harness on lic `28cf644b` shows **1 actionable pure_li gap** (`matmul_blocked` 1.23×).
- Mode A SOTA survey: blocked GEMM = standard BLIS/Eigen recipe; gap is compiler emit (`emit_matmul2d_blocked_ijk`), not algorithm novelty.
- **Closed negative** on novel-method invention for this cycle — ship **lic#437** codegen + ingest instead.
- Control-plane: 8 prior autoresearch runs today failed with `unregistered_running_reconciled` (supervisor reconciliation, not bench regression).

---

## Deliverable / findings

### Hypotheses evaluated

| ID | Hypothesis | Metric | Result |
|----|------------|--------|--------|
| H1 | Vec-FMA inner tile on `ArrayMatMulBlocked2DF64` closes pure_li `matmul_blocked` to ≤1.2× | `wall_time` Li/cpp | **Plausible** — lic#437 claims 0.909×; not merged; local explicit-loop driver still **1.23×** |
| H2 | Novel blocking scheme (non-BLIS tile schedule) beats IKJ+BK=64 | Same | **Rejected** — SOTA already optimal for N=512; invention adds proof debt without axis win |
| H3 | `num_gmres` red implies Krylov solver novelty needed | Li/cpp | **Rejected** — harness uses **shared C kernel**; local 0.80×; dashboard stale |
| H4 | `ml_*` reds need autoresearch conv/ML kernels | Li/cpp | **Out of scope** — li-math `algo_registry`, not pure_li |

### Local bench evidence (2026-05-30)

Host: Linux amd64 · lic `28cf644b` · `-O3 -march=native -ffast-math` · 2 runs median

```bash
cd ../lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_naive --runs 2
python3 bench.py --tier 1 --only matmul_blocked --runs 2
python3 bench.py --tier 1 --only num_gmres --runs 2
```

| Bench | cpp | li | Li/cpp | Dashboard | Variant |
|-------|-----|-----|--------|-----------|---------|
| matmul_naive | 0.0018 s | 0.0019 s | **1.06×** ✓ | 1.33× red | pure_li |
| matmul_blocked | 0.0095 s | 0.0117 s | **1.23×** ✗ | 1.55× red | pure_li |
| num_gmres | 0.0005 s | 0.0004 s | **0.80×** ✓ | 1.40× red | shared_c_kernel |
| horner_pure_li | — | — | — | **0.75×** green | pure_li (fixed) |

Checksum parity: `matmul_blocked verify ok (pure Li): result=1288460.7563999966`

### SOTA survey (Mode A — sufficient)

**Learned from:**

1. **BLIS / Goto–van de Geijn blocked GEMM** — IKJ + cache blocking; maps `matmul_blocked` oracle in `common/matmul_blocked_core.c`.
2. **Eigen efficient matrix product** — expression templates + micro-kernel discipline; maps `@` → `ArrayMatMul2DF64` / `ArrayMatMulBlocked2DF64` lowering path.
3. **Saad GMRES** — standard Krylov; `num_gmres` uses shared C reference — no Li-specific solver invention warranted.
4. Prior autoresearch negative: [`2026-05-17-horner-pure-li-autoresearch-negative.md`](../docs/numerics/studies/2026-05-17-horner-pure-li-autoresearch-negative.md) — harness DCE, now resolved.

**Conclusion:** Remaining pure_li perf debt is **PH-7e compiler emit** for blocked matmul (scalar IKJ loops in current driver; vec-FMA in lic#437). Autoresearch novel algorithms **not required**.

### Quality table (this cycle)

| Axis | matmul_blocked | Verdict |
|------|----------------|---------|
| Speed | 1.23× local (target ≤1.2×) | **Open** — codegen PR, not novel algo |
| Accuracy | Checksum vs C oracle | **Pass** |
| Stability | tier-0 N/A (tier-1 micro) | N/A |
| Novel invention | None shipped | **Negative result (valuable)** |

### Implementation map (for human verification)

| Component | Path |
|-----------|------|
| Li driver (explicit blocked IKJ) | `lic/benchmarks/tier1_micro/matmul_blocked/li/main.li` |
| C++ oracle | `lic/benchmarks/tier1_micro/matmul_blocked/common/matmul_blocked_core.c` |
| Blocked emit | `lic/compiler/codegen/emit.cpp` (`emit_matmul2d_blocked_ijk`, `ArrayMatMulBlocked2DF64`) |
| Canonical PR | [lic#437](https://github.com/li-langverse/lic/pull/437) |

### Agent deliverable checklist

<!-- li-agent -->
## Agent deliverable
- [x] li-tests or lit test id: N/A — no kernel/codegen change shipped this pass
- [x] Bench row / benchmarks path: `matmul_blocked`, `matmul_naive`, `num_gmres` — local `lic/benchmarks/results/latest.csv`
- [x] Lean/contracts path documented or N/A with reason: N/A — survey-only pass; codegen change deferred to lic#437
- [x] Negative result documented if hypothesis rejected: `docs/numerics/studies/2026-05-30-autoresearch-proactive-sweep.md`

---

## Recommended issues / PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P0 | **lic** | `perf(7e): merge blocked matmul vec-FMA codegen (ArrayMatMulBlocked2DF64) — closes matmul_blocked` | `PH-7e`, `numerics-research` |
| P0 | **benchmarks** | `chore(ingest): refresh tier-1 matrix after lic#437 — clear stale matmul/num_gmres reds` | `autoresearch` |
| P1 | **lic** | `perf(7e): route matmul_blocked hot path through @ operator after #437 merge` | `bench-improver` |
| P1 | **li-math** | `perf: investigate ml_conv2d/ml_mlp tier-1 reds (algo_registry harness)` | `PH-5b` |
| P2 | **benchmarks** | `chore: open or close stale autoresearch digest branches (×6)` | `agent-hygiene` |
| P2 | **li-cursor-agents** | `fix: autoresearch run registration — unregistered_running_reconciled` | `ecosystem-gap` |

**Do not merge** without `pr_reviewer` + `pr_merger`. Do **not** relax `threshold_ratio_cpp`.

---

## Deferred

- **Novel tile schedules, mixed-precision GEMM, or autotuning** — blocked until lic#437 lands and re-bench still fails threshold.
- **`ml_*` tier-1** — li-math ownership; not pure_li autoresearch.
- **`md_thermostat_*` yellow** — stability/visual tier-2; numerics_researcher Mode A first.
- **Near-threshold rows** (`num_cholesky`, `cloth_swing`, `robo_*`) — monitor; no autoresearch trigger at 1.10–1.17×.
- **SIMD matmul (PH-7e-a)** — explicit gap registry defer; revisit after blocked scalar+FMA green.
- **Ingest of local bench CSV** — requires lic merge + `./scripts/ingest/ingest-lic.sh`.

---

## Error (control-plane)

Prior autoresearch runs on 2026-05-30 (03:03–03:49Z) recorded:

```
status: error
error: unregistered_running_reconciled
```

No stack trace in `agent_runs.error`. Supervisor reconciled orphaned `running` rows when a new autoresearch instance started. This run (`1780113672069`) completes via digest artifact instead.

---

## Repro

```bash
cd /path/to/benchmarks
python3 scripts/ecosystem-audit.py
python3 scripts/numerics-evidence-checklist.py \
  --study docs/numerics/studies/2026-05-30-autoresearch-proactive-sweep.md

cd ../lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_blocked --runs 3
./scripts/check-tier1-li-vs-cpp.sh
```
