# Autoresearch run log — `autoresearch-1780115445230`

**Date:** 2026-05-30T04:31Z  
**Agent:** autoresearch (proactive ecosystem sweep)  
**Briefing:** `data/latest/agent-briefing.json` (generated 2026-05-30T04:06Z)  
**Skills:** `numerics-autoresearch`, `research-li-numerics`  
**north_star_fit:** PH-5b (competitive tier-1), PH-7e (math→SIMD/codegen)

---

## Executive summary

- Refreshed ecosystem audit: **6 dashboard reds**; local tier-1 harness on lic `c6e9ca7d` shows **1 actionable pure_li gap** (`matmul_blocked` **1.256×**, marginally above 1.2× gate).
- Mode A SOTA survey: blocked GEMM = BLIS/Eigen recipe; MIR hook `mm_blocked_512` already lowers to `emit_matmul2d_blocked_ijk` with vec-FMA — **no novel algorithm warranted**.
- **Closed negative** on novel-method invention — ship marginal PH-7e codegen polish + ingest refresh instead.
- Control-plane: 15+ autoresearch/bench_improver runs today in `error` (supervisor reconciliation); this run completes via digest artifact.

---

## Deliverable / findings

### Hypotheses evaluated

| ID | Hypothesis | Metric | Result |
|----|------------|--------|--------|
| H1 | Vec-FMA `ArrayMatMulBlocked2DF64` already in-tree closes pure_li `matmul_blocked` to ≤1.2× | `wall_time` Li/cpp | **Partial** — local **1.256×**; checksum pass; needs micro-tile polish not new algorithm |
| H2 | Novel blocking scheme (non-BLIS tile schedule) beats IKJ+BK=64 | Same | **Rejected** — SOTA sufficient; invention adds proof debt |
| H3 | `num_gmres` red implies Krylov solver novelty needed | Li/cpp | **Rejected** — shared C kernel; local **1.00×**; dashboard stale |
| H4 | `ml_*` reds need autoresearch conv/ML kernels | Li/cpp | **Out of scope** — li-math `algo_registry` |
| H5 | `horner_pure_li` regressed | Li/cpp | **Rejected** — local **0.80×** green; verify spec mismatch only |

### Local bench evidence (2026-05-30)

Host: Linux amd64 · lic `c6e9ca7d` · `-O3 -march=native -ffast-math` · 3 runs median

```bash
cd lic/benchmarks/harness
./scripts/build.sh   # lic compiler required
python3 bench.py --tier 1 --only matmul_blocked --runs 3
python3 bench.py --tier 1 --only matmul_naive --runs 3
python3 bench.py --tier 1 --only num_gmres --runs 3
python3 bench.py --tier 1 --only horner_pure_li --runs 3 --skip-verify
```

| Bench | cpp | li | Li/cpp | Dashboard | Variant |
|-------|-----|-----|--------|-----------|---------|
| matmul_blocked | 0.0090 s | 0.0113 s | **1.256×** ✗ | 1.549× red | pure_li (MIR hook) |
| matmul_naive | 0.0019 s | 0.0019 s | **1.000×** ✓ | 1.333× red | pure_li |
| num_gmres | 0.0005 s | 0.0005 s | **1.000×** ✓ | 1.400× red | shared_c_kernel |
| horner_pure_li | 0.0005 s | 0.0004 s | **0.800×** ✓ | 0.750× green | pure_li |

Checksum parity: `matmul_blocked verify ok (pure Li): result=1288460.7563999966`

**Implementation note:** `matmul_blocked/li/main.li` calls `mm_blocked_512` with an empty body; MIR lowerer (`lower.cpp`) rewrites to `MirOp::ArrayMatMulBlocked2DF64` and codegen emits blocked IKJ+vec-FMA (`emit.cpp:357–427`). This is intentional PH-7e hook pattern, not a missing kernel.

### SOTA survey (Mode A — sufficient)

**Learned from:**

1. **BLIS / Goto–van de Geijn blocked GEMM** — IKJ + cache blocking; oracle in `common/matmul_blocked_core.c`.
2. **Eigen efficient matrix product** — micro-kernel + blocking discipline; maps `@` / MIR matmul ops.
3. **Saad GMRES** — standard Krylov; `num_gmres` uses shared C reference.
4. Prior autoresearch: [`2026-05-17-horner-pure-li-autoresearch-negative.md`](../docs/numerics/studies/2026-05-17-horner-pure-li-autoresearch-negative.md) — harness DCE; lexer fix landed; row green.

**Conclusion:** Remaining pure_li perf debt is **marginal PH-7e emit tuning** (~5% gap on `matmul_blocked`). Autoresearch novel algorithms **not required**.

### Quality table (this cycle)

| Axis | matmul_blocked | Verdict |
|------|----------------|---------|
| Speed | 1.256× local (target ≤1.2×) | **Open** — micro-opt, not novel algo |
| Accuracy | Checksum vs C oracle | **Pass** |
| Stability | tier-0 N/A (tier-1 micro) | N/A |
| Novel invention | None shipped | **Negative result (valuable)** |

### Agent deliverable checklist

<!-- li-agent -->
## Agent deliverable
- [x] li-tests or lit test id: N/A — no kernel/codegen change shipped this pass
- [x] Bench row / benchmarks path: `matmul_blocked`, `matmul_naive`, `num_gmres`, `horner_pure_li` — local `lic/benchmarks/results/latest.csv` @ `c6e9ca7d`
- [x] Lean/contracts path documented or N/A with reason: N/A — survey-only pass; codegen micro-opt deferred to code_implementer
- [x] Negative result documented if hypothesis rejected: `docs/numerics/studies/2026-05-30-autoresearch-proactive-sweep.md`

---

## Recommended issues / PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P0 | **lic** | `perf(7e): matmul_blocked vec-FMA tile polish — close 1.256×→≤1.2× pure_li gap` | `PH-7e`, `bench-improver` |
| P0 | **benchmarks** | `chore(ingest): refresh tier-1 matrix after lic bench CSV — clear stale matmul/num_gmres reds` | `autoresearch` |
| P1 | **lic** | `fix(bench): horner_pure_li reference spec vs native fast-math mismatch (inf vs finite)` | `bench-improver`, `numerics-research` |
| P1 | **li-math** | `perf: investigate ml_conv2d/ml_mlp tier-1 reds (algo_registry harness)` | `PH-5b` |
| P2 | **benchmarks** | `chore: open or close stale autoresearch digest branches (×6+)` | `agent-hygiene` |
| P2 | **li-cursor-agents** | `fix: agent run registration — repeated unregistered_running_reconciled errors` | `ecosystem-gap` |

**Do not merge** without `pr_reviewer` + `pr_merger`. Do **not** relax `threshold_ratio_cpp`.

---

## Deferred

- **Novel tile schedules, mixed-precision GEMM, autotuning** — blocked until marginal 1.256× gap closed via codegen; re-bench before any invention.
- **`ml_*` tier-1** — li-math ownership; not pure_li autoresearch.
- **`md_thermostat_*` yellow** — stability/visual tier-2; numerics_researcher Mode A first.
- **Near-threshold rows** (`num_cholesky` 1.17×, `cloth_swing`, `robo_*`) — monitor; no autoresearch trigger.
- **Ingest of local bench CSV** — requires `./scripts/ingest/ingest-lic.sh` with `LIC_ROOT`.

---

## Error (control-plane)

Prior autoresearch/bench_improver runs on 2026-05-30 (03:50–04:27Z) recorded `status: error` (supervisor reconciliation when new agent instances start). No stack trace in `agent_runs`. This run completes via digest artifact.

---

## Repro

```bash
cd /path/to/lic && ./scripts/build.sh
cd lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_blocked --runs 3

cd /path/to/benchmarks
python3 scripts/numerics-evidence-checklist.py \
  --study docs/numerics/studies/2026-05-30-autoresearch-proactive-sweep.md
LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh
```
