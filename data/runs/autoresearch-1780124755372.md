# Autoresearch run log — `autoresearch-1780124755372`

**Date:** 2026-05-30T07:05Z  
**Agent:** autoresearch (proactive ecosystem sweep)  
**Briefing:** `data/latest/agent-briefing.json` (generated 2026-05-30T07:03Z)  
**Skills:** `numerics-autoresearch`, `research-li-numerics`  
**north_star_fit:** PH-5b (competitive tier-1), PH-7e (math→SIMD/codegen)

---

## Executive summary

- Refreshed ecosystem audit: **6 dashboard reds** (ingest stale @ 2026-05-29T18:47Z); local tier-1 harness on lic `4d9112ce` shows **1 pure_li codegen gap** (`matmul_blocked` **1.229×**, marginally above 1.2× gate).
- Mode A SOTA survey: blocked GEMM = BLIS/Eigen IKJ+BK=64; naive GEMM = classical IKJ; Horner = FMA recurrence — **no novel algorithm warranted** for any red row.
- **Closed negative** on novel-method invention — remaining debt is PH-7e emit tuning + ingest refresh; delegate to **bench_improver** / **code_implementer**.
- `horner_pure_li` local **2.6×** (PH-7e scalar loop) but SOTA FMA lowering sufficient per prior negative study; verify reports `inf` vs finite spec mismatch (harness fairness, not algorithm gap).
- `num_gmres` harness absent from lic tier-1 scope; workload lives under `benchmarks/benchmarks/workloads/` with shared C oracle — not autoresearch.
- Control-plane: this run registered `status: running` at 07:05Z; completes via digest artifact (no kernel/codegen PR shipped).

---

## Deliverable / findings

### Hypotheses evaluated

| ID | Hypothesis | Metric | Result |
|----|------------|--------|--------|
| H1 | Vec-FMA `ArrayMatMulBlocked2DF64` closes pure_li `matmul_blocked` to ≤1.2× without recipe change | `wall_time` Li/cpp | **Partial** — local **1.229×**; checksum pass; ~3% codegen gap, not novel algo |
| H2 | Novel blocking scheme (non-BLIS tile schedule) beats IKJ+BK=64 | Same | **Rejected** — SOTA sufficient; invention adds proof debt |
| H3 | `matmul_naive` red implies new GEMM algorithm needed | Li/cpp | **Rejected** — local **1.056×** green; dashboard stale at 1.333× |
| H4 | `horner_pure_li` 2.6× local implies Estrin / reordering autoresearch | Li/cpp | **Rejected** — SOTA Horner+FMA; PH-7e vectorize path (Mode A) |
| H5 | `num_gmres` red implies Krylov solver novelty | Li/cpp | **Rejected** — shared C kernel (`LI_EXTRA_C`); harness not in lic tier-1 |
| H6 | `ml_*` reds need autoresearch conv/ML kernels | Li/cpp | **Out of scope** — li-math `algo_registry` |

### Local bench evidence (2026-05-30)

Host: Linux amd64 · lic `4d9112ce` · `-O3 -march=native -ffast-math` · 2–5 runs median

```bash
cd lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_blocked,matmul_naive --runs 2
python3 bench.py --tier 1 --only horner_pure_li --runs 5
```

| Bench | cpp | li | Li/cpp | Dashboard | Variant |
|-------|-----|-----|--------|-----------|---------|
| matmul_blocked | 0.0083 s | 0.0102 s | **1.229×** ✗ | 1.549× red | pure_li (`C = A @ B`) |
| matmul_naive | 0.0018 s | 0.0019 s | **1.056×** ✓ | 1.333× red | pure_li |
| horner_pure_li | 0.0005 s | 0.0013 s | **2.600×** ✗ | 0.750× green | pure_li |

Checksum parity: `matmul_blocked verify ok (pure Li): result=1288460.7563999966`  
Horner verify: `result=inf` (fast-math divergence vs reference spec — known harness fairness issue)

**Implementation note:** `matmul_blocked/li/main.li` uses `C = A @ B` with LUT init + volatile sink; MIR lowers to blocked IKJ+vec-FMA. Gap vs cpp is marginal emit polish, not missing numerical recipe.

### SOTA survey (Mode A — sufficient)

**Learned from:**

1. **BLIS / Goto–van de Geijn blocked GEMM** — IKJ + cache blocking; oracle in `common/matmul_blocked_core.c`.
2. **Eigen efficient matrix product** — micro-kernel + blocking discipline; maps `@` / MIR matmul ops.
3. **Numerical Recipes §5.3** — Horner FMA recurrence; maps `horner_pure_li`.
4. **Saad GMRES** — standard Krylov; `num_gmres` uses shared C reference via `LI_EXTRA_C`.
5. Prior autoresearch: [`2026-05-17-horner-pure-li-autoresearch-negative.md`](../docs/numerics/studies/2026-05-17-horner-pure-li-autoresearch-negative.md).

**Conclusion:** No red row requires novel numerics. Remaining pure_li perf debt is **PH-7e codegen** (~3% on `matmul_blocked`; ~2.6× on `horner_pure_li` scalar loop). Dashboard reds for `matmul_naive` and `num_gmres` are **ingest-stale**.

### Quality table (this cycle)

| Axis | matmul_blocked | horner_pure_li | Verdict |
|------|----------------|----------------|---------|
| Speed | 1.229× local (target ≤1.2×) | 2.600× local | **Open** — codegen, not novel algo |
| Accuracy | Checksum vs C oracle | verify `inf` mismatch | matmul pass; horner harness fairness |
| Stability | tier-0 N/A (tier-1 micro) | N/A | — |
| Novel invention | None shipped | None shipped | **Negative result (valuable)** |

### Agent deliverable checklist

<!-- li-agent -->
## Agent deliverable
- [x] li-tests or lit test id: N/A — no kernel/codegen change shipped this pass
- [x] Bench row / benchmarks path: `matmul_blocked`, `matmul_naive`, `horner_pure_li` — local `lic/benchmarks/results/latest.csv` @ `4d9112ce`
- [x] Lean/contracts path documented or N/A with reason: N/A — survey-only pass; PH-7e micro-opt deferred to code_implementer
- [x] Negative result documented if hypothesis rejected: `docs/numerics/studies/2026-05-30-autoresearch-proactive-sweep-v8.md`

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P0 | **lic** | `perf(7e): matmul_blocked vec-FMA tile polish — close 1.229×→≤1.2× pure_li gap` | `PH-7e`, `bench-improver` |
| P0 | **benchmarks** | `chore(ingest): refresh tier-1 CSV after lic bench — clear stale matmul_naive/num_gmres reds` | `autoresearch` |
| P1 | **lic** | `perf(7e): horner_pure_li FMA+vectorize scalar loop — close 2.6× PH-7e gap (SOTA, not novel)` | `PH-7e`, `numerics-research` |
| P1 | **lic** | `fix(bench): horner_pure_li reference spec vs fast-math mismatch (inf vs finite)` | `bench-improver` |
| P1 | **lic** | `feat(bench): wire num_gmres into tier-1 harness scope (shared C oracle)` | `ecosystem-gap`, `PH-5b` |
| P1 | **li-math** | `perf: investigate ml_conv2d/ml_mlp tier-1 reds (algo_registry harness)` | `PH-5b` |
| P2 | **benchmarks** | `chore: consolidate autoresearch digest branches (×7+)` | `agent-hygiene` |

**Do not merge** without `pr_reviewer` + `pr_merger`. Do **not** relax `threshold_ratio_cpp`.

---

## Deferred

- **Novel tile schedules, mixed-precision GEMM, autotuning, Estrin Horner** — blocked until PH-7e closes marginal gaps; re-bench before any invention.
- **`ml_*` tier-1** — li-math ownership; not pure_li autoresearch.
- **`md_thermostat_*` yellow** — stability/visual tier-2; numerics_researcher Mode A first.
- **Near-threshold rows** (`num_cholesky` 1.17×, `cloth_swing`, `robo_*`) — monitor; no autoresearch trigger.
- **Ingest of local bench CSV** — requires `LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh`.

---

## Repro

```bash
cd /path/to/lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_blocked,matmul_naive --runs 2
python3 bench.py --tier 1 --only horner_pure_li --runs 5

cd /path/to/benchmarks
python3 scripts/numerics-evidence-checklist.py \
  --study docs/numerics/studies/2026-05-30-autoresearch-proactive-sweep-v8.md
LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh
```
