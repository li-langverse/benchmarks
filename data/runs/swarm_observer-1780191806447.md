# Swarm observer digest — `swarm_observer-1780191806447`

**Date:** 2026-05-31T01:46Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai — gap orchestration)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-31T01:00Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (81.5, grade **B**, `unattended_safe: true` — see caveat below)  
**Orchestrator notes:** [`orch-r3`](../../lic/docs/ecosystem/orchestrator-notes/2026-05-31-orch-r3-missing-package-sweep.md) complete this cycle

---

## Executive summary

- **Posture: degraded (recoverable)** — refreshed scorecard grade **B** (81.5) but CP DB shows ~42–44 errors/agent in the last 2h across 20+ agents; scorecard under-counts errors because 115/120 sampled runs remain `running`.
- **`orch-r3` complete** — gap ingest + apply @ 01:45:58Z patched 3 `missing_package` rows (`pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot`) to `ecosystem-package-backlog.md`; handoff to `issue_planner` / `package_architect`.
- **Programmatic observer active @ 01:45Z** — CP `control_plane_state.swarm_health.healthy: true` with `goal_mismatch` finding; remediations dispatch `implementation_gaps` + retry `proof_gap_researcher`.
- **Unattended operation: marginal** — scorecard flags `unattended_safe: true` only because terminal error rate in local sample is 0%; CP history + wave-kill pattern still require meta oversight.
- **Strong lanes:** `ecosystem_posture` **98** (0 red benchmarks); `agent_kit_maintainer` **47 finished / 2h**; `proof_gap_researcher` and governance heap agents now **running** in current wave.
- **Weak lanes:** `gap_pressure` **60** (64 open registry rows); `goal_directed_health` **70** (6/9 runners stopped); `briefing_health` **77** (8 preflights skipped, 1 failed).
- **Briefing drift:** `gap_explorer` and `issue_hygiene` still absent from 6h CP runs despite briefing recommendation; `workspace_sweeper` running but 118 errors vs 28 finished in 6h.
- **`CURSOR_API_KEY` set** — auth available; mass `error` rows are wave preemption / SDK terminalization drift, not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Scorecard vs CP divergence | Grade B / 0% error in scorecard vs ~42 errors/agent in CP 2h window | `ecosystem-quality-report.json` vs Supabase `agent_runs` | critical |
| Swarm execution (CP) | 44 errors each for `swarm_observer`, `implementation_gaps`, `workspace_sweeper` in 2h | Supabase `agent_runs` (2h window) | critical |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` @ 01:45:58Z | high |
| Goal-directed runners | 6/9 stopped (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`) | `lic/data/goal-directed-agents/snapshot.json` | high |
| Wave preemption | 15+ agents × ~42 errors / 2h at identical tick timestamps | Supabase `agent_runs` @ 01:41–01:42Z | high |
| Goal orientation drift | `gap_explorer`, `issue_hygiene` not run in 6h | briefing vs CP `agent_runs` | medium |
| CP report stale | `control_plane_reports.is_latest` @ 2026-05-25; `healthy: true`, 0 runs sampled | Supabase `control_plane_reports` | medium |
| Stuck `running` rows | 115/120 scorecard sample; 20+ agents `running` in CP | `ecosystem-quality-report.json` → `swarm-many-running` | medium |
| `org_agent_kit_audit` | preflight exit 1 — 21 repos flagged | `agent-briefing.preflight_runs` | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) | `agent-briefing.preflight_runs` | medium |
| Failed PR CI | lic#612 workspace sweep — CI fail | `ecosystem-quality-report.json` → `failed-pr-ci` | medium |
| httpd plan loop | 2 pending todos; last iterations exit **124** (timeout) | `snapshot.json` runner `httpd` | medium |
| `orch-r3` plan todo | Still `pending` in snapshot until loop marks complete | `snapshot.json` runner `swarm-observer` | low |

### Error sample classification (current tick + 2h window)

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Simultaneous errors @ tick | 20 agents × ~42 errors / 2h ending 01:42Z | **Supervisor wave kill / preemption** — prior run terminated before completion |
| CP `completion: null` on errors | `swarm_observer-1780191635251`, `implementation_gaps-1780191635273` | **SDK terminalization drift** — error status without completion payload |
| Mass `running` in scorecard | 115/120 sampled locally | **Observer not marking terminal after TTL** — inflates `unattended_safe` |
| `agent_kit_maintainer` finished | 47 finished / 2h | **Healthy lane** — deterministic when not preempted |
| `proof_gap_researcher` retry | CP remediation `:goal-align` @ 01:45Z | **Programmatic heal** — briefing P0 research goal |
| httpd exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state` @ 01:45:35Z | `retry_counts: {}`; `stopped_agents: []` |
| Auto-retry dispatch | `swarm_health.remediations` | `implementation_gaps` healer + `proof_gap_researcher` `:goal-align` |
| Gap ingest + apply | `lic/scripts/swarm-gap-*.py` @ 01:45:58Z | 64 open; 25 backlog patches (incl. 3 package) |
| Scorecard refresh | `ecosystem-quality-grade.py` @ 01:46Z | 81.5 grade B (terminal sample thin) |
| `orch-r3` orchestration | This meta pass | 3 `missing_package` gaps reconciled; note updated |

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile mass `error` rows from wave-kill vs true SDK failures | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Scorecard: weight CP DB terminal errors, not only local JSON `running` skew | `benchmarks/scripts/ecosystem-quality-grade.py` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Dispatch heap P40 `gap_explorer` when briefing recommends + registry open | supervisor heap coordinator |
| Auto-close registry `plan_debt` when snapshot `completed_ids` includes todo | `lic/scripts/swarm-gap-ingest.py` |
| Mark `orch-r3` complete in snapshot when orchestrator note lands | `lic/scripts/goal-directed-agents-snapshot.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- Shipping `std.plot` / `std.summary` / `li-line-profiler` — product work; route via `issue_planner`
- lic workspace dirty on sweep + bench branches — human decides merge vs rebase
- lic#612 failing CI — human triage before merge
- httpd wrk soak timeouts — human env/deadline tuning

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (81.5, grade B @ 01:46Z)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed (registry + actions @ 01:45:58Z)
- [x] Orchestrator note: `orch-r3-missing-package-sweep` (this cycle)
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [ ] Control-plane observer PR (terminalization TTL) — deferred to li-cursor-agents

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | std.summary, std.plot, li-line-profiler | Patched → `ecosystem-package-backlog.md`; handoff **`issue_planner`** (`orch-r3`) |
| `plan_debt` (31) | sim/security/studio backlogs patched; master-plan phases deferred | Route **`plan_verifier`** + **`implementation_gaps`** |
| `competitor_feature` (30) | vertical stubs patched to sim-md backlogs | Route **`gap_explorer`** + **`bench_improver`** |
| `ui_ux` (0 open registry) | `orch-r4` prior cycle | **`gui_ux_tester`** / **`docs_ux_tester`** digests |

Do **not** recommend `install-goal-plan-loop-systemd.sh` — use `li-cursor-agents` research/implement goals only.

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + reconcile wave-kill errors | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Scorecard: include CP DB terminal errors in `swarm_execution` dimension | benchmarks | `swarm`, `metrics` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch `gap_explorer` + `issue_hygiene` when briefing recommends | li-cursor-agents | `swarm`, `orchestration` |
| Package issues: `std.summary`, `std.plot`, `li-line-profiler` seed | lic | `ecosystem`, `PH-IO` |
| Fix GHA red on workspace-sweep PR | lic | `ci`, `bug` ([#612](https://github.com/li-langverse/lic/pull/612)) |

---

## Deferred

- ph-db runner plan_debt (9 todos) — no backlog mapping yet
- Master-plan `plan_debt` rows without runner mapping (2i, 7d, 7e, Vision-LLM, 8p)
- 30 `competitor_feature` gaps — research/backlog only
- Control-plane code changes — PR on `li-cursor-agents` with `npm test`
- Periodic full preflight (drop `--skip-slow`)
- `orch-r4-ui-ux-signals` — next swarm-observer plan todo

---

## Error

No fatal error in this meta-agent pass. Mass CP `error` rows @ 01:41–01:42Z have `completion: null` — classified as wave preemption / SDK terminalization drift, not agent logic failures. No stack trace available in CP `completion` JSON.

---

_Report generated by `swarm_observer` · li-langverse/benchmarks · 2026-05-31T01:46Z_
