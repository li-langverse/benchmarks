# Swarm observer digest — `swarm_observer-1780908855046`

**Date:** 2026-06-08T09:40Z  
**Agent:** `swarm_observer`  
**Worker:** `bbcdbaea`  
**Dimension:** `api-coverage`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai — gap orchestration)  
**Briefing:** `/workspace/benchmarks/data/latest/agent-briefing.json` (2026-06-08T09:38Z)  
**Scorecard:** `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` (60.9, grade **D**, `unattended_safe: false`)  
**Orchestrator note:** [`orch-r17`](../../lic/docs/ecosystem/orchestrator-notes/2026-06-08-orch-r17-api-coverage-gap-orchestration.md)  
**Whitepaper:** [`api-coverage`](../../lic/docs/research/swarm_coverage/api-coverage/2026-06-08-whitepaper-bbcdbaea.md)

---

## Executive summary

- **Posture: degraded (critical recovery path)** — refreshed scorecard grade **D** (60.9); `unattended_safe: false`. Ecosystem posture **45** (103 open PRs, 15 CI-fail, 12 repos missing CI on main).
- **API-coverage pipeline blocked** — `swarm-gap-ingest.py` line 229 `SyntaxError` prevented `ingest_verticals_stubs()` from reading `verticals.toml` (12 stub verticals); **remediated locally** in this pass.
- **PyYAML missing** — `swarm-gap-ingest.py` / `swarm-gap-apply-actions.py` cannot run in the org-research worker image (`ModuleNotFoundError: yaml`; no pip/apt). Gap registry frozen @ 2026-05-31 (64 open).
- **Control-plane artifacts absent** — `data/control-plane/latest-report.json` and `state.json` not present under `/app`; programmatic observer retry/stop state unavailable this cycle.
- **Scorecard runs path wrong** — `ecosystem-quality-grade.py` samples `/workspace/li-cursor-agents/data/runs` (0 files); actual runs at `/app/data/runs` → `swarm_execution` dimension under-reported.
- **Briefing alignment partial** — heap dispatches `pr_merger` (lip#52), `ci_maintainer`, `security_auditor`; scorecard also recommends `swarm_observer`, `gap_explorer`, `plan_verifier` — meta/heal agents not in briefing top-3.
- **Goal-directed runners** — 6/9 stopped per snapshot; 37/110 plan todos pending; `orch-r3` / `orch-r4` still pending in swarm-observer runner.
- **`CURSOR_API_KEY` set** — SDK auth available; prior `swarm_coverage@security` run failed with `tools=0` SDK error (transient), not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Gap ingest (api-coverage) | `SyntaxError: unterminated string literal` line 229 | `/workspace/lic/scripts/swarm-gap-ingest.py` | critical |
| Gap apply | `PyYAML required` — ingest/apply cannot execute | `swarm-gap-ingest.py` / `swarm-gap-apply-actions.py` | critical |
| Ecosystem grade | **D** (60.9); `unattended_safe: false` | `benchmarks/data/latest/ecosystem-quality-report.json` @ 09:39Z | critical |
| Failed PR CI | 15 open PRs with failing CI (8 shown in briefing) | `ecosystem-audit.json` → `metrics.failed_prs` | high |
| Repos missing CI | 12 repos missing CI on main | `ecosystem-audit.json` → `metrics.repos_missing_ci_main` | high |
| Org repo API 404 | 25 repos `audit_incomplete` — `gh: Not Found` | `org-repo-ci-audit.json` → `repos_audit_incomplete` | high |
| Gap registry stale | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `swarm-gap-actions.json` @ 2026-05-31 | high |
| Goal-directed runners | 6/9 stopped; snapshot stale (2026-05-30) | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP state missing | No `latest-report.json` / `state.json` | `/app/data/control-plane/` (sdk-slots only) | medium |
| Runs path skew | `runs_sampled: 0` — wrong `runs_dir` in grader | `ecosystem-quality-report.json` → `inputs.runs_dir` | medium |
| Preflight failures | `org_ci_audit` exit 1; `org_agent_kit_audit` exit 1 | `agent-briefing.json` → `preflight_runs` | medium |
| Preflight skipped | 8 scripts (`--skip-slow`) incl. `plan_audit` | `agent-briefing.preflight_runs` | medium |
| Briefing drift | Scorecard recommends `gap_explorer`/`plan_verifier`; briefing top-3 does not | briefing vs scorecard `recommended_agents` | medium |
| Security observer | Prior `swarm_coverage@security` SDK error `tools=0` | `org-research-audit.jsonl` @ 07:34Z | medium |
| Agent kit sync | `roadmap/agent-kit not found` | `org_agent_kit_audit` stderr | low |
| Merge queue | lip#52 gate-ready, merge-approved — blocked in sequence | `agent-briefing.json` → `merge_plan` | low |

### API-coverage dimension audit (`swarm_coverage`)

| Signal | Count / status | Evidence |
|--------|----------------|----------|
| Competitive vertical stubs | 12 rows `workload_class = "stub"` | `benchmarks/workloads/competitive/verticals.toml` |
| `ingest_verticals_stubs` | **Blocked** until ingest runs | `lic/scripts/swarm-gap-ingest.py` |
| `BENCHMARKS_COMPETITIVE` default | `benchmarks/workloads/competitive` | fixed fallback in ingest (this pass) |
| Org GitHub API coverage | 25 repos return HTTP 404 | `org-repo-ci-audit.json` |
| CWE Top25 catalog gaps | 19 missing in catalog | `security-cwe-feed.json` |
| Open PR API surface | 103 open; 656 redundant-pair warnings | `pr-merge-queue-plan.json` |

### Error sample classification

| Pattern | Evidence | Root cause |
|---------|----------|------------|
| Gap ingest crash | `SyntaxError` line 229 | **Prompt/script gap** — broken Path fallback string |
| Gap apply skip | `ModuleNotFoundError: yaml` | **Env default** — worker image lacks `python3-yaml` |
| Security observer fail | `tools=0; duration_ms=33925` | **SDK transient** — premature termination |
| `org_ci_audit` exit 1 | HTTP 404 on phantom repos | **Repo registry drift** — catalog lists uncreated repos |
| `runs_sampled: 0` | grader `runs_dir` mismatch | **Preflight stale path** — `/workspace/li-cursor-agents` vs `/app` |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | `/app/data/control-plane/` | **No state.json** — observer state unavailable |
| Auto-retry dispatch | N/A | Not observable this cycle |
| Gap ingest + apply | `lic/scripts/swarm-gap-*.py` | **Failed** — SyntaxError + PyYAML (see Error) |
| Scorecard refresh | `ecosystem-quality-grade.py` @ 09:39Z | 60.9 grade D; `unattended_safe: false` |
| Ingest syntax fix | This meta pass (local) | Line 229 Path fallback repaired — pending PR |

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Bake `python3-yaml` in org-research worker image | `li-cursor-agents/deploy/` |
| Merge ingest Path fallback fix | `lic/scripts/swarm-gap-ingest.py` |
| Grader: default `runs_dir` to `LI_CURSOR_AGENTS_ROOT/data/runs` or `/app/data/runs` | `benchmarks/scripts/ecosystem-quality-grade.py` |
| Emit `latest-report.json` + `state.json` each supervisor tick | `li-cursor-agents/src/control-plane/` |
| Dispatch `gap_explorer` when `gap_pressure < 80` | supervisor heap / `src/observer/` |
| Refresh goal-directed snapshot on cadence | `lic/scripts/goal-directed-agents-snapshot.py` |
| Prune phantom repos from org CI catalog | `benchmarks/scripts/ensure-org-repo-ci.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- 15 lic PRs with failing CI — human triage (competitive matrix dupes lic#1310–1326)
- Phantom org repos (li-api-kit, li-os, etc.) — create repos or remove from catalog
- CWE Top25 catalog backfill (19 rows) — security governance
- lip#52 merge — requires `pr_merger` + branch protection approval

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (60.9, grade D @ 09:39Z)
- [x] Gap registry + actions read (64 open @ 2026-05-31)
- [x] Gap ingest syntax remediated locally (pending PR)
- [ ] Gap ingest + apply live run — **blocked by PyYAML**
- [x] API-coverage dimension audit (`verticals.toml` stubs + org 404 repos)
- [x] Orchestrator note: `orch-r17-api-coverage-gap-orchestration`
- [x] Whitepaper staged under `lic/docs/research/swarm_coverage/api-coverage/`
- [x] Briefing vs scorecard `recommended_agents` compared
- [ ] Control-plane observer PR — deferred to `li-cursor-agents`

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | line_profiler, std.summary, std.plot | Backlog patched 2026-05-31; handoff **`issue_planner`** |
| `plan_debt` (31) | sim/security/httpd backlogs + master-plan phases | 14 patched; 17 deferred (no runner mapping) → **`plan_verifier`** |
| `competitor_feature` (30) | vertical stubs + catalog rows | **`gap_explorer`** + **`numerics_researcher`** after ingest unblocked |
| `ui_ux` (0 open registry) | `orch-r4` pending in snapshot | **`gui_ux_tester`** |

Do **not** recommend `install-goal-plan-loop-systemd.sh` — use `li-cursor-agents` research/implement goals only.

**Handoffs (north_star_fit: ecosystem, ai):**

| Target agent | Reason |
|--------------|--------|
| `gap_explorer` | Unblock vertical stub ingest → registry; 30 competitor_feature rows |
| `plan_verifier` | Refresh plan audit preflight; close master-plan plan_debt mapping |
| `issue_planner` | 3 `missing_package` backlog todos pending |
| `ci_maintainer` | 12 repos missing CI; 25 org 404 audit gaps |

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| fix(swarm): gap-ingest Path fallback for api-coverage observer | lic | `swarm`, `api-coverage`, `bug` |
| infra: bake `python3-yaml` in org-research worker image | li-cursor-agents | `swarm`, `infra`, `api-coverage` |
| fix(metrics): grader `runs_dir` default for container `/app` layout | benchmarks | `swarm`, `metrics` |
| chore(metrics): refresh ecosystem grade 2026-06-08 (api-coverage observer) | benchmarks | `swarm`, `metrics` |
| docs(swarm): orch-r17 api-coverage gap orchestration note | lic | `swarm`, `orchestration` |
| chore(deps): bump actions/deploy-pages from 4 to 5 | lip | `dependencies` ([#52](https://github.com/li-langverse/lip/pull/52)) |
| CWE Top25 catalog backfill (19 rows) | lic | `security`, `CWE` |
| Prune phantom org repos from CI audit catalog | benchmarks | `ci`, `api-coverage` |

---

## Deferred

- Live gap re-ingest/apply until PyYAML lands in worker image
- Whitepaper publish to `research-findings` repo (not mounted in worker)
- Full preflight without `--skip-slow` (`plan_audit`, `security_cwe_audit`)
- Refresh goal-directed snapshot; restart 6 stopped runners
- 30 `competitor_feature` gaps — research lane after ingest unblocks
- Control-plane code changes — PR on `li-cursor-agents` with `npm test`
- Duplicate lic competitive-matrix PR stack (lic#1310–1326) — human dedup

---

## Error

```text
# Gap ingest (prior to local fix)
File "/workspace/lic/scripts/swarm-gap-ingest.py", line 229
    vert = Path(os.environ.get("BENCHMARKS_COMPETITIVE", str(LANGVERSE / "benchmarks/workloads/competitive"))/verticals.toml"
                                                                                                                            ^
SyntaxError: unterminated string literal (detected at line 229)

# Gap ingest (after fix — still blocked)
swarm-gap-ingest: PyYAML required (pip install pyyaml)

# Prior security dimension run
Error: SDK run status: error; run_id=run-0b2f7260-1a33-45e1-9f92-314cda311a27; model=default; attempt=1; tools=0; duration_ms=33925
    at errorRunResult (file:///app/dist/backends/cursor-sdk-backend.js:236:43)
```

---

_Report generated by `swarm_observer` · worker `bbcdbaea` · dimension `api-coverage` · 2026-06-08T09:40Z_
