# Swarm observer digest — `swarm_observer-1780137256044`

**Date:** 2026-05-30T10:37Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T10:03Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (71.3, grade **C**, refreshed @ 10:36Z)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md`

---

## Executive summary

- **Posture: degraded** — grade **C** (71.3); `unattended_safe: false`; programmatic observer `needs_meta_observer: false` but **33% error rate** in last 45 terminal runs triggers meta audit.
- **`orch-r2-competitor-stubs` completed** — 30 `competitor_feature` registry rows; 12 `verticals.toml` stub todos patched to `sim-md-research-backlog.md` + `sim-algorithm-backlog.md` (CAE pde/fea/cfd routed this pass).
- **Gap pressure: 65 open** — 30 competitor, 32 plan_debt, 3 missing_package; ingest/apply re-run @ 10:36Z (`benchmarks/data/latest/swarm-gap-actions.json`).
- **Supervisor wave errors @ 10:24–10:34Z** — deliverable-gate failures (`premature: true`, unchecked checklist) dominate; not SDK auth (`CURSOR_API_KEY` set).
- **75/120 runs still `running`** — CP status hygiene drift; `swarm_execution` score 55 (down from 74 prior pass as more runs terminalized).
- **Goal-directed runners: 6/9 stopped** — `agents_live: 0`; httpd active on `gap-phase2-perf-wrk-soak` (perf branch dirty workspace).
- **Briefing alignment partial** — P0 recommends `proof_gap_researcher`, `workspace_sweeper`, governance stack; recent 30m errors on same agents (goal_mismatch finding in CP state).
- **Next orchestrator todo:** `orch-r4-ui-ux-signals` (studio-ux-16/17 already patched by apply-actions).

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C (71.3); error rate 33% | `data/latest/ecosystem-quality-report.json` | critical |
| Gap registry | 65 open (30 competitor, 32 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Goal-directed runners | 6/9 stopped; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| Deliverable gate | 15 errors / 45 terminal runs (30m window) | Supabase `agent_runs.completion` | high |
| CP status hygiene | 75/120 sampled `running` | scorecard `swarm_execution`, `li-cursor-agents/data/runs` | medium |
| CP report stale | Latest `control_plane_reports` @ 2025-05-25 | Supabase `control_plane_reports.is_latest` | medium |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| Competitor infra | `verticals.toml` missing on benchmarks main | `gap-infra-verticals-toml-missing-benchmarks-main` | medium |
| Explorer vs registry | Tier-1 bench **0 red** in audit; registry still has red competitor rows | `ecosystem-audit.json`, registry | low |

### Error sample classification

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Unchecked deliverable checklist | `plan_verifier-1780136680811` | **Prompt gap** — deliverable section present, no `[x]` items |
| PR without checklist in body | `docs_maintainer-1780136680815` | **Deliverable gate** — 3 PRs opened, body missing checklist |
| Complete work marked error | `issue_planner-1780136680807`, `bug_fixer-1780136680758` | **Gate false positive** — `complete: true` but `deliverable_checked: false` |
| Short SDK output | `autoresearch-1780137247556` | **SDK preemption** — `premature: true`, output too short |
| Wave correlation | 10+ agents error @ 10:24:40Z | **Concurrent tick** — shared timestamp, not repo conflict |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | 90 gaps in registry; 0 new competitor/vertical rows |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 23 patches incl. 3 CAE vertical stubs → `sim-algorithm-backlog.md` |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` | 71.3 grade C; `unattended_safe: false` |
| Observer scan | CP `control_plane_state` @ 10:36Z | `retry_counts: {}`; dispatch `implementation_gaps`, retry `proof_gap_researcher` |
| Meta observer | This pass | `orch-r2` closed; orchestrator note + backlog routing |

### `orch-r2-competitor-stubs` reconciliation

| Category | Count | Backlog target | Handoff |
|----------|------:|----------------|---------|
| Vertical stubs (sim-md-research) | 9 | `sim-md-research-backlog.md` | `numerics_researcher` via `sim-md-research` loop |
| Vertical stubs (sim CAE) | 3 | `sim-algorithm-backlog.md` | `numerics_researcher` via `sim` loop |
| HPC / catalog / bench-red | 18 | registry only (deferred patch) | `numerics_researcher`, `bench_improver`, `gap_explorer` |

Closed: `gap-plan-pending-swarm-observer-orch-r2-competitor-stubs`.

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Don't mark `complete: true` runs as `error` when only checklist formatting fails | `li-cursor-agents/src/control-plane/run-completion.ts` |
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports` each tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Throttle concurrent SDK dispatches | `li-cursor-agents/src/supervisor/loop.ts` |
| Close stale benchmark-red registry rows when audit green | `lic/scripts/swarm-gap-ingest.py` |
| Re-enable slow preflights when tick budget allows | `benchmarks/scripts/agent-briefing.py` |

### Human-only blockers

- Merge `verticals.toml` to benchmarks `main` (cross-repo governance).
- Perf branch dirty workspace on lic (`perf/bench-improver-matmul-tier1-green-20260530`).
- Package implementation for std.summary/plot — human `plan-approved`.
- **`trusted.lean`** — out of scope.

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (71.3, grade C)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed @ 10:36Z
- [x] `orch-r2-competitor-stubs` orchestrator note + registry close
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [ ] `orch-r4-ui-ux-signals` (next pass)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Merge competitive `verticals.toml` to benchmarks main | `benchmarks` | `ecosystem`, `competitor_feature`, `no-auto-merge` |
| Fix deliverable gate false positives on complete runs | `li-cursor-agents` | `control-plane`, `swarm_observer` |
| Observer: terminalize stuck `running` runs after SDK TTL | `li-cursor-agents` | `control-plane`, `observer` |
| Refresh stale benchmark-red gap rows when audit is green | `lic` | `gap_explorer`, `orchestration` |
| Agent-kit drift: 9 repos (preflight exit 1) | org repos | `agent_kit_maintainer` |
| Live docs for 8 repos without pages | org repos | `docs_maintainer` |

---

## Deferred

- `orch-r4-ui-ux-signals` — studio-ux-16/17 patched; dedicated UI gap reconciliation next pass.
- Re-enable slow preflights when tick budget allows.
- CP historical error relabeling — batch job.
- Restart stopped goal-directed runners (`compiler-studio`, `sim`, `studio-ui-ux`, research loops).
- Auto-close tier-1 red competitor gaps after catalog ingest confirms green benchmarks.
