# Swarm observer report — `orch-r3-missing-package-sweep`

> `swarm_observer` · research goal `swarm_coverage` · 2026-05-29 · run `swarm_observer-1780083262015`

## Executive summary

- **Posture: degraded.** Ecosystem grade **C** (71.3); `unattended_safe: false` — swarm cannot run fully unattended.
- **Swarm execution:** 102 runs stuck `running`; 22% error rate on last 18 terminal runs; batch supersede at 19:33Z left 10+ agents `error` in DB while JSON artifacts still show `running`.
- **Goal-directed:** 6/8 runners stopped in snapshot; `httpd` runner active with 2 pending perf todos; `agents_live: 0`.
- **Gap orchestration (orch-r3): complete** — 3 open `missing_package` gaps; 5 backlog todos `pending`; `std.io`/`std.csv` closed in registry but backlog not synced.
- **Briefing alignment:** Heap + recommended agents match active dispatch (`bench_improver`, `plan_verifier`, `implementation_gaps`, `workspace_sweeper`); PR-merge lane idle (0 open PRs).
- **Preflight:** 2 failures (`org_agent_kit_audit`, `security_cwe_audit` exit 1); deliverable gate skipped (`LI_CURSOR_AGENTS_ENABLED` unset).
- **Benchmarks:** 6 red tier-1 rows — numerics lane active (`bench_improver` running on `lic`).
- **Credentials:** `CURSOR_API_KEY` and `GH_TOKEN` present on host.

## Findings

| Agent / area | Symptom | Evidence | Severity |
|--------------|---------|----------|----------|
| Swarm SDK | 102 runs `running` (stuck / superseded) | `li-cursor-agents/data/runs`, `ecosystem-quality-report.json` | critical |
| Swarm SDK | 22% terminal error rate | `benchmarks/data/latest/ecosystem-quality-report.json` | high |
| Batch dispatch | 10 agents errored ~19:33:57Z same second | `agent_runs` DB, `plan_verifier-1780083177670.json` | high |
| `pr_alignment` | SDK premature end (1 tool call) | `li-cursor-agents/data/runs/pr_alignment-1780082337477.json` | medium |
| `pr_merger`, `ci_maintainer` | SDK error in terminal sample | ecosystem-quality top_error_agents | medium |
| Control plane | Latest report stale (2026-05-25), `swarm_health.healthy: true` | Supabase `control_plane_reports` | medium |
| Observer | `retry_counts` empty despite error streak | `control_plane_state` payload | medium |
| Goal-directed | 6 runners stopped | `lic/data/goal-directed-agents/snapshot.json` | high |
| Briefing | `gap_explorer` “2 missing std” vs registry | `agent-briefing.json`, `registry.yaml` | medium |
| Package gaps | 3 open missing_package | `registry.yaml`, `ecosystem-package-backlog.md` | low |
| Preflight | org agent-kit + CWE audit fail | `agent-briefing.preflight_runs` | high |
| Benchmarks | 6 red rows | `agent-briefing.ecosystem_audit.benchmarks.red` | high |
| Workspace | 2 dirty repos (lic, benchmarks) | `workspace_dirty_sweep` | low |

## Self-heal actions taken

Programmatic observer (this cycle):

- `observer.last_scan_at`: 2026-05-29T19:35:36Z
- `retry_counts`: `{}` — no auto-retries fired
- `stopped_agents`: `[]`
- Interventions snapshot: empty

Meta-audit (this run):

- Regenerated `ecosystem-quality-report.json` (71.3, grade C)
- Ran `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`
- Patched 3 `missing_package` backlog rows; closed orch-r3 plan_debt gap
- Orchestrator note: `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r3-missing-package-sweep.md`

## Recommended control-plane fixes

| Fix | Path |
|-----|------|
| Finalize superseded `running` runs when new dispatch starts | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Increment `observer.retry_counts` on SDK error streak (same briefing_hash) | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports.is_latest` on supervisor tick | control-plane report writer |
| Map registry `closed` → backlog `completed` in apply-actions | `lic/scripts/swarm-gap-apply-actions.py` |
| Sync briefing `gap_explorer` reason from explorer JSON | `benchmarks/scripts/agent-briefing.py` |
| Enable deliverable gate when agents enabled | `LI_CURSOR_AGENTS_ENABLED=1` |

## Human-only blockers

- Merge governance PRs (`trusted.lean`, master-plan phase claims)
- Human review of `lic` PR #419 and `li-cursor-agents` PR #35 (sweep branches exist)
- `pkg-std-io` / `pkg-std-csv` backlog closure needs product confirmation

## Agent deliverable checklist

- [x] Ecosystem quality grade refreshed
- [x] Gap ingest + apply-actions
- [x] Orchestrator note (`orch-r3`)
- [x] Registry orch-r3 closed; plan backlog orch-r3 completed
- [x] Report under `benchmarks/data/runs/`
- [ ] PR on `li-cursor-agents` for observer fixes (deferred — orchestration-only pass)

**Orchestrator note:** [2026-05-29-orch-r3-missing-package-sweep.md](https://github.com/li-langverse/lic/blob/main/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r3-missing-package-sweep.md)

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| chore(ecosystem): scaffold li-line-profiler package seed | lic | ecosystem, missing-package |
| feat(std): std.summary + std.plot module plan (PH-IO-7, PH-IO-5) | lic | plan-needed, PH-IO-* |
| fix(control-plane): mark superseded SDK runs terminal on re-dispatch | li-cursor-agents | swarm, self-heal |
| chore(benchmarks): sync missing_std_modules briefing with explorer | benchmarks | ecosystem |
| research: tier-1 matmul_naive / num_gmres red row plan | lic | numerics, PH-7e |

## Deferred

- `orch-r4-ui-ux-signals` — studio/gui UX gap surfacing
- Product code for std modules and line profiler
- li-cursor-agents observer PR (no code change this run)
- systemd `li-agents-async-swarm` restart (sweeper reported try-restart failed)
