# Swarm observer digest — `swarm_coverage` / `orch-r2-competitor-stubs`

> `swarm_observer` · research goal `swarm_coverage` · run `swarm_observer-1780143553248`  
> **north_star_fit:** ecosystem, ai — gap orchestration registry + backlog apply + handoffs

---

## Executive summary

- **Posture: degraded (not critical).** Ecosystem grade **C** (70.8); programmatic observer reports `healthy: true` with one `goal_mismatch` finding — meta audit warranted but auto-heal not exhausted.
- **Unattended safe: no** (`unattended_safe: false` after gap refresh) — 100 runs stuck `running`, 6/9 goal-directed runners stopped, 8 preflights skipped (`--skip-slow`).
- **`orch-r2-competitor-stubs` complete.** Ingest confirmed **30 open `competitor_feature`** rows; apply-actions patched **9 vertical stubs** → `sim-md-research-backlog.md`.
- **Briefing alignment drift:** recommended `workspace_sweeper`, `implementation_gaps`, `gap_explorer`, `agent_kit_maintainer` not in recent terminal runs; observer dispatched `implementation_gaps` + `proof_gap_researcher` retry.
- **Wave errors (12:11Z):** 10+ agents errored in parallel pool preemption — likely SDK slot contention, not auth (`CURSOR_API_KEY` present).
- **6 red benchmark rows** (`matmul_blocked`, `matmul_naive`, ML forwards, `num_gmres`) — handoff to `bench_improver` / `numerics_researcher` via competitor registry rows.
- **Gap registry:** 65 open (3 missing_package, 32 plan_debt, 30 competitor_feature); was stale at 27 in grade inputs until this cycle.
- **Evidence paths:** `benchmarks/data/latest/ecosystem-quality-report.json`, `benchmarks/data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml`, `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md`.

---

## Deliverable / findings

### Swarm health table

| Agent / area | Symptom | Evidence | Severity | Root cause |
|--------------|---------|----------|----------|------------|
| Swarm (SDK) | 100 runs `running`; 30% incomplete rate | `ecosystem-quality-report.json` → `swarm_execution` | medium | Stuck SDK sessions; needs observer stale-run sweeper |
| Parallel pool | 10 agents `error` @ 12:11Z, null completion | `li-cursor-agents/data/runs/*-178014308*.json` | medium | Preemption / slot contention (runs show `run_started` only) |
| `code_implementer` | Post-run gate exit 1 despite deliverable | `code_implementer-1780141852162.json` | low | Gate mismatch (work done; PR #489 open) |
| Goal-directed | 6/9 runners stopped; 0 agents live | `lic/data/goal-directed-agents/snapshot.json` | high | Plan loops not running (systemd/user units) |
| Briefing | `org_agent_kit_audit` exit 1 | `agent-briefing.json` → `preflight_runs` | medium | 9 repos missing/drifted agent-kit |
| Benchmarks | 6 red tier-1 rows | `agent-briefing.json` → `ecosystem_audit.benchmarks.red` | high | PH-7e codegen / PH-5b numerics debt |
| Gap registry | 30 competitor_feature open | `lic/data/swarm-gap-registry/registry.yaml` | low | Expected backlog; orch-r2 ingested + partial apply |
| `workspace_sweeper` | PR exists (#395); systemctl restart failed | `workspace_sweeper-1780143071961.json` | low | Orchestration env (not product) |

### Self-heal actions taken (programmatic observer)

| Action | Target | Reason |
|--------|--------|--------|
| `dispatch_healer` | `implementation_gaps` | `agent_deliverable_gaps` in briefing |
| `retry_agent` | `proof_gap_researcher` | research goal `provability_holes` priority 9 |
| Gap ingest/apply | registry + backlogs | swarm_observer orch-r2 prep |
| Grade refresh | `ecosystem-quality-report.json` | stale 2026-05-27 snapshot |

`observer.retry_counts`: `{}` · `stopped_agents`: `[]` · `needs_meta_observer`: false

### Gap orchestration (`swarm_coverage`)

| `gap_kind` | Open | Apply this cycle | Handoff |
|------------|-----:|------------------|---------|
| `competitor_feature` | 30 | 9 vertical stubs → sim-md-research backlog | `numerics_researcher`, `bench_improver`, `gap_explorer` |
| `missing_package` | 3 | pkg-line-profiler, std.summary, std.plot pending | `issue_planner`, `package_architect` |
| `plan_debt` | 32 | sim/security/ph-db/studio patches | runner loops via control plane |
| `ui_ux` | 0 | 2 studio rows patched (orch-r4 overlap) | `studio_ui_ux_builder` |

### Agent deliverable checklist

- [x] Read + refresh `ecosystem-quality-report.json`
- [x] Queried control-plane DB (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Compared `recommended_agents` vs recent runs (goal drift documented)
- [x] Sampled error runs; classified root causes
- [x] Ran `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`
- [x] Closed `orch-r2-competitor-stubs` in registry + backlog
- [x] Orchestrator note written
- [x] Gates passed (`swarm-observer-plan-gates.sh`)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| **observer: stale SDK run sweeper** — mark `running` > 2h incomplete, retry budget | `li-cursor-agents` | `control-plane`, `agent:swarm_observer` |
| **gap-apply: map benchmark-red competitor_feature to numerics backlog** | `lic` | `orchestration`, `agent:swarm_observer` |
| **feat(httpd): gap-phase2 perf wrk soak** — review PR #489 | `lic` | `li-swarm`, `agent:code_implementer` |
| **bench: tier-1 green matmul_naive + num_gmres** | `lic` | `PH-7e`, `agent:bench_improver` |
| **org: agent-kit drift sync (9 repos)** | org-wide | `agent:agent_kit_maintainer` |
| **docs: merge verticals.toml competitive registry to main** | `lic` / `benchmarks` | `competitor_feature`, `agent:gap_explorer` |

---

## Recommended control-plane fixes (file paths)

| Path | Change |
|------|--------|
| `li-cursor-agents/src/observer/` | Stuck-run detector + incomplete finalization |
| `lic/scripts/swarm-gap-apply-actions.py` | Backlog mapping for HPC/benchmark `competitor_feature` |
| `benchmarks/scripts/ecosystem-quality-grade.py` | Use live open-gap count from `swarm-gap-actions.json` |
| `li-cursor-agents/prompts/swarm-observer.md` | Require gap_kind table + orch note path (already partial) |

---

## Human-only blockers

- Governance / provability PRs — no auto-merge
- Compiler tier-1 perf (PH-7e SIMD matmul) — product work, not orchestration
- Goal-directed systemd units stopped — may need `systemctl --user start` on plan loops
- 147 branches without open PR — human triage before mass PR creation

---

## Deferred

- **`orch-r3-missing-package-sweep`** — next observer backlog item
- **`orch-r4-ui-ux-signals`** — studio-ui gap taxonomy
- **21 HPC/benchmark competitor rows** — handoff-only until apply-actions backlog mapping added
- **Full preflight suite** — re-run without `--skip-slow` for unattended certification
- **Control-plane report sync** — DB `control_plane_reports` last generated 2026-05-25 (stale vs live state payload)

---

## Error

No fatal error in this observer run. Sampled agent errors:

```
Error: Implementer post-run gates failed: gates exit 1
    at finalizeAgentRun (.../li-cursor-agents/dist/control-plane/finalize-run.js:51:48)
```

Evidence: `li-cursor-agents/data/runs/code_implementer-1780141852162.json`

---

_Formatted by swarm_observer · 2026-05-30T12:20Z_
