# Swarm observer digest — `swarm_observer-1780188806890`

**Date:** 2026-05-31T00:55Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem + ai — registry, backlog apply, handoffs)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T21:55Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (77.0, grade **C**, `unattended_safe: true`)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-31-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — grade **C** (77.0); strong ecosystem posture (100) vs weak gap pressure (60) and swarm execution (70).
- **Unattended: marginal** — scorecard `unattended_safe: true`, but 64 open registry gaps, briefing/heap drift, and high historical SDK error volume require meta audits.
- **`orch-r3` completed** — missing-package sweep: 3 open `missing_package` gaps patched to `ecosystem-package-backlog.md` (`pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot` all **pending**).
- **Gap pipeline fresh** — ingest + apply @ 2026-05-31T00:53:15Z (64 open: 30 competitor, 31 plan_debt, 3 missing_package); 23 backlog patches idempotent.
- **CURSOR_API_KEY present** — failures are SDK session errors / async-swarm churn, not auth gaps.
- **Goal-directed drift** — `agents_live: 0`; snapshot shows plan runners active but control-plane `agents_live` metric stale; 6 runners historically stopped per scorecard.
- **Briefing vs dispatch** — heap prioritizes governance (`plan_verifier`, `implementation_gaps`, `issue_hygiene`); `gap_explorer` recommended but not in latest CP wave (goal_mismatch finding).
- **Programmatic observer @ 00:52Z** — `retry_counts: {}`, `stopped_agents: []`; remediations: `implementation_gaps`, `workspace_sweeper` goal-align retry; local `healthy: true`, `needs_meta_observer: false`.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Gap registry | 64 open gaps | `benchmarks/data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Missing packages | `std.summary`, `std.plot` missing; line_profiler seed open | `ecosystem-explorer.json`, `ecosystem-package-backlog.md` | high |
| Goal-directed | 6/9 runners stopped (scorecard); `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| Swarm execution | 23% error rate on last 13 terminal runs (local sample) | `ecosystem-quality-report.json` → `swarm_execution` | high |
| SDK errors | Terminal error after productive work (45 tools, 241s) | `li-cursor-agents/data/runs/docs_maintainer-1780177405649.json` | medium |
| GUI capture | SDL `RenderReadPixels` 0 frames | `li-cursor-agents/data/runs/gui_ui_tester-1780177405633.json` | medium |
| Goal orientation | `gap_explorer`, `agent_kit_maintainer`, `pr_branch_opener` not in recent CP window | `li-cursor-agents/data/control-plane/state.json` → `goal_mismatch` | medium |
| Preflight | `org_agent_kit_audit` exit 1; `security_cwe_audit` exit 1 | `agent-briefing.preflight_runs` | medium |
| Workspace | 1 dirty `lic` repo on sweep branch | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| CP report stale | Supabase `control_plane_reports.is_latest` @ 2026-05-25 | MCP `control_plane_reports` | low |
| Benchmarks | 0 red; `matmul_blocked` yellow; 3 near tier-1 | `agent-briefing.json` → `ecosystem_audit` | low |

### Error sample classification

| Pattern | Example run | Root cause |
|---------|-------------|------------|
| SDK terminal after work | `docs_maintainer-1780177405649` | **Cursor SDK session error** — completion audit yes, no deliverable section |
| SDK terminal (UX) | `gui_ui_tester-1780177405633` | **SDK error** after audit artifacts; native SDL capture regression |
| Bench / env | `bench_improver-1780177395064` | **SDK or harness** — tier-1 bench context |
| Historical volume | 1096 `workspace_sweeper`, 1068 `bug_fixer` errors (all-time CP) | Mix of wave-kill, SDK errors, retries |
| httpd plan | `gap-phase2-perf-wrk-soak` exit 124 | **Plan loop timeout** — long wrk soak |

**Error (representative):**

```
Error: SDK run status: error; run_id=run-9bc95d78-efcb-43c2-8130-7bf4b2b4963c; model=default; attempt=1; tools=45; duration_ms=240979
```

Evidence: `li-cursor-agents/data/runs/docs_maintainer-1780177405649.json`

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | `li-cursor-agents/data/control-plane/state.json` @ 00:52:59Z | `healthy: true`; `retry_counts: {}` |
| Remediation queue | `swarm_health.remediations` | `implementation_gaps`; `workspace_sweeper` goal-align |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 00:53:15Z | Registry refreshed (92 total, 64 open) |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 00:53:15Z | 3 package + 20 plan/competitor patches (idempotent) |
| Scorecard | `ecosystem-quality-report.json` @ 21:48Z | 77.0 grade C (current) |
| Stopped agents | CP `control_plane_state` | `stopped_agents: []` |

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Dispatch `gap_explorer` when briefing cites missing std + registry has open `missing_package` | supervisor heap / `benchmarks/scripts/agent-briefing.py` |
| Route `issue_planner` handoff for `pkg-std-summary`, `pkg-std-plot`, `pkg-line-profiler` | `research-goals.yaml` / briefing `implementation_queue` |
| Classify SDK errors with partial completion as `incomplete` not `error` | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh Supabase `control_plane_reports` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Align `needs_meta_observer` with scorecard `swarm_execution < 75` | `li-cursor-agents/src/observer/` |
| Nightly full preflight (no `--skip-slow`) | supervisor env |
| Fix native SDL capture for `gui_ui_tester` | `li-cursor-agents/ux-harness/` + lic `studio-ui-ux-capture-native.sh` |

### Human-only blockers

| Blocker | Why human |
|---------|-----------|
| `std.plot` / `std.summary` implementation | Product modules — provability + API review |
| `li-line-profiler` package seed | New package placement — `package_architect` |
| 21 repos agent-kit drift | `org_agent_kit_audit` exit 1 |
| `verticals.toml` on benchmarks main | Blocks vertical ingest (`gap-infra-verticals-toml-missing-benchmarks-main`) |
| Master-plan / `trusted.lean` | Governance — no auto-merge |

### Agent deliverable checklist

- [x] Read ecosystem quality report + briefing + gap registry/actions
- [x] Queried control-plane DB (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Ran `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`
- [x] Completed `orch-r3-missing-package-sweep` orchestrator note
- [x] Wrote digest to `benchmarks/data/runs/`
- [ ] PR on `li-cursor-agents` for observer prompt fixes (deferred — no prompt edits this pass)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| [Ecosystem gap] PH-IO-7: ship `std.summary` for agent summary.json | `lic` | `master-plan-gap`, `ecosystem` |
| [Ecosystem gap] PH-IO-5: ship `std.plot` for static dashboard | `lic` | `master-plan-gap`, `ecosystem` |
| [Package seed] `li-line-profiler` — line-level profiling (WP-B) | `lic` or new `li-line-profiler` | `ecosystem`, `plan-needed` |
| chore: merge `benchmarks/competitive/verticals.toml` to main | `benchmarks` | `ecosystem`, `swarm-gap` |
| fix(ux-harness): native SDL capture 0 frames (`gui_ui_tester`) | `li-cursor-agents` | `bug`, `ux` |
| chore(agent-kit): sync 21 drifted org repos | `roadmap` / per-repo | `agent-kit` |

---

## Deferred

- **`orch-r4-ui-ux-signals`** — next swarm-observer plan todo; link `studio-ui-ux` gaps (`studio-ux-16`, `studio-ux-17` already patched pending).
- **ph-db plan_debt (9 todos)** — apply deferred (no runner backlog mapping); route via agents control plane, not new systemd loops.
- **Observer prompt / `src/observer/` PR** — recommend after collecting 2+ ticks of wave-kill vs SDK classification data.
- **Regenerate scorecard** — optional if briefing hash changes; current report &lt;1h old.

---

## Gap orchestration summary (`swarm_coverage`)

| `gap_kind` | Open | Primary action this pass |
|------------|-----:|--------------------------|
| `missing_package` | 3 | Patched backlog todos pending; handoff `issue_planner` |
| `plan_debt` | 31 | 12 runner todos patched; master-plan rows deferred |
| `competitor_feature` | 30 | Vertical stubs patched to sim-md-research backlog (orch-r2); tier-1 red tracked |

**north_star_fit:** Ecosystem orchestration for proof-before-perf backlog — package gaps documented without product code in `lic` (orchestration-only pass).
