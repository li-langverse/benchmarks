# Swarm observer digest — `swarm_observer-1780135312809`

**Date:** 2026-05-30T10:02Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T10:00Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (75.0, grade **C**, refreshed)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — refreshed grade **C** (75.0); scorecard `unattended_safe: true` but swarm_execution dimension dropped to 70 (117 stuck `running`, 33% incomplete rate in sample).
- **`orch-r3-missing-package-sweep` completed** — 3 `missing_package` gaps reconciled; `pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot` set **pending** in `ecosystem-package-backlog.md`.
- **Gap pressure high** — **64** open registry gaps (30 `competitor_feature`, 31 `plan_debt`, 3 `missing_package`); ingest/apply re-ran @ 10:02Z (idempotent).
- **Supervisor wave @ 10:01Z** — 108 agents `error` in 5 min window; root cause **SDK preemption / concurrent tick kill** (`completion.error` null); `CURSOR_API_KEY` is set.
- **`workspace_sweeper` blocked** — GitHub **GraphQL rate limit** on push/PR for benchmarks, lic, li-cursor-agents (human-only).
- **Goal-directed runners** — 6/9 stopped (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`); `agents_live: 0`.
- **Programmatic observer idle** — `observer.retry_counts: {}`, `stopped_agents: []`; no auto-retries despite error burst.
- **CP report stale** — `control_plane_reports.swarm_health.healthy: true` from **2026-05-25**; dashboard under-reports live errors.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C (75.0); swarm_execution 70 | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Goal-directed runners | 6/9 stopped; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP error burst | 108 agents `error` @ 10:01Z wave | Supabase `agent_runs` | high |
| CP status hygiene | 117/120 sampled runs `running`; error_rate 0 in scorecard | `li-cursor-agents/data/runs`, scorecard `swarm_execution` | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25 | `control_plane_reports` is_latest | medium |
| `workspace_sweeper` | GH GraphQL rate limit; no PR opened | `workspace_sweeper-1780135262524.json` extra_instruction | high |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| `missing_package` | 3 open: line_profiler, std.summary, std.plot | `ecosystem-package-backlog.md`, registry | medium |
| Briefing vs heap | P0 research/sweep OK; heap lacks `bench_improver` despite yellow matmul | `agent-briefing.json` heap_plan vs ecosystem_audit | low |

### Error sample classification

| Pattern | Example | Root cause |
|---------|---------|------------|
| Simultaneous `error` @ tick | 15+ agents @ 10:01:02–11Z | **Supervisor wave kill / SDK preemption** — `completion.error` null in CP |
| `run_started` only, status flip to error | `plan_verifier-1780135262556` | **Empty terminalization** — not repo conflict |
| GH rate limit | workspace_sweeper sweep digest | **Human-only** — wait for rate limit reset |
| Historical error volume | 300–500 errors/agent lifetime | **Status reconciliation drift** in CP ingest |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | 90 gaps total; idempotent (0 new rows) |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 20 patches incl. 3 missing_package → ecosystem-package-backlog |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` | 75.0 grade C; `unattended_safe: true` |
| Observer scan | CP `control_plane_state.observer` | `retry_counts: {}`; **no retries** @ 10:01Z |

No programmatic heal for 10:01Z error wave (`retry_counts: {}`, `stopped_agents: []`).

### `orch-r3-missing-package-sweep` reconciliation

| Gap id | `target_todo_id` | Backlog patch | Handoff |
|--------|------------------|---------------|---------|
| `gap-line-profiler-001` | `pkg-line-profiler` | pending | `issue_planner` |
| `gap-missing-std-std-summary` | `pkg-std-summary` | pending (PH-IO-7) | `issue_planner`, `package_architect` |
| `gap-missing-std-std-plot` | `pkg-std-plot` | pending (PH-IO-5) | `issue_planner`, `package_architect` |

Closed registry rows: `gap-missing-std-std-io`, `gap-missing-std-std-csv` (explorer status `present`).

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Retry on wave-kill with backoff budget | `li-cursor-agents/src/observer/retry.ts` |
| Refresh `control_plane_reports` swarm_health each tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Throttle concurrent SDK dispatches per tick | `li-cursor-agents/src/supervisor/` |
| GH rate-limit aware workspace_sweeper | `li-cursor-agents` workspace_sweeper prompt / sweep script |
| Re-enable slow preflights when tick budget allows | `benchmarks/scripts/agent-briefing.py` |

### Human-only blockers

- GitHub **API rate limit** — workspace sweeper cannot open PRs until reset.
- **6 goal-directed runners stopped** — may need user unit restart (not auto-merge).
- **Package implementation** — agents must not ship std modules without `plan-approved`.
- **`trusted.lean`** — out of scope for gap orchestration.

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (75.0, grade C)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed
- [x] `orch-r3-missing-package-sweep` orchestrator note + backlog patches
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [ ] `orch-r4-ui-ux-signals` (deferred — studio-ux-16/17 incidentally patched)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Open issues for std.summary, std.plot, li-line-profiler seed | `lic` | `issue_planner`, `PH-IO`, `gap_explorer` |
| Observer: terminalize stuck `running` runs after SDK TTL | `li-cursor-agents` | `control-plane`, `swarm_observer` |
| Refresh control_plane_reports swarm_health each supervisor tick | `li-cursor-agents` | `dashboard`, `observer` |
| Tier-1 matmul yellow → green (`matmul_blocked`, `matmul_naive`) | `lic` | `bench_improver`, `tier-1`, `PH-7e` |
| Agent-kit drift: 9 repos | org repos | `agent_kit_maintainer` |
| Merge `verticals.toml` competitive registry to benchmarks main | `lic` → `benchmarks` | `ecosystem`, `competitor_feature`, `no-auto-merge` |

---

## Deferred

- `orch-r4-ui-ux-signals` — studio-ux-16/17 patched; dedicated UI gap reconciliation next pass.
- Re-enable slow preflights (`plan_audit`, `ci_bug_triage`, `pr_program`) when tick budget allows.
- CP historical error relabeling (8k+ lifetime `error` rows) — batch job, not this pass.
- Restart stopped goal-directed runners (`compiler-studio`, `sim`, `studio-ui-ux`, research loops).
