# Swarm observer digest — `swarm_observer-1780133314234`

**Date:** 2026-05-30T09:30Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T08:04Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (69.0, grade **D**)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded** — ecosystem grade **D** (69.0/100); `unattended_safe: false`.
- **Unattended operation: not safe** — 64 open swarm gaps, 6/9 goal-directed runners stopped, CP shows 709 `error` runs in 2h vs 23 `running`.
- **`orch-r3-missing-package-sweep` completed** — 3 open `missing_package` gaps patched pending in `ecosystem-package-backlog.md`; `std.io`/`std.csv` marked completed (modules present).
- **Briefing alignment OK** — heap `coord_numerics` P20 (`bench_improver`, `numerics_researcher`) matches 6 red benchmarks; P0 `proof_gap_researcher` + `workspace_sweeper` in recommended list.
- **Programmatic observer idle** — CP `observer.retry_counts: {}` @ 09:28Z; no auto-retries despite mass preemption errors.
- **CP report stale** — latest `control_plane_reports.swarm_health.healthy: true` from 2026-05-25; dashboard under-reports degradation.
- **CURSOR_API_KEY / GH_TOKEN** — both set; SDK auth not the primary blocker.
- **Next orchestration todo** — `orch-r4-ui-ux-signals` (studio-ux-16/17 already appended incidentally).

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade D; not unattended-safe | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/9 stopped (compiler-studio, sim, studio-ui-ux, md/chem research, security) | `lic/data/goal-directed-agents/snapshot.json` | high |
| Benchmarks | 6 red rows (matmul_*, ml_*, num_gmres) | `agent-briefing.json` → `ecosystem_audit.benchmarks.red` | high |
| CP run hygiene | 709 errors / 2h; 8383 errors lifetime; 23 `running` | Supabase `agent_runs` | high |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25 | `control_plane_reports` is_latest | medium |
| `org_agent_kit_audit` | preflight exit 1 | `agent-briefing.preflight_runs` | medium |
| `workspace_sweeper` | 2 dirty sibling repos (lic, benchmarks) | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| Missing packages | 3 open: line_profiler, std.summary, std.plot | `ecosystem-package-backlog.md`, registry | medium |
| verticals.toml | On lic branch only; benchmarks main gap open | `gap-infra-verticals-toml-missing-benchmarks-main` | low |

### Error sample classification

| Pattern | Example | Root cause |
|---------|---------|------------|
| Simultaneous errors @ tick | 10 agents @ 09:14Z (workspace_sweeper, docs_maintainer, …) | **Supervisor wave kill** — `retry_counts` empty |
| `run_started` only, no completion | Recent error rows in CP DB | **SDK preemption / empty run** — not repo conflict |
| Historical `error` volume | 400–518 errors/agent lifetime | **Status reconciliation drift** (finished mislabeled) |
| Mass `running` in scorecard sample | 101/120 sampled locally | **SDK terminalization drift** — observer not marking terminal |

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (69.0, grade D)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed (`swarm-gap-ingest.py`, `swarm-gap-apply-actions.py`)
- [x] `orch-r3-missing-package-sweep` orchestrator note + backlog/registry close
- [x] Recommended agents vs heap compared (numerics P20, governance P30)
- [x] Error runs sampled + classified (CP DB; no local JSON in `li-cursor-agents/data/runs/`)
- [ ] `orch-r4-ui-ux-signals` (deferred)

---

## Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | Idempotent; 92 gaps total, 64 open |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 20 backlog patches; `swarm-gap-actions.json` @ 09:28Z |
| Observer scan | CP `control_plane_state.observer` | `last_scan_at: 2026-05-30T09:28:23Z`; **no retries** |
| Meta dispatch | ecosystem-quality scorecard | `swarm_observer` when `swarm_execution` &lt; 75 |

No programmatic heal for SDK batch errors this cycle (`retry_counts: {}`, `stopped_agents: []`).

---

## Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile `error` vs `incomplete`/`finished` in persistence | `li-cursor-agents` store layer |
| Refresh `control_plane_reports.swarm_health` each tick | supervisor report writer |
| Enable slow preflights (drop `--skip-slow`) | benchmarks briefing / supervisor env |
| Auto-complete backlog todos when registry gap closed | `lic/scripts/swarm-gap-apply-actions.py` |
| Land `verticals.toml` on benchmarks main | `benchmarks/competitive/verticals.toml` |

---

## Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- **133 branches** without PR (`pr_branch_opener` assists; human review for stacks)
- lic dirty on `perf/bench-improver-matmul-simd-j-20260530` — human decides merge vs rebase
- **9 repos** agent-kit drift — org rollout needs human approval

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + fix error/incomplete drift | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Land competitive verticals.toml on benchmarks main | benchmarks | `ecosystem`, `docs` |
| Refresh control_plane_reports.swarm_health each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Tier-1 red matmul_blocked / matmul_naive — SIMD lowering follow-up | lic | `PH-7e`, `performance`, `bench` |
| Package issues: std.summary, std.plot, li-line-profiler seed | lic | `ecosystem`, `PH-IO` |
| Apply-actions: mark backlog completed when registry gap closed | lic | `orchestration` |

---

## Deferred

- `orch-r4-ui-ux-signals` — studio-ux-16/17 patched incidentally; needs dedicated `ui_ux` taxonomy pass
- ph-db runner plan_debt (9 todos) — no lic backlog mapping in apply-actions
- security-research sec-r1–r3 — deferred mapping; needs `security-research-backlog.md` wiring
- 30 `competitor_feature` rows — research handoffs to `numerics_researcher` / `bench_improver`, not orch product code

---

## Gap orchestration evidence (`swarm_coverage`)

| Artifact | Path |
|----------|------|
| Registry | `lic/data/swarm-gap-registry/registry.yaml` |
| Apply actions | `benchmarks/data/latest/swarm-gap-actions.json` |
| Package backlog | `lic/docs/ecosystem/ecosystem-package-backlog.md` |
| Orchestrator note | `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md` |
| Observer backlog | `lic/docs/ecosystem/swarm-observer-plan-backlog.md` (`orch-r3` → completed) |
