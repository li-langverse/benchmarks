# Swarm observer digest — `swarm_observer-1780188836675`

**Date:** 2026-05-31T00:56Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem + ai — registry, backlog apply, handoffs)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-31T00:53Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (82.0, grade **B**, `unattended_safe: true`)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-31-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — grade **B** (82.0); ecosystem posture strong (100) vs gap pressure weak (60) and goal-directed health (70).
- **Unattended: marginal** — scorecard `unattended_safe: true`, but 64 open registry gaps, 6 stopped plan runners, and 113 runs still `running` in local sample need periodic meta audits.
- **`orch-r3` completed** — missing-package sweep: 3 open `missing_package` gaps patched to `ecosystem-package-backlog.md` (`pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot` all **pending**).
- **Gap pipeline fresh** — ingest + apply re-run @ 2026-05-31T00:54Z (64 open: 30 competitor, 31 plan_debt, 3 missing_package); 23 backlog patches idempotent.
- **CURSOR_API_KEY present** — failures are SDK session errors / async-swarm wave churn, not auth gaps.
- **Swarm execution improved** — recent terminal sample 0% error (7 terminal, 0 error); historical 24h CP still shows 13k+ `error` rows (legacy wave-kill volume).
- **Goal-directed drift** — `agents_live: 0`; 6/9 runners stopped per scorecard; httpd runner active with 2 pending todos.
- **Briefing alignment** — heap prioritizes governance; `gap_explorer` + `swarm_observer` recommended; `workspace_sweeper` for 1 dirty `lic` repo.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Gap registry | 64 open gaps | `benchmarks/data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Missing packages | `std.summary`, `std.plot` missing; `line_profiler` seed open | `benchmarks/data/latest/ecosystem-explorer.json`, `lic/docs/ecosystem/ecosystem-package-backlog.md` | high |
| Goal-directed | 6/9 runners stopped; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| SDK errors (historical) | Terminal error after productive work (45 tools, 241s) | `li-cursor-agents/data/runs/docs_maintainer-1780177405649.json` | medium |
| GUI capture | SDL `RenderReadPixels` 0 frames | `li-cursor-agents/data/runs/gui_ui_tester-1780177405633.json` | medium |
| Bench improver | SDK error mid matmul_blocked fix; branch pushed | `li-cursor-agents/data/runs/bench_improver-1780177395064.json` | medium |
| Preflight | `org_agent_kit_audit` exit 1 (21 repos drift) | `agent-briefing.preflight_runs` | medium |
| Stuck runs | 113 runs marked `running` in scorecard sample | `ecosystem-quality-report.json` → `swarm-many-running` | medium |
| CP report stale | Supabase `control_plane_reports.is_latest` @ 2026-05-25 | MCP `control_plane_reports` | low |
| Benchmarks | 0 red; `matmul_blocked` yellow; 3 near tier-1 | `agent-briefing.json` → `ecosystem_audit` | low |

### Error sample classification

| Pattern | Example run | Root cause |
|---------|-------------|------------|
| SDK terminal after work | `docs_maintainer-1780177405649` | **Cursor SDK session error** — completion audit yes, deliverable section missing |
| SDK terminal (UX) | `gui_ui_tester-1780177405633` | **SDK error** after audit artifacts; native SDL capture regression |
| Bench / premature | `bench_improver-1780177395064` | **SDK error** — productive codegen edits pushed but completion audit premature |
| Historical volume | 1096 `workspace_sweeper`, 1068 `bug_fixer` errors (all-time CP) | Mix of wave-kill, SDK errors, retries |
| httpd plan | `gap-phase2-perf-wrk-soak` exit 124 | **Plan loop timeout** — long wrk soak |

**Error (representative):**

```
Error: SDK run status: error; run_id=run-9bc95d78-efcb-43c2-8130-7bf4b2b4963c; model=default; attempt=1; tools=45; duration_ms=240979
```

Evidence: `li-cursor-agents/data/runs/docs_maintainer-1780177405649.json`

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | MCP `control_plane_state` @ 00:52:59Z | `retry_counts: {}`, `stopped_agents: []` |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 00:54Z | Registry 92 rows, 64 open (idempotent) |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 00:54Z | 3 package + 20 plan/competitor patches |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` @ 00:54Z | 82.0 grade B (was 77.0 C) |
| Async swarm wave | 24 agents `running` in CP (latest tick) | Normal dispatch; no stopped agents |

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Dispatch `gap_explorer` when briefing cites missing std + registry has open `missing_package` | `benchmarks/scripts/agent-briefing.py` / supervisor heap |
| Route `issue_planner` handoff for `pkg-std-summary`, `pkg-std-plot`, `pkg-line-profiler` | `li-cursor-agents/config/research-goals.yaml` |
| Classify SDK errors with partial completion as `incomplete` not `error` | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh Supabase `control_plane_reports` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Reconcile `agents_live: 0` vs active httpd runner in snapshot | `lic/scripts/goal-directed-agents-snapshot.py` |
| Nightly full preflight (no `--skip-slow`) | supervisor env |
| Fix native SDL capture for `gui_ui_tester` | `li-cursor-agents/ux-harness/` + lic `studio-ui-ux-capture-native.sh` |

### Human-only blockers

| Blocker | Why human |
|---------|-----------|
| `std.plot` / `std.summary` implementation | Product modules — provability + API review |
| `li-line-profiler` package seed | New package placement — `package_architect` |
| 21 repos agent-kit drift | `org_agent_kit_audit` exit 1 — per-repo PRs |
| `verticals.toml` on benchmarks main | Blocks vertical ingest (`gap-infra-verticals-toml-missing-benchmarks-main`) |
| Master-plan / `trusted.lean` | Governance — no auto-merge |

### Agent deliverable checklist

- [x] Read ecosystem quality report + briefing + gap registry/actions
- [x] Queried control-plane DB (`agent_runs`, `control_plane_state`, `control_plane_reports`, `interventions_snapshots`)
- [x] Ran `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`
- [x] Completed `orch-r3-missing-package-sweep` orchestrator note
- [x] Regenerated scorecard (`82.0` / grade B)
- [x] Wrote digest to `benchmarks/data/runs/`
- [ ] PR on `li-cursor-agents` for observer prompt fixes (deferred — no prompt edits this pass)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| [Ecosystem gap] PH-IO-7: ship `std.summary` for agent summary.json | `lic` | `master-plan-gap`, `ecosystem` |
| [Ecosystem gap] PH-IO-5: ship `std.plot` for static dashboard | `lic` | `master-plan-gap`, `ecosystem` |
| [Package seed] `li-line-profiler` — line-level profiling (WP-B) | `lic` or new `li-line-profiler` | `ecosystem`, `plan-needed` |
| chore: merge `benchmarks/competitive/verticals.toml` to main | `benchmarks` | `ecosystem`, `swarm-gap` |
| fix(ux-harness): native SDL capture 0 frames (`gui_ui_tester`) | `li-cursor-agents` | `bug`, `ux` |
| chore(agent-kit): sync 21 drifted org repos | per-repo | `agent-kit` |

---

## Deferred

- **`orch-r4-ui-ux-signals`** — next swarm-observer plan todo; `studio-ux-16` / `studio-ux-17` already patched pending in studio-ui plan loop.
- **ph-db plan_debt (9 todos)** — apply deferred (no runner backlog mapping); route via agents control plane, not new systemd loops.
- **Close `gap-plan-pending-swarm-observer-orch-r3-missing-package-sweep`** — on next ingest after snapshot records `orch-r3` in `completed_ids`.
- **Observer prompt / `src/observer/` PR** — recommend after 2+ ticks of SDK vs wave-kill classification data.

---

## Gap orchestration summary (`swarm_coverage`)

| `gap_kind` | Open | Primary action this pass |
|------------|-----:|--------------------------|
| `missing_package` | 3 | Patched backlog todos pending; handoff `issue_planner` |
| `plan_debt` | 31 | 12 runner todos patched; master-plan rows deferred |
| `competitor_feature` | 30 | Vertical stubs patched to sim-md-research backlog (orch-r2) |

**north_star_fit:** Ecosystem orchestration for proof-before-perf backlog — package gaps documented without product code in `lic` (orchestration-only pass).
