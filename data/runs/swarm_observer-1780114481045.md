# Swarm observer meta-audit — 2026-05-30 (follow-up)

> `swarm_observer` · `swarm_coverage` · run `swarm_observer-1780114481045`  
> **north_star_fit:** ecosystem + ai — gap registry feeds numerics/sim research without weakening proof gates (PH-5b, PH-7e).

## Executive summary

- **Degraded** — ecosystem grade **D** (69.8), `unattended_safe: false`; swarm cannot run fully unattended.
- **`orch-r2-competitor-stubs` complete** on `cursor/swarm-observer-plan-loop` — 30 `competitor_feature` rows; vertical stubs patched to sim backlogs (`benchmarks/data/latest/swarm-gap-actions.json`).
- **24h execution crisis:** 5502 `error` runs vs 288 `finished`; 19 still `running` — SDK premature end (`tools=0`, ~120s) plus **GitHub GraphQL rate limit** (not missing `CURSOR_API_KEY`).
- **Programmatic observer idle:** `retry_counts: {}`, `stopped_agents: []`; Supabase `control_plane_reports` still **healthy** from 2026-05-25 (stale).
- **Goal-directed drift:** 6/8 plan runners stopped; `agents_live: 0` while async swarm dispatches 15+ agents outside heap plan.
- **Briefing alignment partial:** Heap recommends numerics/governance; live runs include `pr_branch_opener`, `docs_ui_tester`, `studio_ui_ux_builder` (breadth > heap).
- **6 red tier-1 benchmarks** — matmul_blocked/naive, ML forwards, num_gmres (`agent-briefing.json` → `ecosystem_audit.benchmarks.red`).
- **Ingest/apply re-run this pass:** registry 79 gaps / 54 open; `verticals_stubs=0` (idempotent — source `lic/benchmarks/competitive/verticals.toml`).

## Deliverable / findings

| Agent / area | Symptom | Evidence | Severity |
|--------------|---------|----------|----------|
| Swarm (async) | Mass SDK `error`; orphan `running` | Supabase `agent_runs` (24h: 5502 err / 19 run); `ecosystem-quality-report.json` | critical |
| `workspace_sweeper` | Push blocked — GraphQL | `workspace_sweeper-1780113491850.json` | high |
| `pr_reviewer` / preflight | `open_prs: 0` under rate limit | `pr_reviewer-1780112844596`, briefing `pr_program` | high |
| `implementation_gaps` | 370 lifetime errors | Supabase `agent_runs` GROUP BY | medium |
| `bench_improver` | 318 lifetime errors | Supabase `agent_runs` GROUP BY | medium |
| Goal-directed | 6 runners stopped | `lic/data/goal-directed-agents/snapshot.json` | high |
| Control plane | Stale healthy report | `control_plane_reports` `generated_at` 2026-05-25 | medium |
| Briefing | `org_agent_kit_audit` exit 1 | `agent-briefing.json` → `preflight_runs` | medium |
| Gap registry | 54 open gaps (30 competitor) | `lic/data/swarm-gap-registry/registry.yaml` | medium |
| Gap infra | `verticals.toml` not on benchmarks main | `gap-infra-verticals-toml-missing-benchmarks-main` | low |

### Root-cause classification (error sample)

| Class | Share | Evidence |
|-------|-------|----------|
| SDK premature (`tools=0`, status error) | High | `bug_fixer-1780106892149.json` — `duration_ms=120308` |
| GitHub GraphQL rate limit | High | `bug_fixer-1780113519733.json` — `API rate limit already exceeded` |
| Preflight stale / skip-slow | Medium | 8 preflights skipped; plan_audit skipped |
| Repo conflict / dirty workspace | Low | 2 dirty repos in briefing; sweeper partial |

### Self-heal actions taken (programmatic observer)

- Auto-retry: **none** (`control_plane_state.observer.retry_counts` empty at 2026-05-30T04:15:55Z)
- Stopped agents: **none**
- Healer dispatch: **none** this tick (no `swarm_degraded` surfaced despite error burst)
- **This meta pass:** `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py` re-run; sim-md-research backlog append refreshed

### Gap orchestration (`orch-r2`)

| Metric | Value |
|--------|------:|
| `competitor_feature` open | 30 |
| Vertical stub rows patched (apply) | 12 → sim-md-research / sim-algorithm backlogs |
| Ingest `verticals_stubs` added | 0 (already in registry) |
| Orchestrator note | `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r2-competitor-stubs.md` (plan-loop branch) |

### Recommended control-plane fixes

| Path | Fix |
|------|-----|
| `li-cursor-agents/src/observer/` | Emit `swarm_degraded` when >N SDK errors/5min or GraphQL 403 in sweep |
| `li-cursor-agents/src/backends/cursor-sdk-backend.js` | Reconcile orphan `running` → `incomplete` after timeout |
| `benchmarks/scripts/pr-program-run.py` | REST fallback when GraphQL exhausted |
| `benchmarks/scripts/workspace-dirty-sweep.py` | Defer `gh pr create` when rate-limited |
| `benchmarks/scripts/ecosystem-quality-grade.py` | Weight live Supabase error rate over local run sample |

### Human-only blockers

- GitHub GraphQL quota recovery (blocks PR program, sweep, merge queue)
- Governance merges on protected branches
- Land `benchmarks/competitive/verticals.toml` on benchmarks **main** (closes `gap-infra-verticals-toml-missing-benchmarks-main`)
- Tier-1 perf / Lean governance PRs — human review required

### Agent deliverable checklist

- [x] Read `ecosystem-quality-report.json` (grade D, 69.8)
- [x] Query control-plane DB (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Confirm ingest + apply (`lic/scripts/swarm-gap-ingest.py`, `swarm-gap-apply-actions.py`)
- [x] `orch-r2` reconciled (closed on plan-loop registry + backlog)
- [x] Orchestrator note exists on `cursor/swarm-observer-plan-loop`
- [x] Digest written: `benchmarks/data/runs/swarm_observer-1780114481045.md`
- [ ] Push plan-loop (if not already on remote)

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| chore(benchmarks): land `competitive/verticals.toml` on main | benchmarks | `surface:bench`, `li-swarm` |
| fix(swarm): REST fallback when GitHub GraphQL rate-limited | li-cursor-agents | `agent:bug_fixer`, `li-swarm` |
| fix(observer): stale-run sweep + `swarm_degraded` on error burst | li-cursor-agents | `agent:bug_fixer` |
| perf(7e): tier-1 matmul_blocked/naive ≤1.2× cpp | lic | `PH-7e`, `PH-5b` |

## Deferred

- Full agent error-streak remediation until GraphQL quota resets
- Restart stopped goal-directed runners (systemd) after rate limit clears
- `orch-r3` / `orch-r4` — already completed on plan-loop; no duplicate work this pass
- Red benchmark codegen merges — human perf review

---

**Error (representative):**

```
SDK run status: error; tools=0; duration_ms=120308
GitHub GraphQL: API rate limit already exceeded for user ID 207167228
```

Evidence: `li-cursor-agents/data/runs/bug_fixer-1780106892149.json`, `bug_fixer-1780113519733.json`
