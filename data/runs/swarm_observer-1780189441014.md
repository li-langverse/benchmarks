# Swarm observer digest — `swarm_observer-1780189441014`

**Date:** 2026-05-31T01:04Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai — gap orchestration)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-31T01:00Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (72.8, grade **C**, `unattended_safe: false`)  
**Orchestrator notes:** `orch-r3` complete · `orch-r4` complete this cycle

---

## Executive summary

- **Posture: degraded (recoverable)** — ecosystem grade **C** (72.8/100); `unattended_safe: false` driven by 32% terminal error rate and 95 stuck `running` rows in the local scorecard sample.
- **Programmatic observer active @ 01:04Z** — CP `control_plane_state.swarm_health.healthy: false`; auto-retries queued for `implementation_gaps` and `swarm_observer`; `sdk_unavailable` finding present alongside wave-kill error streaks.
- **Unattended operation: not safe** — mass preemption pattern (~43–46 errors/agent in 2h on 15+ agents); concurrent duplicate `swarm_observer` runs (`1780189441000`, `1780189441014`) during meta-audit.
- **Strong lanes:** `ecosystem_posture` **98** (0 red benchmarks; `matmul_blocked` yellow only); `agent_kit_maintainer` finishing reliably (593 finished in CP lifetime); docs + GUI UX harnesses **pass** this cycle.
- **Weak lanes:** `swarm_execution` **55**; `gap_pressure` **60** (64 open registry rows); `goal_directed_health` **70** (6/9 runners stopped).
- **Briefing alignment improving** — `workspace_sweeper`, `plan_verifier`, `implementation_gaps` now **running** from 00:52Z wave; still missing recent runs for `proof_gap_researcher`, `gap_explorer`, `issue_hygiene`.
- **`orch-r3` + `orch-r4` orchestration complete** — missing-package sweep patched 3 backlog rows; UI/UX signals linked via fresh docs + GUI digests (see orchestrator notes).
- **`CURSOR_API_KEY` set** — auth available; failures are SDK session abort / wave preemption, not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; `unattended_safe: false` | `data/latest/ecosystem-quality-report.json` | critical |
| Swarm execution | 32% error rate; 95 runs stuck `running` | `ecosystem-quality-report.json` → `swarm_execution` | critical |
| CP observer | `healthy: false`; `sdk_unavailable` + error streaks | Supabase `control_plane_state.payload.swarm_health` @ 01:04Z | critical |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/9 stopped (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`) | `lic/data/goal-directed-agents/snapshot.json` | high |
| Wave preemption | ~43–46 errors/agent / 2h across 15 agents | Supabase `agent_runs` (2h window) | high |
| Meta-audit overlap | 3+ concurrent `swarm_observer` runs | `agent_runs` @ 01:04Z | high |
| Goal orientation drift | `proof_gap_researcher`, `gap_explorer`, `issue_hygiene` not run in 6h | briefing vs CP `agent_runs` | medium |
| CP report stale | `control_plane_reports.is_latest` @ 2026-05-25; `healthy: true`, 0 runs sampled | Supabase `control_plane_reports` | medium |
| `org_agent_kit_audit` | preflight exit 1 — 21 repos flagged | `agent-briefing.preflight_runs` | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) | `agent-briefing.preflight_runs` | medium |
| Failed PR CI | lic#612 workspace sweep — CI fail | `ecosystem-quality-report.json` → `failed-pr-ci` | medium |
| Docs UX P0 | Live Pages stale (5 tabs vs 12 local); search 248 vs 2801 entries | `docs/ecosystem/ux-digests/2026-05-31-docs-ux.md` | medium |
| GUI UX skip | `agents-dashboard` unreachable — blocks live UX-06/07/11 | `docs/ecosystem/ux-digests/2026-05-31-gui-ux.md` | medium |
| httpd plan loop | 2 pending todos; last iterations exit **124** (timeout) | `snapshot.json` runner `httpd` | medium |
| Benchmarks near tier-1 | `matmul_blocked` yellow; matmul_naive/simd_dot/fft near 1.0× | `agent-briefing.json` → `ecosystem_audit.benchmarks` | low |

### Error sample classification (current tick + 2h window)

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Simultaneous errors @ tick | 15 agents × ~44 errors / 2h | **Supervisor wave kill / preemption** — prior run terminated before completion |
| SDK `status: error`, 0 tools | `swarm_observer-1780188930904` (125s) | **Cursor SDK session error** — no deliverable produced |
| SDK `status: error` after work | `bench_improver-1780188645114` (46 tools) | **SDK abort mid-run** — partial deliverable |
| CP `sdk_unavailable` finding | `control_plane_state` @ 01:04Z | **Auth/session instability** — observer flags; key present in env |
| Mass `running` in scorecard | 95/120 sampled locally | **SDK terminalization drift** — observer not marking terminal after TTL |
| `agent_kit_maintainer` finished | 593 finished lifetime | **Healthy lane** — deterministic when drift absent |
| httpd exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state` @ 01:04:00Z | `retry_counts: {}`; remediations queued |
| Auto-retry dispatch | `swarm_health.remediations` | `implementation_gaps` + `swarm_observer` `:retry:1` |
| Gap ingest + apply | `lic/scripts/swarm-gap-*.py` @ 01:02Z | 64 open; 23 backlog patches |
| Scorecard refresh | `ecosystem-quality-grade.py` @ 01:02Z | 72.8 grade C |
| UX signal harvest | `docs_ux_tester-1780189345`, `gui_ux_tester-1780189300` | Digests written; orch-r4 evidence |
| Interventions | `control_plane_reports` / snapshots | Empty since 2026-05-25 |

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile mass `error` rows from wave-kill vs true SDK failures | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Dedupe concurrent `swarm_observer` meta-audits on same briefing | supervisor heap / research goals |
| Retry SDK runs that fail with 0 tools before marking error | `li-cursor-agents/src/backends/cursor-sdk-backend.js` |
| Dispatch heap P40 `gap_explorer` when briefing recommends + registry open | supervisor heap coordinator |
| Merge GUI targets into briefing preflight `ux-audit.json` | `benchmarks/scripts/` + [#32](https://github.com/li-langverse/li-cursor-agents/issues/32) |
| Auto-close registry `plan_debt` when snapshot `completed_ids` includes todo | `lic/scripts/swarm-gap-ingest.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- Shipping `std.plot` / `std.summary` / `li-line-profiler` — product work; route via `issue_planner`
- lic#403 docs Pages deploy — blocks public UX until CI strict build green
- lic workspace dirty on sweep + bench branches — human decides merge vs rebase
- lic#612 failing CI — human triage before merge
- httpd wrk soak timeouts — human env/deadline tuning

### Agent deliverable checklist

- [x] Ecosystem quality report read (72.8, grade C @ 01:02Z)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed (registry + actions @ 01:02Z)
- [x] Orchestrator notes: `orch-r3` (prior), `orch-r4` (this cycle)
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [x] UX digests linked for `ui_ux` gap taxonomy
- [ ] Control-plane observer PR (terminalization TTL) — deferred to li-cursor-agents

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | std.summary, std.plot, li-line-profiler | Patched → `ecosystem-package-backlog.md`; handoff **`issue_planner`** (`orch-r3`) |
| `plan_debt` (31) | sim/security/studio backlogs patched; master-plan phases deferred | Route **`plan_verifier`** + **`implementation_gaps`** |
| `competitor_feature` (30) | vertical stubs patched to sim-md backlogs | Route **`gap_explorer`** + **`bench_improver`** |
| `ui_ux` (0 open registry) | docs P0 Pages stale; GUI palette pass; dashboard skip | **`docs_ux_tester`** + **`gui_ux_tester`** digests (`orch-r4`) |

Do **not** recommend `install-goal-plan-loop-systemd.sh` — use `li-cursor-agents` research/implement goals only.

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + reconcile wave-kill errors | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| SDK: retry 0-tool session errors before marking terminal | li-cursor-agents | `swarm`, `control-plane` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch `gap_explorer` + `proof_gap_researcher` when briefing P0 | li-cursor-agents | `swarm`, `orchestration` |
| Briefing preflight: include GUI `ux-audit` targets | li-cursor-agents | `ux`, `orchestration` ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)) |
| Package issues: `std.summary`, `std.plot`, `li-line-profiler` seed | lic | `ecosystem`, `PH-IO` |
| Fix docs CI strict build — unblock Pages deploy | lic | `ux`, `ci` ([#403](https://github.com/li-langverse/lic/issues/403)) |
| Fix GHA red on workspace-sweep PR | lic | `ci`, `bug` ([#612](https://github.com/li-langverse/lic/pull/612)) |
| Start agents-dashboard for live UX audit | li-cursor-agents | `ux` ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)) |

---

## Deferred

- ph-db runner plan_debt (9 todos) — no backlog mapping yet
- Master-plan `plan_debt` rows without runner mapping (2i, 7d, 7e, Vision-LLM, 8p)
- 30 `competitor_feature` gaps — research/backlog only; no product code in lic from observer
- Control-plane code changes (`src/observer/`, `build-report.ts`) — PR on `li-cursor-agents` with `npm test`
- Periodic full preflight (drop `--skip-slow`)
- Playwright axe/contrast on docs ([#426](https://github.com/li-langverse/lic/issues/426)); studio-ux-17 GPU fail recovery

---

## Error

No fatal error in this meta-agent pass. Prior tick failures (`swarm_observer-1780188930904`, `implementation_gaps-1780188930914`) ended with SDK `status: error` at wave preemption @ 00:55:30Z — no stack trace in CP `completion` JSON.

---

_Report generated by `swarm_observer` · li-langverse/benchmarks · 2026-05-31T01:04Z_
