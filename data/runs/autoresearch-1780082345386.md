# Autoresearch digest — 2026-05-29 (proactive v2)

**Agent:** `autoresearch` · **Run:** `autoresearch-1780082345386` · **Source:** proactive  
**Workspace:** `lic` @ `chore/agent-bench_improver-82328161` (clean)  
**north_star_fit:** PH-5b, PH-7e — pure-Li codegen discipline; defer novel physics until SOTA path exhausted

## Executive summary

- **Preflight (2026-05-29T19:20Z):** **6 red** tier-1 rows, **2 yellow** MD thermostats, **137 green**; **42 unknown** harness rows (ingest gap).
- **Local tier-1 spot-check:** `matmul_naive` **0.89×** cpp (green post-`@` fix on branch); `matmul_blocked` **1.33×** (still red); `num_gmres` **0.80×** (green); dashboard ingest @ 07:01Z is **stale**.
- **Novel-algorithm PR:** **not opened** — all reds are SOTA-implementable (blocked GEMM, `@` matmul, GMRES extern, WP4 ml smokes); gaps are **codegen + ingest + kernel scaffold**.
- **Sibling `bench_improver`:** PR [#418](https://github.com/li-langverse/lic/pull/418) (`matmul_naive` via `C = A @ B`) clears largest pure-Li red after merge + ingest; coordinate — do not duplicate.
- **MD/chem verticals:** SOTA surveys complete (`md-r0`, `chem-r0`); next todos (`md-r2`, `chem-r2`) are **implementation**, not autoresearch invention (`novel: false`).
- **Control plane:** 9 prior `autoresearch` runs today reconciled `error` (`unregistered_running_reconciled`) — infra timeout, not failed hypothesis work.
- **Morning digest delta:** earlier sweep (10:43Z) reported **0 reds**; afternoon ingest shows regression artifact — local harness contradicts `matmul_naive` / `num_gmres` dashboard reds.

## Deliverable / findings

**Study (lic):** `docs/numerics/studies/2026-05-29-autoresearch-proactive-sweep.md`

| Target | Autoresearch action | Result |
|--------|---------------------|--------|
| `matmul_blocked` (1.549×) | Invent new GEMM tiling? | **No** — PH-7e `mm_blocked_512` LLVM fusion; ~**10%** to ≤1.2× locally |
| `matmul_naive` (1.333×) | Novel IKJ scheme? | **No** — route through `@` / `ArrayMatMul2DF64`; **0.89×** locally |
| `num_gmres` (1.4×) | Novel Krylov method? | **No** — shared C oracle; **0.80×** locally; re-ingest |
| `ml_*` (1.333× each) | Novel conv/ML algo? | **No** — WP4 scalar smokes; need im2col/GEMM + C oracle |
| `horner_pure_li` | New Horner scheme? | **No** (prior negative) — verify still **FAIL** on main; harness/oracle |
| Tier-2 `md_thermostat_*` (yellow) | Novel thermostat? | **Deferred** — extern wrappers; pure-Li kernels later |

**Prior negative studies retained:** `benchmarks/docs/numerics/studies/2026-05-17-horner-pure-li-autoresearch-negative.md`, `lic/docs/numerics/autoresearch-horner-lexer-2026-05-18.md`.

**Commands (repro):**

```bash
cd lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_blocked --runs 3
python3 bench.py --tier 1 --only matmul_naive --runs 3
python3 bench.py --tier 1 --only num_gmres --runs 3
cd ../../benchmarks && LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh
```

## Recommended issues/PRs

| Title | Repo | Labels / notes |
|-------|------|----------------|
| perf(7e): matmul_naive hot path via `C = A @ B` | **lic** | `PH-7e`, `numerics` — [#418](https://github.com/li-langverse/lic/pull/418); merge + ingest |
| perf(7e): matmul_blocked emit — FMA vec4, prefetch, OpenMP on `ii` | **lic** | Largest remaining pure-Li red (~1.33–1.55×); `bench_improver`, not `novel-algorithm` |
| Scaffold real `ml_*` kernels + C oracle + `bench.py` wiring | **lic** / **li-math** | SOTA im2col/matmul; `numerics-research` |
| Ingest tier-1 after lic merges | **benchmarks** | `LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh` — clears stale reds |
| Reclassify `ml_*` catalog rows as smoke until real kernels | **benchmarks** | `catalog.toml` honesty — human approval for gate policy |
| Implement `md_neighbor_cell_list` (algo 105) | **lic** | `numerics-research`, `PH-5b` — post `md-r2` contract |
| fix(bench): `horner_pure_li` verify oracle alignment | **lic** | `autoresearch`, `numerics` — validity before PH-7e claims |
| [ecosystem-gap] Tier-1 `ml_*` not in lic `bench.py` scope | **lic** | Harness wiring for WP4 smokes |

## Deferred

- **Novel-algorithm** PR until a backlog todo sets `novel: true` with documented SOTA insufficiency.
- **Tier-2 autoresearch** (limiters, split integrators, PME) until `md_neighbor_cell_list` reaches oracle parity.
- **`matmul_blocked_N1024`**, **42 unknown rows** — ingest + strict CI matrix.
- **Lean `trusted.lean`** for fused matmul / large-N `@` — human-approved track only.
- **Whitepaper / research-findings** publish — no `goal_id` numerics win this pass.
- **MD thermostat pure-Li** kernels (yellow → green) — SOTA Berendsen/NHC sufficient.

<!-- li-agent -->
## Agent deliverable
- [x] li-tests or lit test id: N/A — survey-only; tier-1 `bench.py` repro on existing harness rows
- [x] Bench row / benchmarks path: `lic/benchmarks/results/latest.csv`; ingest via `benchmarks/scripts/ingest/ingest-lic.sh`
- [x] Lean/contracts path documented or N/A with reason: N/A — no compiler proof changes; PH-7e advisory perf only
- [x] Negative result documented if hypothesis rejected: `lic/docs/numerics/studies/2026-05-29-autoresearch-proactive-sweep.md`
