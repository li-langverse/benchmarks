# Autoresearch digest — 2026-05-29 (proactive v5)

**Agent:** `autoresearch` · **Run:** `autoresearch-1780094804882` · **Source:** proactive  
**Workspace:** `lic` @ `fc62e1d8` (built locally, LLVM 22)  
**north_star_fit:** PH-5b, PH-7e — pure-Li codegen discipline; defer novel physics until SOTA path exhausted

## Executive summary

- **Preflight (2026-05-29T22:07Z):** **6 red** tier-1 rows, **2 yellow** MD thermostats, **137 green**; dashboard ingest @ **07:01Z is stale** (42 unknown rows).
- **Fresh local tier-1 (this run):** `matmul_naive` **1.05×**, `matmul_blocked` **1.25×**, `num_gmres` **1.25×**, `horner_pure_li` **1.0×** — four dashboard reds are **ingest/stale artifacts** or **non-pure-Li** rows.
- **No `*_pure_li` reds on dashboard** — sole remaining pure-Li perf gap is **`matmul_blocked` at 1.25×** (5% over 1.2× threshold); `ArrayMatMulBlocked2DF64` MIR hook verified with checksum parity.
- **Novel-algorithm PR not opened** — all reds map to published SOTA (Goto GEMM, Saad GMRES, im2col conv); invention deferred per methodology Mode A sufficiency.
- **Sibling `bench_improver`:** [lic#418](https://github.com/li-langverse/lic/pull/418) open (`matmul_naive` via `C = A @ B`); merge + ingest clears stale `matmul_naive` dashboard red.
- **MD/chem verticals:** SOTA surveys complete (`md-r0`, `chem-r0`); pending handoffs target implementation (`md-r2` neighbor list, `chem-r2` SCF smoke), not autoresearch.
- **Control plane MCP:** `query_control_plane_db` returned empty-query error (infra); `load_research_session` null — triage used briefing JSON + local benches.
- **Study committed:** `lic/docs/numerics/studies/2026-05-29-autoresearch-proactive-sweep.md`.

## Deliverable / findings

**Study (lic):** `docs/numerics/studies/2026-05-29-autoresearch-proactive-sweep.md`

| Target | Autoresearch action | Result |
|--------|---------------------|--------|
| `matmul_blocked` (1.549× dash / 1.25× local) | Invent new GEMM tiling? | **No** — Goto/BLIS blocked GEMM; PH-7e `mm_blocked_512` → `ArrayMatMulBlocked2DF64` |
| `matmul_naive` (1.333× dash / 1.05× local) | Novel IKJ scheme? | **No** — `@` / IKJ SOTA; lic#418 + ingest |
| `num_gmres` (1.4× dash / 1.25× local) | Novel Krylov method? | **No** — Saad GMRES; Li uses shared C oracle today |
| `ml_*` (1.333× each) | Novel conv/ML algo? | **No** — im2col/GEMM SOTA; WP4 smokes are 4096-iter stubs |
| `horner_pure_li` (0.75× dash / 1.0× local) | New Horner scheme? | **No** (prior negative, 2026-05-18 lexer fix) |
| `md_thermostat_*` (yellow) | Novel thermostat? | **Deferred** — Berendsen/NHC SOTA sufficient; stubs share `md_core` |

**Key codegen insight:** `mm_blocked_512` body in `main.li` is intentionally empty; MIR lower (`lower.cpp:995`) replaces the call with `ArrayMatMulBlocked2DF64` before codegen. Verify passes (checksum 1288460.756…). Perf gap is LLVM emit quality, not missing discrete math.

**Local repro (2026-05-29):**

```bash
cd lic && ./scripts/build.sh
cd benchmarks/harness
python3 bench.py --tier 1 --only matmul_naive,matmul_blocked,num_gmres,horner_pure_li --runs 5 --skip-verify
python3 bench.py --tier 1 --only matmul_blocked --runs 3 --verify-results
# → lic/benchmarks/results/latest.csv
```

## Recommended issues/PRs

| Title | Repo | Labels / notes |
|-------|------|----------------|
| perf(7e): matmul_naive hot path via `C = A @ B` | **lic** | `PH-7e`, `numerics` — [#418](https://github.com/li-langverse/lic/pull/418); merge + ingest |
| perf(7e): matmul_blocked emit — FMA vec4, prefetch on `ArrayMatMulBlocked2DF64` | **lic** | `bench_improver`, `PH-7e`; sole remaining pure-Li red (~1.25× local) |
| Ingest tier-1 after lic merges | **benchmarks** | `LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh` |
| Scaffold real `ml_*` kernels + C oracle (im2col/GEMM) | **lic** / **li-math** | `numerics-research`; replace 4096-iter smoke stubs |
| Pure-Li `num_gmres` kernel (no `LI_EXTRA_C`) | **lic** | `numerics-research`, `PH-5b`; standard Arnoldi, not novel |
| Implement `md_neighbor_cell_list` (algo 105) | **lic** | `numerics-research`, `PH-5b`; post `md-r2` contract |
| `ecosystem-gap`: control-plane DB empty-query on SELECT | **li-cursor-agents** | infra; blocks run-history audit |

## Deferred

- **Novel-algorithm** PR until a row documents SOTA insufficiency with `novel: true` in research backlog.
- **Tier-2 autoresearch** (limiters, split integrators, PME) until `md_neighbor_cell_list` oracle parity.
- **`matmul_blocked_N1024`**, **42 unknown rows** — ingest + strict CI matrix.
- **Lean `trusted.lean`** for fused matmul — human-approved track only.
- **Whitepaper / research-findings** publish — no `goal_id` numerics win this pass.
- **MD thermostat pure-Li** kernels — SOTA sufficient; yellow is perf not validity.

<!-- li-agent -->
## Agent deliverable
- [x] li-tests or lit test id: N/A — survey-only; tier-1 `bench.py` repro on existing harness rows
- [x] Bench row / benchmarks path: `lic/benchmarks/results/latest.csv`; ingest via `benchmarks/scripts/ingest/ingest-lic.sh`
- [x] Lean/contracts path documented or N/A with reason: N/A — no compiler proof changes; PH-7e advisory perf only
- [x] Negative result documented if hypothesis rejected: `lic/docs/numerics/studies/2026-05-29-autoresearch-proactive-sweep.md`
