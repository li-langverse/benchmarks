# CI maintainer digest — 2026-05-30

**Agent:** `ci_maintainer`  
**Run id:** `ci_maintainer-1780141910789`  
**Source:** proactive ecosystem sweep  
**North star fit:** Ecosystem **secure** + **provable** gate — org repos require `ci.yml` on default branch before branch protection can enforce proof-bearing CI (PH-8p throughput, WP-H0 hygiene).

Swarm run id: `ci_maintainer-1780141910789`  
Agent id: `ci_maintainer`

---

## Executive summary

- Refreshed `org-repo-ci-audit.json` and `ecosystem-audit.json`; **0 org repos missing `ci.yml`** on default branch.
- **12 primary org repos OK** (REST-verified); **0 missing**, **0 gated**, **0 incomplete** after fallback fixes.
- **No CI template PRs opened** this run — prior `lic-docs` rollout (#3) merged; `research-findings` now has `ci.yml` on `main`.
- GitHub **GraphQL rate limit** blocked `gh repo list` / `gh repo view`; audit script now falls back to `ORG_REPOS` + `main` + REST workflow API.
- Invalid **`GH_TOKEN`** in environment — cleared for audit; `gh` uses stored PAT (REST calls succeed).
- Branch protection policy in `roadmap/scripts/org-branch-protection.json` aligns with per-repo CI job names (`check`, `bootstrap`, `test`, `build-and-test`, etc.).
- Ecosystem metrics: 0 open PRs, 0 failing CI, `repos_missing_ci_main`: 0.

---

## Deliverable / findings

### Preflight audits

| Audit | Result |
|-------|--------|
| `ensure-org-repo-ci.py` | exit **0** — OK: 12 · missing: 0 · gated: 0 · incomplete: 0 |
| `ecosystem-audit.py` | exit **0** — `repos_missing_ci_main`: 0 · `missing_ci_on_main`: [] |
| REST spot-check (22 repos) | all have `ci.yml` on `main` including `lic-docs`, `research-findings`, `lidb` |

### Org CI coverage (primary repos)

| Repo | `ci.yml` on main | Suggested required check | Policy match |
|------|------------------|--------------------------|--------------|
| lic | ✓ | `build-and-test` | ✓ |
| li-language | ✓ | `build-and-test` | ✓ |
| lip | ✓ | `bootstrap` | ✓ |
| lit | ✓ | `test` | ✓ |
| lis | ✓ | (matrix — empty ruleset) | ✓ |
| benchmarks | ✓ | `ingest-smoke` | ✓ |
| roadmap | ✓ | `verify-kit` | ✓ |
| li-net, li-httpd, li-std-core, li-std-math, li-demo | ✓ | `check` | ✓ |

### Script hardening (this run)

- `ensure-org-repo-ci.py`: when `gh repo list` returns empty (GraphQL rate limit), fall back to `ORG_REPOS` instead of vacuous `OK: 0`.
- `default_branch()`: when `gh repo view` fails but a sibling checkout exists, assume `main` per org policy.

### PRs opened this run

None — org CI gate is green; no missing repos to template.

---

## Recommended issues/PRs

| Repo | Title | Labels | Action |
|------|-------|--------|--------|
| **benchmarks** | Merge digest PR — chore(ci_maintainer): proactive org CI audit at 11:55Z | `ecosystem-ci`, `li-swarm`, `agent:ci_maintainer` | Human review |
| **roadmap** | Apply org branch protection rulesets to repos still without rulesets (spot-check `lic-docs`) | `ecosystem-governance` | Human org settings |
| **benchmarks** | Align `ecosystem-audit.py` static fallback repo list with dynamic org list when GraphQL recovers | `ecosystem-ci` | Optional follow-up |

---

## Deferred

- **GraphQL rate limit recovery** — full dynamic org repo enumeration resumes when GitHub API quota resets; REST workflow checks remain authoritative for `ci.yml` presence.
- **`lidb` default branch (WP-H0)** — gated until default branch moves to `main`; sibling checkout has `ci.yml`.
- **Branch protection rollout** for newly CI-enabled repos — human `apply-org-branch-protection.sh`.
- **Self-merge** — no governance repos merged by agent.
- **Failing PR CI triage** — owned by `bug_fixer` / `code_implementer`, not ci_maintainer.

---

## Error

**GraphQL rate limit (non-blocking):** `gh repo list` and `gh repo view` fail with `API rate limit already exceeded for user ID 207167228`. Mitigated via REST `gh api repos/.../contents/.github/workflows` and audit script fallbacks. Invalid `GH_TOKEN` in shell env also caused early failures — unset before audit.
