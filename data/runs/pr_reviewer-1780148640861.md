# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780148640861`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**Generated:** 2026-05-30T13:44Z  
**north_star_fit:** ecosystem pull-requests — PH-5b / PH-7e (tier-1 matmul, proof-before-perf); PH-DB deferred (`plan-needed` without `plan-approved`).

## Executive summary

- **93 open org PRs** per fresh `pr-merge-queue-plan.json` (13:37Z); **0 gate-ready**, **0** in `merge_sequence` — no `merge-approved` label added this run.
- **`pr-program-run.json` stale** (`open_prs: 0` at 12:52Z); `run-pr-program.py` still running >210s at sweep time — treat briefing `pr_program` counts as false negative until refresh completes.
- **REST + per-PR gate OK** with `unset GH_TOKEN`; GraphQL-heavy org sweeps remain slow (prior runs timed out at 60–120s).
- **Canonical tier-1 matmul:** [lic#437](https://github.com/li-langverse/lic/pull/437) — CI green, `merge-approved` retained; **blocked** by merge conflicts (`DIRTY`) + `REVIEW_REQUIRED`.
- **Parallel matmul stack:** [lic#541](https://github.com/li-langverse/lic/pull/541) / [#542](https://github.com/li-langverse/lic/pull/542) / [#544](https://github.com/li-langverse/lic/pull/544) — CI green; redundant with #437 and each other; needs human pick + APPROVED before label.
- **[lit#19](https://github.com/li-langverse/lit/pull/19) merged** during sweep — Pages CI fix no longer in queue.
- **82 redundant PR pairs** (mostly agent digest / workspace-sweep clones) — close/supersede before merge queue hygiene.
- **Governance:** `roadmap#19`, `#21` carry `merge-approved` but require **human merge** + APPROVED (never agent-merge).
- **0 merges, 0 labels** — policy: no PR fully aligned + conflict-free + APPROVED.

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-merge-queue-plan.json` | ✅ Fresh (13:37Z) | 93 open, 2 `merge-approved`, 82 redundant, 1 stack (`lic#489` → `#497`) |
| `pr-program-run.json` | ❌ Stale | `open_prs: 0`; full program run not finished at digest time |
| `run-pr-program.py` | ⚠️ In progress | >210s; prior runs exit 124 on GraphQL path |
| `pr-merge-gate.py` (per-PR) | ✅ | Use `unset GH_TOKEN`; exit 1 when not ready is expected |
| Control plane | ⚠️ | 9+ consecutive `pr_reviewer` runs `error` before this pass; current run `running` |

### Merge queue snapshot

| Metric | Value |
|--------|-------|
| Open PRs | 93 |
| `merge-approved` (labeled) | 2 (`roadmap#19`, `#21`) + others on lic/benchmarks (see below) |
| Gate-ready | 0 |
| `merge_first` / `next_merge` | null |
| Redundant pairs | 82 |
| Stacks | `lic#489` then `lic#497` |

Rank-1 queue item (not gate-ready): [benchmarks#204](https://github.com/li-langverse/benchmarks/pull/204) — agent-kit digest; CI `none`, no `merge-approved`.

### Standards review (gate-verified, CI-green non-draft)

#### Tier-1 matmul / PH-5b / PH-7e (lic)

| PR | CI | Merge | Labels | Verdict |
|----|-----|-------|--------|---------|
| [#437](https://github.com/li-langverse/lic/pull/437) | ✅ | ❌ CONFLICTING | `merge-approved` | **Retain canonical** — rebase, human APPROVED → `pr_merger` |
| [#541](https://github.com/li-langverse/lic/pull/541) | ✅ | unknown | — | **Defer** — overlaps #437; add APPROVED + compare before label |
| [#542](https://github.com/li-langverse/lic/pull/542) | ✅ | unknown | — | Docs/Pages recovery — APPROVED path |
| [#544](https://github.com/li-langverse/lic/pull/544) | ✅ | unknown | — | Duplicate Pages fix vs #542 — pick one |
| [#499](https://github.com/li-langverse/lic/pull/499) | ❌ fail | — | — | **Defer** — CI red |
| [#520](https://github.com/li-langverse/lic/pull/520) / [#524](https://github.com/li-langverse/lic/pull/524) | ❌ none/pending | — | — | **Defer** — wait CI / local-ci sweep |

**Checklist (#437 / #541 — feature perf):**

| Gate | #437 | #541 |
|------|------|------|
| Vision / PH | ✅ PH-5b, PH-7e | ✅ MIR matmul fast paths |
| Strict by default | ✅ lowering; no trusted creep cited | ✅ |
| Security | N/A surface | N/A |
| Performance | ✅ tier-1 paths; no `catalog.toml` weakening | ✅ |
| Release notes | ✅ (prior review) | Gate: waivable / satisfied |
| Ecosystem-first | Post-merge benchmarks ingest for 6 red rows | Same |

#### benchmarks#211 — bench ingest digest

| Gate | Verdict |
|------|---------|
| CI | ✅ |
| Scope | Chore digest — aligned with bench_improver |
| Blockers | `REVIEW_REQUIRED`, `merge-approved` |
| Verdict | Human APPROVED → `merge-approved` after confirming no duplicate of #204–#229 cluster |

#### roadmap (governance)

| PR | CI | `merge-approved` | Blockers |
|----|-----|------------------|----------|
| [#19](https://github.com/li-langverse/roadmap/pull/19) | ✅ | yes | APPROVED + **human merge only** |
| [#21](https://github.com/li-langverse/roadmap/pull/21) | ✅ | yes | same |

#### Feature / plan blocks

| PR | Verdict |
|----|---------|
| [lic#489](https://github.com/li-langverse/lic/pull/489), [#517](https://github.com/li-langverse/lic/pull/517) | **needs plan** — `plan-needed` without `plan-approved` |
| [li-httpd#10](https://github.com/li-langverse/li-httpd/pull/10) | **needs plan** + CI |
| [lis#20](https://github.com/li-langverse/lis/pull/20) | **needs plan** (PH-DB) |

#### lic plan drafts (merge-approved label present)

| PR | Blockers |
|----|----------|
| #530, #531, #532 | Still **draft**; `REVIEW_REQUIRED`; release_notes |

### Actions taken this run

- **0 merges** (policy).
- **0 new labels** — no PR passes all gates + human APPROVED + conflict-free.
- Preflight: `pr-merge-queue-plan.py` succeeded; `run-pr-program.py` not completed within sweep window.
- Gate verification via `pr-merge-gate.py` on priority candidates (see above).

### Error (preflight)

```
pr-program-run.json reports open_prs: 0 (generated 12:52Z) while merge-queue-plan
reports 93 open PRs (13:37Z). run-pr-program.py exceeded 210s without completing
(likely org GraphQL gh pr list). Workaround: unset GH_TOKEN; REST gh search prs +
per-PR pr-merge-gate.py. Recommend REST/timeout fallback in run-pr-program.py.
```

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | **lic** [#437](https://github.com/li-langverse/lic/pull/437) | Rebase → human APPROVED → `pr_merger` (label already on) |
| perf(bench): tier-1 matmul MIR fast paths (PH-5b, PH-7e) | **lic** [#541](https://github.com/li-langverse/lic/pull/541) | Close if superseded by #437; else APPROVED → `merge-approved` |
| fix(docs): ship site/index.html for lic GitHub Pages | **lic** [#542](https://github.com/li-langverse/lic/pull/542) or [#544](https://github.com/li-langverse/lic/pull/544) | Pick one; APPROVED → `merge-approved` |
| chore(bench): ingest tier-1 — horner red cleared | **benchmarks** [#211](https://github.com/li-langverse/benchmarks/pull/211) | APPROVED → `merge-approved` |
| docs(agent-kit): rollout digest v1.3.5 org sync | **benchmarks** [#204](https://github.com/li-langverse/benchmarks/pull/204) | Trigger CI; dedupe vs #213–#216 |
| docs(ecosystem): point compiler to lic (WP-A5) | **roadmap** [#19](https://github.com/li-langverse/roadmap/pull/19) | **Human** APPROVED + merge |
| docs(ecosystem): httpd mirror canonical (WP-B4) | **roadmap** [#21](https://github.com/li-langverse/roadmap/pull/21) | **Human** APPROVED + merge |
| feat(httpd): gap-phase2 perf wrk soak | **lic** [#489](https://github.com/li-langverse/lic/pull/489) | Add `plan-approved`; merge before #497 |
| REST/timeout fallback in pr-program / queue-plan | **benchmarks** | Prevent false `open_prs: 0` on slow GraphQL |
| Batch-close redundant sweep/digest PRs | **benchmarks**, **lic**, **lis** | 82 pairs in `redundant[]` — human triage |

## Deferred

- **Full `run-pr-program.py` refresh** — re-run after completion or timeout fix.
- **Canonical matmul pick** — maintainer: #437 vs #541/#520/#524 after rebase/CI.
- **Post-merge ingest** — refresh 6 dashboard red rows (`matmul_*`, `ml_*`, `num_gmres`) after lic numerics lands.
- **PH-DB / httpd feature stacks** — until `plan-approved` + dependency order (`lic#489` before `#497`).
- **Agent digest backlog** — ~40+ benchmarks workspace-sweep PRs; batch-close per `redundant[]`.
- **li-language agent-kit** — `missing_kit` per org-agent-kit audit (exit 1).
- **Draft plan PRs** lic#530–532 — undraft + release notes before merge-approved sweep.
