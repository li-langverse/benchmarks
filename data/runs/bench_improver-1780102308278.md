# Bench improver digest — 2026-05-30

**Agent:** `bench_improver` · **Run:** `bench_improver-1780102308278` · **Source:** heap `coord_numerics:b97ff11f0fa3e1dd6357`  
**north_star_fit:** PH-5b, PH-7e — tier-1 pure-Li / shared-kernel rows vs C++ oracle; proof-before-perf (checksum parity, no `trusted.lean` edits)

## Executive summary

- **Preflight:** `summary.json` @ 2026-05-29T07:01Z — **6 red** tier-1 rows, **2 yellow** tier-2 thermostats, **137 green**; 42 unknown harness rows (tier 0/5/6, N1024 variants, stdlib smokes).
- **Assess:** `./scripts/benchmark-failures-report.sh` confirms reds: `matmul_blocked` (1.549×), `num_gmres` (1.4×), `matmul_naive` + three `ml_*` smokes (1.333× each).
- **Local harness (this host, lic built, `--runs 5`):** `matmul_naive` **1.16×** cpp (0.0022s / 0.0019s); `matmul_blocked` **1.78×** (0.0155s / 0.0087s); `num_gmres` **0.80×** (0.0004s / 0.0005s, shared C kernel).
- **`matmul_naive` fix already in lic:** hot path is `C = A @ B` → `ArrayMatMul2DF64` + LLVM `fmuladd`; verify checksum unchanged (`161055.1865999999`). Dashboard red is **stale ingest**, not missing source fix.
- **`matmul_blocked` blocked on codegen:** BK=16 source micro-blocking **regressed 7×** (0.060s); scalar IKJ blocked loops need `emit.cpp` prefetch/FMA-chain/SIMD — not more source nesting.
- **`ml_*` rows:** WP4 smoke stubs (scalar recurrence), not real conv2d/MLP — red is measurement artifact until real kernels + C oracle land.
- **Yellow thermostats:** `md_thermostat_berendsen` (1.303×), `md_thermostat_nose_hoover` (1.291×) — shared `md_core.c` wrappers; need pure-Li thermostat step or accept tier-2 advisory band.
- **Shipped this pass:** synced `benchmarks/workloads/tier1_micro/matmul_naive/li/main.li` to lic `@` path; no lic codegen changes; no `summary.json` hand-edits.

## Deliverable / findings

### Dashboard reds (ingest oracle — stale)

| Benchmark | Ratio vs cpp | Repo | Li (s) | C++ (s) | Root cause | Status |
|-----------|--------------|------|--------|---------|------------|--------|
| `matmul_blocked` | **1.549×** | lic | 0.0158 | 0.0102 | Pure-Li 512³ blocked IKJ; no `ArrayMatMul2DF64` on tile path | **Open** — emit sprint |
| `matmul_naive` | **1.333×** | lic | 0.0036 | 0.0027 | Dashboard predates `@` fix | **Fixed in lic** — re-ingest |
| `num_gmres` | **1.400×** | lic | 0.0007 | 0.0005 | Shared `num_gmres_core.c`; Li FFI overhead on smoke scale | **Green locally** (0.80×) |
| `ml_conv2d_forward` | **1.333×** | li-math (smoke) | 0.0036 | 0.0027 | Stub loop | **Deferred** |
| `ml_mlp_forward` | **1.333×** | li-math (smoke) | 0.0036 | 0.0027 | Stub loop | **Deferred** |
| `ml_mlp_train_step` | **1.333×** | li-math (smoke) | 0.0036 | 0.0027 | Stub loop | **Deferred** |

### Local harness (2026-05-30, `lic` @ `fb035300`, Release build)

| Benchmark | cpp (s) | li (s) | Ratio | Verify |
|-----------|---------|--------|-------|--------|
| `matmul_naive` | 0.0019 | 0.0022 | **1.16×** | ok — `161055.1865999999` |
| `matmul_blocked` | 0.0087 | 0.0155 | **1.78×** | ok — `1288460.7563999966` |
| `num_gmres` | 0.0005 | 0.0004 | **0.80×** | ok — shared C kernel |

**Experiment (reverted):** BK=16 micro-blocking inside 64³ tiles → Li **0.060s** (7× regression). Confirms blocked matmul needs **compiler emit** work, not source loop restructuring.

### Yellow / near-threshold (watch)

| Benchmark | Ratio | Notes |
|-----------|-------|-------|
| `md_thermostat_berendsen` | 1.303× | Shared MD oracle; tier-2 advisory ≤1.2× |
| `md_thermostat_nose_hoover` | 1.291× | same |
| `num_cholesky` | 1.167× | Near 1.2× gate |
| `cloth_swing`, `robo_*`, `three_body` | 1.10–1.15× | Shared tier-2 kernels |

### Recent deltas (since last snapshot)

- **Improved:** `matmul_naive` 1.40× → 1.33× (ingest); `harmonic_oscillator_chain` yellow → green (0.86×).
- **Regressed:** `horner_pure_li` 0.44× → 0.75×; `simd_dot` 0.02× → 0.88× (measurement normalization).
- **New greens:** `cloth_swing`, `combustion_passive`, `euler_fluid_2d` unknown → green.

### Evidence (existing)

- Study: `lic/docs/numerics/studies/2026-05-29-matmul-naive-at-codegen.md`
- Release note: `lic/docs/release-notes/2026-05-29-matmul-naive-at-operator.md`
- Sprint note: `benchmarks/docs/release-notes/2026-05-29-tier1-matmul-dashboard-sprint.md`

## Recommended issues/PRs

| Title | Repo | Labels / notes |
|-------|------|----------------|
| perf(7e): matmul_blocked emit — prefetch, FMA chain, inner-j SIMD on blocked IKJ | **lic** | `PH-7e`, `numerics`; closes largest red (1.55–1.78×); emit-only, no Lean |
| chore: merge + re-ingest matmul_naive `@` path | **lic** + **benchmarks** | lic already has fix; `LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh` |
| perf: num_gmres — reduce Li wrapper overhead or widen smoke N | **lic** | Local 0.80×; confirm post-ingest before closing |
| Scaffold real `ml_*` kernels + C oracle + harness wiring | **lic** / **li-math** | Replace WP4 smoke stubs; im2col + matmul SOTA |
| Pure-Li Berendsen / Nosé–Hoover thermostat steps | **lic** | Yellow → green; `li-physics-particles` |
| Reclassify `ml_*` catalog rows as smoke until real kernels | **benchmarks** | Human approval for gate policy |
| Sync workloads `matmul_blocked` driver with lic 512³ oracle | **benchmarks** | Workloads uses 512×64³ scaling trick — document or align |

## Deferred

- `matmul_blocked` codegen sprint (source BK=16 **failed**; needs `emit_matmul2d_ijk_loops` blocked-tile variant).
- `ml_*` stub replacement (dashboard red is measurement artifact).
- MD thermostat pure-Li kernels (1.29–1.30× vs 1.2× tier-2 gate).
- 42 unknown harness rows (tier 0 stability, tier 5/6 lidb/lis, N1024 variants, stdlib micros).
- HPC `registry.toml` `last_reviewed` bump (no competitor release review this pass).
- Lean proof extension for large-N `@` (benches use `--no-lean-verify`; not blocking perf).
- GitHub API rate-limited — could not verify open lic PR state via `gh`.
