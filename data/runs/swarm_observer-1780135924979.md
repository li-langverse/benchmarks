# Swarm observer digest — `swarm_observer-1780135924979`

**Date:** 2026-05-30T10:14Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T10:03Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (75.0, grade **C**, refreshed @ 10:13Z)  
**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — grade **C** (75.0); `unattended_safe: true` on scorecard; live supervisor under concurrent-tick stress.
- **`orch-r3-missing-package-sweep` verified** — 3 `missing_package` gaps reconciled; `pkg-line-profiler`, `pkg-std-summary`, `pkg-std-plot` **pending** in `ecosystem-package-backlog.md` (re-applied @ 10:13Z).
- **Second error wave @ 10:11Z** — 10+ agents flipped `error` with `completion: null` in CP → **SDK preemption / concurrent tick kill**, not repo conflict.
- **Programmatic observer active** — remediations queued: `implementation_gaps` (healer), `proof_gap_researcher` (goal-align retry); `retry_counts: {}` still empty.
- **Gap pressure high** — **64** open registry gaps (30 `competitor_feature`, 31 `plan_debt`, 3 `missing_package`); ingest/apply idempotent @ 10:13:25Z.
- **Goal-directed runners** — 6/9 supervisors off or idle; `agents_live: 0`; httpd/ph-db/swarm-observer shells up but agent subprocess idle.
- **CP report stale** — `control_plane_reports.swarm_health.healthy: true` last updated **2026-05-25**; live state shows goal_mismatch finding.
- **Briefing alignment improving** — P0 `proof_gap_researcher`, `workspace_sweeper`, `plan_verifier` re-dispatched @ 10:12–10:13Z after 10:11Z wave.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C (75.0); `swarm_execution` 70 | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json`, `lic/data/swarm-gap-registry/registry.yaml` | high |
| Goal-directed runners | 6/9 stopped/idle; `agents_live: 0` | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP error wave | 10 errors @ 10:11Z; all `completion: null` | Supabase `agent_runs` | high |
| CP status hygiene | 113/120 sampled runs `running`; incomplete_rate 57% | scorecard `swarm_execution`, `li-cursor-agents/data/runs` | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25 | `control_plane_reports` `is_latest` | medium |
| Briefing preflight | `org_agent_kit_audit` exit 1 | `agent-briefing.preflight_runs` | medium |
| `missing_package` | 3 open: line_profiler, std.summary, std.plot | `ecosystem-package-backlog.md`, registry | medium |
| Workspace dirty | 1 sibling repo (lic perf branch, 5 files) | `agent-briefing.workspace_dirty_sweep` | low |

### Error sample classification

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Simultaneous `error` @ tick | `plan_verifier-1780135843033`, `workspace_sweeper-1780135842992` | **Supervisor wave kill / SDK preemption** — CP `completion: null` |
| Retry wave @ 10:12Z | `proof_gap_researcher-1780135995082` (running) | **Observer goal-align retry** after 10:11Z kill |
| Local JSON still `running` | `swarm_observer-1780135924979.json` on disk | **Status reconciliation lag** — CP has newer terminal state |
| Historical error volume | 300–559 errors/agent lifetime in CP | **Ingest drift** — not actionable per-run failures |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` | 90 gaps in registry; 0 new rows this pass |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` | 20 patches incl. 3 `missing_package` → `ecosystem-package-backlog.md` |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` | 75.0 grade C; `unattended_safe: true` |
| Observer scan | CP `control_plane_state.observer` @ 10:13:15Z | `retry_counts: {}`; remediations: `implementation_gaps`, `proof_gap_researcher` |
| Healer dispatch | `swarm_health.remediations` | `dispatch_healer` + `retry_agent` queued; no `stopped_agents` |

### `orch-r3-missing-package-sweep` reconciliation

| Gap id | `target_todo_id` | Backlog patch | Handoff |
|--------|------------------|---------------|---------|
| `gap-line-profiler-001` | `pkg-line-profiler` | pending | `issue_planner` |
| `gap-missing-std-std-summary` | `pkg-std-summary` | pending (PH-IO-7) | `issue_planner`, `package_architect` |
| `gap-missing-std-std-plot` | `pkg-std-plot` | pending (PH-IO-5) | `issue_planner`, `package_architect` |

Closed registry rows: `gap-missing-std-std-io`, `gap-missing-std-std-csv` (explorer status `present`).

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Increment `retry_counts` on wave-kill retries | `li-cursor-agents/src/observer/remediate.ts` |
| Refresh `control_plane_reports.swarm_health` each tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Throttle concurrent SDK dispatches per tick | `li-cursor-agents/src/supervisor/loop.ts` |
| Re-enable slow preflights when tick budget allows | `benchmarks/scripts/agent-briefing.py` |

### Human-only blockers

- **6 goal-directed runners stopped/idle** — may need user unit restart (not auto-merge).
- **Package implementation** — agents must not ship std modules without `plan-approved`.
- **PH-DB merge wave** — cross-repo PR merges require human review (`wp-pr-merge-wave`).
- **`trusted.lean`** — out of scope for gap orchestration.
- **`CURSOR_API_KEY`** — present (SDK runs active); not an auth gap this cycle.

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (75.0, grade C)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`)
- [x] Gap ingest + apply confirmed (@ 10:13Z)
- [x] `orch-r3-missing-package-sweep` orchestrator note + backlog patches
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified
- [ ] `orch-r4-ui-ux-signals` (deferred — studio-ux-16/17 incidentally patched by apply-actions)

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Open issues for std.summary, std.plot, li-line-profiler seed | `lic` | `issue_planner`, `PH-IO`, `gap_explorer` |
| Observer: terminalize stuck `running` runs after SDK TTL | `li-cursor-agents` | `control-plane`, `swarm_observer` |
| Refresh control_plane_reports swarm_health each supervisor tick | `li-cursor-agents` | `dashboard`, `observer` |
| Tier-1 matmul yellow → green (`matmul_blocked`, `matmul_naive`) | `lic` | `bench_improver`, `tier-1`, `PH-7e` |
| Agent-kit drift: 9 repos | org repos | `agent_kit_maintainer` |
| Merge `verticals.toml` competitive registry to benchmarks main | `lic` → `benchmarks` | `ecosystem`, `competitor_feature`, `no-auto-merge` |

---

## Deferred

- `orch-r4-ui-ux-signals` — studio-ux-16/17 patched; dedicated UI gap reconciliation next pass.
- Re-enable slow preflights (`plan_audit`, `ci_bug_triage`, `pr_program`) when tick budget allows.
- CP historical error relabeling (lifetime `error` rows) — batch job, not this pass.
- Restart stopped goal-directed runners (`compiler-studio`, `sim`, `studio-ui-ux`, research loops).
- Do **not** recommend `install-goal-plan-loop-systemd.sh` — route via swarm goals per `docs/ecosystem/swarm-architecture.md`.
