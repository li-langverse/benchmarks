# Bench improver — proactive sweep (run 1780140538756)

**Agent:** `bench_improver` · **Source:** proactive ecosystem sweep  
**north_star_fit:** blazingly-fast — PH-5b / PH-7e tier-1 lic harness; proof-before-perf (no `trusted.lean` edits)  
**Dashboard:** https://li-langverse.github.io/benchmarks/  
**Oracle:** `data/latest/summary.json` @ `2026-05-29T18:47:13Z` · `benchmarks` branch `main` (published ingest)

## Executive summary

- **Public dashboard: 6 RED, 2 YELLOW** on published `summary.json` (187 rows; 137 green) — tier-1 lic matmul + GMRES are the codegen blockers.
- **Briefing mismatch:** compact preflight (`agent-briefing.json` @ 2026-05-30T10:00) reports **0 red / 0 yellow / 22 green** — that slice does **not** match the on-disk oracle; treat dashboard `summary.json` as canonical until a full ingest lands on `main`.
- **lic PR #499** (`fix(bench): restore tier-1 matmul MIR fast paths`) claims **matmul_naive 1.333× → 1.000×**, **matmul_blocked 1.549× → 1.291×** locally — **CI red** on `build-and-test` (linux + macos); merge blocked.
- **Three lic tier-1 reds** need codegen (`matmul_blocked`, `matmul_naive`, `num_gmres`); **three li-math ML stubs** (`ml_conv2d_forward`, `ml_mlp_forward`, `ml_mlp_train_step`) are out of lic scope.
- **Two tier-2 yellow** thermostats (`md_thermostat_berendsen` 1.303×, `md_thermostat_nose_hoover` 1.291×) — shared-kernel MD oracle; micro-opt after tier-1 crisis.
- **`horner_pure_li`** is **green at 0.75×** cpp (pure-Li FMA); recent history delta shows slowdown vs prior 0.44× but still under gate.
- **Swarm gap registry** still lists stale `gap-benchmark-red-matmul-naive-tier1` / `num_gmres` — close after PR #499 merge + ingest proof.
- **Local runner:** `results/latest.csv` on agent host is sparse (tier-2 MD only) — cannot re-bench tier-1 without lic build; rely on CI ingest post-merge.

## Deliverable / findings

### Failures report (`./scripts/benchmark-failures-report.sh`)

```
RED (6):
  matmul_blocked               tier=1    1.549×  lic  PH=PH-5b
  num_gmres                    tier=1    1.400×  lic  PH=PH-5b
  matmul_naive                 tier=1    1.333×  lic  PH=PH-5b,PH-7e
  ml_conv2d_forward            tier=1    1.333×  li-math  PH=PH-5b
  ml_mlp_forward               tier=1    1.333×  li-math  PH=PH-5b
  ml_mlp_train_step            tier=1    1.333×  li-math  PH=PH-5b

YELLOW (2):
  md_thermostat_berendsen      tier=2    1.303×  lic  PH=PH-5b
  md_thermostat_nose_hoover    tier=2    1.291×  lic  PH=PH-5b

GREEN near threshold (>1.0× cpp, top 8):
  num_cholesky                 tier=1    1.167×  lic
  cloth_swing                  tier=2    1.154×  lic
  robo_* (5 rows)              tier=2    1.105×  lic
  three_body                   tier=2    1.105×  lic
```

Fresh `python3 scripts/ecosystem-audit.py` (2026-05-30T11:31Z) confirms the same red/yellow set.

### Published dashboard rows (`summary.json` → `rows[]`)

| benchmark | tier | repo | ratio vs cpp | status | li (s) | cpp (s) | variant / root cause |
|-----------|------|------|--------------|--------|--------|---------|----------------------|
| `matmul_blocked` | 1 | lic | **1.549×** | red | 0.0158 | 0.0102 | pure `@` — regression to generic matmul (PR #499 restores IKJ MIR) |
| `matmul_naive` | 1 | lic | **1.333×** | red | 0.0036 | 0.0027 | pure `@` N=256 — same regression |
| `num_gmres` | 1 | lic | **1.400×** | red | 0.0007 | 0.0005 | algo_registry — lic wrapper overhead on shared solver |
| `ml_conv2d_forward` | 1 | li-math | **1.333×** | red | 0.0036 | 0.0027 | stub — not lic codegen |
| `ml_mlp_forward` | 1 | li-math | **1.333×** | red | 0.0036 | 0.0027 | stub |
| `ml_mlp_train_step` | 1 | li-math | **1.333×** | red | 0.0036 | 0.0027 | stub |
| `md_thermostat_berendsen` | 2 | lic | **1.303×** | yellow | 3.4496 | 2.6470 | algo_registry — thermostat coupling overhead |
| `md_thermostat_nose_hoover` | 2 | lic | **1.291×** | yellow | 3.3680 | 2.6025 | algo_registry |

**Greens of note (lic tier-1):**

| benchmark | ratio vs cpp | variant |
|-----------|--------------|---------|
| `horner_pure_li` | **0.75×** | pure_li — PH-7e FMA path healthy |
| `simd_dot` | **0.88×** | shared_c_kernel |
| `num_integ_rk4` | **0.91×** | algo_registry |

### History deltas (26 since prior snapshot)

Notable regressions (do not hand-edit JSON — re-bench + ingest):

- `horner_pure_li`: 0.444× → 0.75× (still green)
- `simd_dot`: 0.016× → 0.881× (still green; prior ratio suspect)
- `reduce_sum`: 0.978× → 1.023× (green, near threshold)

Improvements:

- `matmul_naive`: 1.40× → 1.333× (still red)
- `harmonic_oscillator_chain`: yellow → green (1.215× → 0.859×)
- `nbody_gravity`: 0.999× → 0.922×

### lic PR #499 status

- **URL:** https://github.com/li-langverse/lic/pull/499  
- **Labels:** `numerics-research`  
- **CI:** `build-and-test` FAIL (linux), `build-and-test-macos` FAIL; HPC registry + tier0 PASS  
- **Claimed local after:** naive **1.000×**, blocked **1.291×** (blocked still >1.2× advisory)  
- **Study:** `lic/docs/numerics/studies/2026-05-30-bench-improver-matmul-restore.md` (referenced in PR body)

### Mode A numerics levers

| benchmark | SOTA / Learned from | lic fix path |
|-----------|---------------------|--------------|
| `matmul_naive` | BLIS/Goto IKJ; register blocking | Restore IKJ hot loop MIR (PR #499); PH-7e `@` lowering |
| `matmul_blocked` | BLIS micro-kernel + cache blocking | `mm_blocked_512` MIR hook + 4-wide FMA tiles; blocked may need static buffer (human review) |
| `num_gmres` | Saad GMRES; minimize temp alloc | Profile lic call overhead on `common/*_core.c`; fused dot/norm |
| `md_thermostat_*` | Nose-Hoover chain / Berendsen rescale | Shared MD kernel + lic integrator wrapper — tier-2 pass |

### Repro (assessment)

```bash
cd benchmarks
./scripts/benchmark-failures-report.sh
python3 scripts/ecosystem-audit.py

# After lic PR #499 green + merge:
cd ../lic/benchmarks/harness
python3 bench.py --tier 1 --only matmul_naive,matmul_blocked,num_gmres --runs 5
cd ../../benchmarks
LIC_ROOT=../lic ./scripts/ingest/ingest-lic.sh   # full ingest only — no partial CSV greens
./scripts/benchmark-failures-report.sh
```

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| `fix(bench): unblock PR #499 CI — restore tier-1 matmul MIR fast paths` | **lic** | `PH-7e`, `PH-5b`, `numerics-research` — fix linux/macos `build-and-test`, then merge |
| `perf(7e): matmul_blocked static buffer / micro-kernel ≤1.2× cpp` | **lic** | `PH-7e`, `PH-5b` — after #499; human review for aligned alloc |
| `perf(5b): num_gmres lic wrapper — reduce call overhead ≤1.2×` | **lic** | `PH-5b`, `numerics` |
| `chore(bench): full tier-1 ingest after lic #499 merge` | **benchmarks** | `ingest`, `PH-5b` — via `ingest-lic.sh` only |
| `chore(swarm): close stale gap-benchmark-red-matmul-naive / num_gmres` | **lic** | `ecosystem-gap` — after dashboard greens |
| `perf(5b): MD thermostat tier-2 — Berendsen/Nose-Hoover ≤1.2×` | **lic** | `PH-5b`, `numerics`, `tier-2` |
| `feat(li-math): ML forward/train stubs — conv2d + MLP ≤1.2× cpp` | **li-math** | `PH-5b` — separate from lic codegen |
| `ci: tier0_stability ingest on linux runner` | **benchmarks** / **lic** | `PH-5b` |

## Deferred

- **Briefing vs oracle reconciliation** — investigate why preflight compact slice (@ 2026-05-30T10:00) disagrees with published `summary.json`; ensure ingest freshness propagates to `agent-briefing.json`.
- **≤1.0× cpp advisory polish** for near-threshold greens (`num_cholesky` 1.167×, robotics tier-2 cluster ~1.105×) — after tier-1 reds clear.
- **Parallel/simd codegen** beyond matmul — Lean / PH-7d contract evidence required.
- **117 catalog path gaps** under lic root — tracked in plan-completion audit; not bench_improver scope this pass.
- **Do not** edit `data/latest/summary.json` by hand or self-merge PRs.
