# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780099559671`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**north_star_fit:** ecosystem pull-requests / PH-5b, PH-7e — tier-1 numerics merge queue after proof-preserving codegen (proof → easy → fast).

## Executive summary

- Live org scan: **108** open PRs, **78** CI-green (non-draft); preflight `pr-program-run.json` and `pr-merge-queue-plan.json` remain **stale** (`open_prs: 0`) — full-org gate enrichment hung >7 min (two background runs still in progress).
- **No PRs gate-ready** for `pr_merger` (`ready: true` + `blockers: []`); universal blockers: missing human `APPROVED` review and/or `merge-approved` label.
- **Top aligned candidate:** [lic#437](https://github.com/li-langverse/lic/pull/437) — PH-5b/7e tier-1 matmul via `@` lowering; CI green; release notes + numerics study present; no `catalog.toml` threshold edits.
- **Supersede stack:** lic#439, lic#446 overlap #437 on matmul tier-1 — close after #437 merges and benchmarks ingest.
- **Second aligned chore:** [benchmarks#164](https://github.com/li-langverse/benchmarks/pull/164) — centralize `CI_EXEMPT_REPOS`; CI green; release notes waived (internal audit alignment).
- **3 roadmap PRs** (#19, #20, #21) carry `merge-approved` but **governance policy forbids agent merge** — human required.
- **lis#20** blocked on `plan-needed` without `plan-approved` (PH-DB feature work).
- **GitHub API rate limit** hit mid-run (403 on `gh pr diff`) — gate checks on sampled PRs completed before limit; no merges, **0** labels added.

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-program-run.json` | ⚠️ Stale | `open_prs: 0`, `ci_green: 0` — contradicts live `gh pr list` scan |
| `pr-merge-queue-plan.json` | ⚠️ Stale | `merge_first: null`, `redundant: []`, `open_prs: 0` |
| Live org scan | ✅ | 108 open / 78 CI-green / 3 `merge-approved` on roadmap |
| `pr-branch-hygiene.json` | ✅ | 0 branches needing PR; 0 safe-close recommendations |

**Root cause:** `pr-merge-queue-plan.py` and `run-pr-program.py` invoke `pr-merge-gate.py` per PR during enrichment — full-org run exceeds supervisor tick budget. Recommend caching gate results or limiting enrichment to CI-green PRs only.

### Error

```
HTTP 403: API rate limit exceeded for user ID 207167228
(request during gh pr diff on benchmarks#164, lic#446 — timestamp 2026-05-30T00:13:22Z)
```

Gate JSON checks completed on key targets before limit; diff inspection deferred.

### Standards review (CI-green targets)

#### 1. lic#437 — `perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ PH-5b, PH-7e; master-plan Phase 7e |
| Strict by default | ✅ `@` lowering → `ArrayMatMul2DF64`; no `trusted.lean` / `unsafe` |
| Security | ✅ N/A (codegen path) |
| Performance | ✅ Evidence study + release notes; no catalog threshold weakening |
| Release notes | ✅ `docs/release-notes/2026-05-29-bench-improver-matmul-tier1.md` |
| Ecosystem-first | ✅ Post-merge benchmarks ingest documented |

**Gate blockers:** `review_approved`, `merge-approved` (human action).  
**Prior comment retained:** https://github.com/li-langverse/lic/pull/437#issuecomment-4580677450

#### 2. lic#439 / lic#446 — redundant matmul stack

| PR | Verdict | Reason |
|----|---------|--------|
| #439 | **superseded** | Overlaps #437; additional `local_ci_recorded` gate blocker |
| #446 | **superseded** | Same `@` lowering scope; #437 has stronger blocked-GEMM evidence |

Both CI-green; neither receives `merge-approved` while #437 is open.

#### 3. benchmarks#164 — `fix(audit): align ecosystem-audit CI exempt`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ Ecosystem audit hygiene (coord_ecosystem) |
| Strict by default | ✅ Config alignment only |
| Release notes | ✅ Waived (internal `org_repos.py` / audit script) |
| CI | ✅ Green |

**Gate blockers:** `review_approved`, `merge-approved`, `release_notes` (strict gate — waived on human review as chore).

#### 4. lis#20 — `feat(PH-DB): lis db supervisor and control-plane hosting profile`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ PH-DB traceable in title |
| Plan | ❌ `plan-needed` without `plan-approved` |
| CI | ❌ `ci_green: none` — needs `local-ci-sweep.py --repo lis --pr 20` |

**Verdict:** **needs plan** — do not merge until `plan-approved` + CI green.

#### 5. roadmap#19–21 — merge-approved but governance-blocked

| PR | Labels | Gate blockers |
|----|--------|---------------|
| #19 | `merge-approved` | Human review + governance policy |
| #20 | `merge-approved` | Human review + governance policy |
| #21 | `merge-approved` | Human review + governance policy |

**Policy:** Never auto-merge `roadmap` without human (`ALLOW_GOVERNANCE_MERGE=1` not set).

### CI-green noise (batch triage recommended)

| Category | Count (approx) | Action |
|----------|----------------|--------|
| Agent digest / sweep PRs | ~25 benchmarks | Close stale; keep latest per agent type |
| Fuzz-corpus bot sync | ~12 lic | Merge latest only; close older sync branches |
| Autoresearch deliverables | ~4 lic (#455–458) | Human triage or close if artifact-only |
| Workspace sweep fallback | ~8 benchmarks | Redundant chore stack |

### Actions taken this run

- **0 merges**, **0** `merge-approved` labels added (no PR fully gate-ready).
- Review comments from prior pass (`pr_reviewer-1780097200000`) retained on #437, #439, #446, #164 — not duplicated to avoid spam.

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | `lic` #437 | Human: `APPROVED` + `merge-approved` → `pr_merger` |
| fix(audit): align ecosystem-audit CI exempt with org-repo-ci | `benchmarks` #164 | Human: `APPROVED` + `merge-approved` |
| Close superseded matmul PRs (#439, #446) | `lic` | `superseded`, pointer → #437 |
| feat(PH-DB): lis db supervisor… | `lis` #20 | Add `plan-approved`; run local-ci |
| roadmap docs/agent-kit (WP-A1/A5/B4) | `roadmap` #19–21 | **Human merge only** |
| perf: cache gate enrichment in merge-queue plan | `benchmarks` | `ecosystem-ci`, `agent:ci_maintainer` |

## Deferred

- **Human review bottleneck** — agent auth cannot `gh pr review --approve` on owner-authored PRs.
- **Preflight script timeout** — `run-pr-program.py` / `pr-merge-queue-plan.py` hung >7 min; re-run with gate cache before next `pr_merger` tick.
- **Post-merge ingest** — after lic#437, run benchmarks ingest to refresh 6 dashboard red rows (`matmul_*`, `ml_*`, `num_gmres`).
- **PH-DB stack** (lis#17–20, lip#24–30, roadmap#32–35) — partial CI; defer until plan-approved + dependency order clarified.
- **78 CI-green PR backlog** — majority agent noise; `pr_alignment` should batch-close stale digests and fuzz-corpus duplicates.
- **GitHub rate limit recovery** — wait for reset before diff-heavy review passes.
