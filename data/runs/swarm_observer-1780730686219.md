# Swarm observer digest — `swarm_observer-1780730686219`

**Date:** 2026-06-06T08:04Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai — gap orchestration)  
**Supervisor dimension:** `performance` (worker `c3de5145`)  
**Briefing:** `/workspace/benchmarks/data/latest/agent-briefing.json` (2026-06-06T08:04Z)  
**Scorecard:** `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` (regenerated 2026-06-06T08:04:40Z)  
**Orchestrator notes:** [`orch-r5-performance-gap-orchestration`](/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-06-orch-r5-performance-gap-orchestration.md)

---

## Executive summary

- **Posture: degraded** — refreshed scorecard **64.8 / grade D**, `unattended_safe: false` (was C/77.0 on stale 2026-05-31 snapshot).
- **Control-plane artifacts missing locally** — `data/control-plane/latest-report.json` and `state.json` absent in this container; observer remediations not observable this cycle (`runs_sampled: 0`).
- **Gap ingest blocked** — `lic/scripts/swarm-gap-ingest.py` **SyntaxError line 229** prevents registry refresh; last successful apply remains 2026-05-31 (`swarm-gap-actions.json`).
- **Performance lens (this run):** 0 tier-1 **red** rows; **5 near-threshold** benches (lead: `simd_dot` 1.13× cpp); **30** open `competitor_feature` registry gaps including 8 tier-1 red-class perf rows; benchmarks#370 (li-parallel harness) CI failing.
- **Briefing vs scorecard drift:** briefing recommends `ci_maintainer` + `security_auditor`; scorecard adds `swarm_observer`, `gap_explorer`, `ecosystem_grader`, `plan_verifier` — heap under-dispatches meta/gap agents.
- **Ecosystem posture weak:** 78 open PRs, **36 failing CI**, 3 repos missing CI on main; 8 benchmarks metrics-refresh PRs (#353–#373) stuck failing.
- **Goal-directed runners:** 6/9 stopped per snapshot (stale 2026-05-30); httpd loop has 2 pending perf-soak todos with exit **124** timeouts.
- **`CURSOR_API_KEY` set** — SDK auth available; failures are orchestration/infra, not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Gap ingest script | `SyntaxError: unterminated string literal` @ line 229 | `/workspace/lic/scripts/swarm-gap-ingest.py` | **critical** |
| Ecosystem grade | Overall **64.8**, grade **D**, `unattended_safe: false` | `/workspace/benchmarks/data/latest/ecosystem-quality-report.json` | **critical** |
| Failed PR CI | 36 open PRs with failing CI | `agent-briefing.json` → `ecosystem_audit.metrics.failed_prs` | high |
| Metrics refresh PRs | benchmarks #353–#373 (grade/gap actions) all CI fail | `agent-briefing.json` → `ecosystem_audit.failed_prs` | high |
| Gap registry stale | 64 open gaps; apply last run 2026-05-31 | `/workspace/benchmarks/data/latest/swarm-gap-actions.json` | high |
| Goal runners stopped | 6/9 runners not running | `/workspace/lic/data/goal-directed-agents/snapshot.json` | high |
| Preflight failures | `org_ci_audit` exit 1 (3 INCOMPLETE repos 404); `org_agent_kit_audit` exit 1 (roadmap agent-kit missing) | `agent-briefing.json` → `preflight_runs` | high |
| Performance near-threshold | `simd_dot` 1.1279× cpp; 4 MD rows ~1.01–1.02× | `/workspace/benchmarks/data/latest/ecosystem-audit.json` → `benchmarks.near_threshold` | medium |
| li-parallel harness | PR #370 dual-mode Class A variants — CI fail | `agent-briefing.json` → failed_prs | medium |
| Control-plane blind spot | No `latest-report.json` / `state.json`; `runs_sampled: 0` | `/app/data/control-plane/` (sdk-slots only) | medium |
| Briefing goal drift | Scorecard recommends 6 agents; briefing heap only 2 | compare `ecosystem-quality-report.json` vs `agent-briefing.json` | medium |
| Preflight skipped | 8 scripts skipped (`--skip-slow`) incl. `plan_audit`, `security_cwe_audit` | `agent-briefing.json` → `preflight_runs` | medium |
| httpd perf soak | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` pending; exit 124 | `snapshot.json` runner `httpd` | medium |
| Unknown benchmark catalog | 95+ benchmark ids `unknown` (no ratio) | `ecosystem-audit.json` → `benchmarks.unknown` | low |

### Performance dimension — gap orchestration (`swarm_coverage`)

| Signal | Detail | Registry / backlog route |
|--------|--------|--------------------------|
| Near-threshold (watch) | `simd_dot` **1.13×**, MD suite 1.01–1.02× | Handoff **`numerics_researcher`** goal `md_sim_algorithms`; patch `sim-p1-num-dot-axpy` already in `sim-algorithm-backlog.md` |
| Tier-1 red-class (registry) | 8 rows e.g. `matmul_naive`, `num_gmres`, `num_opt_line_search` | **`competitor_feature`** → route **`bench_improver`** + **`autoresearch`** (proof-before-perf) |
| HPC competitor gaps | Kokkos, PETSc, FFTW roofline, OpenMP rubric | Registry ids `gap-hpc-*` → research goals `scientific_distributed_computing`, `physics_sim` |
| PH-7e plan debt | `phase-7e-math-simd-parallel-lowering` partial | `plan_debt` → **`plan_verifier`** + **`implementation_gaps`** (no perf shortcuts) |
| Parallel harness | benchmarks#370 li-parallel dual-mode | **`ci_maintainer`** fix CI → unblocks Class A perf evidence |
| Vertical stub honesty | 95 unknown + stub rows block honest perf posture | **`gap_explorer`** after ingest fix; patched stubs in `sim-md-research-backlog.md` |

**North star fit:** ecosystem orchestration routes perf work through proved codegen (PH-7e) and bench discipline (PH-5b) — never `unsafe` / unproved SIMD shortcuts.

### Error sample classification

| Pattern | Evidence | Root cause |
|---------|----------|------------|
| Gap ingest crash | `swarm-gap-ingest.py:229` SyntaxError | **Script bug** — broken fallback path for `verticals.toml` |
| No local error runs | `data/runs/*.json` — only current `running` observer row | **Fresh container** — CP run history not mounted (`runs_dir` points to `/workspace/li-cursor-agents/data/runs`) |
| httpd exit 124 | `snapshot.json` history | **Plan loop timeout** — wrk soak exceeds batch deadline |
| org_ci_audit exit 1 | 404 on li-api-kit, li-sec-agent, token-telemetry-service | **Missing / private repos** — human governance |
| org_agent_kit_audit exit 1 | roadmap agent-kit not found | **Missing sibling checkout** — sync `roadmap` repo |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | `ecosystem-quality-grade.py` @ 08:04:40Z | Scorecard refreshed; `runs_sampled: 0` (no local run JSON) |
| Gap ingest + apply | Attempted this cycle | **Failed** — ingest SyntaxError; apply not re-run |
| Auto-retry / healers | CP state unavailable | **Not observable** — no `state.json` / `latest-report.json` |
| Briefing preflight | ecosystem_audit ✓; workspace_dirty_sweep ✓ | Partial signal only |
| This meta pass | `swarm_observer` | Report + orchestrator note + perf whitepaper draft |

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Fix `verticals.toml` fallback SyntaxError | `lic/scripts/swarm-gap-ingest.py:229` |
| Mount/sync `data/runs` for scorecard `swarm_execution` sampling | deploy / `ecosystem-quality-grade.py` → `runs_dir` |
| Persist `latest-report.json` + `state.json` in agent container | `li-cursor-agents/src/control-plane/` |
| Merge scorecard `recommended_agents` into briefing heap dispatch | `benchmarks/scripts/agent-briefing.py` or supervisor heap |
| Unblock benchmarks metrics PR CI (#353–#373) | `benchmarks` — fix grade script / workflow |
| Re-enable `plan_audit` + `security_cwe_audit` on scheduled ticks | briefing preflight config (drop `--skip-slow` for audit lane) |
| Terminalize stuck SDK `running` rows after TTL | `li-cursor-agents/src/observer/` |

### Human-only blockers

- **Governance PRs** and **trusted.lean** — never auto-merge
- **Missing repos** (li-api-kit, li-sec-agent, token-telemetry-service) — create or archive from org CI audit
- **roadmap/agent-kit** checkout — required for `org_agent_kit_audit`
- **36 failing PRs** — human triage before merge (esp. lic PR stacks with 258 redundant-pair warnings)
- **httpd wrk soak timeouts** — human deadline / env tuning for exit 124
- **Tier-1 perf codegen** — requires proved PH-7e work; no auto-merge on compiler changes

### Agent deliverable checklist

- [x] Ecosystem quality report refreshed (64.8, grade D @ 08:04:40Z)
- [x] Briefing + ecosystem audit read
- [x] Gap registry + actions reconciled (read; ingest **blocked** by script bug)
- [x] Orchestrator note: `orch-r5-performance-gap-orchestration`
- [x] Recommended agents vs briefing compared (drift documented)
- [x] Performance dimension audit (near-threshold + registry perf gaps)
- [x] Whitepaper draft artifact (local; publish deferred — no `research-findings` checkout)
- [ ] Gap ingest re-run (blocked until SyntaxError fixed)
- [ ] Control-plane observer PR (terminalization / report persistence)

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | line-profiler, std.summary, std.plot | Prior patches in backlog; handoff **`issue_planner`** (unchanged from orch-r3) |
| `plan_debt` (31) | sim/security/studio/ph-db rows | Route **`plan_verifier`** + **`implementation_gaps`**; perf soak todos on httpd |
| `competitor_feature` (30) | tier-1 red + HPC + vertical stubs | Route **`gap_explorer`**, **`bench_improver`**, **`numerics_researcher`** after ingest fix |
| `ui_ux` (0 open registry) | studio-ux GPU runner todos patched | **`gui_ux_tester`** via `ui_ux_quality` goal |

Do **not** recommend `install-goal-plan-loop-systemd.sh` — use `li-cursor-agents` research/implement goals only.

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| fix: swarm-gap-ingest verticals.toml fallback SyntaxError (line 229) | lic | `swarm`, `bug`, `orchestration` |
| fix: benchmarks ecosystem-grade CI for metrics refresh PRs | benchmarks | `ci`, `swarm`, `metrics` |
| Observer: persist control-plane report + sample runs in agent containers | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch gap_explorer + ecosystem_grader when scorecard recommends | li-cursor-agents | `swarm`, `orchestration` |
| CI: add workflows for 3 repos missing CI on main | org repos | `ci`, `ecosystem` |
| Unblock li-parallel dual-mode benchmark harness (PR #370) | benchmarks | `performance`, `PH-7e`, `hpc` |
| PH-7e: simd_dot near-threshold — proved SIMD dot path | lic | `performance`, `PH-7e`, `provability` |
| Package issues: li-line-profiler seed + std.plot/summary | lic | `ecosystem`, `PH-IO` |

---

## Deferred

- Publish whitepaper to `research-findings/whitepapers/2026-06/swarm_coverage/` — **no local checkout**; run `publish-research-whitepaper.sh` after clone
- Re-run `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py` after SyntaxError fix
- Close registry row `gap-plan-pending-swarm-observer-orch-r3-missing-package-sweep` on next successful ingest
- `orch-r4-ui-ux-signals` — deferred until studio-ui plan loop active
- Full `plan_audit` + `security_cwe_audit` preflights (currently `--skip-slow`)
- Control-plane DB cross-check (Supabase) — not available in this container
- Auto-merge of any governance or perf-codegen PRs

---

## Evidence paths

- `/workspace/benchmarks/data/latest/ecosystem-quality-report.json`
- `/workspace/benchmarks/data/latest/agent-briefing.json`
- `/workspace/benchmarks/data/latest/ecosystem-audit.json`
- `/workspace/benchmarks/data/latest/swarm-gap-actions.json`
- `/workspace/lic/data/swarm-gap-registry/registry.yaml`
- `/workspace/lic/data/goal-directed-agents/snapshot.json`
- `/workspace/lic/docs/ecosystem/orchestrator-notes/2026-06-06-orch-r5-performance-gap-orchestration.md`
- `/app/data/runs/swarm_observer-1780730686219-performance-whitepaper.md`
