# ci_maintainer digest — run 1780109873149

**Generated:** 2026-05-30T03:00Z · **Source:** proactive ecosystem sweep

## Executive summary

- Refreshed `org-repo-ci-audit.json`: **32 OK**, **0 missing**, **1 gated**, **0 incomplete**.
- `ensure-org-repo-ci.py` exit 0; no package repos need new `ci.yml` on `main` this pass.
- **Gated:** `lidb` (default `feat/ph-db-2-liorm-liq`) — WP-H0: promote default branch to `main` before enforcing `ci.yml` on default.
- **Audit drift:** `ecosystem-audit.json` still lists `research-findings` in `missing_ci_on_main`; `ensure-org-repo-ci.py` treats it as **CI-exempt** (whitepaper archive).
- Open follow-up: [research-findings#6](https://github.com/li-langverse/research-findings/pull/6) (`ecosystem-ci`, green `check` job) — human merge when policy allows optional CI.
- Branch protection verify (`roadmap/scripts/verify-org-branch-protection.sh`): **11 OK / 22 missing** rulesets; CI workflows present on most repos — protection gap is governance, not missing workflows.
- No isolated clone edits or new `ecosystem-ci` workflow PRs opened (nothing actionable under org CI gate).
- Prior supervisor errors (`unregistered_running_reconciled`) on runs 1780109530757–1780109773610 — infrastructure, not CI audit failure.

## Deliverable / findings

| Signal | Value |
|--------|-------|
| Required workflow | `ci.yml` on default branch (`main`) |
| Template | `lic/scripts/templates/github-repo/ci.yml` |
| Exempt repos | `li-cursor-agents`, `research-findings` |
| Expected check names (hints) | `lic`/`li-language`: `build-and-test`; `benchmarks`: `ingest-smoke`; `roadmap`: `verify-kit`; `lip`: `bootstrap`; `lit`: `test`; package mirrors: `check` |

**North star fit:** Ecosystem functionality gate (provable CI before perf work) — org package mirrors already carry org template CI on `main`; numerics red rows are out of scope for `ci_maintainer`.

**Branch protection vs CI:** Legacy `branches/main/protection` API returns 404 for sampled repos (`lic`, `li-demo`, `lip`, `lit`, `lis`, `roadmap`, `benchmarks`). Active enforcement uses **rulesets** (verify script). Repos with OK rulesets include `lic`, `lip`, `lis`, `lit`, `roadmap`, `benchmarks`, and others; 22 repos lack active rulesets including `lidb`, `research-findings`, and most `sim.*` / `studio.*` mirrors.

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Merge `chore(ci): add org workflow for research whitepapers` | research-findings | `ecosystem-ci`, `li-swarm`, `agent:ci_maintainer` — [#6](https://github.com/li-langverse/research-findings/pull/6) |
| WP-H0: set `lidb` default branch to `main` | lidb | governance — unblocks CI-on-default gate |
| `fix(audit): align ecosystem-audit CI exempt with org-repo-ci` | benchmarks | `ecosystem-ci` — drop `research-findings` from `missing_ci_on_main` when exempt |
| Apply org branch protection rulesets (22 repos) | roadmap | governance — `apply-org-branch-protection.sh` |

## Deferred

- **lidb** default-branch promotion (WP-H0) before requiring `ci.yml` on default.
- **research-findings** optional CI: exempt in org audit; open PR #6 for human decision.
- **ecosystem-audit / org-repo-ci-audit** alignment for exempt repos (benchmarks housekeeping).
- **22 repos** without active GitHub rulesets — separate governance rollout (`apply-org-branch-protection.sh`), not `ci_maintainer` template copy.
- Consolidate stale open `benchmarks` digest PRs (#151–#184) — housekeeping, not blocking CI gate.

## Error

None this run.
