# CI maintainer — org repo CI sweep

**Run id:** `ci_maintainer-1780101265644`  
**Agent id:** `ci_maintainer`  
**Source:** heap · `coord_platform` · `heap:coord_platform:ci_maintainer:506ba4c72b4b76aa1920`  
**north_star_fit:** ecosystem platform / provable CI gate — every package mirror proves `lic check` before merge (proof → easy → fast).

## Executive summary

- Refreshed `ensure-org-repo-ci.py` and `ecosystem-audit.py` — **0 org repos missing `ci.yml`** on default branch (exit 0).
- **32 repos OK**, **1 gated** (`lidb` WP-H0: default branch `feat/ph-db-2-liorm-liq` has `ci.yml` but policy requires `main`).
- Briefing queue cited **12 missing CI** — stale; current audits show the backlog cleared since prior wave.
- `ecosystem-audit.metrics.repos_missing_ci_main: 1` is **`research-findings` only** (CI-exempt per `org_repos.IGNORE_REPOS`; no workflow on `main`).
- Open [research-findings#6](https://github.com/li-langverse/research-findings/pull/6) adds org CI template — **green `check`**, mergeable; human review required (governance).
- Branch protection verify: **11 rulesets active**, **22 repos missing** active rulesets despite CI workflows present.
- **No new `ci.yml` PRs** opened this run — isolated clone workflow not needed for package mirrors.
- Prior digest PRs (#153–#172) remain open on `benchmarks`; consider consolidating after human review.

## Deliverable / findings

### Preflight audit (`org-repo-ci-audit.json`)

| Metric | Count | Notes |
|--------|------:|-------|
| OK / exempt | 32 | All non-exempt repos have `ci.yml` on default branch |
| Missing CI | 0 | — |
| Gated (non-main default) | 1 | `lidb` — WP-H0 |
| Audit incomplete | 0 | GitHub API reachable for all repos |

**Gated repo detail**

| Repo | Default branch | Workflows on default | Gate |
|------|----------------|---------------------|------|
| `lidb` | `feat/ph-db-2-liorm-liq` | `ci.yml` | WP-H0: rename default → `main`, then apply branch protection |

**Exempt:** `research-findings` — listed in `EXEMPT_REPOS`; `ecosystem-audit.has_ci_on_main()` still returns false (404 on `main`).

### Required check alignment (repos with active rulesets)

| Repo | Ruleset | Expected check (`org-branch-protection.json`) | CI job in `ci.yml` |
|------|---------|-----------------------------------------------|-------------------|
| `lic` | ✅ | `version`, `build-and-test` | matches |
| `li-language` | ✅ | `version`, `build-and-test` | matches |
| `benchmarks` | ✅ | `ingest-smoke`, `dashboard-build` | matches |
| `lip` | ✅ | `bootstrap` | matches |
| `lit` | ✅ | `test` | matches |
| `lis` | ✅ | `[]` (matrix) | matrix jobs — empty ruleset OK |
| Package mirrors (`li-demo`, `li-net`, …) | ✅ | `check` | matches lic template job name |

**22 repos with CI but no active ruleset** (protection gap, not missing workflow):

`li-cursor-agents`, `li-gui`, `li-local-ci`, `lidb`, `mmo`, `net.httpd`, `physics.custom`, `physics.runtime`, `proof-library`, `render`, `research-findings`, `sim`, `sim.additive`, `sim.automotive`, `sim.drug_design`, `sim.robotics`, `sim.scientific`, `store.realtime`, `studio`, `studio.ai`, `ui`, `world`

Run `roadmap/scripts/apply-org-branch-protection.sh` (human/org-owner) to close this gap.

### Agent deliverable checklist

- [x] Preflight audit refreshed — `data/latest/org-repo-ci-audit.json`, `ecosystem-audit.json`
- [x] Branch protection verify — `roadmap/scripts/verify-org-branch-protection.sh`
- [x] No `continue-on-error` on OS matrix jobs introduced (no workflow edits)
- [x] Run narrative — this file
- [x] Branch pushed and PR opened on `benchmarks` (digest only)
- [ ] merge-approved (human adds after review)

## Recommended issues/PRs

| Title | Repo | Labels | Action |
|-------|------|--------|--------|
| chore(ci): add org workflow for research whitepapers | `research-findings` | `ecosystem-ci`, `li-swarm`, `agent:ci_maintainer` | Human merge [#6](https://github.com/li-langverse/research-findings/pull/6) — CI green |
| WP-H0: lidb default branch → main | `lidb` | `ecosystem-ci`, governance | Human branch rename; then `apply-org-branch-protection.sh lidb` |
| Apply org branch protection to package mirrors | `roadmap` | `ecosystem-ci`, governance | Human: `./scripts/apply-org-branch-protection.sh` for 22 FAIL repos |
| Consolidate open ci_maintainer digest PRs | `benchmarks` | `ecosystem-ci` | Close redundant #153–#171 after merging latest digest |

## Deferred

- **WP-B1 (`lidb`)** — blocked on WP-H0 human default-branch rename; CI workflow already on feature default.
- **Branch protection rollout** — 22 repos; out of agent scope (org-owner UI / `apply-org-branch-protection.sh`).
- **research-findings on `main`** — exempt from org CI policy; optional merge of #6 for consistency only.
- **Briefing stale count** — regenerate agent briefing so `ci_maintainer` queue reflects 0 missing (not 12).
- **WP-C1–C6 / WP-E1–E3** — lic CI image reuse, reusable `package-ci`, benchmarks dedup — future `ci_maintainer` lic-specialist passes per org hygiene plan.
