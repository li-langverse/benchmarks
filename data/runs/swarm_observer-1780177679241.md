# Swarm observer digest — `swarm_observer-1780177679241`

**Date:** 2026-05-30T21:48Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai-first — PH orchestration)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T20:30Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (73.3, grade **C**, `unattended_safe: false` @ 21:47Z)  
**Orchestrator notes:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`, `2026-05-30-orch-r4-ui-ux-signals.md`, `2026-05-30-orch-ph-db-backlog-mapping.md`, `2026-05-30-orch-swarm-meta-audit-2148Z.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — ecosystem grade **C** (73.3/100, down from 79.5); scorecard now flags **`unattended_safe: false`** due to 27% terminal error rate in the local run sample.
- **Unattended operation: not recommended** — CP shows **870 errors / 2h** across ~20 agents (~40 errors/agent); supervisor `waiting`, `supervisor_loop_running: false` while **24 runs** still `running`.
- **Strong lanes:** `ecosystem_posture` **100** (0 red benchmarks; `matmul_blocked` yellow only); main-branch CI posture green; no failed PRs in audit.
- **Weak lanes:** `swarm_execution` **55** (109/120 local runs stuck `running`; terminal error rate 27%); `gap_pressure` **60** (64 open gaps); `goal_directed_health` **70** (6/9 runners stopped).
- **Briefing alignment partial** — current wave dispatches heap/governance agents but **`gap_explorer`**, **`issue_hygiene`**, **`security_auditor`** remain absent from the active 2h window despite briefing recommendation.
- **Programmatic observer @ 21:45Z** — `retry_counts: {}`, `stopped_agents: []`; remediations queued `implementation_gaps` + goal-align retry for `swarm_observer`; `needs_meta_observer: false` (stale threshold).
- **Gap pipeline current** — ingest + apply re-run @ 21:48Z (registry **90** rows, **64** open); 23 backlog patches idempotent.
- **`CURSOR_API_KEY` set** — auth available; mass errors are wave preemption / SDK terminalization drift, not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; `unattended_safe: false` | `data/latest/ecosystem-quality-report.json` | critical |
| CP execution churn | 870 errors / 2h; 16 179 errors all-time | Supabase `agent_runs` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/9 stopped (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`) | `lic/data/goal-directed-agents/snapshot.json` | high |
| SDK status drift | 109/120 sampled runs still `running` | `li-cursor-agents/data/runs` | high |
| Goal orientation drift | `gap_explorer`, `issue_hygiene`, `security_auditor` not in 2h dispatch window | briefing vs CP `agent_runs` | medium |
| CP report stale | `control_plane_reports.is_latest` @ 2026-05-25; `healthy: true`, 0 sampled | Supabase `control_plane_reports` | medium |
| `org_agent_kit_audit` | preflight exit 1 — 21 repos flagged | `agent-briefing.preflight_runs` | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) | `agent-briefing.preflight_runs` | medium |
| Workspace dirty | 2 sibling repos (`lic`, `benchmarks`) on sweep/agent branches | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| httpd plan loop | 2 pending todos; history exit **124** on wrk soak | `snapshot.json` runner `httpd` | medium |
| ph-db plan_debt | 9 todos deferred (no backlog mapping) | `orch-ph-db-backlog-mapping.md` | medium |
| Benchmarks near tier-1 | `matmul_blocked` yellow; matmul_naive/simd_dot/fft ~1.0–1.1× | `agent-briefing.json` → `ecosystem_audit` | low |

### Error sample classification (2h window + local sample)

| Pattern | Example | Root cause |
|---------|---------|------------|
| Uniform ~40 errors/agent / 2h | `bug_fixer`, `pr_alignment`, `plan_verifier`, … | **Supervisor wave kill / preemption** — prior run terminated before completion |
| Mass `running` in scorecard | 109/120 sampled | **SDK terminalization drift** — finalize-run not marking terminal after TTL |
| Terminal errors in sample | `gui_ui_tester`, `bench_improver`, `docs_maintainer` (1 each / 11 terminal) | **Cursor SDK session error** after partial work — not auth |
| `swarm_observer` 24 errors / 2h | `swarm_observer-1780177378947` pre-empted while `running` | **Meta-agent wave overlap** — concurrent observer dispatches |
| Lifetime error volume | 1028 `workspace_sweeper`, 991 `bug_fixer` all-time | **Historical status reconciliation** — includes wave-kill mislabels |
| httpd exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state` @ 21:45:03Z | `last_scan_at` set; `retry_counts: {}` |
| Remediation dispatch | `swarm_health.remediations` | `implementation_gaps` (deliverable gaps); `swarm_observer` goal-align retry |
| Stopped agents | CP `control_plane_state` | `stopped_agents: []` — no budget exhaustion |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 21:48Z | Registry 90 rows (64 open) |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 21:48Z | 23 backlog patches re-confirmed |
| Scorecard refresh | `benchmarks/scripts/ecosystem-quality-grade.py` @ 21:47Z | 73.3 grade C; `unattended_safe: false` |
| Interventions | `interventions_snapshots` | Empty since 2026-05-25 |

No auto-retries recorded in `retry_counts` for the 21:45Z scan.

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile wave-kill `error` vs true SDK failures | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Dispatch heap P40 `gap_explorer` when briefing + registry open | supervisor heap coordinator |
| Set `needs_meta_observer` when `unattended_safe: false` | `li-cursor-agents/src/observer/` |
| Refresh `ci-bug-triage` before `bug_fixer` when PR merged | `benchmarks/scripts/ci-bug-triage.py` |
| Periodic full preflight (drop `--skip-slow`) | benchmarks briefing / supervisor env |
| Auto-close registry `plan_debt` when snapshot `completed_ids` includes todo | `lic/scripts/swarm-gap-ingest.py` |
| ph-db plan_debt backlog mapping (9 todos) | `lic/docs/ecosystem/ph-db-plan-backlog.md` |
| Reconcile `org_agent_kit_audit` false positives | `benchmarks/scripts/ensure-org-agent-kit.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- **16 PRs** flagged close/supersede (`pr_alignment`) — human review required
- lic + benchmarks workspace dirty on agent/sweep branches — human decides merge vs rebase
- httpd plan loop exit **124** on wrk soak — human timeout/env tuning
- `agents_live: 0` while httpd/compiler-studio/studio runners show `running: true` — restart policy needs human confirmation

### Agent deliverable checklist

- [x] Ecosystem quality report read + refreshed (73.3 @ 21:47Z)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`, `interventions_snapshots`)
- [x] Gap ingest + apply confirmed @ 21:48Z
- [x] Orchestrator notes referenced + meta-audit note added
- [x] Recommended agents vs recent runs compared
- [x] Error runs sampled + classified (CP DB + local JSON)
- [ ] Control-plane observer PR (terminalization TTL) — deferred to li-cursor-agents

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | std.summary, std.plot, li-line-profiler | Patched → `ecosystem-package-backlog.md`; handoff **`issue_planner`** |
| `plan_debt` (31) | sim/security/studio patched; master-plan phases deferred | Route **`plan_verifier`** + **`implementation_gaps`**; ph-db via orchestrator note |
| `competitor_feature` (30) | vertical stubs patched to sim-md backlogs | Route **`gap_explorer`** + **`bench_improver`**; land `verticals.toml` on benchmarks main |
| `ui_ux` (0 open registry) | studio-ux-16/17 patched to plan loop md | **`gui_ux_tester`** / studio-ui-ux control-plane goal (not systemd) |

Do **not** recommend `install-goal-plan-loop-systemd.sh` — use `li-cursor-agents` research/implement goals only.

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + reconcile wave-kill errors | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Meta-observer gate when `unattended_safe: false` | li-cursor-agents | `swarm`, `control-plane` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch `gap_explorer` when registry open + briefing recommends | li-cursor-agents | `swarm`, `orchestration` |
| ci-bug-triage: drop merged PRs from work queue | benchmarks | `agent-kit`, `triage` |
| Land competitive `verticals.toml` on benchmarks main | benchmarks | `ecosystem`, `benchmarks` |
| Package issues: `std.summary`, `std.plot`, `li-line-profiler` seed | lic | `ecosystem`, `PH-IO` |
| ph-db plan_debt backlog + apply-actions mapping | lic | `orchestration`, `ph-db` |
| Fix org agent-kit audit false positives post-install | benchmarks | `agent-kit`, `ecosystem` |
| Close/supersede review — 16 flagged PRs | multi | `pr-hygiene` |

---

## Deferred

- ph-db runner plan_debt (9 todos) — backlog mapping documented; no lic product code
- Master-plan `plan_debt` rows without runner mapping (2i, 7d, 7e, Vision-LLM, 8p) — human/phased implementer lanes
- 30 `competitor_feature` gaps — research/backlog only until tier-1 bench owners assigned
- Control-plane code changes (`src/observer/`, `build-report.ts`) — PR on `li-cursor-agents` with `npm test` evidence
- Periodic full preflight (drop `--skip-slow`) — schedule via supervisor, not this digest
- `orch-r3` / `orch-r4` runner todos — complete via `issue_planner` + ui testers, not new systemd loops

---

## Error

No fatal error in this meta-agent pass. Mass CP `error` rows are predominantly wave preemption; sample terminal SDK failures documented above. Prior run `swarm_observer-1780177378947` was pre-empted while still `running` in local JSON.

---

_Report generated by `swarm_observer` · li-langverse/benchmarks · 2026-05-30T21:48Z_
