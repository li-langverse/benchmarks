# Swarm observer digest — `swarm_observer-1780172953244`

**Date:** 2026-05-30T20:30Z  
**Agent:** `swarm_observer`  
**Research goal:** `swarm_coverage` (north_star_fit: ecosystem, ai-first — PH orchestration)  
**Briefing:** `data/latest/agent-briefing.json` (2026-05-30T20:23Z)  
**Scorecard:** `data/latest/ecosystem-quality-report.json` (79.5, grade **C**, `unattended_safe: true`)  
**Orchestrator notes:** `lic/docs/ecosystem/orchestrator-notes/2026-05-30-orch-r3-missing-package-sweep.md`, `2026-05-30-orch-r4-ui-ux-signals.md`, `2026-05-30-orch-ph-db-backlog-mapping.md`

---

## Executive summary

- **Posture: degraded (recoverable)** — ecosystem grade **C** (79.5/100); scorecard marks `unattended_safe: true` but live execution shows wave churn and stale CP health signals.
- **Unattended operation: marginal** — programmatic observer active (`retry_counts: {}`, `stopped_agents: []` @ 20:29Z); last 24h CP: **12 168 error** / **27 running** / **353 finished** — dominated by wave preemption + status drift, not auth outage.
- **Strong lanes:** `ecosystem_posture` **100** (0 red benchmarks; `matmul_blocked` yellow only); org CI on main green; scorecard `failed_prs: 0`.
- **Weak lanes:** `gap_pressure` **60** (64 open registry rows); `goal_directed_health` **70** (6/8 lic runners stopped); scorecard flags **110** local runs still `running` (terminalization drift).
- **Briefing alignment partial** — current tick dispatches heap P10 `pr_alignment`, P30 governance trio, P50 `agent_kit_maintainer`, plus research P0 `swarm_observer`; **`gap_explorer`**, **`issue_hygiene`**, **`security_auditor`** still absent from active wave despite briefing recommendation.
- **Programmatic observer @ 20:29Z** — scan only; no auto-retries (`retry_counts: {}`); simultaneous ~76 errors/agent/2h pattern = supervisor wave kill, not exhausted retry budget.
- **Gap pipeline current** — ingest + apply @ 20:24Z (idempotent); 64 open gaps (30 competitor, 31 plan_debt, 3 missing_package); 23 backlog patches confirmed.
- **`CURSOR_API_KEY` set** — SDK auth available; current-wave `docs_ui_tester` / `bug_fixer` failures are `SDK run status: error` after substantive tool use (model/session), not missing key.

---

## Deliverable / findings

### Findings

| Agent / area | Symptom | Evidence path | Severity |
|--------------|---------|---------------|----------|
| Swarm (aggregate) | Grade C; marginal unattended | `data/latest/ecosystem-quality-report.json` | high |
| Gap registry | 64 open (30 competitor, 31 plan_debt, 3 pkg) | `data/latest/swarm-gap-actions.json` | high |
| Goal-directed runners | 6/8 stopped (`compiler-studio`, `sim`, `studio-ui-ux`, `sim-md-research`, `sim-chem-research`, `security-research`) | `lic/data/goal-directed-agents/snapshot.json` | high |
| CP run hygiene | 12 168 errors / 24h; ~76 errors/agent / 2h on 15 agents | Supabase `agent_runs` | high |
| Goal orientation drift | `gap_explorer`, `issue_hygiene`, `security_auditor` not dispatched | briefing `recommended_agents` vs CP recent runs | medium |
| CP report stale | `swarm_health.healthy: true` @ 2026-05-25; 0 runs sampled | `control_plane_reports` `is_latest` | medium |
| `org_agent_kit_audit` | preflight exit 1 — 21 repos flagged; installs often no-op | `agent-briefing.preflight_runs`, `agent_kit_maintainer-*` runs | medium |
| Preflight skipped | 8 slow scripts (`--skip-slow`) + 1 failure | `agent-briefing.preflight_runs` | medium |
| Workspace dirty | 1 sibling repo (`lic`, 4 files on sweep branch) | `agent-briefing.json` → `workspace_dirty_sweep` | medium |
| httpd plan loop | 2 pending todos; last iterations exit **124** (timeout) | `snapshot.json` runner `httpd` | medium |
| ph-db plan_debt | 9 todos deferred in apply-actions (no backlog mapping) | `swarm-gap-actions.json`, `orch-ph-db-backlog-mapping.md` | medium |
| SDK terminal errors | `docs_ui_tester`, `bug_fixer` error after 48/7 tools | `li-cursor-agents/data/runs/docs_ui_tester-1780172425707.json`, `bug_fixer-1780172696832.json` | medium |
| Benchmarks near tier-1 | `matmul_blocked` yellow; matmul_naive/simd_dot/fft near 1.0× | `agent-briefing.json` → `ecosystem_audit.benchmarks` | low |

### Error sample classification (current tick + 2h window)

| Pattern | Example run_id | Root cause |
|---------|----------------|------------|
| Simultaneous errors @ tick | 15 agents × ~76 errors / 2h (`pr_alignment`, `bug_fixer`, `plan_verifier`, …) | **Supervisor wave kill / preemption** — prior run terminated before completion |
| SDK `status: error` after work | `docs_ui_tester-1780172425707` (48 tools, 246s) | **Cursor SDK session error** — not auth; deliverable section missing in completion audit |
| Stale triage target | `bug_fixer-1780172696832` chasing merged lic#439 | **Preflight stale** — `ci-bug-triage.json` not refreshed; should target lic#573 GHA red |
| Mass `running` in scorecard | 110/120 sampled locally | **SDK terminalization drift** — observer not marking terminal after TTL |
| `agent_kit_maintainer` finished | Rollouts skip when install finds no drift | **Audit vs install mismatch** — preflight exit 1 but deterministic install no-op |
| httpd exit 124 | `gap-phase2-perf-wrk-soak`, `gap-phase2-streaming-wrk` | **Plan loop timeout** — wrk soak exceeds batch deadline |
| Lifetime `error` volume | 1007 `workspace_sweeper`, 959 `bug_fixer` all-time | **Status reconciliation drift** (historical mislabel) |

### Self-heal actions taken (programmatic observer)

| Action | Source | Result |
|--------|--------|--------|
| Observer scan | CP `control_plane_state.observer` @ 20:29:13Z | `last_scan_at: 2026-05-30T20:29:13.165Z`; `retry_counts: {}` |
| Stopped agents | CP `control_plane_state` | `stopped_agents: []` — no agents budget-exhausted |
| Gap ingest | `lic/scripts/swarm-gap-ingest.py` @ 20:24Z | Registry idempotent refresh; 64 open gaps |
| Gap apply | `lic/scripts/swarm-gap-apply-actions.py` @ 20:24Z | 23 backlog patches re-confirmed |
| Scorecard | `benchmarks/scripts/ecosystem-quality-grade.py` @ 20:24Z | 79.5 grade C (fresh; not re-run this tick) |
| Interventions | `interventions_snapshots` | Empty since 2026-05-25 — no human interventions queued |

No auto-retries for the 20:29Z wave (`retry_counts: {}`).

### Recommended control-plane fixes

| Fix | Repo / path |
|-----|-------------|
| Terminalize stuck `running` after SDK TTL | `li-cursor-agents/src/observer/` |
| Reconcile mass `error` rows from wave-kill vs true SDK failures | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | `li-cursor-agents/src/control-plane/build-report.ts` |
| Dispatch heap P40 `gap_explorer` when briefing recommends + registry open | supervisor heap coordinator |
| Refresh `ci-bug-triage` before `bug_fixer` when PR merged | `benchmarks/scripts/ci-bug-triage.py` |
| Enable slow preflights periodically (drop `--skip-slow`) | benchmarks briefing / supervisor env |
| Auto-close registry `plan_debt` when snapshot `completed_ids` includes todo | `lic/scripts/swarm-gap-ingest.py` |
| ph-db plan_debt backlog mapping (9 todos deferred) | `lic/docs/ecosystem/ph-db-plan-backlog.md` |
| Reconcile `org_agent_kit_audit` false positives after install | `benchmarks/scripts/ensure-org-agent-kit.py` |

### Human-only blockers

- Governance PRs and **trusted.lean** — never auto-merge
- **16 PRs** flagged close/supersede (`pr_alignment`) — human review required
- lic workspace dirty on `chore/workspace-sweep-*` — human decides merge vs rebase
- httpd plan loop exit **124** on wrk soak — needs human timeout/env tuning
- `agents_live: 0` despite 3 runners `running: true` — supervisor/accounting mismatch needs human restart policy

### Agent deliverable checklist

- [x] Ecosystem quality report read (79.5, grade C @ 20:24Z)
- [x] CP DB queried (`agent_runs`, `control_plane_state`, `control_plane_reports`, `interventions_snapshots`)
- [x] Gap ingest + apply confirmed (registry + actions @ 20:24Z)
- [x] Orchestrator notes referenced (`orch-r3`, `orch-r4`, `orch-ph-db`)
- [x] Recommended agents vs recent runs compared (gap_explorer / issue_hygiene / security_auditor drift)
- [x] Error runs sampled + classified (CP DB + local JSON)
- [ ] Control-plane observer PR (terminalization TTL) — deferred to li-cursor-agents

### Gap orchestration (Mode B — `swarm_coverage`)

| `gap_kind` | Open | Action this cycle |
|------------|------|-------------------|
| `missing_package` (3) | std.summary, std.plot, li-line-profiler | Patched → `ecosystem-package-backlog.md`; handoff **`issue_planner`** via `research-goals.yaml` / briefing |
| `plan_debt` (31) | sim/security/studio backlogs patched; master-plan phases deferred | Route **`plan_verifier`** + **`implementation_gaps`**; ph-db via orchestrator note |
| `competitor_feature` (30) | vertical stubs patched to sim-md backlogs | Route **`gap_explorer`** + **`bench_improver`**; land `verticals.toml` on benchmarks main |
| `ui_ux` (0 open) | studio-ux-16/17 patched to plan loop md | **`gui_ux_tester`** / studio-ui-ux runner (stopped — needs control-plane goal, not systemd) |

Do **not** recommend `install-goal-plan-loop-systemd.sh` for retired loops — use `li-cursor-agents` research/implement goals only.

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| Observer: terminalize stuck SDK runs + reconcile wave-kill errors | li-cursor-agents | `swarm`, `control-plane`, `bug` |
| Refresh `control_plane_reports.swarm_health` each supervisor tick | li-cursor-agents | `swarm`, `control-plane` |
| Heap: dispatch `gap_explorer` when registry open + briefing recommends | li-cursor-agents | `swarm`, `orchestration` |
| ci-bug-triage: drop merged PRs from work queue | benchmarks | `agent-kit`, `triage` |
| Land competitive `verticals.toml` on benchmarks main | benchmarks | `ecosystem`, `benchmarks` |
| Package issues: `std.summary`, `std.plot`, `li-line-profiler` seed | lic | `ecosystem`, `PH-IO` |
| ph-db plan_debt backlog + apply-actions mapping | lic | `orchestration`, `ph-db` |
| Fix org agent-kit audit false positives post-install | benchmarks | `agent-kit`, `ecosystem` |
| Close/supersede review — 16 flagged PRs | multi | `pr-hygiene` |
| lic#573 — fix GHA red on workspace sweep PR | lic | `ci`, `bug` |

---

## Deferred

- ph-db runner plan_debt (9 todos) — backlog mapping documented; no lic product code
- Master-plan `plan_debt` rows without runner mapping (2i, 7d, 7e, Vision-LLM, 8p) — human/phased implementer lanes
- 30 `competitor_feature` gaps — research/backlog only until tier-1 bench owners assigned
- Control-plane code changes (`src/observer/`, `build-report.ts`) — PR on `li-cursor-agents` with `npm test` evidence
- Periodic full preflight (drop `--skip-slow`) — schedule via supervisor, not this digest
- `orch-r3-missing-package-sweep` / `orch-r4-ui-ux-signals` runner todos — complete via `issue_planner` + ui testers, not new systemd loops

---

## Error

No fatal error in this meta-agent pass. Sample SDK errors documented above with stack traces in linked run JSON files.

---

_Report generated by `swarm_observer` · li-langverse/benchmarks · 2026-05-30T20:30Z_
