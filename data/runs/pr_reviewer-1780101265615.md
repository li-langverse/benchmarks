# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780101265615`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**north_star_fit:** ecosystem pull-requests / PH-5b, PH-7e — tier-1 numerics merge queue after proof-preserving codegen (proof → easy → fast).

## Executive summary

- Live org scan: **140** open PRs, **91** CI-green (non-draft); preflight `pr-program-run.json` / `pr-merge-queue-plan.json` remain **stale** (`open_prs: 0`) — full-org gate enrichment still running >8 min in background.
- **No PRs fully gate-ready** (`ready: true`); universal remaining blocker on product PRs: human `APPROVED` review (agent cannot approve owner-authored PRs).
- **Top aligned candidate:** [lic#437](https://github.com/li-langverse/lic/pull/437) — PH-5b/7e tier-1 matmul via `@` lowering + blocked GEMM tile fix; CI green; release notes present; **`merge-approved` label added this run**.
- **New overlap:** [lic#465](https://github.com/li-langverse/lic/pull/465) — CI green but **release-notes gate fails**; supersede guidance posted (prefer #437).
- **Supersede stack:** lic#439, lic#446, lic#465 overlap #437 — close after #437 merges and benchmarks ingest.
- **Second aligned chore:** [benchmarks#164](https://github.com/li-langverse/benchmarks/pull/164) — CI green audit alignment; strict gate still flags release notes (waivable on human review).
- **3 roadmap PRs** (#19–21) carry `merge-approved` but **governance policy forbids agent merge** — human required.
- **lis#20** blocked on `plan-needed` without `plan-approved` (PH-DB feature work).

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-program-run.json` | ⚠️ Stale | `open_prs: 0`, `ci_green: 0` — contradicts live scan (140 / 91) |
| `pr-merge-queue-plan.json` | ⚠️ Stale | `merge_first: null`, `redundant: []` — gate-per-PR enrichment exceeds tick budget |
| Live org scan | ✅ | 140 open / 91 CI-green / 9 `merge-approved` (incl. roadmap + benchmarks#132) |
| `pr-merge-gate.py` (sampled) | ✅ | Gate JSON completed on lic#437/#439/#446/#465, benchmarks#164/#132 |

**Root cause (unchanged):** `pr-merge-queue-plan.py` invokes `pr-merge-gate.py` per PR during enrichment — full-org run exceeds supervisor tick budget. Recommend caching gate results or limiting enrichment to CI-green PRs only.

### Standards review (CI-green targets)

#### 1. lic#437 — `perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ PH-5b, PH-7e; master-plan Phase 7e |
| Strict by default | ✅ `@` lowering → `ArrayMatMul2DF64`; Lean CI green; no `trusted.lean` |
| Security | ✅ N/A (codegen path) |
| Performance | ✅ Evidence study + release notes; no `catalog.toml` threshold edits |
| Release notes | ✅ `docs/release-notes/2026-05-29-bench-improver-matmul-tier1.md` |
| Ecosystem-first | ✅ Post-merge benchmarks ingest documented |

**Gate after this run:** only `review_approved` blocker remains.  
**Actions:** `merge-approved` label added; review comment posted.

#### 2. lic#465 — `fix(bench): matmul_naive @ lowering — tier-1 ≤1.2× cpp` (new)

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ PH-5b, PH-7e |
| Performance | ✅ Local `matmul_naive` ≤1.2× evidence |
| Release notes | ❌ Missing `docs/release-notes/*` / `CHANGELOG.md` |
| CI | ✅ Green |

**Verdict:** **needs release notes** or close in favor of #437. Review comment posted.

#### 3. lic#439 / lic#446 — redundant matmul stack

| PR | Verdict | Reason |
|----|---------|--------|
| #439 | **superseded** | Overlaps #437; extra `local_ci_recorded` gate blocker |
| #446 | **superseded** | Same `@` lowering scope; #437 has stronger blocked-GEMM evidence |

Supersede comments posted; neither receives `merge-approved` while #437 is open.

#### 4. benchmarks#164 — `fix(audit): align ecosystem-audit CI exempt`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ Ecosystem audit hygiene |
| Strict by default | ✅ Config alignment only |
| CI | ✅ Green |
| Release notes | ⚠️ Strict gate flags missing RN — waivable as internal chore |

**Gate blockers:** `review_approved`, `merge-approved`, `release_notes` (strict).

#### 5. lis#20 — `feat(PH-DB): lis db supervisor…`

| Checklist | Verdict |
|-----------|---------|
| Plan | ❌ `plan-needed` without `plan-approved` |
| CI | ❌ No green checks |

**Verdict:** **needs plan** — do not merge.

#### 6. roadmap#19–21 — merge-approved but governance-blocked

| PR | Labels | Policy |
|----|--------|--------|
| #19–21 | `merge-approved` | Human merge only (`roadmap` governance repo) |

### CI-green noise (batch triage recommended)

| Category | Count (approx) | Action |
|----------|----------------|--------|
| Agent digest / sweep PRs | ~30 benchmarks | Close stale; keep latest per agent type |
| Fuzz-corpus bot sync | ~12 lic | Merge latest only; close older sync branches |
| Autoresearch deliverables | ~4 lic (#455–458) | Human triage or close if artifact-only |
| Workspace sweep fallback | ~10 benchmarks | Redundant chore stack |

### Actions taken this run

- **0 merges** (policy: never self-merge).
- **`merge-approved`** added to **lic#437** (via REST API; `gh pr edit` GraphQL deprecation warning).
- Review comments posted: lic#437 (approval path), lic#465 (release-notes blocker), lic#439/#446 (supersede).

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | `lic` #437 | Human: `APPROVED` → `pr_merger` (label set) |
| fix(bench): matmul_naive @ lowering | `lic` #465 | Add release notes or close → #437 |
| fix(audit): align ecosystem-audit CI exempt | `benchmarks` #164 | Human: `APPROVED` + waive RN + `merge-approved` |
| Close superseded matmul PRs (#439, #446, #465) | `lic` | After #437 merges |
| feat(PH-DB): lis db supervisor… | `lis` #20 | Add `plan-approved`; run local-ci |
| roadmap docs/agent-kit (WP-A1/A5/B4) | `roadmap` #19–21 | **Human merge only** |
| perf: cache gate enrichment in merge-queue plan | `benchmarks` | `ecosystem-ci`, `agent:ci_maintainer` |

## Deferred

- **Human review bottleneck** — lic#437 awaits `APPROVED` before `pr_merger` can execute.
- **Preflight script timeout** — background `run-pr-program.py` / `pr-merge-queue-plan.py` still running; refresh with gate cache before next tick.
- **Post-merge ingest** — after lic#437, run benchmarks ingest to refresh 6 dashboard red rows (`matmul_*`, `ml_*`, `num_gmres`).
- **PH-DB stack** (lis#17–20, lip#24–30, roadmap#32–35) — partial CI; defer until plan-approved + dependency order clarified.
- **91 CI-green PR backlog** — majority agent noise; `pr_alignment` should batch-close stale digests and fuzz-corpus duplicates.
