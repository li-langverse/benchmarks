# CI maintainer — proactive ecosystem sweep

**Run id:** `ci_maintainer-1780087568651`  
**Generated:** 2026-05-29T20:55Z  
**Source:** proactive  
**north_star_fit:** ecosystem platform — provable CI gate on every package repo (`main`); proof-before-perf (PH-8p compile throughput, org hygiene WP-A2/B1)  
**Preflight:** `data/latest/org-repo-ci-audit.json`, `data/latest/ecosystem-audit.json`  
**Template:** `lic/scripts/templates/github-repo/ci.yml`

---

## Executive summary

- **Org CI gate is green:** `ensure-org-repo-ci.py` → **32 OK**, **0 missing**, **1 gated**, **0 incomplete** (refreshed this run).
- **No new `ci.yml` PRs opened** — every non-exempt repo has `ci.yml` on its GitHub default branch; isolated clone rollout not required.
- **`lidb` gated (WP-H0):** default branch `feat/ph-db-2-liorm-liq` has `ci.yml` but org policy requires `main` before counting as OK — human branch rename first.
- **`research-findings` exempt** per `ensure-org-repo-ci.py` (`EXEMPT_REPOS`); `ecosystem-audit.py` still lists it in `missing_ci_on_main` (metrics noise — align scripts or document).
- **Branch protection gap:** `verify-org-branch-protection.sh` reports **22 repos** without active `Li: protected branches` ruleset (CI present but rulesets not applied org-wide).
- **Template hygiene:** canonical `ci.yml` has no `continue-on-error` on OS matrix jobs (compliant).
- **Prior digest PRs still open:** `benchmarks#144`, `#151`, `#153` — this run adds run `1780087568651` narrative without duplicating package CI work.
- **Deferred:** WP-H0 `lidb` → `main`, ruleset rollout for 22 repos, align `ecosystem-audit` exempt list, lic CI throughput (WP-C1–C6).

---

## Deliverable / findings

### Preflight refresh

```text
$ python3 scripts/ensure-org-repo-ci.py
OK: 32  missing: 0  gated: 1  incomplete: 0
  GATED lidb (default=feat/ph-db-2-liorm-liq): WP-H0: set default branch to main before requiring ci.yml on default

$ python3 scripts/ecosystem-audit.py
wrote data/latest/ecosystem-audit.json
```

| Audit | Field | Value |
|-------|-------|-------|
| `org-repo-ci-audit.json` | `repos_ok` | 32 repos (incl. `benchmarks`, `lic`, all package mirrors) |
| | `repos_missing_ci` | `[]` |
| | `repos_gated_non_main_default` | `lidb` only |
| | `repos_audit_incomplete` | `[]` |
| `ecosystem-audit.json` | `metrics.repos_missing_ci_main` | 1 (`research-findings` — not in org gate) |
| | `missing_ci_on_main` | `["research-findings"]` |
| | `metrics.open_prs` / `failed_prs` / `ready_prs` | 122 / 41 / 42 |

### Per-repo status (actionable)

| Repo | Status | Notes |
|------|--------|-------|
| All org package repos (32) | **ok** | `ci.yml` on `main` via GitHub API |
| `lidb` | **gated** | `ci.yml` present on `feat/ph-db-2-liorm-liq`; WP-H0 blocks until default → `main` |
| `research-findings` | **exempt** | No workflows on `main` by design; open PR #6 adds optional CI (green `check`) |
| `roadmap` | **ok** | Governance — PR review only, no self-merge |

### Branch protection vs required checks

`roadmap/scripts/verify-org-branch-protection.sh` (2026-05-29):

| Category | Count | Examples |
|----------|-------|----------|
| Ruleset OK | 11 | `benchmarks`, `lic`, `lip`, `lit`, `lis`, `li-demo`, `li-httpd`, `li-net`, `li-std-core`, `li-std-math`, `roadmap` |
| Ruleset missing | 22 | `li-gui`, `lidb`, `mmo`, `sim*`, `studio*`, `world`, `render`, `proof-library`, … |

`ensure-org-repo-ci.py` `latest_check_job()` hints vs rulesets:

| Repo | Required check hint | Ruleset status |
|------|---------------------|----------------|
| `lip` | `bootstrap` | OK (ruleset 16484079) |
| `lic` | `build-and-test` | OK (ruleset 16484070) |
| `benchmarks` | `ingest-smoke` | OK (ruleset 16483923) |
| Package mirrors (default) | `check` | Many lack ruleset — CI runs but merge gate not enforced |

No `continue-on-error` on OS matrix jobs in `lic/scripts/templates/github-repo/ci.yml` (single `check` job on `ubuntu-24.04`).

### Existing swarm PRs (no duplicate opened)

| PR | Repo | State | CI |
|----|------|-------|-----|
| [#6](https://github.com/li-langverse/research-findings/pull/6) | research-findings | open | pass (`check`) |
| [#144](https://github.com/li-langverse/benchmarks/pull/144) | benchmarks | open | prior digest |
| [#151](https://github.com/li-langverse/benchmarks/pull/151) | benchmarks | open | prior digest (run 1780084632399) |
| [#153](https://github.com/li-langverse/benchmarks/pull/153) | benchmarks | open | prior digest (run 1780085588859) |

### Control plane

- Run `ci_maintainer-1780087568651` registered **running**; prior attempts `1780087255735`–`1780087463959` ended **error** (orchestration / workspace setup, not missing-CI logic).

### Agent deliverable checklist (this run)

- [x] Refreshed `org-repo-ci-audit.json` and `ecosystem-audit.json`
- [x] Verified 0 repos need template rollout PRs
- [x] Documented `lidb` WP-H0 gate and `research-findings` exempt vs audit mismatch
- [x] Ran `verify-org-branch-protection.sh` — 22 repos missing rulesets
- [x] Run narrative: this file
- [ ] No code PRs to package repos (nothing missing)
- [ ] Did not merge PRs or push to `main`

---

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| WP-H0: set `lidb` default branch to `main` | **lidb** | `governance`, `ecosystem-ci` — human; then `ci_maintainer` confirms gate |
| Apply org branch protection rulesets to 22 repos | **roadmap** | `governance`, `ecosystem-ci` — run `apply-org-branch-protection.sh` (human/org-owner) |
| Align `ecosystem-audit` exempt repos with `ensure-org-repo-ci` | **benchmarks** | `ci`, `ecosystem-ci` — drop `research-findings` from `missing_ci_on_main` metric |
| Close or merge optional CI on research-findings | **research-findings** | `ecosystem-ci` — #6 if whitepaper builds wanted; else close as out-of-policy |
| Merge/consolidate prior digest PRs | **benchmarks** | #144, #151, #153 — human review |
| lic CI: consume `ghcr.io/li-langverse/lic-ci` (WP-C1) | **lic** | `ecosystem-ci`, PH-8p — separate lic pass |

---

## Deferred

- **Package `ci.yml` rollouts** — none missing; no isolated clones prepared for package repos.
- **`lidb` CI on `main`** — blocked on WP-H0 human default-branch change.
- **`research-findings#6`** — optional; repo is CI-exempt for org gate; human decides merge vs close.
- **Ruleset rollout (22 repos)** — human/org-owner action via `roadmap/scripts/apply-org-branch-protection.sh`; out of scope for template copy.
- **lic CI throughput** (WP-C1–C6, WP-E1–E3) — numerics/platform lane, not org-missing-CI.
- **Self-merge on `roadmap`** — never; governance PRs for humans only.

<!-- li-agent -->
## Agent deliverable
- [x] Preflight refreshed: `data/latest/org-repo-ci-audit.json` (0 missing, 1 gated)
- [x] Run narrative: `data/runs/ci_maintainer-1780087568651.md`
- [x] Verified org CI template gate — no new package PRs required
- [x] Branch protection audit documented (11 OK / 22 missing rulesets)
- [ ] Package repo PRs — N/A (0 missing CI)
- [ ] merge-approved (human adds after review)
