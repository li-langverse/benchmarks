# Swarm observer report — `orch-r2-competitor-stubs`

> `swarm_observer` · research goal `swarm_coverage` · 2026-05-29 · run `swarm_observer-1780086771776`

## Executive summary

- **Posture: degraded (recovering).** Ecosystem grade **C** (76.3); `unattended_safe: true` per scorecard heuristics — still not fully hands-off due to stopped runners and stuck SDK runs.
- **Gap orchestration (orch-r2): complete** — 30 `competitor_feature` rows reconciled; 12 vertical stub todos patched across sim + sim-md-research backlogs; infra gap closed.
- **verticals.toml:** Present on `lic` branch (`benchmarks/competitive/verticals.toml`, 14 stubs); ingest idempotent (`verticals_stubs: 0`).
- **Registry routing fix:** Added `suggested_loop: sim` for PDE/FEA/CFD stubs so apply-actions patches `sim-algorithm-backlog.md`.
- **Swarm execution:** 108 runs stuck `running`; DB shows 3043 errors / 24h but scorecard samples 0 terminal errors (stale finalize); batch supersede at 20:28Z errored 10+ agents.
- **Goal-directed:** 6/8 runners stopped; `httpd` active (2 perf todos); `swarm-observer` orch-r2 now completed.
- **Programmatic observer:** Dispatched `implementation_gaps` + `workspace_sweeper` retry; `retry_counts` empty; `stopped_agents` empty.
- **Credentials:** `CURSOR_API_KEY` and `GH_TOKEN` present — SDK auth not the primary blocker.

## Findings

| Agent / area | Symptom | Evidence | Severity |
|--------------|---------|----------|----------|
| Swarm SDK | 108 runs `running` (stuck / superseded) | `ecosystem-quality-report.json`, `li-cursor-agents/data/runs` | critical |
| Swarm SDK | Batch supersede: 10 agents `error` ~20:28Z | Supabase `agent_runs` | high |
| Control plane | Latest report stale (2026-05-25), `swarm_health.healthy: true` | `control_plane_reports` | medium |
| Observer | `retry_counts` empty despite error streak | `control_plane_state` | medium |
| Goal-directed | 6 runners stopped | `lic/data/goal-directed-agents/snapshot.json` | high |
| Preflight | org agent-kit + CWE audit fail | `agent-briefing.preflight_runs` | high |
| Benchmarks | 6 red tier-1 rows | `agent-briefing.ecosystem_audit.benchmarks.red` | high |
| Gap registry | 52 open gaps | `benchmarks/data/latest/swarm-gap-actions.json` | medium |
| Briefing drift | Top recommended agents not in recent DB runs | `control_plane_state.swarm_health.findings` | medium |
| Competitor stubs | PDE/FEA/CFD lacked backlog routing | `registry.yaml` (fixed orch-r2) | low (resolved) |

## Self-heal actions taken

Programmatic observer (this cycle):

- `observer.last_scan_at`: 2026-05-29T20:32:20Z
- Remediations: dispatch `implementation_gaps`; retry `workspace_sweeper` (goal-align)
- `retry_counts`: `{}` — no increment yet
- `stopped_agents`: `[]`

Meta-audit (this run):

- Regenerated `ecosystem-quality-report.json` (76.3, grade C)
- Ran `swarm-gap-ingest.py` + `swarm-gap-apply-actions.py`
- Patched 12 vertical stub todos; closed infra + orch-r2 plan_debt gaps
- Orchestrator note: `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r2-competitor-stubs.md`

## Recommended control-plane fixes

| Fix | Path |
|-----|------|
| Default `suggested_loop: sim` for PDE/CAE vertical stubs in ingest | `lic/scripts/swarm-gap-ingest.py` |
| Finalize superseded `running` runs on new dispatch | `li-cursor-agents/src/control-plane/finalize-run.ts` |
| Increment `observer.retry_counts` on SDK error streak | `li-cursor-agents/src/observer/` |
| Refresh `control_plane_reports.is_latest` each supervisor tick | control-plane report writer |
| Route competitor tier-1 red gaps → heap `coord_numerics` | `benchmarks/scripts/agent-briefing.py` |

## Human-only blockers

- Merge `verticals.toml` + gap registry to `main` (governance)
- `trusted.lean` / master-plan phase claims
- Tier-1 numerics perf (matmul_blocked, gmres) — proof-before-perf product work
- Enable `LI_CURSOR_AGENTS_ENABLED=1` for agent deliverable gate scan

## Agent deliverable checklist

- [x] Ecosystem quality grade refreshed
- [x] Gap ingest + apply-actions
- [x] 12 vertical stub backlogs patched (sim + sim-md-research)
- [x] Orchestrator note (`orch-r2`)
- [x] Registry orch-r2 + infra gaps closed; plan backlog orch-r2 completed
- [x] Report under `benchmarks/data/runs/`
- [ ] PR on `li-cursor-agents` for finalize-run / observer retry (recommended)

**Orchestrator note:** `lic/docs/ecosystem/orchestrator-notes/2026-05-29-orch-r2-competitor-stubs.md`

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| fix(ingest): default suggested_loop for verticals.toml CAE stubs | lic | `orchestration`, `gap-registry` |
| fix(observer): finalize superseded running SDK runs | li-cursor-agents | `control-plane`, `swarm` |
| chore(benchmarks): merge verticals.toml competitive registry to main | benchmarks / lic | `ecosystem`, `governance` |
| perf(numerics): matmul_blocked tier-1 red row (PH-7e) | lic | `numerics`, `PH-7e` |
| feat(gap-explorer): handoff competitor HPC library gaps to issue_planner | li-cursor-agents | `swarm`, `research` |

## Deferred

- `orch-r3-missing-package-sweep` — next swarm-observer plan todo
- `orch-r4-ui-ux-signals` — ingested prior run; backlog status still pending (sync)
- Product work on vertical stub honesty (LAMMPS/OpenFOAM parity) — sim research loops
- Observer PR with `npm test` evidence (control-plane fixes batch)
