# PR reviewer — proactive ecosystem sweep

**Run id:** `pr_reviewer-1780146978254`  
**Agent id:** `pr_reviewer`  
**Source:** proactive  
**north_star_fit:** ecosystem pull-requests / PH-5b, PH-7e — tier-1 matmul numerics (proof → easy → fast); PH-DB deferred pending plan-approved.

## Executive summary

- **Preflight scripts timeout** — `pr-merge-queue-plan.py` and `run-pr-program.py` exceed 60–90s (GraphQL `gh pr list` path); JSON artifacts report **`open_prs: 0`** (false negative).
- **REST org search + gate script OK** with `unset GH_TOKEN`; `pr-merge-gate.py` enriches individual PRs reliably.
- **0 PRs gate-ready** — no `merge-approved` label added (merge conflicts, missing APPROVED, or plan/CI blockers).
- **Canonical matmul candidate:** [lic#437](https://github.com/li-langverse/lic/pull/437) — CI green, `merge-approved` retained; **blocked** by merge conflicts + human review.
- **Redundant MIR stack:** lic#541 ≡ lic#543 — close one; compare survivor vs #437 before label sweep.
- **Near-ready chore:** [lit#19](https://github.com/li-langverse/lit/pull/19) — CI green, mergeable; needs human APPROVED + `merge-approved`.
- **Aligned chore (conflicted):** [benchmarks#164](https://github.com/li-langverse/benchmarks/pull/164) — CI green; rebase + APPROVED required.
- **Feature blocks:** li-httpd#10, lis#20 — `plan-needed` without `plan-approved`; roadmap PRs — human merge only.

## Deliverable / findings

### Preflight signals

| Artifact | Status | Notes |
|----------|--------|-------|
| `pr-program-run.json` | ❌ False empty / stale | `open_prs: 0`; script timed out (exit 124) |
| `pr-merge-queue-plan.json` | ❌ False empty / stale | Timed out at 60s; prior snapshot empty |
| REST org search | ✅ | 50 open PRs sampled |
| `pr-merge-gate.py` (per-PR) | ✅ | Works with `unset GH_TOKEN` |
| Control plane | ⚠️ | 9 consecutive `pr_reviewer` runs errored before this pass |

### Error

```
Preflight timeout: pr-merge-queue-plan.py and run-pr-program.py exceed 60–90s
(likely GraphQL gh pr list org sweep — artifacts remain open_prs: 0).

Workaround: unset GH_TOKEN; use gh search prs (REST) + pr-merge-gate.py --repo --pr.
Recommend: REST fallback in queue-plan when GraphQL limit/timeout hit.
```

### Standards review (gate-verified)

#### Tier-1 matmul stack (lic) — PH-5b / PH-7e

| PR | CI | Mergeable | Labels | Verdict |
|----|-----|-----------|--------|---------|
| [#437](https://github.com/li-langverse/lic/pull/437) | ✅ | ❌ dirty | `merge-approved` | **Retain canonical** — rebase + human APPROVED |
| [#541](https://github.com/li-langverse/lic/pull/541) | ✅ | ❌ dirty | — | **Defer** — redundant with #543 |
| [#543](https://github.com/li-langverse/lic/pull/543) | ✅ (7 checks) | unknown | — | **Defer** — redundant with #541 |
| [#524](https://github.com/li-langverse/lic/pull/524) | — | — | — | Bench harness — merge after canonical codegen PR |
| [#520](https://github.com/li-langverse/lic/pull/520) | pending | — | — | **Defer** — overlaps blocked emit path |

**Checklist (matmul PRs):**

| Gate | #437 | #541/#543 |
|------|------|-----------|
| Vision / PH | ✅ PH-5b, PH-7e | ✅ |
| Strict by default | ✅ codegen/lowering; no trusted creep | ✅ |
| Performance | ✅ bench paths; no catalog.toml weakening | ✅ |
| Release notes | ✅ | ✅ |
| Ecosystem-first | Post-merge benchmarks ingest for 6 red rows | Same |

#### benchmarks#164 — `fix(audit): align ecosystem-audit CI exempt`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ Ecosystem audit hygiene |
| Strict by default | ✅ Script alignment only |
| CI | ✅ audit, dashboard-build, ingest-smoke |
| Release notes | ⚠️ Waivable chore |
| Blockers | Rebase (dirty); `review_approved`; `merge-approved` |

#### lit#19 — `fix(ci): add deploy job permissions for GitHub Pages`

| Checklist | Verdict |
|-----------|---------|
| Vision / PH | ✅ live Pages / docs_maintainer |
| CI | ✅ `test` success |
| Merge | ✅ mergeable (branch protection blocked until review) |
| Blockers | `review_approved`; `merge-approved`; release_notes (waivable) |

#### Satellite handbook PRs (docs_maintainer batch)

| PR | Verdict |
|----|---------|
| li-net#15, li-httpd#17, li-std-core#11, li-std-math#12 | **Defer** — rebase; trigger CI |

#### li-httpd#10 — feature mirror split

| Checklist | Verdict |
|-----------|---------|
| Plan | ❌ `plan-needed` without `plan-approved` |
| CI | ❌ check failure |

#### lis#20 — PH-DB feature

| Checklist | Verdict |
|-----------|---------|
| Plan | ❌ `plan-needed` without `plan-approved` |
| Verdict | **needs plan** — do not merge |

#### roadmap PRs (#43, #46)

Governance policy: **human merge only** — never agent-merge.

### Actions taken this run

- **0 merges** (policy).
- **0 labels** — no PR fully aligned + conflict-free + APPROVED.
- **4 review comments posted:** lic#437, lic#541, benchmarks#164, lit#19.
- Preflight scripts attempted; timed out — documented above.

## Recommended issues/PRs

| Title | Repo | Labels / action |
|-------|------|-----------------|
| perf(7e): tier-1 matmul naive + blocked ≤1.2× cpp | **lic** [#437](https://github.com/li-langverse/lic/pull/437) | Rebase → human `APPROVED` → `pr_merger` |
| perf(bench): tier-1 matmul MIR fast paths | **lic** [#541](https://github.com/li-langverse/lic/pull/541) or [#543](https://github.com/li-langverse/lic/pull/543) | Close duplicate; compare vs #437 |
| fix(ci): add deploy job permissions for GitHub Pages | **lit** [#19](https://github.com/li-langverse/lit/pull/19) | Human APPROVED → `merge-approved` |
| fix(audit): align ecosystem-audit CI exempt | **benchmarks** [#164](https://github.com/li-langverse/benchmarks/pull/164) | Rebase → APPROVED + `merge-approved` |
| docs(*): GitHub Pages handbook landing | **li-net**, **li-httpd**, **li-std-core**, **li-std-math** | Rebase batch; trigger CI |
| feat(li-net-httpd): plan-loop split | **li-httpd** [#10](https://github.com/li-langverse/li-httpd/pull/10) | Add `plan-approved`; fix CI |
| feat(PH-DB): lis db supervisor | **lis** [#20](https://github.com/li-langverse/lis/pull/20) | Add `plan-approved` |
| REST/timeout fallback in queue-plan scripts | **benchmarks** | Prevent false `open_prs: 0` on GraphQL slow path |

## Deferred

- **Preflight refresh** — re-run `run-pr-program.py` after GraphQL sweep path stabilizes.
- **Canonical matmul pick** — maintainer: #437 vs #541/#543 survivor after duplicate close.
- **Post-merge ingest** — refresh 6 dashboard red rows after lic numerics PR lands.
- **PH-DB stack** (lis, lip, roadmap) — defer until `plan-approved` + dependency order.
- **Agent digest backlog** (~30 benchmarks/lic sweep PRs) — batch-close after numerics unblocked.
- **li-language agent-kit** — missing_kit per org-agent-kit audit (exit 1).
