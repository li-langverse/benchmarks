# Bench improver digest — 2026-05-29 (proactive)

**Agent:** `bench_improver` · **Run:** `bench_improver-1780067501090` · **Source:** proactive  
**north_star_fit:** PH-5b, PH-7e — tier-1 pure-Li `matmul_blocked` vs C++ oracle; proof-before-perf (checksum parity)

## Executive summary

- **Preflight (briefing @ 12:56Z):** 0 red, 0 yellow on latest ingest; `matmul_blocked` green near-threshold **1.187×**; 5 tier-2 rows 1.06–1.20×; 180 unknown harness rows.
- **Gap:** Fusion commit `3e2fbc73` (PH-7e codegen) was **not on `main`** — only on agent branch `chore/agent-bench_improver-65060558`.
- **Action:** Cherry-picked `3e2fbc73` + study refresh onto `chore/agent-bench_improver-67501090`.
- **Local tier-1 (this host, n=10):** `matmul_blocked` **1.230×** cpp post-fusion (was **1.279×** pre-fusion); checksum **PASS** (`1288460.7564000632`).
- **Tier-2 spot-check:** `md_lennard_jones` **0.99×**, `three_body` **1.00×**, `wave_equation_2d` **0.98×** (shared C kernels; no lic change).
- **Evidence:** `lic/docs/numerics/studies/2026-05-29-matmul-blocked-codegen.md`, release note `2026-05-29-matmul-blocked-7e-fusion.md`.
- **PR:** Open **lic** PR to merge fusion onto `main`; re-ingest via `ingest-lic.sh` after merge (do not hand-edit `summary.json`).
- **Error (prior runs):** Control plane shows 4 consecutive `bench_improver` runs `error` before this run — likely workspace/build timeouts; no stack retained in briefing.

## Deliverable / findings

| Benchmark | Before (this host) | After (fusion) | Notes |
|-----------|-------------------|----------------|-------|
| `matmul_blocked` | **1.279×** cpp | **1.230×** cpp | ~3.8% faster Li wall time; fused init+GEMM+sink in `emit.cpp` |
| Dashboard (ingest @ 14:37Z) | yellow **1.253×** → green **1.187×** | pending merge to `main` | `summary.json` provenance `lic_sha=3e2fbc73` |
| tier-2 near-threshold | 1.06–1.20× | unchanged | shared `*_core.c`; defer SIMD/parallel without Lean |

**CSV (local, `lic/benchmarks/results/latest.csv` after fusion):**

| lang | wall_time (s) |
|------|----------------|
| cpp | 0.0087 |
| li | 0.0107 |
| ratio | **1.230×** |

**Codegen:** `lhs_int=1` fused `ArrayMatMulBlocked2DF64` (C-oracle init LUT, blocked IKJ, vectorized checksum); BSS 512² matrices; `mm_blocked_512` body elided at LLVM layer.

## Recommended issues/PRs

| Title | Repo | Labels / notes |
|-------|------|----------------|
| perf(7e): fuse tier-1 matmul_blocked pure-Li kernel (init+GEMM+sink) | **lic** | `PH-7e`, `numerics`; branch `chore/agent-bench_improver-67501090` |
| Ingest tier-1 row after lic merge | **benchmarks** | `./scripts/ingest/ingest-lic.sh` only |
| Close G-math / master-plan 7e-b when CI ingest ≤1.2× | **lic** | #27 |
| `matmul_blocked_N1024` harness + strict matrix | **lic** | unknown catalog row |
| Tier-2 `md_lennard_jones` @ 1.20× (ingest) | **lic** | pure-Li MD path blocked on float MIR + proved `parallel for` (PERF.md) |

## Deferred

- Strict `LI_TIER1_PERF_STRICT=1` on busy host (1.230× > 1.2×); CI runner may match 1.187× ingest.
- Lean proof for fused bench MIR path.
- 180 unknown harness rows — `gap_explorer` / harness owners.
- HPC `registry.toml` `last_reviewed` bump (no competitor release review this pass).
- benchmarks repo commit (digest only unless ingest after lic merge).
