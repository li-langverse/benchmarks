# TUI UX digest — 2026-05-31 (proactive refresh cycle 12)

**Agent:** `tui_ux_tester` · **Run:** `tui_ux_tester-1780188668581` · **Source:** proactive ecosystem sweep  
**Surfaces:** `tui_app`, `tui_gen` · **Repos:** `li-cursor-agents` (fixtures); no production Li TUI shipped yet  
**Preflight:** `ux_audit` — `data/latest-tui-ux-run/ux-audit.json` (2026-05-31T01:01Z); org `data/latest/ux-audit.json` remains **docs-only** (`lic-docs`)  
**north_star_fit:** AI-first pillar + **Vision-LLM** (PH Vision-LLM) — agent-facing terminal UX must expose keyboard flows, help overlays, structured errors, and plain-text export for `lic diagnose`-class tooling without weakening proof gates on shipped `lic` code.

---

## Executive summary

- **No production Li TUI** in org repos; harness audits **fixtures only** (`tui-app-fixture`, `tui-gen-fixture` in `li-cursor-agents/config/ux-targets.yaml`).
- **`tui-app-fixture` fails** UX audit: blocking `read -r` causes 30s `subprocess.TimeoutExpired`; `run_audit.py` **crashes uncaught** (no fail row until manual merge) ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)).
- **Piped stdin workaround** (`printf '\n\n' | tui-demo.sh`) exits 0 in &lt;50ms — fixture flow is harness-ready once adapter injects stdin.
- **`tui-gen-fixture` passes** rubric (adapter defaults) but **`cli_to_tui` journey never executed** — scores not journey-derived ([#33](https://github.com/li-langverse/li-cursor-agents/issues/33)).
- **Error + a11y export gap:** fixtures lack `LI_TUI_ERROR=1` failure branch and `LI_TUI_EXPORT_A11Y` structured plain snapshot ([#34](https://github.com/li-langverse/li-cursor-agents/issues/34)).
- Global preflight `ux_audit` still **docs-only**; TUI targets require proactive harness under `data/latest-tui-ux-run/`.
- **SOTA (manifest `tui` + web):** Textual `ModalScreen` + Footer BINDINGS for help; Bubble Tea vim/arrow nav, always-on `q`/`ctrl+c` quit, errors on model not panics.
- **Control plane:** 8+ `tui_ux_tester` / `tui_ui_tester` runs in the last hour ended **`error`** (harness `TimeoutExpired`); this pass completes digest `tui_ux_tester-1780188668581`.

---

## Deliverable / findings

### Preflight (`ux_audit`)

| Target | Class | UX status | Notes |
|--------|-------|-----------|-------|
| `tui-app-fixture` | `tui_app` | **fail** | 30s timeout; `key_nav_help` not run; missing help/error/a11y states |
| `tui-gen-fixture` | `tui_gen` | pass (stub rubric) | `cli_to_tui` journey defined but not run; always `Status: ok` |

**Evidence:** `benchmarks/data/latest-tui-ux-run/ux-audit.json` (2026-05-31T01:01Z; live harness re-run: `tui-app` timeout crash, `tui-gen` pass)

### Flow audit — focus areas

#### Key navigation

| Area | Fixture | SOTA | Gap |
|------|---------|------|-----|
| Focus / roving keys | Mock text only | Textual `set_focus`, Footer BINDINGS | No widget focus model |
| Vim bindings | N/A | Bubble Tea `up`/`k`, `down`/`j`, `q`/`ctrl+c` | Not demonstrated |
| Journey `key_nav_help` | Defined, **not run** | Textual Pilot `press("h")` | [#33](https://github.com/li-langverse/li-cursor-agents/issues/33) |

#### Help overlays

- `tui-demo.sh` prints help only after **two** `read` lines; banner says `h` for help but key is not interpreted.
- Textual: Footer auto-displays BINDINGS ([Footer widget](https://textual.textualize.io/widgets/footer/)); extended help via `ModalScreen` + `push_screen` ([ModalScreen API](https://textual.textualize.io/api/screen/)).
- Bubble Tea: help as dedicated view state; quit always available via `tea.Quit` ([input handling guide](https://www.mintlify.com/charmbracelet/bubbletea/guides/input-handling)).

#### Error display

- `tui-gen-fixture`: always `Status: ok` — no failure path.
- `tui-app-fixture`: no `LI_TUI_ERROR=1` branch.
- Bubble Tea: store error on model, inspect after `p.Run()` — do not panic on recoverable failures ([#1279](https://github.com/charmbracelet/bubbletea/issues/1279)).
- [#34](https://github.com/li-langverse/li-cursor-agents/issues/34).

#### Screen reader / plain-text export

- No `LI_TUI_EXPORT_A11Y=1` artifact; ANSI clear (`\033[2J`) without semantic structure.
- Textual: widget labels + binding tooltips in Footer ([binding API](https://textual.textualize.io/api/binding/)).
- Li should adopt **structured plain export** (headings, lists — not layout spaces) for agent ingestion alongside human TUI.
- [#34](https://github.com/li-langverse/li-cursor-agents/issues/34).

### SOTA comparison (manifest `tui` + web research)

| Source | URL | Relevant pattern |
|--------|-----|------------------|
| Textual (manifest) | https://textual.textualize.io/ | App framework; BINDINGS, screens, command palette |
| Bubble Tea (manifest) | https://github.com/charmbracelet/bubbletea | Elm Architecture; `up`/`k`, `down`/`j`, `q`/`ctrl+c` quit |
| Textual ModalScreen (web) | https://textual.textualize.io/api/screen/ | Help overlays; bindings take precedence over app |
| Textual Footer (web) | https://textual.textualize.io/widgets/footer/ | Auto-display keybindings; `?` help via modal |
| Bubble Tea input (web) | https://www.mintlify.com/charmbracelet/bubbletea/guides/input-handling | Navigation keys; always handle quit; model error msgs |

*Manifest `tui` category lists textual + bubbletea (2 URLs); supplementary guides satisfy ≥3 SOTA web queries.*

### Li ecosystem posture

- **Vision-LLM** ships `lic check --format=json` and `lic diagnose` (JSON stdout) — terminal UX for agents is **JSON-first**, not interactive TUI today ([spec](https://github.com/li-langverse/lic/blob/main/docs/superpowers/specs/2026-05-16-li-llm-first-design.md)).
- **Proof-before-perf:** future `li-tui` / generator must not use `unsafe` or unproved shortcuts; TUI is presentation layer over proved diagnostics.
- **Harness gap:** `adapters/tui.py` hardcodes `error_handling: 0.75` on pass — not derived from journeys or missing states; `journeys: []` always empty.

### Control-plane signal

Recent `tui_ux_tester` / `tui_ui_tester` runs in `agent_runs` show repeated **`error`** status (harness crash on timeout). This pass: `tui_ux_tester-1780188668581` (2026-05-31T00:51Z start, digest complete 01:01Z).

### Evidence paths

- UX audit: `benchmarks/data/latest-tui-ux-run/ux-audit.json`
- Captures: `benchmarks/data/latest-tui-ux-run/captures/tui-{app,gen}-fixture*.txt`
- Config: `li-cursor-agents/config/ux-targets.yaml` (`key_nav_help`, `cli_to_tui` journeys)

### Issues filed (prior runs — still open)

- [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) — non-interactive harness timeout (blocks UX journey runs)
- [#33](https://github.com/li-langverse/li-cursor-agents/issues/33) — journey execution
- [#34](https://github.com/li-langverse/li-cursor-agents/issues/34) — error + a11y export

---

## Recommended issues/PRs

| Pri | Repo | Title | Labels |
|-----|------|-------|--------|
| P1 | `li-cursor-agents` | [ui-audit] tui-app-fixture blocks on read in non-interactive harness ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)) | `ux-audit`, `surface:tui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | [ux-audit] TUI adapter does not execute journey steps (key_nav_help) ([#33](https://github.com/li-langverse/li-cursor-agents/issues/33)) | `ux-audit`, `surface:tui`, `ready-for-implement` |
| P2 | `li-cursor-agents` | [ux-audit] TUI fixtures lack error display and screen-reader plain-text export ([#34](https://github.com/li-langverse/li-cursor-agents/issues/34)) | `ux-audit`, `surface:tui`, `ready-for-implement` |
| P2 | `li-cursor-agents` | **feat(ux-harness): catch TimeoutExpired in tui adapter; pipe stdin for journey steps** | `ux-audit`, `surface:tui` |
| P2 | `lic` | **spec(llm-first): document TUI a11y export contract for agent ingestion (plain + JSON)** | `surface:tui`, Vision-LLM |

---

## Deferred

| Item | Reason |
|------|--------|
| VHS / PTY visual regression for fixtures | `adapters/tui.py` notes PTY reserved; depends on [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) |
| Production `lic` / `lis` TUI targets | No shipped interactive terminal app; JSON diagnostics first |
| Bubble Tea / Textual binding in Li stdlib | Language surface not started; fixtures only |
| Merge TUI rows into global `data/latest/ux-audit.json` preflight | Requires benchmarks preflight orchestration (docs-only today) |
| `tui_ui_tester` parallel pass | Companion UI digest `2026-05-30-tui-ui.md` — frame/encoding findings |

---

**Remediation manifest:** `benchmarks/data/latest/remediation_manifest.json`
