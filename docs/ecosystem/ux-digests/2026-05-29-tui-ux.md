# TUI UX digest — 2026-05-29 (proactive refresh)

**Agent:** `tui_ux_tester` · **Source:** proactive ecosystem sweep  
**Surfaces:** `tui_app`, `tui_gen` · **Repos:** `li-cursor-agents` (fixtures); no production Li TUI shipped yet  
**Preflight:** `ux_audit` → `data/latest-tui-ux-run/ux-audit.json` (re-run **2026-05-29T22:03Z**); UI slice `data/latest-tui-ui-run/ui-audit.json`  
**north_star_fit:** AI-first pillar — agent-facing terminal UX must expose keyboard flows, help, errors, and plain-text export for `lic diagnose`-class tooling (**Vision-LLM** / PH agent diagnostics) without weakening proof gates on shipped `lic` code.

---

## Executive summary

- **No production Li TUI** in org repos; harness audits **fixtures only** (`tui-app-fixture`, `tui-gen-fixture` in `li-cursor-agents/config/ux-targets.json`).
- **`tui-app-fixture` UX fail:** `read -r` blocks non-interactive harness → 30s `TimeoutExpired`; `run_audit.py` crashes uncaught ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)).
- **Piped stdin workaround:** `printf 'h\nq\n' | tui-demo.sh` exits 0 in &lt;1s — proves layout is capturable once harness injects input (Textual `run_test` / Pilot pattern).
- **`tui-gen-fixture` UX pass** (stub rubric) but **journeys empty** — `key_nav_help` / `cli_to_tui` never executed ([#33](https://github.com/li-langverse/li-cursor-agents/issues/33)).
- **Error + a11y export gap:** no `LI_TUI_ERROR=1` branch, no `LI_TUI_EXPORT_A11Y` plain snapshot ([#34](https://github.com/li-langverse/li-cursor-agents/issues/34)).
- **UTF-8 harness gap:** piped capture shows em-dash mojibake without `LANG=C.UTF-8` — include in #30 acceptance criteria.
- Global `data/latest/ux-audit.json` remains **docs-only** (`lic-docs`); TUI targets require proactive harness invocation.
- **SOTA (manifest `tui`):** [Textual](https://textual.textualize.io/) BINDINGS + `run_test()` Pilot + `ModalScreen` help; [Bubble Tea](https://github.com/charmbracelet/bubbletea) vim/arrow nav, `tea.Quit`, errors via `errMsg` in `View` not stderr-only.
- **Control plane:** four prior `tui_ux_tester` runs today ended `error` (harness crash); this run completes digest + artifacts.

---

## Deliverable / findings

### Preflight (`ux_audit`)

| Target | Class | UX status | Journeys | Notes |
|--------|-------|-----------|----------|-------|
| `tui-app-fixture` | `tui_app` | **fail** | `[]` (defined: `key_nav_help`) | TimeoutExpired @ 30s; uncaught in `run_audit.py` |
| `tui-gen-fixture` | `tui_gen` | pass | `[]` (defined: `cli_to_tui`) | Rubric defaults; not journey-validated |

**Rubric when pass:** nav_clarity 0.85, task_efficiency 0.80, error_handling **0.75 (hardcoded)** — not evidence-based until journeys run.

### Flow audit — focus areas

#### Key navigation

| Area | Fixture | SOTA | Gap |
|------|---------|------|-----|
| Focus / roving keys | Mock text only | Textual `set_focus`, footer BINDINGS | No widget focus model |
| Vim bindings | N/A | Bubble Tea `up`/`k`, `down`/`j`, `q`/`ctrl+c` | Not demonstrated in fixtures |
| Journey `key_nav_help` | Defined, **not run** | Textual `await pilot.press("h")` via [run_test()](https://textual.textualize.io/guide/testing/) | [#33](https://github.com/li-langverse/li-cursor-agents/issues/33) |

#### Help overlays

- `tui-demo.sh` prints help only after **two** `read` lines; `h` in prompt is not interpreted.
- Textual: `push_screen` / [ModalScreen](https://textual.textualize.io/api/screen/) stack for help ([screens guide](https://textual.textualize.io/guide/screens/)); command palette `Ctrl+P`.
- Bubble Tea: help as dedicated `View` state; quit via `tea.Quit` command ([input handling](https://charmbracelet-bubbletea-43.mintlify.app/guides/input-handling)).

#### Error display

- `tui-gen-fixture`: always `Status: ok` — no failure path.
- `tui-app-fixture`: no `LI_TUI_ERROR=1` branch.
- Bubble Tea: `errMsg` handled in `Update`, rendered in `View` ([commands tutorial](https://github.com/charmbracelet/bubbletea/blob/f25595a8/tutorials/commands/README.md)).
- [#34](https://github.com/li-langverse/li-cursor-agents/issues/34).

#### Screen reader / plain-text export

- No `LI_TUI_EXPORT_A11Y=1` artifact; ANSI clear (`\033[2J`) without semantic structure.
- Textual: widget labels + accessibility themes; CI via `run_test()` [Pilot](https://textual.textualize.io/api/pilot/).
- Li should adopt **structured plain export** (headings, lists) for agent ingestion alongside human TUI — aligned with Vision-LLM JSON diagnostics.
- [#34](https://github.com/li-langverse/li-cursor-agents/issues/34).

### SOTA comparison (≥3 manifest `tui` URLs)

| Source | URL | Relevant pattern |
|--------|-----|------------------|
| Textual | https://textual.textualize.io/ | BINDINGS footer, fuzzy command palette `Ctrl+P` |
| Textual testing | https://textual.textualize.io/guide/testing/ | `run_test()` headless + `Pilot.press()` for journey automation |
| Bubble Tea | https://github.com/charmbracelet/bubbletea | Elm `Update`/`View`; keyboard routing; `tea.Quit`; `errMsg` in view |

### Error (harness)

```
subprocess.TimeoutExpired: Command '['bash', '.../tui-demo.sh']' timed out after 30 seconds
  at ux-harness/adapters/tui.py:33 in _run_fixture
  at run_audit.py:97 — uncaught; ux-audit.json not auto-written for tui-app-fixture
```

**Workaround:** manual merge into `data/latest-tui-ux-run/ux-audit.json` for this digest (both targets).

### Li ecosystem posture

- **Vision-LLM** ships `lic check --format=json` and `lic diagnose` — terminal UX for agents is **JSON-first**, not interactive TUI today.
- **Proof-before-perf:** future `li-tui` is presentation over proved diagnostics; no `unsafe` / unproved shortcuts.

### Evidence paths

- UX audit: `benchmarks/data/latest-tui-ux-run/ux-audit.json`
- UI audit: `benchmarks/data/latest-tui-ui-run/ui-audit.json`
- Capture: `benchmarks/data/latest-tui-ux-run/captures/tui-app-piped.txt`
- Config: `li-cursor-agents/config/ux-targets.json` (`key_nav_help`, `cli_to_tui`)

---

## Recommended issues/PRs

| Pri | Repo | Title | Labels |
|-----|------|-------|--------|
| P1 | `li-cursor-agents` | [ui-audit] tui-app-fixture blocks on read in non-interactive harness ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)) | `ux-audit`, `surface:tui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | [ux-audit] TUI adapter does not execute journey steps (key_nav_help) ([#33](https://github.com/li-langverse/li-cursor-agents/issues/33)) | `ux-audit`, `surface:tui`, `ready-for-implement` |
| P2 | `li-cursor-agents` | [ux-audit] TUI fixtures lack error display and screen-reader plain-text export ([#34](https://github.com/li-langverse/li-cursor-agents/issues/34)) | `ux-audit`, `surface:tui`, `ready-for-implement` |
| P2 | `li-cursor-agents` | **feat(ux-harness): register `lic diagnose` interactive REPL as `tui_app` when mode ships** | `ux-audit`, `surface:tui` |
| P2 | `lic` | **spec(llm-first): document TUI a11y export contract (plain + JSON) for agent ingestion** | `surface:tui`, Vision-LLM |

---

## Deferred

| Item | Reason |
|------|--------|
| VHS / PTY visual regression | `adapters/tui.py` PTY reserved; depends on [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) |
| Production `lic` / `lis` TUI targets | No shipped interactive terminal app; JSON diagnostics first |
| Bubble Tea / Textual binding in Li stdlib | Language surface not started; fixtures only |
| Merge TUI rows into global `data/latest/ux-audit.json` preflight | Requires benchmarks `ui-ux-audit` orchestration (docs-only today) |
| `tui_ui_tester` frame baselines | Coordinate after [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) harness fix |

---

**Remediation manifest:** `benchmarks/data/latest-tui-ux-run/remediation_manifest.json`  
**Harness:** `python3 li-cursor-agents/ux-harness/run_audit.py --target tui-gen-fixture --mode ux --out-dir benchmarks/data/latest-tui-ux-run`
