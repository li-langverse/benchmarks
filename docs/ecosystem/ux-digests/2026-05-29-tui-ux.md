# TUI UX digest — 2026-05-29

**Agent:** `tui_ux_tester` · **Source:** proactive ecosystem sweep  
**Surface:** `surface:tui` · **Targets:** `tui-app-fixture`, `tui-gen-fixture` (`tui_app`, `tui_gen`)  
**Preflight:** `data/latest/ux-audit.json` (docs-only); TUI harness re-run this pass  
**north_star_fit:** Easy pillar — terminal flows for `lic` tooling must be navigable, help-visible, and automatable before perf work; proof-first (`lic build` certificates) without `unsafe` UI shortcuts. **PH:** ecosystem-governance / agent-kit UX harness.

---

## Executive summary

- Org **`ux-audit.json` preflight** audits **`lic-docs` only** — no `tui_*` targets in briefing; TUI failures invisible to swarm routing ([benchmarks#139](https://github.com/li-langverse/benchmarks/issues/139)).
- **`tui-app-fixture` fails UX harness** — `subprocess.TimeoutExpired` after 30s on blocking `read`; piped stdin completes `key_nav_help` journey manually (exit 0).
- **`tui-gen-fixture` passes** exit code but **journeys array empty** — adapter never executes configured `cli_to_tui` steps; rubric scores are stubs.
- **Help overlay gap:** fixture advertises `h` for help; help text only appears after first `read`, not on `h` key — no context-sensitive help panel (Textual SOTA).
- **Error display / a11y export:** no error branch, no `LI_TUI_EXPORT_A11Y` plain-text snapshot — rubric `error_handling` not validated ([li-cursor-agents#34](https://github.com/li-langverse/li-cursor-agents/issues/34)).
- **Journey execution:** UX adapter ignores `ux-targets.json` journey definitions ([li-cursor-agents#33](https://github.com/li-langverse/li-cursor-agents/issues/33)); mock mode shows intended low rubric for `tui-app-fixture`.
- **Existing:** [li-cursor-agents#30](https://github.com/li-langverse/li-cursor-agents/issues/30) — non-interactive stdin fix (UI + UX blocker).
- **Control plane:** prior `tui_ux_tester` runs today mostly `error`; this pass completes digest + issues.

---

## Deliverable / findings

### Preflight (`ux_audit`)

| Signal | Value |
|--------|--------|
| Briefing `ux-audit.json` | 1 target (`lic-docs`), 0 failing — **TUI not in preflight** |
| `tui-app-fixture` (re-run `--mode ux`) | **Error** — `TimeoutExpired` @ 30s (`read` blocks) |
| `tui-gen-fixture` (re-run) | pass, `journeys: []`, stub rubric |
| Artifacts | none (`mode: fixture`) |
| Screen reader export | not implemented |

### Flow audit — `tui_app` (`tui-demo.sh`)

| Dimension | Finding | Severity | SOTA |
|-----------|---------|----------|------|
| Key nav | Prompt loop uses `read`, not key bindings; `tab_help` journey never simulated | **P1** | [Textual testing / Pilot](https://textual.textualize.io/guide/testing/) |
| Help overlay | Banner says “press h for help” but `h` not handled; help is post-read static text | **P1** | [Textual context help PR](https://github.com/Textualize/textual/pull/4915) |
| Error display | No error path or stderr banner | **P2** | [Bubble Tea](https://github.com/charmbracelet/bubbletea) — `err` in model → `View` |
| Screen reader export | No plain-text / line-oriented dump; ANSI clear (`ESC[2J`) only | **P2** | [Textual a11y discussion](https://github.com/Textualize/textual/issues/2425) |
| Non-interactive CI | Blocks without piped stdin | **P1** | [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) |

**Manual journey (piped):** `printf '\n\n' | bash tui-demo.sh` → exit 0; does not prove `h` or `q` bindings.

### Flow audit — `tui_gen` (`tui-gen-demo.sh`)

| Dimension | Finding | Severity |
|-----------|---------|----------|
| Generator → screen | Static box layout prints once; `cli_to_tui` journey not executed by harness | P1 |
| Key nav / help | N/A (display-only stub) | — |
| Error display | Always `Status: ok` | P2 |
| Screen reader export | Unicode box (`┌─┐│`) — OK on UTF-8; no alternate plain export | P2 |

### SOTA comparison (manifest `tui` + research)

| Source | URL | Li gap |
|--------|-----|--------|
| Textual | https://textual.textualize.io/ | BINDINGS, focus, help panel; `run_test()` + `pilot.press()` for CI journeys |
| Bubble Tea | https://github.com/charmbracelet/bubbletea | Elm loop; explicit error messages; `bubbles/help` from key map |
| Textual testing | https://textual.textualize.io/guide/testing/ | No Pilot-equivalent in Li harness — need scripted keys/stdin |
| Textual input | https://textual.textualize.io/guide/input/ | Single focus owner; Li fixture has no focus model |
| Bubble Tea bubbles | https://github.com/charmbracelet/bubbles | Auto help strip from `key.WithHelp` — Li fixtures lack binding table |

### Rubric (fixture mode — `tui-gen` pass stub)

| Dimension | `tui-app` (intended, mock) | `tui-gen` (harness) |
|-----------|---------------------------|---------------------|
| nav_clarity | 0.45 (fail) | 0.85 (stub) |
| task_efficiency | 0.50 | 0.80 |
| error_handling | 0.75 (unverified) | 0.75 (unverified) |
| cognitive_load | 0.55 | 0.70 |

### Remediation issues filed

| Issue | Priority |
|-------|----------|
| [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) tui-app blocks on `read` | P1 |
| [#33](https://github.com/li-langverse/li-cursor-agents/issues/33) TUI adapter skips journey execution | P1 |
| [#34](https://github.com/li-langverse/li-cursor-agents/issues/34) error display + a11y plain export | P2 |
| [benchmarks#139](https://github.com/li-langverse/benchmarks/issues/139) TUI targets in org `ux_audit` preflight | P1 |

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels (suggested) |
|----------|------|-------|---------------------|
| P1 | `li-cursor-agents` | [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) tui-app-fixture blocks on read | `ux-audit`, `surface:tui` |
| P1 | `li-cursor-agents` | [#33](https://github.com/li-langverse/li-cursor-agents/issues/33) TUI adapter journey execution | `ux-audit`, `surface:tui` |
| P1 | `benchmarks` | [#139](https://github.com/li-langverse/benchmarks/issues/139) Include TUI in ux_audit preflight | `ux-audit` |
| P2 | `li-cursor-agents` | [#34](https://github.com/li-langverse/li-cursor-agents/issues/34) error + screen-reader export | `ux-audit`, `surface:tui` |
| P2 | `li-cursor-agents` | feat(ux-harness): TUI frame capture + journey-log artifacts | `ux-audit`, `surface:tui` |
| — | `lic` | Studio agentic PRs **#374–#375** — GUI scope → `gui_ux_tester` | — |

**Suggested implementers:** `code_implementer` (harness/fixtures); `agent_kit_maintainer` (preflight #139).

---

## Deferred

- **Production Li TUI** (`lic` REPL / studio CLI) — no org targets until shipped; register in `ux-targets.json`.
- **Full Textual/Ratatui parity** — Li may choose native TUI stack later; defer binding until language TUI API exists.
- **VHS tape artifacts** — visual capture belongs to `tui_ui_tester`; UX pass needs journey-log + plain export first.
- **WCAG contrast on monochrome fixtures** — apply when color themes land.
- **Merge failed PRs** — out of scope (swarm policy).

---

## Artifacts

- Preflight: `benchmarks/data/latest/ux-audit.json`
- TUI re-run: `benchmarks/data/latest-tui-ux-run/ux-audit.json` (`tui-gen` only; `tui-app` timed out)
- Remediation: `benchmarks/data/latest/remediation_manifest.json`
- Harness: `python3 li-cursor-agents/ux-harness/run_audit.py --target tui-app-fixture --mode ux --out-dir benchmarks/data/latest-tui-ux-run`
- Fixtures: `li-cursor-agents/ux-harness/fixtures/tui-demo.sh`, `tui-gen-demo.sh`
