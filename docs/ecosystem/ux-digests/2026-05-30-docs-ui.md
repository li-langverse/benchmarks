# Docs UI digest — 2026-05-30 (proactive refresh)

**Agent:** `docs_ui_tester` · **Surface:** `docs` · **Source:** proactive  
**Generated:** 2026-05-30T13:24Z · **Briefing:** `data/latest/agent-briefing.json` (2026-05-30T12:07Z)  
**Preflight:** `ui_audit` — global `data/latest/ui-audit.json` (2026-05-30T04:44Z, static pass); proactive run `data/latest-docs-ui-run/ui-audit.json`  
**North star fit:** easy · provable — handbook must be accessible (WCAG) and visually consistent local ↔ live (**Vision-LLM** handover surfaces)

---

## Executive summary

- **Harness static pass, proactive fail:** Preflight `lic-docs` **pass** (377 HTML, 0 broken links in 40-file sample); proactive Playwright + axe → **fail** (2 serious rules, 17 nodes).
- **Axe (home desktop):** `color-contrast` (**12 nodes**) and `link-in-text-block` (**5 nodes**) — tracked in [lic#426](https://github.com/li-langverse/lic/issues/426); tab chrome alone passes WCAG AA (ratio **21:1** on `.md-tabs__link`).
- **Viewport matrix captured locally:** 6 PNGs (home, hello-world, provability-gaps × desktop/mobile) under `data/latest-docs-ui-run/artifacts/lic-docs/`; live Pages home + provability-gaps captured for drift comparison.
- **Live Pages partial recovery:** Home **200** (`Last-Modified: 2026-05-30T12:17Z`) but still **5 tabs** vs **12** locally; new IA paths **404** (`/guide/hello-world/`, `/ecosystem/overview/`, `/language/overview/`).
- **Baseline drift:** `ux-harness/baselines/docs/` **missing** — harness reports `baseline_status: ok` without PNGs ([#37](https://github.com/li-langverse/li-cursor-agents/issues/37)); pixel diff not measured.
- **Broken links:** Full-tree scan **0** broken relative links; harness `links_checked: 0` (40-file sample uses mostly external/font hrefs — adapter limitation).
- **Raw `.md` hrefs:** **0** in current `lic/site` build (was 93 — [lic#404](https://github.com/li-langverse/lic/issues/404) may be closable after strict rebuild).
- **Deploy blocker unchanged:** [lic#403](https://github.com/li-langverse/lic/issues/403) — strict MkDocs CI must green before full 12-tab site ships to Pages.

---

## Deliverable / findings

### Preflight (`ui_audit`)

| Artifact | Target | Harness | Proactive | Notes |
|----------|--------|---------|-----------|-------|
| `data/latest/ui-audit.json` | `lic-docs` | **pass** | — | Briefing preflight; static_site only |
| `data/latest-docs-ui-run/ui-audit.json` | `lic-docs` | pass* | **fail** | *Static sub-check pass; axe + live drift fail |

### Screenshots (desktop vs mobile)

| Page | Desktop | Mobile | Local path |
|------|---------|--------|------------|
| Home | ✅ | ✅ | `artifacts/lic-docs/home-{desktop,mobile}.png` |
| Hello world | ✅ | ✅ | `artifacts/lic-docs/hello-world-{desktop,mobile}.png` |
| Provability gaps | ✅ | ✅ | `artifacts/lic-docs/provability-gaps-{desktop,mobile}.png` |
| Live home (drift) | ✅ | ✅ | `artifacts/lic-docs-live/home-{desktop,mobile}.png` |

Evidence: `data/latest-docs-ui-run/artifacts/lic-docs/capture-summary.json`

### Axe violations (home desktop, axe-core 4.10.3)

| Rule | Impact | Nodes | Help |
|------|--------|-------|------|
| `color-contrast` | serious | 12 | Minimum contrast ratio thresholds |
| `link-in-text-block` | serious | 5 | Links distinguishable without color alone |

**Contrast spot-check:** `.md-tabs__link` → **21:1** (passes WCAG AA) on all 3 pages × both viewports — failures are body/content elements, not tab bar.

### Live vs local baseline drift

| Signal | Local `lic/site` | Live Pages | Drift |
|--------|------------------|------------|-------|
| Nav tabs | 12 (Guide, Ecosystem, Language handbook, …) | 5 (Guides, Reference, …) | **Major** — IA not deployed |
| `/guide/hello-world/` | 200 | **404** | Tutorial entry missing on live |
| `/ecosystem/overview/` | 200 | **404** | Ecosystem quadrant missing |
| `/language/overview/` | 200 | **404** | Reference handbook missing |
| `/verification/provability-gaps/` | 200 | 200 | OK (legacy path) |
| Pixel diff vs `baselines/docs/` | — | — | **Not run** (no baseline dir) |

Evidence: `data/latest-docs-ui-run/artifacts/lic-docs-live/live-summary.json`

### Broken links & raw hrefs

| Check | Result |
|-------|--------|
| Relative broken links (full 377 HTML tree) | **0** |
| Harness sample (`links_checked`) | 0 (sample cap + external-only hrefs in first 40 files) |
| Raw `.md` hrefs in built site | **0** (was 93 per #404) |

### Harness gaps (docs adapter)

| Check | Result |
|-------|--------|
| Playwright in `docs.py` | **Not wired** — empty `artifacts[]`, `axe_violations: []` on pass |
| `ux-harness/baselines/docs/` | **Missing** — cannot fail on pixel drift |
| Proactive capture script | `li-cursor-agents/dashboard-ui/scripts/docs-ui-capture.mjs` (one-off; not in CI) |

---

## Recommended issues/PRs

| Priority | Title | Repo | Labels |
|----------|-------|------|--------|
| P0 | [ux-audit] Docs CI strict build failing — GitHub Pages stale | `lic` | `ux-audit`, `surface:docs` — **existing [#403](https://github.com/li-langverse/lic/issues/403)** |
| P1 | [ui-audit] lic-docs: axe color-contrast + link-in-text-block (Material theme) | `lic` | `ui-audit`, `surface:docs`, `ready-for-implement` — **existing [#426](https://github.com/li-langverse/lic/issues/426)** |
| P1 | [ui-audit] Seed baselines/docs + Playwright desktop/mobile capture for lic-docs | `li-cursor-agents` | `ui-audit`, `surface:docs`, `ready-for-implement` — **existing [#37](https://github.com/li-langverse/li-cursor-agents/issues/37)** |
| P2 | [ui-audit] Built docs site contains raw .md hrefs (93 unique) | `lic` | `ui-audit`, `surface:docs` — **existing [#404](https://github.com/li-langverse/lic/issues/404)** — verify close after #403 merge |
| P2 | docs(theme): tune Material extra CSS for inline link underline + muted text contrast | `lic` | `ui-audit`, `surface:docs`, `ready-for-implement` |
| P2 | chore(benchmarks): wire `docs-ui-capture.mjs` into preflight `ui_audit` | `benchmarks` / `li-cursor-agents` | `ui-audit`, `ready-for-implement` |

---

## Deferred

- Commit baseline PNGs under `ux-harness/baselines/docs/` until #37 lands (avoid duplicate baselines in benchmarks git).
- Full-tree link crawl in harness (replace 40-file sample + fix `links_checked: 0`).
- Axe on every nav tab page (only home desktop scanned this pass).
- Live Pages Playwright after #403 deploy — re-baseline 12-tab IA and search index size.
- Pixel diff quantification local ↔ live (needs ImageMagick/PIL in harness or committed baselines).

## Remediation manifest

See `data/latest/remediation_manifest.json` (agent: `docs_ui_tester`).
