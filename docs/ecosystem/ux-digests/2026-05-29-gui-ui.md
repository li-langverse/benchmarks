# GUI UI tester digest — 2026-05-29 (proactive refresh)

**Agent:** `gui_ui_tester` · **north_star_fit:** ecosystem / PH-UX (web GUI honesty, accessibility)  
**Preflight:** `ui_audit` → `data/latest/ui-audit.json` (refreshed **2026-05-29T23:51Z**)  
**Source:** proactive ecosystem sweep  
**Harness:** `li-cursor-agents/ux-harness/run_audit.py --mode ui` (GUI targets only)

## Executive summary

- Refreshed GUI preflight: **0 fail**, **3 pass**, **3 skip** across six targets (including manual `benchmarks-dashboard-overview`).
- **`benchmarks` dashboard (current branch)** passes static a11y review: labelled nav/search, focus rings, WCAG AA contrast; minor `--muted` token drift vs design spec.
- **`agents-dashboard`** skipped: nothing listening on `http://127.0.0.1:3000`; Playwright CI uses port **3099** ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)).
- **`lic-tetris`** and **`world-studio-native`** skipped: SDL2 dev libs missing (`libsdl2-dev`); capture script now on lic `main` — [#394](https://github.com/li-langverse/lic/issues/394) scope should shift to CI deps.
- **`world-studio-demo`** and **`gui-gen-fixture`** pass fixture-existence probes only — no axe, pixel diff, or journey execution.
- **`/gpu-matrix/`** route not on current branch; ARIA tab remediation tracked in [#147](https://github.com/li-langverse/benchmarks/issues/147) for unmerged feat branch.
- No new issues filed this run (open remediation set unchanged); manifest at `data/latest/remediation_manifest.json`.
- Control plane: run `gui_ui_tester-1780098567581` **running**; seven prior runs **error** (dispatch/reconcile, not rubric failures).

## Deliverable / findings

### Design review (`benchmarks` dashboard — overview)

| Dimension | Score | Note |
|-----------|------:|------|
| UX-01 | 2 | Clear bento hierarchy; ingest meta and honesty strips above fold |
| UX-07 | 2 | Empty search state copy; regression alert when reds present |
| UX-09 | 2 | Dual nav regions labelled; benchmark search filters have `<label htmlFor>` |
| UX-10 | 2 | No motion transitions on current branch; no reduced-motion gap yet |
| UX-13 | 3 | Coverage honesty, validity-before-perf, measured vs pending counts |
| UX-14 | 3 | Real ingest data in summary; not marketing mock |

**Regressions:** none (no Playwright baseline on this branch)  
**Capture:** static source review only — no PNG committed

### Harness targets (`ux-harness`)

| Target | Status | Note |
|--------|--------|------|
| `agents-dashboard` | skip | `:3000` unreachable |
| `lic-tetris` | skip | SDL2 dev libs missing |
| `world-studio-native` | skip | SDL2 dev libs missing; script present |
| `world-studio-demo` | pass | HTML fixture probe; `system-ui` vs IBM Plex in prod |
| `gui-gen-fixture` | pass | HTML fixture probe; Li tokens OK |
| `benchmarks-dashboard-overview` | **pass** | Manual static review of shipped dashboard-next |

### Token / theme check

- **benchmarks dashboard:** `globals.css` `:root` matches Li dark theme (`#0d1117`, `#161b22`, `#58a6ff`); IBM Plex Sans — no purple-gradient slop.
- **Fixtures:** `#0d1117` / `#161b22` backgrounds; fixtures label themselves as mock (UX-14 honest).
- **Minor drift:** `--muted` is `#9ba3af` vs design-system `#8b949e` (both ≥4.5:1 on `#0d1117`).

### Viewport / pixel / axe matrix

| Layer | Status |
|-------|--------|
| Viewport matrix (Playwright) | Not run — agents dashboard down; benchmarks unregistered in `ux-targets.json` |
| Pixel diff | No baselines for GUI targets |
| axe | Not run live; static review found no violations on current branch overview |
| Design tokens vs Li dark | Pass on shipped dashboard CSS; fixture font stack minor drift |

### Error (harness note)

Full `--all` harness run on Linux timed out on TUI fixtures (`tui-demo.sh`, 30s). GUI-only subset completed successfully. Stack trace:

```
subprocess.TimeoutExpired: Command '['bash', '.../tui-demo.sh']' timed out after 30 seconds
  at ux-harness/adapters/tui.py:33 in _run_fixture
```

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `benchmarks` | [#148](https://github.com/li-langverse/benchmarks/issues/148) Register benchmarks dashboard in ux-targets + Playwright matrix | `ui-audit`, `surface:gui` |
| P1 | `li-cursor-agents` | [#38](https://github.com/li-langverse/li-cursor-agents/issues/38) Align agents-dashboard harness URL with Playwright port 3099 | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P1 | `lic` | [#394](https://github.com/li-langverse/lic/issues/394) Update: SDL2/Xvfb CI deps for native capture (script on main) | `ui-audit`, `surface:gui` |
| P2 | `benchmarks` | [#147](https://github.com/li-langverse/benchmarks/issues/147) GPU chip picker ARIA + keyboard (feat branch) | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P2 | `li-cursor-agents` | [#37](https://github.com/li-langverse/li-cursor-agents/issues/37) Seed Playwright+axe baselines for harness targets | `ui-audit` |

## Deferred

- Extended Playwright capture for `agents-dashboard` when dev server or CI matrix is up.
- SDL/Xvfb Tetris and Studio native frames after `libsdl2-dev` available in audit runner.
- Full axe pass on `world-studio-workspace.html` / `gui-gen-demo.html` when Playwright adapter ships.
- Re-audit `/gpu-matrix/` when feat branch merges ([#147](https://github.com/li-langverse/benchmarks/issues/147)).
- `studio-design-review` PNG iteration uploads to GitHub release `studio-ui-ux-progress` (never commit binaries).

## Evidence paths

- Preflight: `benchmarks/data/latest/ui-audit.json`
- Per-target: `benchmarks/data/latest-gui-ui-run/`
- Manifest: `benchmarks/data/latest/remediation_manifest.json`
- Shared rules: `li-cursor-agents/prompts/ui-ux-tester-shared.md`
