# GUI UI tester digest — 2026-05-29 (proactive refresh)

**Agent:** `gui_ui_tester` · **north_star_fit:** ecosystem / PH-UX (web GUI honesty, accessibility)  
**Preflight:** `ui_audit` → `data/latest/ui-audit.json` (refreshed **2026-05-29T22:43Z**)  
**Source:** proactive ecosystem sweep  
**Harness artifacts:** `data/latest-gui-ui-run/` (re-run **22:43Z**)

## Executive summary

- Refreshed GUI preflight: **1 fail**, **2 pass**, **3 skip** across six targets (including manual `benchmarks-dashboard-gpu-matrix`).
- **`benchmarks` `/gpu-matrix/`** still fails static a11y review: `tablist` without `tabpanel`, no `prefers-reduced-motion` on chip cards — tracked in [#147](https://github.com/li-langverse/benchmarks/issues/147).
- **`agents-dashboard`** skipped: nothing listening on `http://127.0.0.1:3000`; Playwright CI uses port **3099** ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)).
- **`world-studio-native`** and **`lic-tetris`** skipped: `scripts/studio-ui-ux-capture-native.sh` missing on **lic** `main` ([#394](https://github.com/li-langverse/lic/issues/394)).
- **`world-studio-demo`** and **`gui-gen-fixture`** pass fixture-existence probes only — no axe, pixel diff, or journey execution.
- Li dark tokens in `dashboard-next/app/globals.css` align with org palette (`#0d1117`, `#161b22`, `#58a6ff`); IBM Plex Sans — no purple-gradient slop.
- No new issues filed this run (open remediation set unchanged); manifest at `data/latest/remediation_manifest.json`.
- Control plane: run `gui_ui_tester-1780094509743` **running**; four prior runs **error** (dispatch/reconcile, not rubric failures).

## Deliverable / findings

### Design review (`benchmarks` `/gpu-matrix/`)

| Dimension | Score | Note |
|-----------|------:|------|
| UX-07 | 2 | Empty-state copy when zero contributions; donate banner clear |
| UX-09 | 1 | Chip picker tabs lack arrow-key roving and `tabpanel` pairing |
| UX-10 | 1 | `prefers-reduced-motion` not applied to `.gpu-chip-card` transitions |
| UX-13 | 3 | Pilot timing strip, blocked backend cells, funding-gap list — honest metrics |
| UX-14 | 3 | Real ingest data (`lig-gpu-matrix.json`), not marketing mock |

**Regressions:** none (surface new; no Playwright baseline)  
**Capture:** static source review only — no PNG committed

### Harness targets (`ux-harness`)

| Target | Status | Note |
|--------|--------|------|
| `agents-dashboard` | skip | `:3000` unreachable |
| `lic-tetris` | skip | no native `capture_script` in manifest |
| `world-studio-native` | skip | `studio-ui-ux-capture-native.sh` absent on lic main |
| `world-studio-demo` | pass | HTML fixture probe; `system-ui` vs IBM Plex in prod |
| `gui-gen-fixture` | pass | HTML fixture probe; Li tokens OK |
| `benchmarks-dashboard-gpu-matrix` | **fail** | Manual static review — not in `ux-targets.json` |

### Token / theme check

- **benchmarks dashboard:** `globals.css` `:root` matches Li dark theme; focus rings on interactive elements.
- **Fixtures:** `#0d1117` / `#161b22` backgrounds; fixtures label themselves as mock (UX-14 honest).
- **Anti-patterns:** none observed (no purple gradients, no fake FPS on gpu-matrix).

### Viewport / pixel / axe matrix

| Layer | Status |
|-------|--------|
| Viewport matrix (Playwright) | Not run — dashboard down; gpu-matrix unregistered |
| Pixel diff | No baselines for GUI targets |
| axe | Manual: `aria-required-children` on gpu-matrix tablist only |
| Design tokens vs Li dark | Pass on shipped dashboard CSS; fixture font stack minor drift |

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `benchmarks` | [#147](https://github.com/li-langverse/benchmarks/issues/147) GPU chip picker ARIA + keyboard | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P1 | `benchmarks` | [#148](https://github.com/li-langverse/benchmarks/issues/148) ux-targets + Playwright for benchmarks dashboard | `ui-audit`, `surface:gui` |
| P1 | `lic` | [#394](https://github.com/li-langverse/lic/issues/394) Add `studio-ui-ux-capture-native.sh` on main | `ui-audit`, `surface:gui` |
| P2 | `li-cursor-agents` | [#38](https://github.com/li-langverse/li-cursor-agents/issues/38) agents-dashboard port alignment | `ui-audit`, `surface:gui` |
| P2 | `li-cursor-agents` | [#37](https://github.com/li-langverse/li-cursor-agents/issues/37) Seed Playwright+axe for harness | `ui-audit` |

## Deferred

- Extended Playwright capture for `agents-dashboard` when dev server or CI matrix is up.
- SDL/Xvfb Tetris frames after native capture script lands on lic main.
- Full axe pass on `world-studio-workspace.html` / `gui-gen-demo.html` when Playwright adapter ships.
- `studio-design-review` PNG iteration uploads to GitHub release `studio-ui-ux-progress` (never commit binaries).

## Evidence paths

- Preflight: `benchmarks/data/latest/ui-audit.json`
- Per-target: `benchmarks/data/latest-gui-ui-run/`
- Manifest: `benchmarks/data/latest/remediation_manifest.json`
- Shared rules: `li-cursor-agents/prompts/ui-ux-tester-shared.md`
