# GUI UI tester digest — 2026-05-30 (proactive refresh)

**Agent:** `gui_ui_tester` · **Source:** proactive ecosystem sweep  
**Surface:** `surface:gui` (`gui_app`, `gui_gen`)  
**Preflight:** `ui_audit` → `data/latest/ui-audit.json` (2026-05-30T03:22Z, docs-only slice) + per-target `data/latest-gui-ui-run/`  
**north_star_fit:** Easy pillar — honest GUI surfaces, Li dark tokens, WCAG-oriented focus paths (PH-UX); proof-before-perf unchanged.

---

## Executive summary

- Refreshed five GUI harness targets: **0 fail**, **2 pass**, **3 skip**; org preflight `ui-audit.json` still docs-only (`lic-docs` pass).
- **`agents-dashboard`** skipped — `:3000` and `:3099` unreachable; static CSS still drifts from Li dark (`#0f1419` / `#6eb5ff` vs `#0d1117` / `#58a6ff`); no `:focus-visible` in `dashboard-ui` ([#40](https://github.com/li-langverse/li-cursor-agents/issues/40)).
- **`lic-tetris`** and **`world-studio-native`** skipped — `studio-ui-ux-capture-native.sh` absent on **lic** `main` (exists in `lic-studio-ui` worktree only) ([#394](https://github.com/li-langverse/lic/issues/394)).
- **`world-studio-demo`** and **`gui-gen-fixture`** pass fixture-existence probes only — no Playwright viewport matrix, pixel diff, or axe ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)).
- **`benchmarks/dashboard-next`** remains canonical Li token reference with `:focus-visible`; not yet registered in `ux-targets.json` ([#148](https://github.com/li-langverse/benchmarks/issues/148)).
- Studio PH-UX bench snapshot (`lic/data/studio-ui-ux-plan-loop/latest-bench.json`) shows all MD tiers **fail** — `bench.py` rejects `--filter md_lennard_jones` (builder scope; document for `studio_ui_ux_builder`).
- **No new GitHub issues** this run — open remediation set unchanged; manifest refreshed below.
- Control plane: `gui_ui_tester-1780111959565` **running**; prior `gui_ui_tester` / `tui_ux_tester` runs **error** (full `--all` harness timeout pattern).

---

## Deliverable / findings

### Harness targets (`ux-harness`)

| Target | Status | Mode | Note |
|--------|--------|------|------|
| `agents-dashboard` | skip | http_probe | Dashboard down; token + focus gaps — [#40](https://github.com/li-langverse/li-cursor-agents/issues/40), port [#38](https://github.com/li-langverse/li-cursor-agents/issues/38) |
| `lic-tetris` | skip | native_gui | No capture script on lic `main` |
| `world-studio-native` | skip | native_gui | Same — [#394](https://github.com/li-langverse/lic/issues/394) |
| `world-studio-demo` | pass | http_probe | HTML mock; empty dock/viewport (UX-07) |
| `gui-gen-fixture` | pass | http_probe | Li tokens OK; labeled fixture |

Evidence: `benchmarks/data/latest-gui-ui-run/<target>/{ui,ux}-audit.json`

### Design review (fixtures + static CSS)

| Dimension | Score | Note |
|-----------|------:|------|
| UX-07 | 1 | Studio fixture: empty dock + blank viewport — no empty-state copy |
| UX-09 | 2 | Agents drawers have `role="dialog"` + `aria-labelledby`; fixture nav not keyboard-complete |
| UX-10 | 1 | Agents dashboard: no `:focus-visible`; no `prefers-reduced-motion` on `.btn` transitions |
| UX-13 | 3 | Inspector “Particle tier: 10k (mock)” — honest perf labeling |
| UX-14 | 3 | Fixtures titled/labeled as harness mocks, not native captures |

**Regressions:** none (no Playwright baselines for GUI targets)  
**Capture:** per-target JSON only — no PNG committed (per studio-design-review skill)

### Token / theme check

| Surface | Li dark alignment | Notes |
|---------|:-----------------:|-------|
| `benchmarks/dashboard-next` | ✓ | `#0d1117`, `#58a6ff`, IBM Plex Sans/Mono, `:focus-visible` |
| `li-cursor-agents/dashboard-ui` | ✗ | `#0f1419`, `#6eb5ff`, `system-ui` only — [#40](https://github.com/li-langverse/li-cursor-agents/issues/40) |
| `world-studio-workspace.html` | ✓ (colors) | `system-ui` font drift vs IBM Plex in prod target |
| `gui-gen-demo.html` | ✓ | Minimal panel; fixture label present |

**Anti-patterns:** none (no purple gradients, no fake FPS on shipped dashboards).

### Viewport / pixel / axe matrix

| Layer | Status |
|-------|--------|
| Viewport matrix (Playwright) | Not run — dashboard down; native capture script missing on lic `main` |
| Pixel diff | No baselines for GUI targets |
| axe | Not run — fixture probe only |
| Design tokens vs Li dark | Fail on `dashboard-ui`; pass on benchmarks dashboard + fixtures |

### PH-UX performance gates (lic plan-loop snapshot)

| Gate | Observed | Target |
|------|----------|--------|
| MD 1k/10k/100k tiers | **fail** (bench CLI `--filter` unsupported) | 60 / 60 / 30 fps |
| Native viewport PNG | **skip** | `native_pixels=true` when capture script on main |

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `li-cursor-agents` | [#40](https://github.com/li-langverse/li-cursor-agents/issues/40) agents-dashboard: Li dark tokens + focus-visible | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P1 | `lic` | [#394](https://github.com/li-langverse/lic/issues/394) Add `studio-ui-ux-capture-native.sh` on main | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | [#32](https://github.com/li-langverse/li-cursor-agents/issues/32) Playwright + axe for HTML fixtures | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P1 | `benchmarks` | [#148](https://github.com/li-langverse/benchmarks/issues/148) Register benchmarks dashboard in ux-targets | `ui-audit`, `surface:gui`, `ready-for-implement` |
| P2 | `li-cursor-agents` | [#38](https://github.com/li-langverse/li-cursor-agents/issues/38) agents-dashboard port alignment (:3000 vs :3099) | `ui-audit`, `surface:gui` |
| P2 | `benchmarks` | [#138](https://github.com/li-langverse/benchmarks/issues/138) Expand org preflight ui-audit to GUI/TUI targets | `ui-audit` |
| P2 | `benchmarks` | [#147](https://github.com/li-langverse/benchmarks/issues/147) GPU chip picker ARIA (when `/gpu-matrix/` lands) | `ui-audit`, `surface:gui` |
| P2 | `lic` | (new — `studio_ui_ux_builder`) Fix `bench-studio-viewport-perf.sh` → `bench.py --filter md_lennard_jones` | `surface:gui` |

---

## Deferred

- Extended Playwright capture for `agents-dashboard` when dev server or CI matrix is up ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)).
- SDL/Xvfb Tetris frames after native capture script lands on lic `main` ([#394](https://github.com/li-langverse/lic/issues/394)).
- Full axe pass on studio/gui-gen fixtures when Playwright adapter ships ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)).
- `studio-design-review` PNG iteration uploads to GitHub release `studio-ui-ux-progress` (never commit binaries).
- `/gpu-matrix/` static review — route absent on current main; revisit when surface ships.
- Merge `lic-studio-ui/scripts/studio-ui-ux-capture-native.sh` to lic `main` before re-scoring UX-01/UX-14 as native truth.

---

## Evidence paths

- Preflight (docs): `benchmarks/data/latest/ui-audit.json`, `ux-audit.json`
- GUI per-target: `benchmarks/data/latest-gui-ui-run/`
- Manifest: `benchmarks/data/latest/remediation_manifest.json`
- Shared rules: `li-cursor-agents/prompts/ui-ux-tester-shared.md`
