# GUI UI digest — 2026-05-30 (proactive refresh)

**Agent:** `gui_ui_tester` · **Source:** proactive ecosystem sweep  
**Surfaces:** `gui_app`, `gui_gen` · **Repos:** `li-cursor-agents`, `lic` (fixtures + `lic-studio-ui` native capture)  
**Preflight:** `ui_audit` — proactive GUI run at `data/latest-gui-ui-run/ui-audit.json` (2026-05-30T15:47Z)  
**north_star_fit:** AI-first pillar — agent dashboard and Studio GUI surfaces must meet Li dark-theme tokens, WCAG-oriented contrast, and honest native-vs-mock labeling for swarm handoffs (**Vision-LLM** / PH agent diagnostics).

---

## Executive summary

- Proactive harness refresh: **4 pass**, **1 skip**, **0 fail** across five GUI targets (`data/latest-gui-ui-run/ui-audit.json`).
- **`agents-dashboard` skipped** — dev server not reachable at `http://127.0.0.1:3000`; extended axe/viewport/pixel audit blocked ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)).
- **`web_gui` adapter remains `http_probe` only** — no Playwright snapshots, axe, viewport matrix, or measured pixel diff ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)).
- **Li dark tokens stable** — `dashboard-ui/app/globals.css` uses `#0d1117` / `#58a6ff` + `:focus-visible`; HTML fixtures match.
- **`npm run build`** green on dashboard-ui; **`test:e2e:mock`** **1 pass / 1 fail** (live-stream `text-delta` assertion — [#31](https://github.com/li-langverse/li-cursor-agents/issues/31)).
- **Native SDL capture passes** for `world-studio-native` and `lic-tetris` with `LIC_ROOT=lic-studio-ui` ([#47](https://github.com/li-langverse/li-cursor-agents/issues/47)); script absent on main `lic` ([#394](https://github.com/li-langverse/lic/issues/394)).
- **`lic-tetris` false pass** — harness reuses studio SDL stub, not `examples/tetris` ([#46](https://github.com/li-langverse/li-cursor-agents/issues/46)).
- Global briefing `data/latest/ui-audit.json` still **docs-only** (`lic-docs`); GUI rows require proactive `data/latest-gui-ui-run/`.

---

## Deliverable / findings

### Preflight (`ui_audit`)

| Target | Class | Status | Mode | Notes |
|--------|-------|--------|------|-------|
| `agents-dashboard` | `gui_app` | **skip** | `http_probe` | Not reachable at `http://127.0.0.1:3000` |
| `gui-gen-fixture` | `gui_gen` | pass | `http_probe` | Fixture exists; Li tokens OK; no axe/pixel |
| `world-studio-demo` | `gui_app` | pass | `http_probe` | HTML mock; dock/viewport ARIA labels |
| `lic-tetris` | `gui_app` | pass* | `native_gui` | *Studio stub — [#46](https://github.com/li-langverse/li-cursor-agents/issues/46) |
| `world-studio-native` | `gui_app` | pass | `native_gui` | 3 PNGs; `capture_mode: xvfb_sdl` |

**Evidence:** `benchmarks/data/latest-gui-ui-run/ui-audit.json` (2026-05-30T15:47Z)

### Viewport matrix / pixel diff / axe

| Check | Result |
|-------|--------|
| Viewport matrix (mobile/tablet/desktop) | **Not run** — live dashboard not probed |
| Pixel diff vs baseline | **Not run** — adapter default `max_ratio: 0.0` |
| axe violations | **Not run** — empty arrays on all targets |
| Contrast scan | **Not run** on live dashboard; static tokens aligned |

### Design tokens vs Li dark theme

| Surface | Background | Accent | Focus ring |
|---------|------------|--------|------------|
| Li canonical (fixtures) | `#0d1117` | `#58a6ff` | N/A (static HTML) |
| `dashboard-ui/app/globals.css` | `#0d1117` ✓ | `#58a6ff` ✓ | `:focus-visible` ✓ |
| `world-studio-workspace.html` | `#0d1117` | `#238636` badge | Dock/viewport ARIA labels |
| `gui-gen-demo.html` | `#0d1117` | `#58a6ff` h1 | N/A |

### Design review (UX-01…UX-14, 0–3)

| Dimension | Score | Note |
|-----------|------:|------|
| UX-01 | 2 | Studio viewport stub draws grid |
| UX-02 | 1 | Timeline not in HTML fixture |
| UX-03 | 2 | Inspector panel in workspace mock |
| UX-04 | 1 | Command palette journey not executed |
| UX-05 | 2 | Vertical profile badge only (mock) |
| UX-06 | 2 | Dashboard chrome buildable; live audit pending |
| UX-07 | 2 | Empty states not probed on live dashboard |
| UX-08 | 1 | No error-state capture |
| UX-09 | 1 | Keyboard paths not exercised live |
| UX-10 | 3 | Dashboard + fixture tokens aligned with Li dark theme |
| UX-11 | 1 | No loading/skeleton audit |
| UX-12 | 2 | Native pixels true but stub labeled |
| UX-13 | 2 | Mock badge + bench 60 fps @ 10k native (lic-studio-ui) |
| UX-14 | 1 | lic-tetris pass misleading ([#46](https://github.com/li-langverse/li-cursor-agents/issues/46)) |

**Regressions:** none since prior pass (15:00Z).  
**Capture:** prior [lic#182 comment](https://github.com/li-langverse/lic/issues/182#issuecomment-4583125826) / release `studio-ui-ux-progress`

### Accessibility & web quality (checklist)

| Area | Check | Result |
|------|-------|--------|
| Contrast | Text/icons ≥ WCAG AA on `#0d1117` | Tokens aligned; live axe pending |
| Focus | Visible focus on interactive elements | **Pass** — `:focus-visible` in globals.css |
| Keyboard | Tab order dock → viewport → inspector | **Not tested** (server down at probe) |
| Motion | `prefers-reduced-motion` | **Not tested** |
| Perf honesty | FPS / particle count | Mock badge only |
| Loading | Skeleton for >300ms shell | **Not tested** |

### Playwright / dashboard

```text
npm run build → pass
npm run test:e2e:mock → 1 passed, 1 failed
PASS: run drawer shows thinking and output when trace has no deltas yet
FAIL: run drawer shows live stream deltas while run is live (text-delta assertion)
```

### Harness robustness note

Running `run_audit.py --all --mode ui` **crashes** on `tui-app-fixture` (`TimeoutExpired` @ 30s) before writing combined JSON. GUI targets must be run individually or TUI excluded ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)).

---

## Recommended issues/PRs

| Pri | Repo | Title | Labels |
|-----|------|-------|--------|
| P1 | `li-cursor-agents` | Fix live-stream mock e2e delta assertion ([#31](https://github.com/li-langverse/li-cursor-agents/issues/31)) | `surface:gui`, `ui-audit`, `ready-for-implement` |
| P1 | `li-cursor-agents` | Align harness probe to Playwright webServer port ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)) | `surface:gui`, `ui-audit`, `ready-for-implement` |
| P1 | `li-cursor-agents` | Wire Playwright snapshots + axe for HTML fixtures ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)) | `surface:gui`, `ui-audit`, `ready-for-implement` |
| P1 | `li-cursor-agents` | lic-tetris must not reuse studio SDL stub ([#46](https://github.com/li-langverse/li-cursor-agents/issues/46)) | `surface:gui`, `ui-audit`, `ready-for-implement` |
| P2 | `lic` | Add `studio-ui-ux-capture-native.sh` on main ([#394](https://github.com/li-langverse/lic/issues/394)) | `PH-UX`, `surface:gui` |
| P2 | `lic` | Tetris-specific native capture ([#515](https://github.com/li-langverse/lic/issues/515)) | `surface:gui`, `ui-audit` |
| P2 | `benchmarks` | Include GUI targets in org preflight `ui_audit` (not docs-only) | `ui-audit` |

**Resolved (prior run):** [#40](https://github.com/li-langverse/li-cursor-agents/issues/40) — Li dark tokens + `:focus-visible` in `globals.css`.

---

## Deferred

| Item | Reason |
|------|--------|
| Live dashboard axe + viewport matrix | Start dev server + extend harness ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)) |
| Real Tetris/SDL frame capture | [#515](https://github.com/li-langverse/lic/issues/515) + [#46](https://github.com/li-langverse/li-cursor-agents/issues/46) |
| PH-UX bench on main `lic` | Studio builder domain; capture script on `lic-studio-ui` only |
| Merge GUI rows into global `data/latest/ui-audit.json` | Preflight orchestration (docs-only today) |
| PNG/MP4 commit to git | Per studio-design-review skill — release/issue assets only |
| New GitHub issue filing this run | **Error:** GitHub REST/GraphQL rate limit exceeded |

---

**Remediation manifest:** `benchmarks/data/latest/remediation_manifest.json`
