# GUI UI tester digest — 2026-05-30 (proactive refresh)

**Agent:** `gui_ui_tester` · **Source:** proactive ecosystem sweep  
**Surface:** `surface:gui` (`gui_app`, `gui_gen`)  
**Preflight:** `ui_audit` → `data/latest/ui-audit.json` (refreshed **2026-05-30T01:27Z**)  
**north_star_fit:** Easy pillar — honest GUI surfaces, Li dark tokens, accessibility (PH-UX); no proof shortcuts.

---

## Executive summary

- Refreshed GUI preflight: **0 fail**, **2 pass**, **3 skip** across five harness targets (+ `lic-docs` static pass in sibling run dir).
- **`agents-dashboard`** skipped — nothing on `:3000`; static CSS review finds **Li dark token drift** and missing `:focus-visible` ([#40](https://github.com/li-langverse/li-cursor-agents/issues/40), port [#38](https://github.com/li-langverse/li-cursor-agents/issues/38)).
- **`lic-tetris`** and **`world-studio-native`** skipped — `studio-ui-ux-capture-native.sh` absent on **lic** `main` ([#394](https://github.com/li-langverse/lic/issues/394)).
- **`world-studio-demo`** and **`gui-gen-fixture`** pass fixture-existence probes only — no Playwright axe, pixel diff, or viewport matrix ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)).
- **Benchmarks** `dashboard-next` uses canonical Li tokens (`#0d1117`, `#58a6ff`, IBM Plex) with `:focus-visible`; `/gpu-matrix/` route **not present** on current main — open issues [#147](https://github.com/li-langverse/benchmarks/issues/147)/[#148](https://github.com/li-langverse/benchmarks/issues/148) deferred.
- Fixtures label mock metrics honestly (UX-13); no purple-gradient slop; empty dock/viewport in studio fixture flagged (UX-07).
- Filed **1 new issue** this run: agents-dashboard token + focus alignment ([#40](https://github.com/li-langverse/li-cursor-agents/issues/40)).
- Control plane: run `gui_ui_tester-1780104266691` **running**; three prior runs **error** (TUI harness timeout on full `--all` audit).

---

## Deliverable / findings

### Harness targets (`ux-harness`)

| Target | Status | Mode | Note |
|--------|--------|------|------|
| `agents-dashboard` | skip | http_probe | `:3000` unreachable; tokens deviate — see [#40](https://github.com/li-langverse/li-cursor-agents/issues/40) |
| `lic-tetris` | skip | native_gui | No `capture_script` / `lic_root` in manifest paths |
| `world-studio-native` | skip | native_gui | `scripts/studio-ui-ux-capture-native.sh` missing on lic main ([#394](https://github.com/li-langverse/lic/issues/394)) |
| `world-studio-demo` | pass | http_probe | HTML fixture only; `system-ui` vs IBM Plex in prod |
| `gui-gen-fixture` | pass | http_probe | Li tokens OK; no axe run |

### Design review (fixtures + static CSS)

| Dimension | Score | Note |
|-----------|------:|------|
| UX-07 | 1 | Studio fixture: empty dock + blank viewport — no empty-state copy |
| UX-09 | 2 | Drawers in agents dashboard have `role="dialog"` + `aria-labelledby`; fixture nav has no focusable tools |
| UX-10 | 1 | Agents `.btn` transitions lack `prefers-reduced-motion`; benchmarks dashboard OK |
| UX-13 | 3 | Inspector shows “Particle tier: 10k (mock)” — honest perf labeling |
| UX-14 | 3 | Fixtures titled/labeled as harness mocks, not native captures |

**Regressions:** none (no Playwright baselines for GUI targets)  
**Capture:** `data/latest-gui-ui-run/*/ui-audit.json` — no PNG committed

### Token / theme check

| Surface | Li dark alignment | Notes |
|---------|:-----------------:|-------|
| `benchmarks/dashboard-next` | ✓ | `#0d1117`, `#161b22`, `#58a6ff`, IBM Plex Sans |
| `li-cursor-agents/dashboard-ui` | ✗ | `#0f1419`, `#6eb5ff`, `.btn-primary` Tailwind blue ([#40](https://github.com/li-langverse/li-cursor-agents/issues/40)) |
| `world-studio-workspace.html` | ✓ (colors) | Font stack drift; radial viewport gradient minor |
| `gui-gen-demo.html` | ✓ | Minimal panel; fixture label present |

**Anti-patterns:** none (no purple gradients, no fake FPS on shipped dashboards).

### Viewport / pixel / axe matrix

| Layer | Status |
|-------|--------|
| Viewport matrix (Playwright) | Not run — dashboard down; native capture script missing |
| Pixel diff | No baselines for GUI targets |
| axe | Not run — fixture probe only; manual: agents dashboard lacks focus-visible CSS |
| Design tokens vs Li dark | Fail on `dashboard-ui`; pass on benchmarks dashboard + fixtures |

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `li-cursor-agents` | [#40](https://github.com/li-langverse/li-cursor-agents/issues/40) agents-dashboard: Li dark tokens + focus-visible | `ui-audit`, `surface:gui` |
| P1 | `lic` | [#394](https://github.com/li-langverse/lic/issues/394) Add `studio-ui-ux-capture-native.sh` on main | `ui-audit`, `surface:gui` |
| P1 | `li-cursor-agents` | [#32](https://github.com/li-langverse/li-cursor-agents/issues/32) Playwright + axe for HTML fixtures | `ui-audit`, `surface:gui` |
| P1 | `benchmarks` | [#148](https://github.com/li-langverse/benchmarks/issues/148) Register benchmarks dashboard in ux-targets | `ui-audit`, `surface:gui` |
| P2 | `li-cursor-agents` | [#38](https://github.com/li-langverse/li-cursor-agents/issues/38) agents-dashboard port alignment (:3000 vs :3099) | `ui-audit`, `surface:gui` |
| P2 | `benchmarks` | [#147](https://github.com/li-langverse/benchmarks/issues/147) GPU chip picker ARIA (when `/gpu-matrix/` lands) | `ui-audit`, `surface:gui` |

---

## Deferred

- Extended Playwright capture for `agents-dashboard` when dev server or CI matrix is up ([#38](https://github.com/li-langverse/li-cursor-agents/issues/38)).
- SDL/Xvfb Tetris frames after native capture script lands on lic main ([#394](https://github.com/li-langverse/lic/issues/394)).
- Full axe pass on studio/gui-gen fixtures when Playwright adapter ships ([#32](https://github.com/li-langverse/li-cursor-agents/issues/32)).
- `studio-design-review` PNG iteration uploads to GitHub release `studio-ui-ux-progress` (never commit binaries).
- `/gpu-matrix/` static review — route absent on current main; revisit when surface ships.

---

## Evidence paths

- Preflight: `benchmarks/data/latest/ui-audit.json`
- Per-target: `benchmarks/data/latest-gui-ui-run/`
- Manifest: `benchmarks/data/latest/remediation_manifest.json`
- Shared rules: `li-cursor-agents/prompts/ui-ux-tester-shared.md`
