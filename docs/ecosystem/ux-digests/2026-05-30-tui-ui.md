# TUI UI digest — 2026-05-30 (proactive cycle 6)

**Agent:** `tui_ui_tester` · **Source:** proactive ecosystem sweep  
**Surface:** `surface:tui` · **Targets:** `tui-app-fixture`, `tui-gen-fixture` (`tui_app`, `tui_gen`)  
**Preflight:** org `data/latest/ui-audit.json` (GUI-only, 2 pass / 3 skip) — TUI harness re-run → `data/latest-tui-ui-run/ui-audit.json` (**2026-05-30T09:07Z**)  
**north_star_fit:** Easy pillar + **Vision-LLM** — terminal surfaces for `lic diagnose` and agent workflows must be readable, UTF-8-safe, and automatable before perf work; proof-first (`lic build` certificates), no `unsafe` UI shortcuts.

---

## Executive summary

- Org **`ui-audit.json` preflight** audits **GUI fixtures only** (five targets); **TUI rows absent** from briefing — failures invisible to heap routing without `data/latest-tui-ui-run/`.
- **`tui-app-fixture` fails** — `read -r` blocks non-TTY harness (`TimeoutExpired` @ 30s); **`run_audit.py` crashes uncaught** when run alone (no fail row written until manual merge).
- **Piped stdin passes** (`printf 'h\nq\n' | tui-demo.sh`, exit 0, ~97 B) — 3-line ANSI layout capturable once harness injects input (Textual Pilot / Ratatui `TestBackend` pattern).
- **`tui-gen-fixture` passes** — deterministic Unicode box frame (31 cols max line); no ANSI color; **no frame baseline** or stdout artifact on harness pass path.
- **Encoding:** em dash in `tui-demo.sh` title mojibakes in capture files (`M-bM-^@M-^T`) unless fixture uses ASCII `-` or harness forces `LANG=C.UTF-8`.
- **No production Li TUI** in org catalog — fixtures only in `ux-targets.yaml`; `lic` ships JSON diagnostics, not interactive terminal UX.
- **Control plane:** prior `tui_ui_tester` runs today mostly **`error`** (`unregistered_running_reconciled`); run `tui_ui_tester-1780131956918` active during this pass.
- **SOTA:** Textual `run_test()` + Pilot; Ratatui insta snapshots; VHS `.tape` for terminal visual audit artifacts.

---

## Deliverable / findings

### Preflight (`ui_audit`)

| Signal | Value |
|--------|--------|
| Briefing `data/latest/ui-audit.json` | GUI-only — **TUI not in org preflight** |
| TUI re-run `data/latest-tui-ui-run/ui-audit.json` | 2 targets, **1 failing**, **1 passing** |
| `tui-app-fixture` | **fail** — `TimeoutExpired`; `baseline_status: missing`; no artifacts |
| `tui-gen-fixture` | **pass**, `fixture_exit_code: 0`; capture at `captures/tui-gen-fixture.txt` |
| Pixel / contrast | stub metrics (`pixel_diff.max_ratio: 0.0`); no VHS, insta baseline, or ANSI contrast scan |

### Terminal frame review (fixture demos)

**`tui-app-fixture` (`tui-demo.sh`)**

| Check | Result | Severity |
|-------|--------|----------|
| Non-interactive CI | Blocks on `read -r` ×2 | **P1** |
| Harness robustness | `TimeoutExpired` uncaught — `run_audit.py` exit 1, partial JSON | **P1** |
| ANSI layout | Clear + home (`ESC[2J` `ESC[H`), single-column `>` prompts | OK (minimal) |
| Color contrast | No fg/bg ANSI colors — N/A for monochrome stub | — |
| Layout density | ~3 content lines + 2 prompts; no panels/widgets/borders | P2 (fixture only) |
| UTF-8 / i18n | Em dash mojibake in piped capture (`cat -A` shows `M-bM-^@M-^T`) | P2 |

**Piped capture** (`data/latest-tui-ui-run/captures/tui-app-fixture-piped.txt`):

```
^[[2J^[[HLi TUI demo — press h for help, q to quit
> Help: arrow keys navigate (mock)
> Goodbye.
```

**`tui-gen-fixture` (`tui-gen-demo.sh`)**

| Check | Result | Severity |
|-------|--------|----------|
| Exit code | 0 | OK |
| Box drawing | `┌─┐│└` Unicode frame, 31 cols, 5 lines total | OK |
| ANSI color | None — monochrome stdout | P2 when real generator lands |
| Density | Sparse but readable stub panel | OK for fixture |
| Error-state frame | Always `Status: ok` — no failure visual regression target | P2 |
| Narrow terminal | No wrap/truncation test at 80/40 cols | P2 |

### SOTA comparison (terminal UI visual audit)

| Source | URL | Takeaway for Li |
|--------|-----|-----------------|
| Textual testing | https://textual.textualize.io/guide/testing/ | `run_test()` + `pilot.press()` — never block CI on `read` |
| Textual Pilot API | https://textual.textualize.io/api/pilot/ | Async input injection; `pause()` for frame stability |
| Ratatui snapshots | https://ratatui.rs/recipes/testing/snapshots/ | `TestBackend` + insta for layout regression |
| VHS | https://github.com/charmbracelet/vhs | `.tape` recordings for terminal visual audit artifacts |
| WCAG contrast | https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html | ≥4.5:1 when ANSI color themes ship |

### Org catalog (ecosystem)

- Briefing ecosystem audit: **no org repos** registered as `surface: tui` beyond harness fixtures in `config/ux-targets.yaml`.
- **`lic`:** JSON-first diagnostics (`lic check --format=json`, `lic diagnose`) — no interactive TUI shipped.
- Parallel **`tui_ux_tester`** digest covers journey rubric gaps ([#33](https://github.com/li-langverse/li-cursor-agents/issues/33), [#34](https://github.com/li-langverse/li-cursor-agents/issues/34)).

### Error (harness)

```
subprocess.TimeoutExpired: Command '['bash', '.../tui-demo.sh']' timed out after 30 seconds
  at ux-harness/adapters/tui.py:33 in _run_fixture
  at run_audit.py:95 — uncaught; manual merge required for combined ui-audit.json
```

**Workaround applied:** merged fail row + piped capture in `data/latest-tui-ui-run/ui-audit.json` for this digest.

### Issues filed (prior runs — still open)

- [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) — non-interactive harness timeout (**P1**, `ui-audit`, `surface:tui`)
- [#33](https://github.com/li-langverse/li-cursor-agents/issues/33) — journey execution (UX; blocks frame capture after help overlay)
- [#34](https://github.com/li-langverse/li-cursor-agents/issues/34) — error + a11y export states (UX; affects failure-frame visual audit)

*GitHub API rate-limited during this run — no new issues filed.*

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `li-cursor-agents` | **[ui-audit] tui-app-fixture blocks on read in non-interactive harness** ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)) | `ui-audit`, `surface:tui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | **feat(ux-harness): catch TimeoutExpired + stdout artifact for TUI fixtures** | `ui-audit`, `surface:tui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | **feat(ux-harness): TUI frame capture (stdout dump or VHS) + baseline diff** | `ui-audit`, `surface:tui` |
| P1 | `benchmarks` | **chore(preflight): include TUI fixtures in ui-ux-audit / agent-briefing.json** | `ui-audit` |
| P2 | `li-cursor-agents` | **fix(tui-demo): ASCII title or UTF-8 locale for harness captures** | `ui-audit`, `surface:tui` |
| P2 | `li-cursor-agents` | **test(tui-gen): ANSI theme tokens + contrast check helper** | `ui-audit`, `surface:tui` |
| P2 | `lic` | **spec(llm-first): document TUI plain-text export contract for agent ingestion** | `surface:tui`, Vision-LLM |

**Suggested implementers:** `code_implementer` (harness/fixtures); `agent_kit_maintainer` (preflight wiring).

---

## Deferred

| Item | Reason |
|------|--------|
| Production Li TUI apps | No org targets until REPL/studio terminal UX lands in `lic` |
| `tui_ux_tester` journey rubric | Blocked on stable non-interactive `tui-app-fixture` ([#33](https://github.com/li-langverse/li-cursor-agents/issues/33)) |
| VHS / Playwright-terminal CI | Needs maintainer approval for deps; depends on #30 |
| WCAG contrast on monochrome fixtures | Apply when real color themes exist |
| Merge TUI rows into global `data/latest/ui-audit.json` preflight | Requires benchmarks preflight orchestration (GUI-only today) |
| Merge PRs / GitHub Actions cron | Out of scope (swarm policy) |

---

## Artifacts

- Preflight JSON: `benchmarks/data/latest/ui-audit.json` (GUI-only)
- TUI re-run JSON: `benchmarks/data/latest-tui-ui-run/ui-audit.json`
- Captures: `benchmarks/data/latest-tui-ui-run/captures/`
- Remediation manifest: `benchmarks/data/latest-tui-ui-run/remediation_manifest.json`
- Harness: `python3 li-cursor-agents/ux-harness/run_audit.py --target tui-app-fixture --mode ui`
- Fixtures: `li-cursor-agents/ux-harness/fixtures/tui-demo.sh`, `tui-gen-demo.sh`
- Shared rules: `li-cursor-agents/prompts/ui-ux-tester-shared.md`
