# TUI UI digest — 2026-05-29 (proactive refresh)

**Agent:** `tui_ui_tester` · **Source:** proactive ecosystem sweep  
**Surface:** `surface:tui` · **Targets:** `tui-app-fixture`, `tui-gen-fixture` (`tui_app`, `tui_gen`)  
**Preflight:** `data/latest/ui-audit.json` (org = docs-only); TUI harness re-run → `data/latest-tui-ui-run/ui-audit.json`  
**north_star_fit:** Easy pillar (PH-2i adjacent tooling UX) — terminal surfaces for `lic` diagnostics and agent workflows must be readable and automatable before perf work; proof-first (`lic build` certificates), no `unsafe` UI shortcuts.

---

## Executive summary

- Org **`ui-audit.json` preflight** still audits **`lic-docs` only** — zero `surface:tui` targets in briefing snapshot (coord gap for `tui_ui_tester`).
- **`tui-app-fixture` fails** in harness — `read -r` blocks non-TTY runs (`TimeoutExpired` @ 30s); passes with piped stdin (exit 0).
- **`tui-gen-fixture` passes** — deterministic Unicode box layout; no ANSI color; no frame PNG artifacts.
- **Remediation tracked:** [li-cursor-agents#30](https://github.com/li-langverse/li-cursor-agents/issues/30) (open, no labels yet).
- **Harness blind spot:** TUI adapter sets `artifacts: []` — no VHS/PTY frame capture or ANSI contrast scan.
- **No org TUI repos** registered in `ux-targets.json` beyond fixtures; `ui` repo is immediate-mode GUI, not terminal.
- **Control plane:** six prior `tui_ui_tester` runs today ended `error`; current proactive run refresh.
- **Manual captures:** `data/latest-tui-ui-run/captures/` (piped `tui-demo`, `tui-gen` stdout).

---

## Deliverable / findings

### Preflight (`ui_audit`)

| Signal | Value |
|--------|--------|
| Briefing `ui-audit.json` | 1 target (`lic-docs`), 0 failing — **TUI not in org preflight** |
| TUI re-run `ui-audit.json` | 2 targets, **1 failing** (`tui-app-fixture`) |
| `tui-app-fixture` | **fail** — `harness_error: TimeoutExpired after 30s` |
| `tui-gen-fixture` | pass, `fixture_exit_code: 0` |
| Artifacts (harness) | none (`mode: fixture`) |
| Pixel / contrast | stub metrics only |

### Terminal frame review (fixture demos)

**`tui-app-fixture` (`tui-demo.sh`)**

| Check | Result | Severity |
|-------|--------|----------|
| Non-interactive CI | Blocks on `read -r` ×2 | **P1** |
| ANSI layout | Clear + home (`ESC[2J` `ESC[H`), single-column prompts | OK (minimal) |
| Color contrast | No fg/bg colors — N/A | — |
| Layout density | ~3 lines + prompts; no panels/widgets | P2 (fixture only) |
| Help path | Text-only “arrow keys (mock)” — not exercised in harness | P2 |

**`tui-gen-fixture` (`tui-gen-demo.sh`)**

| Check | Result | Severity |
|-------|--------|----------|
| Exit code | 0 | OK |
| Box drawing | `┌─┐│└` Unicode frame, 31 cols | OK |
| ANSI color | None | P2 when real generator lands |
| Density | 4 lines — sparse but readable | OK for stub |
| Narrow terminal | No wrap/truncation test | P2 |

### Org catalog (ecosystem)

- **33 org repos** (briefing); **none** as `surface: tui` in `config/ux-targets.json`.
- **`ui` repo:** agent-first immediate-mode GUI for World Studio — not TUI.
- **`lic`:** no production TUI apps in tree; studio UX is GUI/web (`li-gui` PRs in flight).

### SOTA reference (terminal UI)

| Source | URL | Takeaway for Li |
|--------|-----|-----------------|
| Textual testing | https://textual.textualize.io/guide/testing/ | Inject input via pilot/driver — never block CI on `read` |
| Ratatui | https://ratatui.rs/ | Buffer + widget layout; snapshot tests for regression |
| VHS | https://github.com/charmbracelet/vhs | `.tape` recordings for visual audit artifacts |
| WCAG contrast | https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html | ≥4.5:1 when color is used on default bg |

### Remediation issues

| Issue | Priority | Status |
|-------|----------|--------|
| [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) tui-app-fixture blocks on read in non-interactive harness | P1 | Open |

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `li-cursor-agents` | **[ui-audit] tui-app-fixture blocks on read in non-interactive harness** ([#30](https://github.com/li-langverse/li-cursor-agents/issues/30)) | `ui-audit`, `surface:tui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | **feat(ux-harness): TUI frame capture (stdout dump or VHS) + baseline diff** | `ui-audit`, `surface:tui` |
| P1 | `benchmarks` | **chore(preflight): include `tui-app-fixture` + `tui-gen-fixture` in `ui-ux-audit` / briefing** | `ui-audit` |
| P2 | `li-cursor-agents` | **feat(ux-targets): register org TUI repos when `lic` CLI TUI ships** | `ui-audit`, `surface:tui` |
| P2 | `li-cursor-agents` | **test(tui-gen): ANSI theme tokens + contrast check helper** | `ui-audit`, `surface:tui` |
| — | `lic` | PRs **#374–#375** (studio agentic) — GUI scope; route to `gui_ui_tester` | — |

**Suggested implementers:** `code_implementer` (harness/fixtures); `agent_kit_maintainer` (preflight wiring).

---

## Deferred

- **Production Li TUI apps** — no org targets until REPL/studio terminal UX lands in `lic`.
- **`tui_ux_tester` journey rubric** — blocked on stable non-interactive `tui-app-fixture`.
- **VHS / Playwright-terminal** integration — needs maintainer approval for CI deps.
- **Merge failed PRs** — out of scope (swarm policy).
- **WCAG contrast on monochrome fixtures** — apply when real color themes exist.

---

## Artifacts

- Preflight JSON: `benchmarks/data/latest/ui-audit.json`
- TUI re-run JSON: `benchmarks/data/latest-tui-ui-run/ui-audit.json`
- Captures: `benchmarks/data/latest-tui-ui-run/captures/`
- Remediation manifest: `benchmarks/data/latest/remediation_manifest.json`
- Harness: `python3 li-cursor-agents/ux-harness/run_audit.py --target tui-*-fixture --mode ui`
- Fixtures: `li-cursor-agents/ux-harness/fixtures/tui-demo.sh`, `tui-gen-demo.sh`
