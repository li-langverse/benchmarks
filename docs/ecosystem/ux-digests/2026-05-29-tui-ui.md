# TUI UI digest — 2026-05-29

**Agent:** `tui_ui_tester` · **Source:** proactive ecosystem sweep  
**Surface:** `surface:tui` · **Targets:** `tui-app-fixture`, `tui-gen-fixture` (`tui_app`, `tui_gen`)  
**Preflight:** `data/latest/ui-audit.json` (org preflight = docs-only); TUI harness re-run this pass  
**north_star_fit:** Easy pillar — terminal surfaces for `lic` tooling and agent workflows must be readable and automatable before perf work; aligns with proof-first CLI (`lic build` certificates) without `unsafe` UI shortcuts.

---

## Executive summary

- Org **`ui-audit.json` preflight** still audits **`lic-docs` only** — no `tui_*` targets in the shared briefing snapshot (refresh gap for `tui_ui_tester`).
- **`tui-app-fixture` fails in harness** — interactive `read` blocks non-TTY runs (`TimeoutExpired` @ 30s); passes with piped stdin (exit 0).
- **`tui-gen-fixture` passes** — deterministic box-drawing layout, no ANSI color, no frame artifacts captured.
- **No org TUI repos cataloged** in `ux-targets.json` beyond harness fixtures; `ui` package is immediate-mode **GUI**, not terminal.
- **Harness blind spot:** TUI adapter sets `artifacts: []` — no VHS/PTY frame PNGs, no ANSI contrast scan ([Textual](https://textual.textualize.io/), [Ratatui](https://ratatui.rs/) SOTA use tape tests).
- **Visual capture (manual):** `tui-demo` clears screen (`ESC[2J`), 3-line prompt flow; `tui-gen` uses Unicode box borders — OK on UTF-8, untested on narrow terminals.
- **Filed:** [li-cursor-agents#30](https://github.com/li-langverse/li-cursor-agents/issues/30) — non-interactive fixture fix.
- **Control plane:** prior `tui_ui_tester` runs today mostly `error`; current run proactive refresh.

---

## Deliverable / findings

### Preflight (`ui_audit`)

| Signal | Value |
|--------|--------|
| Briefing `ui-audit.json` | 1 target (`lic-docs`), 0 failing — **TUI not in preflight** |
| `tui-app-fixture` (re-run) | **fail** (timeout / blocked on `read`) |
| `tui-gen-fixture` (re-run) | pass, `fixture_exit_code: 0` |
| Artifacts | none (adapter `mode: fixture`, no capture) |
| Pixel / contrast | stub metrics only (`max_ratio: 0`, `contrast_failures: []`) |

### Terminal frame review (fixture demos)

**`tui-app-fixture` (`tui-demo.sh`)**

| Check | Result | Severity |
|-------|--------|----------|
| Non-interactive CI | Blocks on `read -r` ×2 | **P1** |
| ANSI layout | Clear + home (`\033[2J\033[H`), single-column prompts | OK (minimal) |
| Color contrast | No foreground/background colors — N/A | — |
| Layout density | ~3 lines + prompts; no panels/widgets | P2 (fixture only) |
| Help path | Text-only “arrow keys (mock)” — not exercised in harness | P2 |

**`tui-gen-fixture` (`tui-gen-demo.sh`)**

| Check | Result | Severity |
|-------|--------|----------|
| Exit code | 0 | OK |
| Box drawing | `┌─┐│└` Unicode frame, 31 cols | OK |
| ANSI color | None | P2 — add theme tokens when real generator lands |
| Density | 4 lines total — sparse but readable | OK for stub |
| Wide/narrow terminal | Not wrapped/truncation-tested | P2 |

### Org catalog (ecosystem)

- **33 org repos** listed; **none** registered as `surface: tui` in `config/ux-targets.json`.
- **`ui` repo:** agent-first **immediate-mode GUI** for World Studio — not a TUI surface ([README](https://github.com/li-langverse/ui)).
- **`lic`:** no production TUI apps in tree yet; studio UX is GUI/web ([`li-gui`](https://github.com/li-langverse/li-gui) PRs in flight).
- **Deferred:** catalog `lic` REPL/studio CLI TUI when shipped; add targets `lic-repl-tui`, etc.

### SOTA reference (terminal UI)

| Source | URL | Takeaway for Li |
|--------|-----|-----------------|
| Textual testing | https://textual.textualize.io/guide/testing/ | Inject input via pilot/driver — never block CI on `read` |
| Ratatui | https://ratatui.rs/ | Buffer + widget layout; snapshot tests for regression |
| VHS | https://github.com/charmbracelet/vhs | `.tape` recordings for visual audit artifacts |
| WCAG (terminal) | https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html | When color is used, aim ≥4.5:1 on default bg |

### Remediation issues

| Issue | Priority |
|-------|----------|
| [#30](https://github.com/li-langverse/li-cursor-agents/issues/30) tui-app-fixture blocks on read in non-interactive harness | P1 — filed this run |

---

## Recommended issues/PRs

| Priority | Repo | Title | Labels |
|----------|------|-------|--------|
| P1 | `li-cursor-agents` | **[ui-audit] tui-app-fixture blocks on read in non-interactive harness** | `ui-audit`, `surface:tui`, `ready-for-implement` |
| P1 | `li-cursor-agents` | **feat(ux-harness): TUI frame capture (stdout dump or VHS) + baseline diff** | `ui-audit`, `surface:tui` |
| P1 | `benchmarks` | **chore(preflight): include `tui-app-fixture` + `tui-gen-fixture` in `ui-ux-audit` / briefing** | `ui-audit` |
| P2 | `li-cursor-agents` | **feat(ux-targets): register org TUI repos when `lic` CLI TUI ships** | `ui-audit`, `surface:tui` |
| P2 | `li-cursor-agents` | **test(tui-gen): ANSI theme tokens + contrast check helper** | `ui-audit`, `surface:tui` |
| — | `lic` | PRs **#374–#375** (studio agentic) — GUI scope; route to `gui_ui_tester` | — |

**Suggested implementers:** `code_implementer` (harness/fixtures); `agent_kit_maintainer` (preflight wiring).

---

## Deferred

- **Production Li TUI apps** — no org targets until REPL/studio terminal UX lands in `lic`.
- **`tui_ux_tester` journey rubric** — blocked on stable non-interactive `tui-app-fixture`.
- **VHS / Playwright-terminal** integration — needs maintainer approval for CI deps.
- **Merge failed PRs** — out of scope (swarm policy).
- **WCAG contrast on monochrome fixtures** — apply when real color themes exist.

---

## Artifacts

- Preflight JSON: `benchmarks/data/latest/ui-audit.json` (docs-only snapshot)
- TUI re-run JSON: `benchmarks/data/latest-tui-ui-run/ui-audit.json`
- Remediation manifest: `benchmarks/data/latest/remediation_manifest.json`
- Harness: `python3 li-cursor-agents/ux-harness/run_audit.py --target tui-gen-fixture --mode ui --out-dir benchmarks/data/latest-tui-ui-run`
- Fixtures: `li-cursor-agents/ux-harness/fixtures/tui-demo.sh`, `tui-gen-demo.sh`
