# Docs maintainer digest — 1780144262784

**Generated:** 2026-05-30 · **Agent:** `docs_maintainer` · **north_star_fit:** easy (coord_ecosystem · PH-Doc)

Preflight: `ecosystem-audit.json` @ 2026-05-30T12:32Z

## Executive summary

- **1 repo** without live handbook Pages: **`lic`** (`repos_without_live_docs` / `live_docs_down`).
- Root cause: PR **#535** merged `pages.yml` but **omitted `site/index.html`** — workflow failed on `test -f site/index.html`.
- Opened **lic PR #542** adding `site/index.html` (compiler handbook hub + satellite Pages table + plan cross-links nav).
- Enabled **GitHub Pages** on `lic` via API (`build_type=workflow`); deploy pending merge + green **Handbook (Pages)** run.
- Cross-links (master plan ↔ provability-gaps ↔ phase plans) already landed in **#534** — no duplicate work this pass.
- **11/12** handbook URLs green; **lis** also 404 (separate heap item — not in `repos_without_live_docs` because audit uses `HANDBOOK_PAGES` set only).
- Post-merge: re-run `python3 scripts/ecosystem-audit.py` — expect `repos_without_live_docs: []` for lic.
- No **G-*** proof status changes (benchmark honesty preserved).

## Deliverable / findings

| Item | Detail |
|------|--------|
| Audit signal | `metrics.repos_without_live_pages: 1`, `repos_without_live_docs: ["lic"]`, `live_docs_down: ["lic"]` |
| Failed workflow | [Handbook (Pages) run 26683179853](https://github.com/li-langverse/lic/actions/runs/26683179853) — missing `site/index.html` on `main` |
| Fix PR | [li-langverse/lic#542](https://github.com/li-langverse/lic/pull/542) — `site/index.html`, release note, CHANGELOG |
| Pages enable | `gh api repos/li-langverse/lic/pages -X POST -f build_type=workflow` succeeded |
| Prior docs PRs | #533/#534 handbook cross-links merged; #535 workflow without artifact |

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| **fix(docs): add lic Pages site artifact (404 recovery)** | `lic` | `li-swarm`, `agent:docs_mmaintainer`, `docs` — **#542** (this run) |
| Human merge **lic#542** then verify Pages 200 | `lic` | `merge-approved` (human) |
| **docs(lis): GitHub Pages handbook** (if still 404) | `lis` | `docs`, `easy` — existing open PRs #23/#16 CI failing |
| Re-run ecosystem audit after lic Pages green | `benchmarks` | `automation` |

## Deferred

- **lis** live Pages (404 at audit time; not counted in `repos_without_live_docs` static set but listed in handbook satellite table).
- MkDocs full build for lic (static `site/index.html` hub only — matches lip/lit mirror pattern).
- Human **Settings → Pages** confirmation if API enable insufficient for first deploy.
- Plan-completion debt (166 findings), provability **G-*** rows — out of scope for docs_maintainer.
- Self-merge **roadmap** governance PRs — human review only.
