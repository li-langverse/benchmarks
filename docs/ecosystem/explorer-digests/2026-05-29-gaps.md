# Implementation gaps digest — 2026-05-29

**Agent:** `implementation_gaps` · **Heap:** `coord_governance` · **Preflight:** `plan-completion-audit.py` + `ecosystem-explorer.py` (`LIC_ROOT=../lic`) + briefing `agent-briefing.json` @ 2026-05-29T22:07Z  
**Vision:** [org roadmap](https://github.com/li-langverse/roadmap/blob/main/docs/ecosystem/vision-and-roadmap.md) · proof → easy → fast  
**Sources:** [master plan](https://github.com/li-langverse/lic/blob/main/docs/superpowers/plans/2026-05-14-li-master-plan.md), [provability-gaps.md](https://github.com/li-langverse/lic/blob/main/docs/verification/provability-gaps.md), `data/latest/plan-completion-audit.json` (2026-05-29T22:28Z), `data/latest/ecosystem-explorer.json` (2026-05-29T22:28Z), `data/latest/ecosystem-audit.json` (2026-05-29T22:25Z)

**north_star_fit:** Scientific/HPC domain · PH-2i, PH-5b, PH-7d/7e (tier-1 numerics + codegen), PH-IO-5/7 (easy ingest/dashboard), G-par/G-math provability honesty · proof before perf — no unproved shortcuts

---

## Executive summary

- **166** plan-audit findings with `LIC_ROOT=../lic`: **5** master-plan tracker rows partial, **26** sub-plan gate checkboxes open (+ **20** suppressed, **9** stale normative spec lists), **13** partial + **4** missing **G-*** gaps, **117** catalog path rows flagged (see interpretation below).
- **P0 numerics (PH-5b/7e):** **6** tier-1 reds vs C++ — `matmul_blocked` **1.55×**, `matmul_naive` **1.33×**, three ML microbenches **1.33×**, `num_gmres` **1.40×**; `horner_pure_li` no longer red (major progress since May).
- **P1 easy (PH-IO):** `std.io` / `std.csv` **shipped** on disk (`lic/std/io/io.li`, `std/csv/`); **`std.summary`** and **`std.plot`** still **missing** — ingest/dashboard `.li` scripts import them — **lic#13**, registry `gap-missing-std-std-summary/plot`.
- **P1 plan drift:** Five partial master-plan phases (**2i**, **7d**, **7e**, **8p**, **Vision-LLM**) block honest “done” claims; tier-1 perf gates in sub-plans remain open — **lic#16**, **lic#27**.
- **P2 catalog/harness:** **117** `catalog_gaps` are **audit false positives** — workloads live under `benchmarks/benchmarks/workloads/`, not `lic/benchmarks/`; tracked — **benchmarks#20**, **#54**, triage `[PH-5b] LIC_ROOT layout`.
- **P2 HPC stack:** Kokkos, PETSc, FFTW, hypre, HPX, RAJA rubric **missing**; Eigen/OpenMP/SUNDIALS **partial** — decorators ≠ execution spaces — **lic#15**, **#117**.
- **Registry first:** `lic/data/swarm-gap-registry/registry.yaml` + `swarm-gap-actions.json` (57 open gaps, 18 `plan_debt`) already cover std/plan/numerics debt; **0** new GitHub issues filed this cycle (issue budget reserved; loops exist).
- **Next week:** **numerics_research** + **bench_improver** (matmul/ML/GMRES reds) → **plan_verifier** (tracker sync for 2i/7e/8p) — defer **code_implementer** on PH-IO until `plan-approved`.

---

## Deliverable / findings

### Top 5 gaps (priority)

| Rank | Gap | PH / IDs | Repo | Action |
|------|-----|----------|------|--------|
| 1 | Tier-1 **matmul** + **ML** microbenches red (1.33–1.55× cpp) | PH-5b, PH-7e | lic | `@` lowering / blocked GEMM — **lic#439** PR; **numerics_research** + **bench_improver** |
| 2 | **`std.summary` / `std.plot`** missing | PH-IO-5, PH-IO-7 | lic | Finish PH-IO track — **lic#13**; registry backlog `pkg-std-summary/plot` |
| 3 | **G-par** + decorator lowering incomplete | PH-7d, G-par | lic | MIR proc tags + portable policies — **lic#15**, **#109** |
| 4 | **Phase 2i** linalg partial (broadcast, matrix `@`, reductions) | PH-2i, G-math | lic | Plan-approved codegen — **lic#148** thread; sim loop `sim-p1-num-dot-axpy` |
| 5 | **117 catalog_gaps** false positive + ingest honesty | PH-5b | benchmarks | Fix audit `LIC_ROOT` path rule — **benchmarks#20**; do **not** delete catalog rows |

### Plan debt table

| Source | Open / partial | Notes |
|--------|----------------|-------|
| Master plan tracker | **5** rows | 2i, 7d, 7e, 8p, Vision-LLM — **lic#16** |
| Sub-plan gate checkboxes | **26** open | HPC tier-1 perf gate, plots/social PNGs, governance PKG table, linalg Lean **P-linalg** |
| Suppressed (phase complete) | **20** | Phase 0–3, 5b benchmark infra — safe to check off with evidence PR |
| Stale normative spec lists | **9** | `phase-02-typechecker.md` implementation checklist — archive, not gates |
| **G-*** partial | **13** | lean, vc, par, stdlib, dec, math, bnd, oop, math-syn, async, net, trust, narrow |
| **G-*** missing | **4** | ann, gpu, meta, authz — long horizon |
| Catalog (`plan-completion-audit`) | **117** flagged | **False positive** when audit expects `lic/benchmarks/tier*`; real paths: `benchmarks/benchmarks/workloads/tier*` |
| `implementation_signals` | **1** | `feat/physics-module-packages` branch may be unpublished — **lic#50** |

### A. Plan vs shipped code

| Finding | Interpretation |
|---------|----------------|
| Phase **2i** partial — `norm`, `sum`/`dot`, reductions, broadcast, matrix `@` | **2i-a/c** landed (#148); **2i-b** and rank/broadcast still open — do not check master-plan row until evidence PR |
| Phase **7d** partial — `@vectorized` → `ArraySimdScope`; `@parallel(disjoint=)` policy | Decorators elaborate for some loops; **G-par** Lean + full MIR proc tags still open |
| Phase **7e** partial — loop matmul + FMA horner; tier-1 ≤1.2× advisory | Horner improved (off red list); **matmul_naive/blocked** + ML still red — numerics owns, not catalog threshold tweaks |
| Phase **8p** partial — Ninja `-j` C++ only; `lic --jobs` not wired | Parallel `run_all` progress on branches; tracker still says sequential workspace — **lic#385** |
| **Vision-LLM** partial — JSON diagnostics stub | Agent handoff schema exists; manifest/IDE integration open — governance, not codegen |
| **`std.io` / `std.csv` present** but plan/issue still lists four modules | Update **lic#13** / registry: mark io/csv **closed**; remaining debt is summary + plot only |
| Physics packages on disk (12 `li-physics-*`) | Domain `lib.li` scaffolds — not tier-2 APIs — **lic#14**; org mirrors — **lic#50** |

**Scaffold → real API (physics):** typed field layouts, integrator hooks, boundary ops, verification hashes, tier-2 `pure_li` variants per `GAME_DEV.md`.

### B. Ecosystem / std gaps (explorer JSON)

| Module | PH | Status | Blocked consumer |
|--------|-----|--------|------------------|
| `std.io` | PH-IO-4 | **present** | `csv_ingest_smoke.li` |
| `std.csv` | PH-IO-4 | **present** | ingest |
| `std.summary` | PH-IO-7 | **missing** | `build_summary.li` |
| `std.plot` | PH-IO-5 | **missing** | `render_dashboard.li` |

**HPC rubric (missing):** Kokkos, PETSc, FFTW, hypre, HPX, RAJA — Li has `std/execution/decorators.li` + shared C kernels + physics stubs.

**On disk (lic std):** 23 modules including `std.io`, `std.csv`, `std.math`, physics verticals; **not** summary/plot.

### C. Web spot-check (2026-05-29)

**PETSc + Kokkos PDE stack.** PETSc 3.25 integrates Kokkos views (`VecGetKokkosView`, `DMDAVecGetKokkosOffsetViewDOF`) and GPU preconditioners like **PCBJKOKKOS** for batched block Jacobi — exascale practice couples KSP/SNES with device-native kernels and careful host/device sync. **Li gap:** no implicit solver / AMG / distributed mesh story; tier-2 physics uses shared C cores and package scaffolds — route via **sim** research loops and **lic#117**, not ad-hoc codegen.

**Stdlib numerics / SIMD roadmaps.** C++26 **`std::simd`** (P1928) standardizes portable SIMD types; production HPC still relies on BLAS/MKL/Eigen for GEMM — library SIMD ≠ full linalg stack. **Li gap:** PH-2i matrix `@` and PH-7e blocked matmul lowering must close before claiming Eigen-class dense LA — **lic#27**, **#33**; reds are codegen not catalog edits.

**Kokkos vs OpenMP portability.** Recent plasma/fusion studies report Kokkos **~96%** performance portability vs OpenMP **~78%** across CPU/GPU vendors; OpenMP GPU offload remains compiler-sensitive (30–70% of native in worst cases). **Li gap:** `std.execution.decorators` is policy AST without memory spaces or backend mapping — **lic#15**, **#109**; proof-first **G-par** before GPU stories.

**Tier-1 matmul expectations.** Eigen/MKL-class GEMM is the baseline for scientific languages; 1.2× cpp gate matches community “same machine, autovec C++ reference” norms. **Li gap:** six red/near-red rows (`matmul_*`, ML forwards, `num_gmres`) — active **lic#439** and bench_improver branch on `lic` — numerics lane owns.

---

## Issues filed (this cycle)

| Repo | # | Title |
|------|---|--------|
| — | — | **None** — registry + existing issues cover all top gaps (budget max 5; defer duplicates) |

**Registry:** `swarm-gap-actions.json` @ 2026-05-29T22:11Z — std.summary/plot patched to ecosystem backlog; 18 `plan_debt` rows deferred or mapped to sim/security loops.

---

## Recommended issues / PRs

| Kind | Repo | Item | Labels | Why |
|------|------|------|--------|-----|
| Issue | lic | [#13](https://github.com/li-langverse/lic/issues/13) | plan-needed, feature, ecosystem-gap | Close io/csv; ship summary/plot |
| Issue | lic | [#15](https://github.com/li-langverse/lic/issues/15) | plan-needed, explorer-finding | Kokkos-class lowering / G-par |
| Issue | lic | [#16](https://github.com/li-langverse/lic/issues/16) | plan-needed, ecosystem-gap | Master plan + G-* reconciliation |
| Issue | lic | [#27](https://github.com/li-langverse/lic/issues/27) | master-plan-gap | PH-7e done criteria |
| Issue | lic | [#385](https://github.com/li-langverse/lic/issues/385) | master-plan-gap | PH-8p parallel compile SLO |
| Issue | benchmarks | [#20](https://github.com/li-langverse/benchmarks/issues/20) | plan-needed | plan-completion-audit LIC_ROOT / catalog path layout |
| Issue | benchmarks | [#18](https://github.com/li-langverse/benchmarks/issues/18) | ecosystem-gap | FFT catalog row |
| PR | lic | [#439](https://github.com/li-langverse/lic/pull/439) | — | matmul_naive ≤1.2× — **bench_improver** |
| PR | lic | — | — | Dirty workspace: `chore/agent-bench_improver-matmul-naive-at-2026-05-29` (4 files) — sweep before merge |

**Do not merge** without `merge-approved` + gate. No feature code without `plan-approved`.

---

## Suggested agent — next week

| Priority | Agent | Scope |
|----------|--------|--------|
| 1 | **numerics_research** + **bench_improver** | Close matmul/ML/GMRES reds; land lic#439 or successor |
| 2 | **plan_verifier** | Evidence PR: check suppressed Phase 0–3/5b boxes; update 2i/7e/8p tracker text |
| 3 | **gap_explorer** | Re-run after std.summary/plot land; close registry io/csv rows |
| 4 | **issue_planner** | PH-IO remainder (summary/plot) plan-approved spec |
| Defer | **code_implementer** | PH-IO implementation until planner gate |
| Defer | **pr_branch_opener** | 132 branches — after numerics CI green |

---

## Deferred

- **44** failed PRs / **137** open PRs — **pr_alignment** + **bug_fixer** before new feature work
- **Agent-kit drift** (9 repos) — **agent_kit_maintainer**
- **PH-DB** battle plan — triage issue exists; lis/lidb branches ahead of main — human merge governance
- **G-gpu**, **G-meta**, **G-authz**, **G-ann** — long-horizon provability
- Studio UX gaps (6 `ui_ux` registry rows) — studio goal-directed loop, not lic codegen
- Increase **pure_li** catalog variants — **gap-competitor-pure-li-ph7e-catalog** (competitor_feature, no new issue)

---

*Generated by implementation-gaps-agent. No feature code without `plan-approved`.*
