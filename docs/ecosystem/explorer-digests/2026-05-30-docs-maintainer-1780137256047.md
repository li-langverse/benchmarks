# Docs maintainer digest — 2026-05-30 (run 1780137256047)

## Executive summary

- Preflight: **8** repos in `repos_without_live_docs` and `live_docs_down` (handbook on `main`, Pages not enabled).
- Root cause: `deploy-pages` failed with **404** — GitHub Pages (Actions) disabled per repo, not missing content.
- Enabled Actions Pages via API and re-dispatched `pages.yml` on **lip**, **lit**, **li-net**, **li-httpd**, **li-std-core**, **li-std-math**, **li-demo** — all return **HTTP 200** now.
- Post-fix audit: **1** repo remaining (**lis**) — no `pages.yml` on `main`; opened [lis#32](https://github.com/li-langverse/lis/pull/32).
- Added `scripts/enable-org-handbook-pages.sh` in **benchmarks** for repeatable Pages enable + dispatch.
- **lip** docs branch rebased; no PR needed (main already live).
- `agent-repo-workflow.sh prepare` blocked by GraphQL rate limit — used sibling clones + REST API.
- Human merge **lis#32** then enable Pages on **lis** (same API as other repos).

## Deliverable / findings

| Repo | Action | Live URL |
|------|--------|----------|
| lip, lit, li-net, li-httpd, li-std-core, li-std-math, li-demo | Enabled Pages + workflow dispatch | `https://li-langverse.github.io/<repo>/` → **200** |
| lis | Pages scaffold + handbook on branch | [PR #32](https://github.com/li-langverse/lis/pull/32) — **404** until merge |
| benchmarks | `enable-org-handbook-pages.sh`, audit refresh | https://li-langverse.github.io/benchmarks/ |
| lic / roadmap | Already live (li-language + roadmap redirect) | No change this run |

**Cross-links:** Handbook landings link master plan, provability-gaps, benchmarks dashboard, and development overview (no **G-*** status changes).

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| docs(lis): GitHub Pages handbook landing | **lis** | `documentation`, `li-swarm`, `agent:docs_maintainer` — [PR #32](https://github.com/li-langverse/lis/pull/32) |
| chore(docs): org handbook Pages enable script | **benchmarks** | `documentation`, `li-swarm` |
| After lis merge: run `enable-org-handbook-pages.sh` or `gh api POST .../lis/pages` | **lis** | ops |

## Deferred

- Full mkdocs handbooks for package mirrors (static landing sufficient for audit).
- `roadmap` site root — already redirects via external progress site.
- GraphQL quota: batch `gh pr create` for stale `chore/agent-docs_maintainer-20260530` branches (close as superseded).

## Error

- `agent-repo-workflow.sh prepare --repo lip`: `GraphQL: API rate limit already exceeded` — worked around with REST (`gh api`) and sibling clones.
- `gh pr create` (GraphQL) for lis failed once; **REST** `POST /repos/.../pulls` succeeded → PR #32.
