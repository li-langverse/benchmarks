# Docs maintainer digest — run 1780136680815

**Generated:** 2026-05-30 · **Heap:** `coord_ecosystem` · **Preflight:** `ecosystem-audit.json`

## Executive summary

- **8** org repos in `repos_without_live_docs` and `live_docs_down` (HEAD 404 on `https://li-langverse.github.io/<repo>/`).
- **li-language** and **benchmarks** handbook URLs respond (**200** / **301**).
- **lic:** added `docs/handbook/README.md` and canonical `docs/ecosystem/plan-cross-links.md`; wired master plan Doc-a and provability-gaps alignment table.
- **Satellites:** plan-cross-links row in package `docs/handbook.md` + lip Pages nav (branches `chore/agent-docs_maintainer-*`).
- Pages scaffolds (`site/index.html`, `pages.yml`) already on agent branches — **merge to `main`** required before audit clears.
- **No G-*** status changes; bench honesty preserved on static landings.
- Re-run `python3 scripts/ecosystem-audit.py` after satellite Pages deploy.

## Deliverable / findings

| Area | Finding | Action this run |
|------|---------|-----------------|
| Audit | `repos_without_live_docs` = lip, lit, lis, li-net, li-httpd, li-std-core, li-std-math, li-demo | PR merge + Pages enable |
| Cross-links | Master plan ↔ provability-gaps ↔ phase plans | **lic** `plan-cross-links.md` + handbook index |
| Failed docs PRs | li-demo#15–18, lip#22–27, lis#16/#23, lit#13–15 (CI red) | **ci_maintainer** / human — fix workflow before merge |
| Ready | lic#481–482 (docs cross-links) stack with this pass | Human merge queue |

**Live URL check (2026-05-30):**

| URL | HTTP |
|-----|------|
| https://li-langverse.github.io/li-language/ | 200 |
| https://li-langverse.github.io/benchmarks/ | 301 |
| https://li-langverse.github.io/lip/ (and 7 satellites) | 404 (not deployed on `main`) |

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| docs(lic): handbook index + plan cross-links | lic | `li-swarm`, `agent:docs_maintainer`, `documentation` |
| docs(lip): plan cross-links on Pages landing | lip | same |
| docs: merge satellite Pages handbook branches | li-net, li-httpd, li-std-core, li-std-math, li-demo, lit, lis | `documentation` |
| chore(ci): fix Pages workflow on li-demo open PRs | li-demo | `agent:ci_maintainer` |
| docs(benchmarks): mirror plan-cross-links pointer to lic | benchmarks | `documentation` |

## Deferred

- Full mkdocs per satellite (static `site/` is v1).
- `roadmap` development-overview live-docs table refresh after deploy.
- Enable GitHub Pages (Actions) org-wide where workflow exists but environment missing.
- Close/supersede duplicate docs PRs (lip, lis, lit stacks) via **pr_alignment**.

## Error

None this run (`agent-repo-workflow.sh prepare` succeeded for lic and lip).
