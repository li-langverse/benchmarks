# Implementation gaps digest — 2026-05-30

**Agent:** `implementation_gaps` · **Heap:** `coord_governance` · **Task:** `heap:coord_governance:implementation_gaps:dc0607f724702e028fad`  
**Preflight:** `./scripts/agent-preflight.sh` (exit 1 — org-agent-kit + security-cwe JSON parse); `plan-completion-audit.py` + `ecosystem-explorer.py` with `LIC_ROOT=../lic`  
**Vision:** [org roadmap](https://github.com/li-langverse/roadmap/blob/main/docs/ecosystem/vision-and-roadmap.md) · proof → easy → fast  
**Sources:** [master plan](https://github.com/li-langverse/lic/blob/main/docs/superpowers/plans/2026-05-14-li-master-plan.md), [provability-gaps.md](https://github.com/li-langverse/lic/blob/main/docs/verification/provability-gaps.md), `data/latest/plan-completion-audit.json` (2026-05-30T11:30Z), `data/latest/ecosystem-explorer.json` (2026-05-30T11:31Z), `data/latest/ecosystem-audit.json` (2026-05-30T11:52Z)

**north_star_fit:** Scientific/HPC · PH-2i (linalg partial), PH-5b/7e (tier-1 numerics), PH-IO-5/7 (ingest/dashboard), PH-7d/G-par (execution portability) · proof before perf

---

## Executive summary

- **166** audit findings with `LIC_ROOT` present: **5** master-plan partial rows, **26** sub-plan gate checkboxes (down from 96 on 2026-05-19), **13** partial + **4** missing **G-*** provability gaps, **117** catalog path gaps.
- **P0 numerics:** **6 RED** tier-1 rows vs C++ — `matmul_naive` (1.33×), `matmul_blocked` (1.55×), `num_gmres` (1.40×), three `li-math` ML stubs (1.33×); `horner_pure_li` **green** (0.75×) — PH-7e slice closed for Horner, matmul regression open.
- **P1 PH-2i:** `norm`, `axpy`, `reductions/`, same-length `**` shipped on `main`; **length-1 broadcast** and full NumPy-rank broadcast still open — tracker row correctly partial; do not check box until broadcast + G-math proofs land.
- **P1 PH-IO:** `std.io` / `std.csv` **present** (registry closed); **`std.summary`** and **`std.plot`** still missing — benchmarks `.li` imports block on lic codegen — **lic#13** + registry `gap-missing-std-std-summary/plot`.
- **P1 catalog/harness drift:** **117** `catalog.toml` rows declare `repo=lic` paths (`benchmarks/tier1_micro/…`, `tier2_physics/…`) absent under lic checkout — ingest overclaims; triage via **benchmarks#179** (mirror harness vs `repo=benchmarks` vs `catalog_lifecycle=planned`).
- **P2 execution/HPC:** PH-7d/7e partial — decorators parse but Kokkos-class memory spaces + MIR proc tags open (**lic#15**, **lic#109**); PETSc/Kokkos stack expectation is implicit solvers + device views, not physics scaffolds alone.
- **Swarm registry:** 92 open gaps (57 `plan_debt`, 30 `competitor_feature`, 5 `missing_package`); ingest run added 0 new rows — existing loops cover debt; **0 new GitHub issues** this pass (issue budget preserved).
- **Next week agent:** **numerics_researcher** + **bench_improver** (unblock matmul PR #499) → **plan_verifier** (stale Phase 02/07 gate checkboxes) → **code_implementer** on PH-IO `std.summary`/`std.plot` after `plan-approved`.

---

## Deliverable / findings

### Top 5 gaps (priority)

| Rank | Gap | PH / IDs | Repo | Action |
|------|-----|----------|------|--------|
| 1 | Tier-1 **matmul** + **GMRES** RED vs C++ | PH-5b, PH-7e | lic | Unblock/merge matmul MIR fast-path PR; GMRES wrapper overhead — numerics_researcher |
| 2 | **`std.summary` / `std.plot`** missing | PH-IO-5, PH-IO-7 | lic | Plan-approved codegen — **lic#13**; closes Python fallback in `ingest-lic.sh` |
| 3 | **117 catalog path** rows without lic harness | PH-5b | benchmarks + lic | Honest `repo`/`lifecycle` triage — **benchmarks#179**; do not delete rows to green audit |
| 4 | PH-**2i-b** broadcast + G-math proofs | PH-2i, G-math | lic | Length-1 broadcast + loop-dot Lean closure before master-plan checkbox |
| 5 | Decorators ≠ portable parallel lowering | PH-7d, G-par | lic | Execution-space model — **lic#15**, **lic#109** (Kokkos/RAJA rubric) |

### Plan debt table

| Source | Count | Notes |
|--------|------:|-------|
| Master plan tracker (partial) | 5 | 2i, 7d, 7e, 8p, Vision-LLM — **lic#16**, **lic#27**, **lic#385** |
| Sub-plan gate checkboxes | 26 | Phase 02 typechecker gates likely **stale** (shipped); Phase 07 HPC perf gate **live** (reds on dashboard) |
| Suppressed (phase complete) | 20 | Phase 0/3/5b items correctly suppressed |
| Stale normative spec lists | 9 | Phase 02 implementation checklist — archive, not gates |
| **G-*** partial | 13 | lean, vc, par, stdlib, dec, math, bnd, oop, math-syn, async, net, trust, narrow |
| **G-*** missing | 4 | ann, gpu, meta, authz |
| Catalog path gaps | 117 | `repo=lic` paths missing under lic — **benchmarks#179** |
| `implementation_signals` | 1 | `feat/physics-module-packages` unpublished — **lic#14**, **lic#50** |

### A. Plan vs shipped code

| Finding | Interpretation |
|---------|----------------|
| Phase **2i** partial — 2i-b items listed open | **Mixed:** `norm`, `axpy`, `reductions/`, prelude dot/sum tests exist under `li-tests/math_linalg/`; **length-1 broadcast** and full rank broadcast **not** shipped — tracker accurate; sub-plan gate “P-linalg loop ≡ Lean ensures” still open |
| Phase **7e** partial | Horner tier-1 **green**; **matmul_naive/blocked RED** — plan gate “Tier 1 perf ≤1.2×” correctly open; provability-gaps **G-math** partial slice honest |
| Phase **7d** partial | `@vectorized` → `ArraySimdScope` shipped (#150); full MIR proc tags + G-par Lean proofs open |
| Phase **8p** partial | Ninja `-j` C++ only; `lic --jobs` not wired — **lic#385** |
| Phase **02** plan gates (`fib.li`, `bad_*.li`) | **Stale** — typechecker ships; plan_verifier should check boxes with `run_all.sh` evidence, not new feature code |
| Phase **07** perf gate | **Live** — six RED rows; aligns with open 7e tracker |
| Physics branch signal | Scaffold packages on disk; domain APIs + org mirrors unpublished — **lic#14**, **lic#50** |

**Scaffold → real API (physics):** field layouts, integrator hooks, boundary ops, verification hashes, tier-2 `pure_li` variants per `GAME_DEV.md` — not `<80`-line `return 0` stubs.

### B. Ecosystem / std gaps (explorer)

| Module | PH | Status | Blocked consumer |
|--------|-----|--------|------------------|
| `std.io` | PH-IO-4 | **present** | `csv_ingest_smoke.li` |
| `std.csv` | PH-IO-4 | **present** | ingest |
| `std.summary` | PH-IO-7 | **missing** | `build_summary.li` |
| `std.plot` | PH-IO-5 | **missing** | `render_dashboard.li` |

**HPC rubric (missing):** Kokkos, PETSc, FFTW, hypre, HPX, RAJA — Li has `std.execution.decorators` + shared C kernels + physics stubs.  
**HPC rubric (partial):** Eigen, OpenMP, SUNDIALS, C++ `std::execution` analog — tier-2 Euler/symplectic stubs only.

**On disk (lic std):** 22 modules including `std.math.*`, `std.physics.*`, `std.io`, `std.csv` — ingest/dashboard still blocked on summary/plot.

### C. Web spot-check (2026-05-30)

**PETSc + Kokkos PDE stack.** PETSc 3.25 treats Kokkos as a first-class backend (`aijkokkos`, `PCBJKOKKOS`, `VecGetKokkosView`) for device-resident implicit solves and batched preconditioners; production stacks expect single-source selection of matrix/vector types at runtime without API rewrites. **Li gap:** tier-2 physics catalog rows and `li-physics-*` packages are scaffold/stub level — no KSP/SNES/DM story, no device view ABI — **lic#14**, **lic#117**.

**Stdlib numerics / SIMD roadmaps.** Go 1.26 `simd/archsimd`, C++26 `std::simd`, and portable layers (Highway-class) treat vectorized GEMM and math primitives as baseline stdlib expectations across x86 and ARM SVE. **Li gap:** PH-2i matrix `@` and PH-7e blocked matmul still RED at 1.33–1.55× vs C++ despite closed Horner slice — numerics/codegen work, not catalog threshold edits — **lic#27**, **lic#33**.

**Kokkos vs OpenMP portability.** Recent HPC studies and PETSc/OpenMP-target work show Kokkos consistently maps to CUDA/HIP/SYCL backends while raw OpenMP GPU offload often trails native backends and is sensitive to construct choice; Princeton 2026 workshops still position Kokkos as the portable C++ layer above directives. **Li gap:** `std/execution/decorators.li` is policy AST + partial `@vectorized` lowering — no execution/memory spaces, no nested `@parallel(disjoint=)` inheritance to MIR — **lic#15**, **lic#109**.

**FFTW / vendor FFT roofline.** Community and vendor guidance still benchmark FFTW against cuFFT/VkFFT with explicit roofline crossover points for tier-1 micro rows. **Li gap:** `num_fft_r2c` catalog row points to missing lic path; no pure-Li FFT — **benchmarks#18**, **#26**, **#52**.

---

## Registry actions (this pass)

```bash
cd ../lic && python3 scripts/swarm-gap-ingest.py   # 0 new rows; 92 open gaps
cd ../lic && python3 scripts/swarm-gap-apply-actions.py --dry-run  # 63 actionable; plan_debt → plan_verifier handoff
```

No new `plan_debt` rows required — registry already tracks PH-IO summary/plot, matmul RED competitor features, and master-plan partial phases.

---

## Recommended issues / PRs

| Kind | Repo | Item | Labels | Why |
|------|------|------|--------|-----|
| Issue (existing) | lic | [#13](https://github.com/li-langverse/lic/issues/13) PH-IO std.summary/plot | plan-needed, feature | Ingest/dashboard Li-native path |
| Issue (existing) | lic | [#14](https://github.com/li-langverse/lic/issues/14) physics scaffold → APIs | plan-needed, feature | Tier-2 honesty |
| Issue (existing) | lic | [#15](https://github.com/li-langverse/lic/issues/15) Kokkos-class lowering | plan-needed, explorer-finding | G-par / PH-7d |
| Issue (existing) | lic | [#16](https://github.com/li-langverse/lic/issues/16) master plan + G-* debt | plan-needed | Tracker sync |
| Issue (existing) | lic | [#27](https://github.com/li-langverse/lic/issues/27) PH-7e Done criteria | master-plan-gap | Matmul regression |
| Issue (existing) | lic | [#385](https://github.com/li-langverse/lic/issues/385) PH-8p parallel compile | master-plan-gap | `lic --jobs` |
| Issue (existing) | benchmarks | [#179](https://github.com/li-langverse/benchmarks/issues/179) 117 catalog path gaps | ecosystem-gap | Harness drift |
| Issue (existing) | benchmarks | [#18](https://github.com/li-langverse/benchmarks/issues/18) FFT catalog row | ecosystem-gap | FFTW rubric |
| PR (unblock) | lic | [#499](https://github.com/li-langverse/lic/pull/499) matmul MIR fast paths | — | Claims naive → 1.0×; CI red |
| PR (docs) | lic | plan_verifier | — | Check Phase 02 stale gates + partial 2i sub-plan notes |

**Issues filed this cycle:** 0 (budget reserved; existing issues + swarm registry cover all top gaps).

**Do not merge** without `merge-approved` + gate. No feature code without `plan-approved`.

---

## Suggested agent — next week

| Priority | Agent | Scope |
|----------|--------|--------|
| 1 | **numerics_researcher** + **bench_improver** | Unblock lic#499; matmul_blocked ≤1.2×; num_gmres |
| 2 | **plan_verifier** | Stale Phase 02 checkboxes; partial 2i sub-plan sync with shipped `norm`/`axpy`/`reductions/` |
| 3 | **code_implementer** | `std.summary` + `std.plot` after lic#13 plan-approved |
| 4 | **gap_explorer** | Close registry rows for std.io/csv (already present); refresh HPC rubric after matmul green |
| Defer | **agent_kit_maintainer** | 8 repos missing/drifted kit (preflight exit 1) |

---

## Deferred

- **security_cwe_audit** JSON parse failure in preflight (`security-cwe-audit.py` empty/malformed feed) — security_auditor when feed restored
- **147** branches without open PR — pr_branch_opener (not implementation scope)
- **li-math** ML stub reds (conv2d, MLP) — separate package repo, not lic codegen
- Tier-2 thermostat yellows (`md_thermostat_*` ~1.29×) — after tier-1 crisis
- **G-gpu**, **G-meta**, **G-authz**, **G-ann** — long-horizon; no handbook overclaim
- Physics org mirror publish — **lic#50** (human governance for 12 repos)
- Live GitHub Pages for **lic** docs site — docs_maintainer

---

## Errors (preflight)

| Step | Exit | Detail |
|------|------|--------|
| `org_agent_kit_audit` | 1 | 8 repos missing/drifted agent-kit (canonical `1.3.5+6018e18bf2ed91f4`) |
| `security_cwe_audit` | 1 | `json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)` — CWE feed empty or corrupt |

All other preflight scripts exit 0. Intelligence (this digest) is Cursor-side per `agent-briefing.json` note.

---

*Generated by implementation-gaps-agent. No feature code without `plan-approved`.*
