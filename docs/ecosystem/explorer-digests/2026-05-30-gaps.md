# Implementation gaps digest — 2026-05-30 (coord_governance pass)

**Agent:** `implementation_gaps` · **Heap:** `coord_governance` · **Task:** `heap:coord_governance:implementation_gaps:dc0607f724702e028fad`  
**Preflight:** `plan-completion-audit.py` + `ecosystem-explorer.py` (`LIC_ROOT=../lic`, 2026-05-30T12:18Z); briefing `agent-briefing.json` @ 2026-05-30T12:07Z  
**Vision:** [org roadmap](https://github.com/li-langverse/roadmap/blob/main/docs/ecosystem/vision-and-roadmap.md) · proof → easy → fast  
**Sources:** [master plan](https://github.com/li-langverse/lic/blob/main/docs/superpowers/plans/2026-05-14-li-master-plan.md), [provability-gaps.md](https://github.com/li-langverse/lic/blob/main/docs/verification/provability-gaps.md), `data/latest/plan-completion-audit.json`, `data/latest/ecosystem-explorer.json`, `data/latest/ecosystem-audit.json`

**north_star_fit:** Scientific/HPC · PH-2i (linalg partial), PH-5b/7e (tier-1 numerics), PH-IO-5/7 (ingest/dashboard), PH-7d/G-par (execution portability), PH-DB (control-plane/lidb) · proof before perf

---

## Executive summary

- **65** audit findings (down from 166 when catalog paths resolve under `repo=benchmarks`): **5** master-plan partial rows, **40** sub-plan gate checkboxes, **0** catalog path gaps, **16** partial + **3** missing **G-*** provability gaps.
- **P0 numerics:** **6 RED** tier-1 rows vs C++ — `matmul_naive` (1.33×), `matmul_blocked` (1.55×), `num_gmres` (1.40×), three `li-math` ML stubs (1.33×); `horner_pure_li` green — PH-7e Horner slice closed, matmul/GMRES open.
- **P1 PH-2i:** `norm`, `axpy`, `reductions/`, same-length `**` shipped (`li-tests/math_linalg/`); **length-1 broadcast** and full NumPy-rank broadcast still open — tracker row correctly partial.
- **P1 PH-IO:** `std.io` / `std.csv` present; **`std.summary`** and **`std.plot`** still missing — blocks `build_summary.li` / `render_dashboard.li` — **lic#13**, registry `gap-missing-std-*`.
- **P1 PH-DB:** **29** open gates in `ph-db-battle-plan.md` (pytest failures, liorm/lidb embed, registry-min harness) — new dominant plan debt; not covered by numerics loops.
- **Catalog audit:** **0** path gaps after `plan-completion-audit.py` honors `repo=benchmarks` and `catalog_lifecycle=planned` — prior “117 lic-only path” signal was audit false positive, not missing harnesses.
- **Swarm registry:** 90 open gaps (55 `plan_debt`, 30 `competitor_feature`, 5 `missing_package`); ingest added **0** rows — existing loops cover debt.
- **Next week agent:** **numerics_researcher** + **bench_improver** (tier-1 RED) → **plan_verifier** (PH-DB + partial 2i sync) → **code_implementer** (`std.summary`/`std.plot` after `plan-approved`).

---

## Deliverable / findings

### Top 5 gaps (priority)

| Rank | Gap | PH / IDs | Repo | Action |
|------|-----|----------|------|--------|
| 1 | Tier-1 **matmul** + **GMRES** RED vs C++ | PH-5b, PH-7e | lic | Unblock matmul MIR fast-path work; GMRES wrapper — numerics_researcher |
| 2 | **`std.summary` / `std.plot`** missing | PH-IO-5, PH-IO-7 | lic | Plan-approved codegen — **lic#13** |
| 3 | **PH-DB battle plan** (29 gates) | PH-DB | lic + benchmarks | Fix pytest/liorm/lidb; registry-min harness — goal-directed loop, not numerics |
| 4 | PH-**2i-b** broadcast + G-math proofs | PH-2i, G-math | lic | Length-1 broadcast + loop-dot Lean before master-plan checkbox |
| 5 | Decorators ≠ portable parallel lowering | PH-7d, G-par | lic | Execution-space model — **lic#15**, **lic#109**; new plan `2026-05-30-ph-7d-mir-proc-tags-g-par.md` gates `plan-approved` |

### Plan debt table

| Source | Count | Notes |
|--------|------:|-------|
| Master plan tracker (partial) | 5 | 2i, 7d, 7e, 8p (8p-a/b partial per #186/#200), Vision-LLM |
| Sub-plan gate checkboxes | 40 | **29×** `ph-db-battle-plan.md`; 7× plots/governance/HPC; 2× PH-7d MIR tags |
| Suppressed (phase complete) | 10 | Phase 3/5/5b items |
| Stale normative spec lists | 0 | Phase 02 checklist no longer flagged stale |
| **G-*** partial | 16 | Includes G-gpu, G-proof-db, G-physics, G-erdos (new vs prior pass) |
| **G-*** missing | 3 | ann, meta, authz |
| Catalog path gaps | 0 | Paths resolve under `benchmarks` root or `planned` lifecycle |
| `implementation_signals` | 1 | `feat/physics-module-packages` unpublished — **lic#14**, **lic#50** |

### A. Plan vs shipped code

| Finding | Interpretation |
|---------|----------------|
| Phase **2i** partial | **Mixed:** `norm`, `axpy`, `reductions/`, dot/sum tests on disk; **length-1 broadcast** not shipped — tracker accurate |
| Phase **7e** partial | Horner tier-1 green; **matmul_naive/blocked RED** — perf gate in `2026-05-16-li-math-linalg-surface.md` correctly open |
| Phase **7d** partial | `@vectorized` → `ArraySimdScope` shipped; MIR proc tags + G-par Lean open; sub-plan requires **`plan-approved`** before implementation PRs |
| Phase **8p** partial | 8p-a parallel `run_all` + isolated `LI_BUILD_DIR` landed (#186, #200); `lic --jobs` still reserved — **lic#385** |
| Phase **02** typechecker gates | **Removed from open list** — no longer counted as stale gates (audit change) |
| **ph-db-battle-plan** | **Live** — 29 gates for lidb/lis/liorm/registry-min; dominates checkbox debt |
| Physics branch signal | Scaffold packages on disk; domain APIs unpublished — **lic#14**, **lic#50** |

**Scaffold → real API (physics):** field layouts, integrator hooks, boundary ops, verification hashes, tier-2 `pure_li` variants per `GAME_DEV.md` — not stub `return 0` libs.

### B. Ecosystem / std gaps (explorer)

| Module | PH | Status | Blocked consumer |
|--------|-----|--------|------------------|
| `std.io` | PH-IO-4 | **present** | `csv_ingest_smoke.li` |
| `std.csv` | PH-IO-4 | **present** | ingest |
| `std.summary` | PH-IO-7 | **missing** | `build_summary.li` |
| `std.plot` | PH-IO-5 | **missing** | `render_dashboard.li` |

**Std on disk (25 modules):** adds `std.algorithms`, `std.collections`, `std.heap` since prior explorer pass.

**HPC rubric (missing):** Kokkos, PETSc, FFTW, hypre, HPX, RAJA — physics stubs + shared C kernels only.  
**HPC rubric (partial):** Eigen, OpenMP, SUNDIALS, C++ `std::execution` analog.

### C. Web spot-check (2026-05-30)

**PETSc + Kokkos PDE stack.** Production stacks use `VecGetKokkosView`, shared execution spaces (`PetscGetKokkosExecutionSpace`), and matrix types like `aijkokkos` so user Kokkos kernels and PETSc solvers share device streams; COO/Jacobian paths still need manual tuning (known perf cliffs on CPU Kokkos backends). **Li gap:** no KSP/SNES/DM, no device view ABI — tier-2 physics and `li-physics-*` remain scaffold — **lic#14**, **lic#117**.

**Stdlib numerics / SIMD roadmaps.** NumPy is investing in Highway-backed universal SIMD; C++26 `std::simd` and mdspan-BLAS interfaces treat vectorized GEMM as baseline; Java Panama Vector API shows explicit portable SIMD beating scalar fallbacks. **Li gap:** PH-2i surface partial; PH-7e matmul still 1.33–1.55× vs C++ despite closed Horner — codegen/numerics, not catalog edits — **lic#27**, **lic#33**.

**Kokkos portability (2026 KUG).** Kokkos ecosystem focus: GPU tuning, Fortran interop limits (no automatic memory management on `develop`), distributed execution packaging. **Li gap:** `std/execution/decorators.li` is policy AST + partial `@vectorized` lowering — no memory spaces, incomplete nested `@parallel(disjoint=)` → MIR — **lic#15**, **lic#109**.

**FFTW / vendor FFT roofline.** Tier-1 FFT rows still need roofline crossover discipline (FFTW vs cuFFT/VkFFT). **Li gap:** no pure-Li FFT in std; catalog FFT row tracked under benchmarks issues — **benchmarks#18**, **#26**, **#52**.

---

## Registry actions (this pass)

```bash
cd ../lic && python3 scripts/swarm-gap-ingest.py   # 0 new rows; 90 open gaps
cd ../lic && python3 scripts/swarm-gap-apply-actions.py --dry-run  # handoffs to sim/studio plan loops
```

No new `plan_debt` rows — registry already tracks PH-IO summary/plot, matmul RED competitor features, master-plan partial phases, PH-DB vertical stubs.

---

## Recommended issues / PRs

| Kind | Repo | Item | Labels | Why |
|------|------|------|--------|-----|
| Issue (existing) | lic | [#13](https://github.com/li-langverse/lic/issues/13) PH-IO std.summary/plot | plan-needed, feature | Ingest/dashboard Li-native path |
| Issue (existing) | lic | [#14](https://github.com/li-langverse/lic/issues/14) physics scaffold → APIs | plan-needed, feature | Tier-2 honesty |
| Issue (existing) | lic | [#15](https://github.com/li-langverse/lic/issues/15) Kokkos-class lowering | plan-needed, explorer-finding | G-par / PH-7d |
| Issue (existing) | lic | [#16](https://github.com/li-langverse/lic/issues/16) master plan + G-* debt | plan-needed | Tracker sync |
| Issue (existing) | lic | [#27](https://github.com/li-langverse/lic/issues/27) PH-7e Done criteria | master-plan-gap | Matmul regression |
| Issue (existing) | lic | [#385](https://github.com/li-langverse/lic/issues/385) PH-8p parallel compile | master-plan-gap | `lic --jobs` |
| Issue (existing) | benchmarks | [#18](https://github.com/li-langverse/benchmarks/issues/18) FFT catalog row | ecosystem-gap | FFTW rubric |
| PR (track) | lic | matmul MIR fast paths (search open PRs) | — | Unblock tier-1 matmul RED |
| PR (docs) | lic | plan_verifier | — | Sync partial 2i sub-plan; PH-DB gate triage |

**Issues filed this cycle:** 0 (max 5 budget; existing issues + swarm registry cover top gaps).

**Do not merge** without `merge-approved` + gate. No feature code without `plan-approved`.

---

## Suggested agent — next week

| Priority | Agent | Scope |
|----------|--------|--------|
| 1 | **numerics_researcher** + **bench_improver** | matmul_blocked ≤1.2×; num_gmres; ML stub triage |
| 2 | **plan_verifier** | PH-DB battle-plan gate triage; partial 2i checkbox sync |
| 3 | **code_implementer** | `std.summary` + `std.plot` after lic#13 plan-approved |
| 4 | **gap_explorer** | Close registry rows for std.io/csv; refresh HPC rubric after matmul green |
| Defer | **agent_kit_maintainer** | org-agent-kit audit exit 1 in full preflight |

---

## Deferred

- Full `agent-preflight.sh` — background run exceeded 5 min; intelligence scripts ran directly
- **security_cwe_audit** JSON parse failure when slow preflight enabled
- **137** open org PRs / **35** failed CI — merge coordinator scope, not implementation gaps
- **li-math** ML stub reds — package-level, not lic codegen core
- Tier-2 thermostat yellows — after tier-1 crisis
- **G-ann**, **G-meta**, **G-authz** — long-horizon; no handbook overclaim
- Physics org mirror publish — **lic#50** (human governance)

---

*Generated by implementation-gaps-agent. No feature code without `plan-approved`.*
