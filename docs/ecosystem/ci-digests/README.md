# CI maintainer digests

Markdown snapshots from **ci_maintainer** agent runs (org `ci.yml` gate sweeps).

- **`YYYY-MM-DD-proactive.md`** — dated proactive / scheduled sweeps
- Source JSON: `data/latest/org-repo-ci-audit.json`, `data/latest/ecosystem-audit.json`

Template: `lic/scripts/templates/github-repo/ci.yml`
